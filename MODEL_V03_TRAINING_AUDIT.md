# v0.3 模型训练审计结论

审计时间：2026-06-25

## 总结

v0.3 当前不能作为“10 万篇高质量笔记训练出的可靠交付标准”。它可以继续临时作为启发式评分器使用，但不能把 `72+` 直接等同于真实爆文质量。

最严重的问题不是 Claude 生成能力本身，而是评分模型的训练数据、标签、切分和行业覆盖存在系统性缺陷，导致模型会奖励字数、重复率、标签数、价格数字等表层特征，出现“分数够高但标题/正文不自然”的假高分。

## 关键问题

1. 数据来源不是单一干净的 10 万篇训练集。
   - Path A 使用 `features.parquet`。
   - Path B 混入 10,000 条封面/标题样本，正文和 PLAD 大量为 0。
   - Path C 的 51,878 条人工样本在 `train_v03.py` 中被显式禁用。

2. 训练集被重复样本和 many-to-many merge 膨胀。
   - `features.parquet`：91,517 行，但只有 60,161 个唯一 `note_id`。
   - `timing_features.parquet` 同样有重复 `note_id`。
   - 两者按 `note_id` merge 后，Path A 从 91,517 行膨胀到 156,783 行。
   - 最终训练集变成 166,783 行，不是注册表写的 101,517 行。

3. 验证集存在严重泄漏。
   - `train_test_split` 是普通随机切分，没有按 `note_id` 分组。
   - 在复现同样随机切分后，验证集约 81.0% 的行，其 `note_id` 也出现在训练集。
   - 因此 MLflow 里的 `RMSE=12.30`、`Accuracy±10pt=82.34%` 不能代表真实泛化能力。

4. 标签口径不统一。
   - Path A 的 CES 使用 `liked + collected + comments * 4` 后按领域 rank。
   - Path B 只用 `liked_count` 按领域 rank。
   - `exploring_set.jsonl` 中约 41,315 行使用另一套字段名，旧特征脚本没有读取这些字段，容易造成正文/互动数据为空或为 0。

5. 行业覆盖有明显偏差。
   - `features.parquet` 中美食只有 2,538 行。
   - `features_human.parquet` 中美食有 8,006 行，但该人工集没有进入 v0.3 训练。
   - 线上 prompt 自己写着“美食/宠物训练数据存在标注缺陷”，但后续仍把这些规则当成高可信标准注入 Claude。

6. 特征重要性会诱导机械文案。
   - 当前模型 gain 占比：正文长度约 37.72%，PLAD 表层语言特征约 30.60%，标签约 4.04%。
   - 标题特征只有约 2.85%，语义三维只有约 2.00%。
   - 这解释了为什么“长、重复、塞价格/标签/关键词”的稿件容易涨分，但人读起来不高级。

7. 当前模型在被禁用的人类样本集上明显失准。
   - `Human_disabled_dedup`：标签均值约 50.01，模型预测均值约 69.09。
   - RMSE 约 33.96，±10 分准确率约 20.59%。
   - 各行业普遍高估约 18-21 分，说明它没有稳定学到跨来源内容质量。

8. 模型注册信息与真实训练记录不一致。
   - `model_registry.json` 写 `train_rows=101517`。
   - MLflow 最终 v0.3 记录显示 `total_rows=166783`、`train_rows=133426`、`val_rows=33357`。

## 对产品的影响

- AI 诊断和爆文生成如果继续把 v0.3 分数当唯一目标，会诱导 Claude 追逐表层特征。
- `72+` 不能直接代表用户愿意付费的好内容。
- 多行业质量尤其危险：美食、本地生活、健身、宠物等行业的训练基础不足或口径异常。
- 当前必须把 v0.3 降级为“辅助特征雷达”，交付标准需要同时依赖事实源、行业 brief、自然度门禁、人工 golden set 和真实生成回归。

## 修复方向

1. 先冻结 v0.3 为 legacy heuristic，不再宣称它是可靠爆文标准。
2. 重建训练数据管线：统一字段 schema、去重 `note_id`、保留来源标识、禁止 many-to-many merge。
3. 统一标签口径：同一模型内不能混用 CES 公式和 liked-only rank。
4. 重新启用并正确校准人工样本，尤其补齐美食/本地生活/健身/宠物。
5. 验证集必须改为 `GroupKFold/GroupShuffleSplit` 按 `note_id` 分组，并增加 source holdout、domain holdout、时间切分。
6. 建立行业 golden set：每个行业至少包含高质量、普通、低质、事实越界、标题不自然样本。
7. 新模型不要只优化 RMSE/±10 分，还要报告 per-domain calibration、pairwise ranking、人工偏好一致率和生成后可交付率。
8. 线上生成继续采用“双标准”：模型分只是一个信号，自然度、事实准确、行业表达、人感可读性必须并列通过。

# NoteAI Pro 交付修复台账

更新时间：2026-06-25

目标：把当前内测 Demo 修复为对用户真正有价值、愿意付费、能稳定生成高质量小红书爆文的 SaaS 系统。

## 状态说明

- `[待处理]` 尚未开始。
- `[进行中]` 正在修复或验证。
- `[已完成]` 已完成代码修复并通过本地验证。
- `[需复测]` 已改代码，但需要真实 API、真实样例或人工验收进一步确认。

## 核心发现

### A. 爆文生成质量链路

- `[需复测]` 主前端调用 `/generate/stream`，但流式生成链路只有“草稿评分 -> 仲裁 -> 最终评分 -> complete”，低分不会触发真正重写闭环。已接入最多 2 轮质量修复与最终质量门禁，本地低质草稿回归通过；仍需真实 Claude/Kimi 样例复测。
- `[已完成]` 非流式 `/generate` 有 `user_tier` 未定义运行时错误，且所谓 refine loop 实际只跑 1 轮评分，不会二次重写。已补默认/登录套餐读取，并把 refine 轮数改为 3。
- `[需复测]` 主流式生成没有提取封面 14 维视觉特征，LightGBM 最终评分拿到的是空 `cover_feats`。已在 `/generate/stream` SSE 内补确定性封面特征提取，并传入最终评分。
- `[已完成]` 最终评分未注入语义特征，导致 62 维模型退化为部分特征评分。已统一通过 `_score_generated_note()` 注入语义特征后评分。
- `[已完成]` 生成 agent 输出 XML 标签与仲裁解析不一致：管理端/默认 prompt 可能输出 `<draft_title>`、`<draft_body>`、`<keyword_tip>`、`<tag_recommendation>`、`<user_angle>`，但后续只读 `<title>`、`<body>`、`<keywords>`、`<tags>`、`<hook>`，导致内容/增长/用户专家意见丢失。已增加 XML tag alias 解析并覆盖流式/非流式生成链路。
- `[已完成]` 标题超过 20 字时多处直接硬截断，且真实验收样例出现 20 字卡边界标题，用户感知为超限。已把主生成/诊断/聊天解析改为语义压缩或边界兜底，并把交付安全上限收紧为 ≤18 字（平台硬上限仍为 20 字）。
- `[已完成]` 生成 prompt、诊断 prompt、代码 checklist 对“标题城市词/疑问句/标签数量/正文长度”的要求互相矛盾。已建立后端统一质量契约，并注入生成、诊断、对话三条链路；疑问句改为可选结构，标签目标改为按品类区间。
- `[已完成]` 餐饮正文只有最低字数门禁，没有目标上限门禁；真实验收样例正文约 471 字，超过美食目标 260-360 字仍被放行。已增加按“不含标签正文主体”统计的 `body_max` 质量门禁，正文超过品类目标上限会触发修复/阻断。
- `[已完成]` 生成链路中正文压缩/节奏修复仍绕回 Kimi 文本接口，削弱 Claude 主脑一致性。已改为统一 `_mr.call("content_gen")`，视觉/视频理解仍由 Kimi Vision 承担。
- `[待处理]` 模型对极短/空内容评分偏高，需要质量门禁和后处理校准层。
- `[需复测]` PLAD 特征方向存在反向解释：代码曾把短语重复率当成越低越好、TTR 当成越高越好，和 prompt/生成修复器相反。已修正方向和提示文案；验证发现 `plad_phrasal_repetition` 当前是 4 字符 n-gram 重复，不适合直接解释为“2字核心词重复”，后续需在模型重训中新增更可解释的关键词重复特征。

### B. AI 诊断质量链路

- `[已完成]` `_predict()` 给 LightGBM 喂入 62 维，但返回给诊断/agent 的只有 37 个文本特征，语义、视觉、时机弱项不可见。已改为返回完整 62 维，并验证 `/score`、`/diagnose` 兼容。
- `[已完成]` `/analyze` 中 `composite_score = percentile * timing_coefficient` 与 PRD“timing 已内嵌模型，避免二次乘法”的设计相冲突。已改为 `composite_score = ces_percentile`，market_timing 仅作为解释/建议。
- `[已完成]` Kimi Vision 封面评分路径使用未定义 `_KIMI_TIMEOUT`，可能静默降级为默认视觉分。已补 legacy timeout alias。
- `[已完成]` 前端实时诊断 agent 气泡使用旧 feature key（如 `body_word_count`、`hashtag_count`、`has_face`），导致内容/视觉/增长 agent 可能拿不到自己的真实弱项。已同步当前 62 维字段，并保留旧字段兼容历史结果。
- `[已完成]` `/analyze` 判断是否启动五 agent 时只检查 `MOONSHOT_API_KEY`，Claude-only 部署会退回旧单 prompt 诊断，绕过 harness agent 框架。已改为 Claude 或 Kimi 任一可用都走统一模型路由。
- `[已完成]` `/analyze` 没有注入本地长期记忆，只尝试读取 mem0；多数本地部署下用户偏好不会进入 AI 诊断。已把 `_memory.build_memory_prompt(user_id)` 与 mem0 召回一起注入五 agent 上下文。

### C. 对话优化与记忆

- `[已完成]` 用户说“标题太长/正文太短”等反馈时，偏好记录方向写反，会导致后续越改越偏。已改成明确偏好值：更短标题、信息更完整的标题、更短正文、信息更完整的正文。
- `[已完成]` 聊天会话允许未登录用户伪造 `user_id`，且 session 读取没有所有权校验。已改为 `/chat/start` 与 `/chat/message` 必须使用 JWT；session 只信任 JWT user_id；消息接口先校验 session 所有权再扣费。
- `[已完成]` Chat 对话重写只要解析到 `<note>` 就评分并保存，没有生成质量门禁。已接入 chat 重写后的评分、质量问题检测、自动修复一轮；仍阻断时发送 `quality_warning`，并暂不自动保存到作品库。

### D. 付费、权限、交付

- `[已完成]` 多个高成本接口匿名可用，只在已登录时扣费，无法形成可收费 SaaS 闭环。已强制 `/score`、`/diagnose`、`/extract-screenshot`、`/validate-ocr`、`/analyze`、`/upload-video`、`/generate/stream`、`/generate`、`/chat/start`、`/chat/message` 使用 JWT 并按用户扣费/记录用量；前端上传视频、截图 OCR 去重和对话入口已补 token 与 401 处理。
- `[需复测]` `/billing/topup`、`/billing/upgrade` 是测试后门，没有真实支付闭环。已默认关闭，只有显式设置 `NOTEAI_ENABLE_TEST_BILLING=1/true/yes` 才能启用；正式交付仍需接入真实支付回调、订单状态和对账。
- `[已完成]` 管理端“发布模型”写 `model_a_current.lgb`，但 API 固定读取 `model_a_v0.3.lgb`，发布不生效。已改为 API 优先读取 current symlink、无 symlink 时 fallback 到 v0.3，并按 symlink 目标/mtime 变化自动重载。
- `[已完成]` Docker/compose 只启动 API，没有管理端；前端和管理端硬编码 localhost。已补 `noteai-admin` 服务、共享 `data/artifacts` 挂载、镜像 healthcheck 依赖 `curl`，并把用户端/管理端 API 地址改为按当前 hostname/origin 推导且支持 window 变量覆盖。
- `[已完成]` 缺少 `.gitignore`、`.env.example` 和基础部署说明，真实 `.env` 容易被误提交或复制到生产。已补最小交付配置文件和部署说明。
- `[需复测]` 缺少系统化单元测试、生成质量 golden set、端到端测试和 CI。已补第一组后端合同测试与离线质量 gate，覆盖匿名成本接口 401、chat 所有权先于扣费、测试充值默认关闭、生成质量契约标签区间、XML tag alias、chat 重写质量修复；仍需真实 Claude/Kimi 样例 golden set、浏览器 E2E 和 CI。

## 当前冲刺计划

### Sprint 1：先救爆文生成质量主链路

1. `[已完成]` 修复 `/generate` 的 `user_tier` 运行时错误。
2. `[已完成]` 给 `/generate/stream` 补齐封面特征提取和语义特征评分。
3. `[已完成]` 建立生成质量门禁：标题、正文长度上下限、标签、CTA、占位符、品类必备信息、最低分。明确不把朋友反应、环境描述、个人体验等自然叙事作为硬拦截。
4. `[需复测]` 把流式生成改为真正的 `生成 -> 评分 -> 质量门禁 -> 修复 -> 重评` 闭环。本地 fake model 回归已通过；真实模型样例待验收。
5. `[已完成]` 替换所有标题硬截断为语义压缩，失败时才使用保守兜底。
6. `[已完成]` 修复聊天偏好记录方向。
7. `[已完成]` 做本地无外部 API 的低质量草稿回归测试，确认不会再把“好吃。#美食”当作成功爆文。

### Sprint 2：统一评分、诊断和 prompt 规则

1. `[已完成]` `_predict()` 返回完整 62 维特征，同时保留兼容。
2. `[已完成]` 为 62 维建立统一 feature governance：每个训练特征都有 label、分组、方向、基准/目标、适用品类、修复策略和 hard/repair/diagnostic/monitor 分层；语义特征进入软修复，不作为硬拦截。
3. `[已完成]` 清理 prompts/agent 规则漂移：用统一质量契约覆盖管理端 prompt 与代码 checklist 的冲突，所有 agent 调用前都会注入同一套交付标准。
4. `[已完成]` 修复 `composite_score` 二次乘法问题。
5. `[已完成]` 修复 Kimi Vision timeout 变量错误。
6. `[已完成]` 修复 AI 诊断三方案正文同质化：后端不再在正文生成失败时把三个方案统一回填为 `suggested_body/body_candidate`，前端不再把空方案正文用共享正文冒充，并删除重复渲染覆盖逻辑。
7. `[已完成]` 为 Claude/Kimi 文本路由增加并发闸门、空响应/429/5xx/超时重试和可观测日志；诊断三篇最终正文改为串行生成，避免核心交付内容抢同一速率窗口。
8. `[已完成]` 核查手动填写、截图上传、视频上传三入口：最终报告均进入 `/analyze` 统一链路；视频入口已改为理解失败显性报错，不再用空视频上下文继续生成伪诊断。

### Sprint 3：SaaS 交付硬化

1. `[已完成]` 强制高成本接口登录和额度校验。
2. `[需复测]` 接入真实支付或关闭测试充值/升级后门。当前已关闭测试后门；真实支付仍待接入。
3. `[已完成]` 修复用户身份、聊天 session 所有权、管理端 auth。用户身份与聊天所有权已完成；管理端 logout 已能真正清除 admin token。
4. `[已完成]` 修复模型发布链路，让管理端部署真实影响 API。
5. `[已完成]` 补齐 Docker/compose/env/static serving。
6. `[需复测]` 建立质量评测 harness、API 测试、E2E 测试和上线检查清单。已建立第一组 API 合同测试和 `tools/quality_gate.py`；真实 Claude/Kimi 回归、浏览器 E2E 和 CI 仍待补齐。

## 执行日志

- 2026-06-28：启动 Git 仓库治理与上线资产边界固化。已在项目根目录初始化本地 Git 仓库（`main` 分支），清理无引用的 Codex turn-diff Git 对象并确认它们不会进入远端推送；新增 `.gitattributes`，对模型二进制、parquet/jsonl、文档和图片启用 Git LFS 规则；扩展 `.gitignore`，阻止真实 `.env`、本地依赖、MLflow、运行 DB、RedNote 原始数据、封面缓存、训练中间表、质量大包和历史实验模型误入仓库。新增 `docs/GIT_DEPLOYMENT_SCOPE.md`，明确全栈源码、生产模型三件套、模型发布证据、训练大数据、密钥和运行状态的 Git/云端存储边界；新增 `model/artifacts/MODEL_RELEASE_MANIFEST.md`，记录当前 V0.4 production run `20260628T013926Z` 的三模型文件、核心报告与 SHA256。下一步提交前必须按该文档逐项 staged，禁止无筛选 `git add .`。
- 2026-06-28：完成远程 Git 仓库推送准备。新增 `docs/REMOTE_REPOSITORY_SETUP.md`，明确私有远程仓库、Git LFS、密钥/原始数据禁入、推送后核验、干净克隆验收和代码+模型同步回滚规则；新增 `scripts/push_remote.sh`，统一执行工作区干净检查、`origin` 配置、`main` 推送、tag 推送和 LFS 对象推送。当前本机 `gh` 已安装但未登录，且没有任何远端 URL，因此不能擅自上传商业项目代码；待用户登录 GitHub/Gitee/GitLab 或提供远端 URL 后，可直接用脚本执行首次云端推送。
- 2026-06-28：完成 GitHub 私有仓库首次发布。GitHub CLI 已授权账号 `iamyusen1314`，创建私有仓库 `https://github.com/iamyusen1314/noteai`，推送 `main`、tag `v0.4-production-baseline-20260628` 和 `19` 个 Git LFS 对象（约 `21 MB`）；远端 `main` 当前提交 `10fca15`，tag 指向同一提交。已做临时干净克隆验收并执行 `git lfs pull`，V0.4 三个 `.lgb` 模型均可拉取为真实文件；禁入文件检查确认 `.env`、`model/.env`、`.venv/`、`node_modules/`、MLflow、本地原始大数据和生成大包没有进入远端；Python 核心文件 `py_compile` 与 `scripts/push_remote.sh` 语法检查通过。
- 2026-06-28：完成 GitHub Actions CI、production 环境变量/密钥和模型云端加载策略第一版。新增 `.github/workflows/ci.yml`，在 push/PR 到 `main` 时执行 LFS 拉取、依赖安装、Python 编译、artifact SHA 校验、139 项单测、质量 gate 和 Docker Compose 配置检查；新增 GitHub Environment `production`，写入 `ADMIN_PASSWORD`、`ANTHROPIC_API_KEY`、`MOONSHOT_API_KEY`、`AMAP_WEB_KEY`、`MEITUAN_OPEN_TOKEN` 等 Secrets，写入 `NOTEAI_ENABLE_TEST_BILLING=0`、`NOTEAI_USE_V04_COMPOSITE=1`、`NOTEAI_MODEL_ARTIFACT_REQUIRED=1`、事实搜索和端口等 Variables，未在日志中输出任何 secret 值。新增 `model/artifact_loader.py`、`scripts/fetch_model_artifacts.py` 和 `model/artifacts/model_release_manifest.v04.json`，生产启动可校验/下载 V0.4 模型 artifact；修复 `api.py` 读取训练报告中本机绝对路径导致云端找不到 V0.4 模型的问题，改为自动映射到部署环境 `model/artifacts/`。新增 `docs/DEPLOYMENT_SECRETS.md`、`docs/MODEL_ARTIFACT_CLOUD_STRATEGY.md`，明确 Secrets、Variables、对象存储前缀和模型/代码同步回滚规则。CI 首跑暴露 `model/requirements.txt` 漏列 `jieba`、`scikit-learn` 与 `python-multipart`，已补齐为 `jieba==0.42.1`、`scikit-learn==1.8.0`、`python-multipart==0.0.30`。验证记录：artifact check 通过；`find model tools tests -name '*.py' -print0 | xargs -0 .venv/bin/python -m py_compile` 通过；`.venv/bin/python -m unittest discover -s tests -p 'test_*.py'` 通过 `139` 项；`.venv/bin/python tools/quality_gate.py quality/golden_notes.sample.json` 通过；`docker compose config --quiet` 通过。
- 2026-06-24：完成全栈审查与生成质量链路复查，确认当前不是可交付 SaaS，首要问题是生成链路没有真正闭环、评分校准偏松、prompt/代码/模型规则不一致。
- 2026-06-24：创建本台账，后续所有修复都在此记录状态和验证证据。
- 2026-06-24：修复 `/generate` 的 `user_tier` 未定义问题，并把非流式生成 refine 轮数从 1 调整为 3。
- 2026-06-24：`/generate/stream` 接入确定性封面特征提取、语义特征最终评分、质量门禁、最多 2 轮质量修复；最终返回 `quality_issues` 和完整 `features`。
- 2026-06-24：标题处理从直接 `[:20]` 改为语义压缩/边界兜底，覆盖主生成、非流式仲裁、诊断标题解析和聊天笔记解析。
- 2026-06-24：修复聊天偏好学习中的长短方向歧义，改为明确偏好文本。
- 2026-06-24：验证记录：`python -m py_compile model/api.py model/model_router.py model/feature_extraction.py model/train_v03.py` 通过；fake 低质量草稿“好吃。#美食”现在返回 `quality_failed`，不再成功 complete；结构完整样例可 complete，并携带未达 72 分的软质量提示。
- 2026-06-24：修复 Kimi Vision legacy timeout 变量，避免 `_KIMI_TIMEOUT` 未定义导致封面 AI 评分静默失败。
- 2026-06-24：修复 `composite_score` 二次乘法，改为与 `ces_percentile` 同口径；前端旧注释同步更新。
- 2026-06-24：`_predict()` 返回完整 62 维特征；验证记录：`_predict()` feature_count=62，且 `/score`、`/diagnose` 均返回 62 维并保持弱项诊断正常。
- 2026-06-24：修正 PLAD 方向：`plad_phrasal_repetition` 改为越高越好，`plad_ttr` 改为越低越聚焦；同步修正诊断文案。验证发现当前 phrasal repetition 是 4 字符 n-gram，后续重训需改进为 token/关键词级重复特征。
- 2026-06-24：同步前端 PLAD 展示口径：短语重复率不再被维度分扣分，TTR 改为“词汇离散度”；同时修复实时诊断 agent 到后端 62 维 feature key 的映射。
- 2026-06-24：建立统一质量契约并注入诊断/生成/对话链路；修复强制疑问句、固定 5 个标签、旅行标签目标错误等规则冲突。验证记录：美食 4 标签会触发质量问题、5 标签通过；旅行 8 标签达标、5 标签不达标；不再生成“补疑问句”修复指令。
- 2026-06-24：修复 Claude-only 环境 `/analyze` 不跑五 agent 的路由判断；monkeypatch 验证在仅有 `ANTHROPIC_API_KEY` 时 `agents_called=True` 且 `model_used=claude-routed-5-agents`。
- 2026-06-24：修复模型发布链路：API 现在优先读取 `model_a_current.lgb`，没有 current 时回退 `model_a_v0.3.lgb`，并在 symlink 目标或 mtime 变化后自动重载；管理端部署路径兼容 `model_xxx.lgb` 与 `artifacts/model_xxx.lgb`。
- 2026-06-24：强制高成本接口登录与扣费：`/score`、`/diagnose`、`/extract-screenshot`、`/validate-ocr`、`/analyze`、`/upload-video`、`/generate/stream`、`/generate`、`/chat/start`、`/chat/message` 均改为 JWT 用户身份；`/analyze` 自动保存诊断改用 JWT user_id，清除旧 `opt_user` 残留。
- 2026-06-24：修复聊天 session 安全：`/chat/start` 不再信任前端 `user_id`；`/chat/message` 增加 session 所有权校验，并修复“未授权访问先扣费再 403”的账务顺序问题。
- 2026-06-24：默认关闭测试充值/升级接口，需显式 `NOTEAI_ENABLE_TEST_BILLING=1/true/yes` 才能启用，避免生产环境无支付直接开通套餐或积分。
- 2026-06-24：前端补齐视频上传与对话入口的登录守卫、Authorization header 和 401 处理，避免后端强制登录后出现错误响应被当成成功 session 的问题。
- 2026-06-24：验证记录：`python3 -m py_compile model/api.py model/admin_server.py` 通过；HTML 3 个内联脚本 `vm.Script` 语法检查通过；monkeypatch 回归确认 `/analyze` 与 `/generate` 均按 JWT 用户扣费，测试充值默认 403，非法访问他人 chat session 返回 403 且不扣费。
- 2026-06-24：修复部署配置：Dockerfile 补 `curl`，compose 增加 `noteai-admin` 管理后台服务，API 与 admin 共享 `model/data` 和 `model/artifacts`，保证管理端发布模型后 API 容器可读；移除废弃 `version` 字段。
- 2026-06-24：修复环境硬编码：用户端 `API_BASE/API_AUTH` 改为当前 hostname 推导，管理端 `ADMIN_BASE` 改为当前 origin 推导，并保留 `window.NOTEAI_API_BASE` / `window.NOTEAI_ADMIN_BASE` 覆盖能力。
- 2026-06-24：修复管理端 logout 只返回 ok、不清 token 的问题；`get_admin_user` 返回当前 token，`/admin/logout` 调用 `admin_logout()` 真正失效会话。
- 2026-06-24：验证记录：`python3 -m py_compile model/api.py model/admin_server.py model/admin_auth.py` 通过；用户端与管理端 HTML 内联脚本语法检查通过；`docker compose config --quiet` 通过。
- 2026-06-24：补充验证记录：`python3 -m py_compile model/*.py` 通过；未登录请求 `/score`、`/diagnose`、`/validate-ocr`、`/analyze`、`/generate`、`/generate/stream`、`/chat/start`、`/chat/message`、`/extract-screenshot` 均返回 401。
- 2026-06-24：补齐基础交付配置：新增 `.gitignore` 防止 `.env`、虚拟环境、node_modules、运行 DB/日志误入版本库；新增 `model/.env.example`；新增 `README_DEPLOYMENT.md`，记录 Docker Compose 启动、共享目录、测试支付开关和真实支付待办。
- 2026-06-24：新增 `tests/test_api_contracts.py`，用 `unittest` 固化核心回归：匿名成本接口必须 401、chat 所有权必须先于扣费、测试充值默认 403、生成质量契约按品类标签区间生效。验证记录：`.venv/bin/python -m unittest tests/test_api_contracts.py` 通过；后续已扩展到 8 项测试。
- 2026-06-24：修复生成 agent XML 契约错位：新增 `_xtag_any()`，兼容历史 prompt 的 `<draft_title>/<draft_body>/<keyword_tip>/<tag_recommendation>/<user_angle>`，避免专家输出被仲裁链路丢弃；非流式 `feature_hits` 同步为统一质量命中字段。
- 2026-06-24：把生成链路中纯文本正文压缩和健身节奏修复从 Kimi 文本接口改为 Claude 路由 `_mr.call("content_gen")`，保持 Claude 为主脑；Kimi 仅保留视觉/视频理解。
- 2026-06-24：`/analyze` 注入本地长期记忆 `_memory.build_memory_prompt()`，让截图上传、手动输入、视频诊断都能继承用户偏好，而不是只依赖外部 mem0。
- 2026-06-24：Chat 对话优化接入质量门禁：新版本解析后先评分和质量检查，若阻断则自动修复一轮；仍阻断时发送 `quality_warning` 并不自动保存到作品库。
- 2026-06-24：新增 `tools/quality_gate.py` 与 `quality/golden_notes.sample.json`，可对真实生成输出跑 v0.3 分数、质量契约和阻断规则；默认离线不消耗 API，`--semantic` 可启用 Claude 语义特征。验证记录：`.venv/bin/python tools/quality_gate.py quality/golden_notes.sample.json` 通过，正例 PASS，低质反例 EXPECTED_FAIL。
- 2026-06-24：真实 Claude 生成烟测（美食/上海南京西路蟹黄面）：三位 Claude Haiku 专家 + Claude Sonnet 仲裁跑通；初稿 `pre_score=65.3`，首轮质量检查 `score=66.6` 且命中“低于72、关键词聚焦不足、标题缺情绪/数字”等问题；自动修复后 `final_score=75.0`、grade=优秀、`quality_issues=[]`、关键命中 `score_target_72/title_len_ok/tag_count_target/has_price/has_address/has_cta/title_has_location=True`。
- 2026-06-24：根据用户验收反馈修正交付质量口径：标题交付安全上限从“≤20 字”收紧为“≤18 字”；美食正文目标固定为 260-360 字（不含标签），新增正文超上限硬门禁；撤回“实事一致性 Verifier”作为硬拦截方案，仅保留占位符/待补信息等明显不可交付内容拦截，不拦截自然叙事中的朋友反应、环境描述和个人体验。
- 2026-06-24：验证记录：`python3 -m py_compile model/api.py model/admin_server.py model/admin_auth.py tools/quality_gate.py` 通过；`.venv/bin/python -m unittest tests/test_api_contracts.py` 通过 8 项；`.venv/bin/python tools/quality_gate.py quality/golden_notes.sample.json` 通过，正例 `score=80.2 issues=0`，低质反例 EXPECTED_FAIL。
- 2026-06-24：真实 Claude 复测（同“南京西路蟹黄拌面/人均58”场景）：最终标题 `上海宝藏蟹黄拌面，人均58元排队也值` 为 18 字；正文主体 342 字；最终 `score=72.2`、grade=良好、`quality_issues=[]`，确认新标题/正文门禁生效。复测同时发现 `feature_hits.body_len_target` 使用模型特征体长（含标签归一化）导致展示误报，已改为有正文时按去标签主体长度判断，并用单测覆盖。
- 2026-06-24：把多行业历史品类知识库中的旧字数口径同步到统一质量契约：旅行 320-520、穿搭 180-280、美妆 200-300、家居 280-420、健身 280-420、母婴 260-380；新增全行业正文上限回归测试，确认所有主要品类超过 `body_max` 都会被质量门禁阻断。验证记录：`python3 -m py_compile model/api.py model/admin_server.py model/admin_auth.py tools/quality_gate.py` 通过；`.venv/bin/python -m unittest tests/test_api_contracts.py` 通过 9 项；质量 gate 通过。
- 2026-06-24：落地“必须补齐的核心质量工程”第一层：新增 `FEATURE_GOVERNANCE` 覆盖 62 维训练特征，把特征分为硬门禁、生成修复、诊断解释、监控四层；语义情绪/共情/修辞进入生成修复建议，封面和时机类默认监控，不强行硬塞进文案；弱项诊断改为按治理表和品类适用性过滤。新增回归测试确认 62 维全部登记、语义治理会给修复建议且不会硬阻断可交付内容。验证记录：`python3 -m py_compile model/api.py model/admin_server.py model/admin_auth.py tools/quality_gate.py` 通过；`.venv/bin/python -m unittest tests/test_api_contracts.py` 通过 11 项；`.venv/bin/python tools/quality_gate.py quality/golden_notes.sample.json` 通过。
- 2026-06-24：把 62 维治理表注入诊断五 agent、生成五 agent、单体 Claude 诊断、chat 对话优化和质量修复 prompt；新增 `_get_feature_governance_brief()`，让 Claude 明确区分硬门禁/生成修复/诊断解释/监控特征。修复动态 benchmark：标题按 14-18，正文和标签按品类区间计算（如美食正文 260-360、旅行正文 320-520/标签 8-10），并把价格硬要求限定到美食、旅行、穿搭、美妆、家居，避免误伤母婴/健身。验证记录：`python3 -m py_compile model/api.py model/admin_server.py model/admin_auth.py tools/quality_gate.py` 通过；`.venv/bin/python -m unittest tests/test_api_contracts.py` 通过 12 项；`.venv/bin/python tools/quality_gate.py quality/golden_notes.sample.json` 通过。
- 2026-06-24：扩展离线质量 golden set 到美食、旅行、穿搭、美妆、家居、健身、母婴，并保留低质反例；`tools/quality_gate.py` 改为按品类判断硬阻断。审计发现 v0.3 对 `健身/运动` domain encoding 存在系统性压分：结构合格、同内容换域可到 80+，健身域约 50 分；短期加入健身品类校准下限 48 分，原始分和“未达72分”提示仍展示，硬质量按契约守住，后续必须用真实健身样本重训/校准。验证记录：`.venv/bin/python -m unittest tests/test_api_contracts.py` 通过 13 项；`.venv/bin/python tools/quality_gate.py quality/golden_notes.sample.json` 通过，全行业正例 PASS，低质反例 EXPECTED_FAIL。
- 2026-06-24：定位并修复 AI 诊断“截图上传/手动上传三套标题对应同一篇正文”的根因：`_agent_arbitrate()` 受旧 prompt 契约影响，正文生成空响应/失败时会把三个方案都 fallback 成同一个 `body_candidate`；`/analyze` 评分保存又用 `suggested_body` 回填空方案；前端 `populateReport()` 还存在第二套旧渲染块，会再次覆盖方案正文和共享正文卡片。已改为阶段1强制只产出三标题 XML，阶段2正文失败返回空串并触发去重/重试/差异化兜底，保存时不再用共享正文回填方案正文；前端删除重复渲染块，tab 正文只显示独立方案正文，空正文明确提示重新诊断，复制/对话入口拒绝提示语。验证记录：`python3 -m py_compile model/api.py model/admin_server.py model/admin_auth.py tools/quality_gate.py` 通过；`node vm.Script` 检查 3 个内联脚本通过；`.venv/bin/python -m unittest tests/test_api_contracts.py` 通过 15 项（新增两项三方案正文回归）；`.venv/bin/python tools/quality_gate.py quality/golden_notes.sample.json` 通过；Chrome 无头 DOM 验证确认 `rp0/rp1/rp2` 为三篇不同正文，`bodySuggested` 同步最优方案正文且无前端错误。
- 2026-06-24：针对“Claude 正文生成失败/空响应/标题解析失败是否由并发导致”完成链路核查：一次 `/analyze` 会先并发 4 个诊断专家，再仲裁，再生成 3 篇正文；旧代码没有全局模型并发闸门和重试，遇到 Claude 空响应/429/5xx/超时会被 fallback 逻辑放大。已在 `model_router.py` 增加默认 Claude 并发上限 2、Kimi 文本并发上限 2、瞬时失败默认 3 次重试（可用 `NOTEAI_CLAUDE_CONCURRENCY`、`NOTEAI_KIMI_TEXT_CONCURRENCY`、`NOTEAI_MODEL_RETRY_ATTEMPTS` 调整），并记录 attempt/elapsed/chars 日志；AI 诊断三篇最终正文从错峰并发改为串行生成。验证记录：新增 `test_model_router_retries_empty_claude_response_before_fallback`，确认 Claude 第一次空响应会先重试 Claude，不会立刻切 fallback。
- 2026-06-24：三入口质量链路核查：手动填写读取标题/正文/领域/封面/内容图后提交 `/analyze`；截图上传先 `/extract-screenshot` + 必要时 `/validate-ocr` 去重，再提交同一个 `/analyze`，并继续携带 cover/extra_images 做视觉分析；视频上传先 `/upload-video` 抽帧返回 `video_file_id`，再由 `/analyze` 调 `_kimi_video_understand` 拼入 `desc` 后进入同一五 agent/62维/记忆链路。修复视频理解失败静默降级：`video_file_id` 丢失返回 422，视频理解为空/过短返回 502，不再继续跑 v0.3 和 agent。前端 `quick-diagnose` 补 Authorization header，保持诊断请求行为一致。验证记录：`python3 -m py_compile model/api.py model/model_router.py model/admin_server.py model/admin_auth.py tools/quality_gate.py` 通过；HTML 内联脚本语法通过；`.venv/bin/python -m unittest tests/test_api_contracts.py` 通过 17 项（新增视频空理解 fail-fast 回归）；质量 gate 全行业通过。
- 2026-06-25：使用 `珑厨万博店2人套餐首图` 8 张真实素材做完整回归。修复前真实结果显示：手动 `/analyze`、截图流 `/analyze`、非流式 `/generate` 均 200 且三方案正文不再重复，但内容质量不可交付；生成稿分数约 43.4 仍被当成功返回，并编造/暗示未提供的人均、价格、营业时间、排队、地址等结构化事实。已修复：诊断/生成 prompt 注入“事实边界”；新增结构化事实门禁（价格/人均/套餐价数字、营业时间数字、排队时长数字、无价格依据的“不贵/划算/性价比/物有所值”等判断）；非流式 `/generate` 低质或事实越界时返回 422 `QUALITY_FAILED`，不再伪装成功。验证记录：`python3 -m py_compile model/api.py model/model_router.py model/admin_server.py model/admin_auth.py tools/quality_gate.py` 通过；HTML 内联脚本语法通过；`.venv/bin/python -m unittest tests/test_api_contracts.py` 通过 19 项；质量 gate 通过。
- 2026-06-25：继续真实回归发现 AI 诊断建议层仍会把低分方案直接展示为可用稿，并偶发把“方案一/方案二/标题/正文”等内部格式混入正文。已修复：诊断正文生成加入格式污染检测和重试；`/analyze` 对每个 `suggested_plan` 重新按生成后的标题+正文计算 62 维质量分，追加 `quality_issues` 与 `quality_failed`；前端诊断页不再把失败方案标为最优，并在方案正文和提示里显示“需精修”。新增测试覆盖多方案格式污染、无价格依据价值判断、低质 `/generate` 阻断。验证记录：`python3 -m py_compile model/api.py model/model_router.py model/admin_server.py model/admin_auth.py tools/quality_gate.py` 通过；HTML 内联脚本语法通过；`.venv/bin/python -m unittest tests/test_api_contracts.py` 通过 21 项；`.venv/bin/python tools/quality_gate.py quality/golden_notes.sample.json` 通过。
- 2026-06-25：真实素材最终回归结果：手动上传 `/analyze` 200，耗时 98.7s，原始分 49.8，三套方案正文唯一数 3，标题均 ≤18 字，无内部格式污染；三套方案分数 44.8/46.1/41.7，全部 `quality_failed=true`，第一套因“最划算”被事实边界命中。截图上传入口 `/extract-screenshot` 8/8 均 200、`/validate-ocr` 200、截图流 `/analyze` 200，耗时 95.8s，原始分 34.1，三套方案正文唯一数 3、无格式污染；三套方案分数 37.7/46.1/45.5，全部 `quality_failed=true`。非流式 `/generate` 真实调用最终返回 422 `QUALITY_FAILED`，耗时 312.4s，未把 47.6 分稿交付给用户。视频目录未发现真实视频素材，只验证失效 `video_file_id` 立即 422。结论：接口稳定性和质量拦截已达底线，但这组真实美食素材上的 Claude 产出质量仍未达到付费交付标准，下一步必须优化诊断/生成策略本身，而不是放宽门禁。
- 2026-06-25：质量策略优化第一轮：清理美食品类知识库中诱导“人均89/排队3小时/性价比绝了”的旧示例，改为“缺失事实用安全表达”；新增交付塑形层，把 AI 诊断三方案和生成仲裁结果压回品类目标字数、补入安全实用信息、移除无价格依据的“不贵/划算/性价比/物有所值”。验证发现安全表达（如“套餐价格以门店套餐页为准”“营业时间以门店公示为准”“周末建议提前预订”）可命中 v0.3 的价格/营业/预订特征且不触发事实越界。真实复测结果：手动诊断不再编造价格/时间/排队，三方案仍仅 37.0/45.7/44.3；非流式生成最高 48.3，仍 422。结论：安全兜底必要但不足，必须引入真实外部事实补全。
- 2026-06-25：根据“中国用户/中国本地生活场景”调整联网事实补全策略：新增 `model/fact_enrichment.py`，默认优先国内本地生活 API，自动顺序为高德地图 Web 服务 `AMAP_WEB_KEY` > 百度地图 Place API `BAIDU_MAP_AK` > 腾讯位置服务 `TENCENT_MAP_KEY` > SerpApi/Bing/Google CSE 海外通用搜索。联网层只提取白名单字段（位置/地址、价格/人均、营业时间、预订/排队）和来源，不直接生成文案；`/analyze`、`/generate`、`/generate/stream` 都把补全结果追加为“事实边界”，Claude 只能引用这些事实，查不到则回落安全表达。`model/.env.example` 已新增相关配置。验证记录：`python3 -m py_compile model/api.py model/fact_enrichment.py model/model_router.py model/admin_server.py model/admin_auth.py tools/quality_gate.py` 通过；HTML 内联脚本语法通过；`.venv/bin/python -m unittest tests/test_api_contracts.py` 通过 23 项；质量 gate 通过。待办：配置真实国内 API key 后做联网真实复测；大众点评/美团信息只应通过正式开放平台/商务合作 API 接入，不建议爬虫抓取。
- 2026-06-25：已在 `model/.env` 启用 `NOTEAI_FACT_SEARCH=1` 并配置高德 Web 服务 Key。真实高德冒烟测试通过：provider=`amap`，query=`广州 番禺 万博 珑厨万博店 地址 营业时间 人均 预订`，返回 5 个来源，提取到真实地址“南村镇汉溪大道东386号广晟万博城A座7层”，confidence=0.67，无接口错误。发现 `ADMIN_PASSWORD` 含 shell 特殊字符，直接 `source model/.env` 会触发 zsh 解析错误；应用按 dotenv 读取通常不受影响，但部署脚本应避免 shell source 或为该值加引号。
- 2026-06-25：加固联网事实补全的安全/合规边界：默认 `NOTEAI_FACT_SEARCH_CACHE_TTL=0`，不跨请求缓存第三方地图/搜索结果；第三方请求错误会脱敏 `key/ak/api_key/token` 等参数；测试环境默认关闭真实联网补全，避免合同测试误触发高德请求、消耗额度或在日志中泄露 Key。验证记录：`python3 -m py_compile model/api.py model/fact_enrichment.py model/model_router.py model/admin_server.py model/admin_auth.py tools/quality_gate.py` 通过；`.venv/bin/python -m unittest tests/test_api_contracts.py` 通过 25 项；真实高德冒烟复测仍通过，`cached=false`。
- 2026-06-25：真实生成质量复测：仅高德地址补全时，爆文生成最终最高 67.2，因低于 72 且缺 CTA 被 `QUALITY_FAILED` 拦截；接入本地核验事实库（用户素材/人工核验）后，一次真实生成达到 79.4 分，证明“高价值事实字段”能显著提升质量；但随后的重跑仍退回 63.7，暴露出仲裁/修复稳定性不足和正文超长问题，不能视为稳定交付。已新增本地核验事实库 `model/data/authorized_facts.json`，字段覆盖套餐/团购、必点菜、评价高频词、预订提示；新增确定性 CTA 兜底，避免模型漏写点赞/收藏导致低级失败。验证记录：`python3 -m py_compile model/api.py model/fact_enrichment.py model/model_router.py model/admin_server.py model/admin_auth.py tools/quality_gate.py` 通过；`.venv/bin/python -m unittest tests/test_api_contracts.py` 通过 27 项。
- 2026-06-25：已在 `model/.env` 写入美团 token（不在日志/回复中明文展示），并新增 `MEITUAN_OPEN_TOKEN/APP_KEY/APP_SECRET/SIGN/AES_KEY` 环境变量示例及 `meituan_credentials_status()` 就绪检查。实测结果：`token_present=true`，但 `oauth_ready=false`、`enterprise_ready=false`，缺少 `MEITUAN_OPEN_APP_KEY/MEITUAN_OPEN_APP_SECRET` 或 `MEITUAN_OPEN_SIGN/MEITUAN_OPEN_AES_KEY`，因此当前不能调用美团官方业务 API；事实补全实际仍为 `local_verified+amap`，confidence=0.9。验证记录：`.venv/bin/python -m unittest tests/test_api_contracts.py` 通过 28 项。
- 2026-06-25：修正美团能力判断：用户提供的是 AI Hub / Skill token，可用于酒旅 `meituan-travel`，不等同于服务零售结构化 API 的 `developerId/signKey/appAuthToken`。已安装官方酒旅 CLI `@meituan-travel/travel-cli`，命令路径 `/opt/homebrew/bin/mttravel`；已把 token 写入 `~/.config/meituan-travel/config.json` 并设置权限 `600`。真实冒烟测试 `mttravel 广州 "广州番禺万博附近周末亲子酒店推荐，预算500以内"` 成功，返回酒店推荐、真实评分、价格、券包、亲子设施和美团短链；说明酒旅行业可接入美团 Skill 作为事实源。
- 2026-06-25：确认事实源策略改为按行业事实源路由，不再按餐饮商家权限状态切分。产品策略调整为：餐饮/本地生活以高德 Web 服务作为基础事实源，提取地址、人均、评分、特色标签、商圈、电话、图片等；酒旅行业使用官方 `meituan-travel` Skill/CLI 作为优先事实源；用户素材、官方 API 快照、人工核验记录作为本地核验事实库补充。美团 `mt-tech` 服务零售/餐饮结构化 API 不作为餐饮基础事实源，只在具体官方 API 产品明确授予业务资源访问时按需接入。
- 2026-06-25：复查高德 Web 服务能力，确认它不只是地图点位：`v3/place/text?extensions=all` 可返回餐饮/酒店/景点/影院的 `biz_ext.rating`、`biz_ext.cost`，餐饮 POI 常返回特色内容 `tag`，并可返回电话、商圈、图片等；POI 2.0 文档也声明 `show_fields=business,photos` 支持商圈、营业时间、电话、特色标签、评分、人均和图片。已修复 `model/fact_enrichment.py`：高德 POI 现在结构化提取地址、人均、评分、特色菜/必点、商圈、门店类型、商家电话、图片；同时修复事实查询整句导致错店召回的问题，先从查询中提纯门店关键词，再对返回 POI 做名称/地址/类型/标签相关性过滤，避免把“万博”相关酒店/服装店的价格评分混进餐饮笔记。真实高德烟测：query=`广州 番禺 万博 珑厨万博店 地址 营业时间 人均 预订` 被提纯为 `珑厨万博店`，仅命中 `长禧家.珑厨(万博广晟店)`，提取到地址、人均98元、高德评分4.5、特色菜、商圈、电话和3张门店图片；`enrich_content_facts` 返回 `local_verified+amap`、confidence=0.9，未再混入错店事实。验证记录：`python3 -m py_compile model/fact_enrichment.py model/api.py model/model_router.py model/admin_server.py model/admin_auth.py tools/quality_gate.py` 通过；`.venv/bin/python -m unittest tests/test_api_contracts.py` 通过 30 项；`.venv/bin/python tools/quality_gate.py quality/golden_notes.sample.json` 通过。
- 2026-06-25：已把事实源策略落到代码路由：`_provider_name(domain)` 对美食/餐饮/本地生活保持高德优先；对旅行/酒店/住宿/酒旅/民宿/出行在 `meituan-travel` CLI 与 token/config 可用时优先返回 `meituan_travel`。本地核验事实源对外标签统一为 `local_verified`，避免产品策略继续使用餐饮商家权限状态分类。`model/.env.example` 已同步说明餐饮/本地生活用高德、酒旅用 `meituan-travel`，mt-tech 仅作为特定官方 API 产品按需接入。
- 2026-06-25：使用 `长禧家.珑厨(万博广晟店)` 做真实 `/analyze` 完整诊断时发现新事实污染：原查询词被提纯成括号内 `万博广晟店`，高德召回了广晟万博城商场、美宜佳、巴比馒头等 POI，导致事实池混入 `人均14元`、购物中心评分/电话，第三套标题生成“人均14元的粤菜馆”。已修复：括号门店名优先取括号外品牌核心（如 `长禧家.珑厨`），并按品牌匹配、位置词、餐饮类目对高德 POI 重新排序；新增回归测试防止商场 POI 进入餐厅事实池。修复后真实高德复测：query=`万博 广州 番禺 长禧家.珑厨(万博广晟店) 地址 营业时间 人均 预订`，keyword=`长禧家.珑厨`，第一来源为 `长禧家.珑厨(万博广晟店)`，事实为人均98元、高德评分4.5、餐饮类目和正确地址。重跑完整诊断耗时75.9s，事实源正确但三套方案分数仅 62.2/60.7/57.8，均 `quality_failed=true`，结论：事实源污染已修复，AI 诊断三方案生成质量仍未达付费交付线，下一步需要继续优化生成提示、必点识别和文案表达质量。
- 2026-06-25：按用户确认的产品口径调整质量拦截线：`60分以下`仍硬拦截，`60分及以上`不再因“未达72分、正文略长、标签/CTA/必点不足”等优化项被拦截，而是作为质量提醒进入对话优化；72 分继续作为爆文目标和优化方向。保留事实编造、空内容、极短标题/正文、占位符、内部格式污染等致命问题硬拦截，避免明显不是稿件或含错误事实的内容放行。回放长禧家诊断结果：62.2 分、60.7 分两稿在新规则下 `quality_failed=false` 可进入对话优化，57.8 分稿仍拦截。健身 golden 样本因当前 v0.3 仅 51.5 分，按新线改为 EXPECTED_FAIL，作为后续健身域重训/校准待办。验证记录：`python3 -m py_compile model/api.py` 通过；`.venv/bin/python -m unittest tests/test_api_contracts.py` 通过 32 项；`.venv/bin/python tools/quality_gate.py quality/golden_notes.sample.json` 通过。
- 2026-06-25：继续优化事实源与多行业生成策略。高德事实补全增加 POI 2.0/v5 detail 合并，优先提取 `business.opentime_today/opentime_week`、电话、图片和标签；真实长禧家高德冒烟已拿到营业时间 `09:00-14:00 17:00-21:00`。AI 诊断三方案从餐饮专用 A/B/C 改为多行业策略映射：A 通用“决策信息型”，B 按品类映射为美食“菜品种草型”、旅行“体验路线型”、穿搭“搭配公式型”、美妆“效果实测型”、家居“空间改造型”、健身“动作计划型”、母婴“安全实操型”，C 为“避坑取舍/决策型”。验证记录：新增多行业策略标签回归测试；`.venv/bin/python -m unittest tests/test_api_contracts.py` 通过 37 项；质量 gate 通过。
- 2026-06-25：真实长禧家 `/analyze` 回归后继续收紧标题与事实边界：标题会确定性替换“绝了”等低级爆词，未提供来源的“五星/满分/黑珍珠/米其林”背书和“五一/国庆/春节”等节假日场景会触发事实边界；正文交付出口会软化未提供的停车/地铁/步行与适用人数信息（如“停车便利”“2-6人”），避免本地生活笔记给用户错误到店决策。真实回归文件：`tmp_changxi_lc_analyze_result_after_title_guard.json`，三方案分数 `62.2/60.0/65.8`，均按新口径 `quality_failed=false`，事实源 `local_verified+amap`，标题无坏词。确定性回放确认三篇正文无 `绝了/五一/五星/停车便利/2-6人`，事实边界问题为空；剩余主要问题为正文偏长、分数尚未稳定达到 72+，进入下一轮表达压缩与语义提纯。验证记录：`python3 -m py_compile model/fact_enrichment.py model/api.py model/model_router.py model/admin_server.py model/admin_auth.py tools/quality_gate.py` 通过；`.venv/bin/python -m unittest tests/test_api_contracts.py` 通过 38 项；`.venv/bin/python tools/quality_gate.py quality/golden_notes.sample.json` 通过。
- 2026-06-25：根据产品口径调整事实边界：停车便利、地铁口、步行距离等到店便利表达不再清洗、不再拦截，允许作为本地生活文案的实用表达保留；同步移除相关 prompt 中“不得新增地铁站”的硬限制。未提供的适用人数/规模（如 `2-6人`）暂时仍软化为“多人”，避免影响用户预订判断。验证记录：`python3 -m py_compile model/fact_enrichment.py model/api.py model/model_router.py model/admin_server.py model/admin_auth.py tools/quality_gate.py` 通过；`.venv/bin/python -m unittest tests/test_api_contracts.py` 通过 38 项；`.venv/bin/python tools/quality_gate.py quality/golden_notes.sample.json` 通过。
- 2026-06-25：真实进入对话优化链路测试。第一轮从 65.8 分稿出发，chat thinking 耗时 147.8s，文案表达更自然但分数降至 64.0，并暴露 chat 修复后未再次走事实/格式交付塑形的问题。已修复：chat system prompt 注入已核验事实边界；chat 重写和自动修复后的标题/正文都会经过标题清洗、emoji 实用信息规范化、事实补足、人数软化和结构化事实复核；新增 emoji 实用信息不重复追加事实行的回归测试。第二轮真实 chat 耗时 68.3s，结果 `65.7` 分、无硬拦截，文案质感优于原稿但未提升到 72+，仍提示正文超 360 字和必点/招牌关键词识别不足。结论：现有对话优化能改善人感与可读性，但不能稳定提升模型分，下一步需做“分数导向压缩器/关键词命中器”，而不是只依赖自由聊天。验证记录：`.venv/bin/python -m unittest tests/test_api_contracts.py` 通过 39 项；质量 gate 通过。
- 2026-06-25：落实“62维特征前移到生成前规划 brief”的方案，并明确做成多行业策略而非餐饮写死模板。新增 `_build_generation_planning_brief()`：按品类生成标题公式、正文结构、标签范围、显性高分信号、核心词重复策略、事实槽位和表达边界；已覆盖美食、旅行、穿搭、美妆、家居、健身、母婴，未知品类走通用策略。该 brief 已注入 AI 诊断阶段1标题规划、阶段2三正文生成、正文重试/兜底、非流式爆文生成、流式生成仲裁、chat 对话优化、chat 自动修复和单 Claude fallback。新增回归测试确认 brief 按品类切换，且“必点/招牌/推荐/值得/很稳”等高质量推荐词不会被低级爆词清洗误伤。真实长禧家对话优化复测：从 65.8 分稿出发，brief 后结果 `66.3` 分，无硬拦截、无事实边界问题，标题为“广州番禺万博｜人均98这家粤菜馆必点”，正文已命中“招牌/必点/推荐/营业时间/高德评分/地址/人均/CTA”等关键交付信号。结论：brief 前置规划提升了特征命中和事实合规，但单靠 prompt 规划仍不能稳定把真实稿推到 70+，后续需要继续做分数导向压缩、特征缺口定向二修或评分模型校准。验证记录：`.venv/bin/python -m unittest tests/test_api_contracts.py` 通过 41 项；`.venv/bin/python tools/quality_gate.py quality/golden_notes.sample.json` 通过；`python3 -m py_compile model/fact_enrichment.py model/api.py model/model_router.py model/admin_server.py model/admin_auth.py tools/quality_gate.py` 通过。
- 2026-06-25：落地“分数导向二修”质量闭环。新增 `_score_directed_second_pass()`，对 60-72 分且无硬错误的稿件自动尝试追分，接入 AI 诊断三方案、非流式爆文生成、流式生成和 chat 对话优化；二修候选必须重新评分，只有分数上升且无事实越界/硬质量问题才采纳，低于60仍按硬拦截处理。二修策略从“Claude整篇重写”改为“确定性微修优先 + Claude兜底”：先根据 62 维缺口生成标题候选、节奏短句候选、标签/CTA/事实缺口修复候选，用本地评分器筛选并对前序候选真实评分确认；若达到72直接返回，避免大模型过度改写导致掉分。同步修正旧修复建议中“绝了/宝藏”等低级爆词，改用“值得/推荐/必点/很稳”等克制推荐信号。真实长禧家复测：旧稿 `66.3` 分，第一次 Claude 大修候选降至 `61.4` 被安全阀拒绝；确定性微修后标题改为“广州番禺万博98元值得点”，正文仅在菜品段收尾补入“很稳。”，最终 `72.1` 分、`issues=[]`、`score_target_72=true`，耗时约5秒。验证记录：`python3 -m py_compile model/fact_enrichment.py model/api.py model/model_router.py model/admin_server.py model/admin_auth.py tools/quality_gate.py` 通过；`.venv/bin/python -m unittest tests/test_api_contracts.py` 通过 43 项；`.venv/bin/python tools/quality_gate.py quality/golden_notes.sample.json` 通过；真实输出保存于 `tmp_changxi_lc_score_lift_result.json`。
- 2026-06-25：根据用户验收反馈修正“模型分达标但人感不自然”的质量策略。复盘发现 `72.1` 分标题“广州番禺万博98元值得点”属于评分特征拼接：命中了标题数字/正向推荐/城市，但真实读感差、吸引力弱。已新增 `_title_readability_issues()` 并纳入质量门禁：拦截“价格数字后直接接推荐词”“品类+价格+推荐词缺少明确对象”“分隔符后接这家/这个等半截句”等标题；`标题不自然` 作为硬质量问题，不再允许因为分数高而交付。二修流程同步调整：标题不自然时允许进入修复，但候选必须通过自然度门槛；候选生成改为“菜品/套餐+价格+推荐/值得试”，不再生成“98元值得点”这类拼接标题。真实复测：自然度门槛后系统拒绝生硬72分标题，选择“广州番禺万博98元小青龙值得试”，分数 `69.4`、标题自然度问题为0；Claude自然版候选“万博城有家粤菜小青龙挺实在”人感明显更好，模型分 `71.9`，说明后续需要把“人感评分/自然度”作为与 v0.3 并列的交付标准，而不是单纯追 72。验证记录：`python3 -m py_compile model/fact_enrichment.py model/api.py model/model_router.py model/admin_server.py model/admin_auth.py tools/quality_gate.py` 通过；`.venv/bin/python -m unittest tests/test_api_contracts.py` 通过 44 项；`.venv/bin/python tools/quality_gate.py quality/golden_notes.sample.json` 通过。
- 2026-06-25：完成 v0.3 训练质量专项审计，结论记录于 `MODEL_V03_TRAINING_AUDIT.md`。确认当前 v0.3 不能作为“10万篇高质量笔记训练出的可靠交付标准”：训练数据存在重复 note_id 与 many-to-many merge 膨胀，随机验证集约 81.0% 行与训练集共享 note_id，Path C 人工样本被禁用，Path A/Path B 标签口径不一致，模型注册表与 MLflow 真实行数不一致；生产模型在人类样本集上 RMSE 约 33.96、±10 分准确率约 20.59%，普遍高估 18-21 分。后续修复方向：冻结 v0.3 为辅助启发式评分器，重建去重/统一标签/分组切分/行业 golden set 的新训练管线。
- 2026-06-25：启动 v0.4 模型训练工程重建，按“训练工程为最高优先级”落地第一版严谨管线。新增 `tools/sync_rednote_vibe.py` 从 RedNote-Vibe Google Drive 同步素材并写入 snapshot manifest；新增 `model/rednote_vibe_v04.py` 统一 `desc/note_content`、`liked_count/likes` 两套 schema，去重 note_id，排除无真实互动标签的 AIGC 评分训练样本，按 `source_type::domain` 构造 CES 分位；新增 `model/train_v04.py` 使用 GroupShuffleSplit(note_id) 训练候选模型并输出行业/来源/校准报告；新增 `docs/MODEL_V04_TRAINING_ENGINEERING.md` 固化训练标准。已同步当前 RedNote-Vibe 快照：commit `a9bc6e29654986c4bfc5d0c61d8d801c0a855291`，三份 jsonl sha256 已记录于 `model/data/rednote_vibe_raw/20260625T062120Z_a9bc6e296549/sync_manifest.json`。
- 2026-06-25：完成 v0.4 第一版 candidate 训练。数据集重建后 `normalized=150,649`，真实评分样本去重后 `110,717`，其中 `exploration=59,045`、`human=51,672`，美食样本从旧 v0.3 的 `2,538` 提升到 `10,515`；去除重复 note_id `31,562`。Group split 验证通过：train unique note_id `88,573`，val unique note_id `22,144`，overlap `0`。诚实无泄漏指标为 `RMSE=26.46`、`MAE=22.68`、`±10分=21.58%`、`Spearman=0.349`；结论：v0.4 管线可信但第一版文本特征评分器尚未达到可上线质量，不能替换线上模型。候选文件：`model/artifacts/model_a_v0.4_candidate.lgb`，报告：`model/artifacts/model_a_v0.4_report.json`。新增 `tests/test_training_v04_pipeline.py`，验证 schema 兼容、AIGC 排除、去重策略和泄漏审计；`.venv/bin/python -m unittest tests/test_training_v04_pipeline.py` 通过 4 项。
- 2026-06-25：追加 v0.4 能力边界验证：用 candidate 对现有 golden set 评分，能把极差美食稿压到 `26.7`，但多行业好稿多数仅 `45-55`，说明第一版候选过于保守，不能作为生成质量主信号。补做 `CES>=70` Top30 分类实验，AUC 约 `0.657`，仍不足以单独承担付费级爆文判断。下一阶段必须新增语义/自然度/AI味/实用密度等更强特征和人工 golden 标签，而不是继续调 LightGBM 参数。
- 2026-06-25：启动 RedNote-Vibe 原始任务能力复用，训练 `naturalness-v0.1-candidate` AI味/自然度辅助模型。新增 `model/text_naturalness_features.py`（61维自然度特征，排除发布时间和 domain encoded 防来源泄漏）、`model/train_naturalness_v01.py`、`model/naturalness.py` 和 `tools/naturalness_probe.py`。验证集刻意 holdout `claude-sonnet-4/gpt-4.1/qwen3/gemini-2.5`，结果：ROC AUC `0.983`、Average Precision `0.940`、human 误伤率 `1.13%`、AI 漏检率 `16.64%`。golden set 上美食好稿 naturalness `98.9`、极差稿 `0.3`，但美妆好稿仅 `9.8`，且当前 Claude 生成稿几乎全部 AI 概率饱和；结论：该模型可作为 AI味风险监控和训练辅助，不可直接作为全行业硬门禁。新增 `tests/test_naturalness_pipeline.py`，自然度特征测试通过。
- 2026-06-25：启动 Composite Golden v0.1 人工标签工程，新增 `quality/golden_label_schema.v01.json`、`quality/GOLDEN_LABELING_GUIDE.md`、`quality/golden_labels.v01.seed.json`、`tools/composite_quality_report.py` 和 `tests/test_composite_quality_report.py`。首批种子 11 条，覆盖多行业 golden、v0.3 健身假阴性、长禧家自然稿、长禧家评分黑客稿。报告输出 `quality/composite_quality_report.v01.json`：ready 平均 v0.3 `75.05`，not-ready 平均 v0.3 `65.37`；v0.4 仍偏保守；naturalness 能分开美食好/坏但误伤美妆和长禧家自然稿。关键 mismatch：`v03_false_negative=1`、`naturalness_model_false_positive=2`、`signal_false_negative=3`。结论：复合目标工程已建立；`30-50 条/行业`只能作为校准种子集，正式训练最低需要 `300 条/核心行业`，推荐上线目标为 `500 条/核心行业`。
- 2026-06-25：落地 Golden Corpus 正式标注队列工程。新增 `quality/GOLDEN_CORPUS_PLAN.md`，明确 Seed 校准集、正式人工 Golden 训练集、生成偏好数据和线上反馈闭环四层；新增 `tools/export_golden_annotation_queue.py`，按行业从 RedNote-Vibe v0.4 去重评分样本中分层抽样 `real_top/real_good/real_boundary/real_mid/real_low`，并加入 AIGC 样本和已锁定 seed。首批导出 `quality/annotation_queue.v01.jsonl`、`quality/annotation_queue.v01.csv`、`quality/annotation_queue.v01.report.json`：总计 `3,821` 条，其中 `3,810` 条待人工标注、`11` 条 locked seed；11 个 RedNote 观察行业基本达到 `350` 条/行业，`其他` 因无 AIGC 只有 `310` 条；美妆、家居、母婴缺少独立 RedNote domain，必须用产品素材/用户样例/生成稿补样。新增 `tests/test_golden_annotation_queue.py`，验证分层抽样、去重和标签占位；修复 JSONL 中 Unicode 行分隔符导致逐行解析失败的问题。验证记录：`py_compile` 通过；`tests/test_golden_annotation_queue.py tests/test_composite_quality_report.py tests/test_naturalness_pipeline.py tests/test_training_v04_pipeline.py tests/test_api_contracts.py` 共 56 项通过；`annotation_queue.v01.jsonl` 逐行解析 `3,821` 条且 annotation_id 唯一。
- 2026-06-25：根据用户对样本量的质疑，升级 Golden 标准：`300-500 条/核心行业`只作为 candidate/灰度前验收门槛，生产训练最低改为 `1000 条/核心行业`，推荐上线目标改为 `2000 条/核心行业`，生成内容 A/B 偏好目标改为 `3000+ 组/核心行业`。`quality/GOLDEN_CORPUS_PLAN.md`、`docs/MODEL_V04_TRAINING_ENGINEERING.md` 和 `quality/annotation_queue.v01.report.json` 已同步新门槛。
- 2026-06-25：落地生成稿 A/B 偏好标注队列第一版。新增 `quality/preference_label_schema.v01.json`、`quality/PREFERENCE_LABELING_GUIDE.md`、`quality/preference_pairs.v01.seed.json`、`tools/export_preference_annotation_queue.py` 和 `tests/test_preference_annotation_queue.py`。首批以长禧家同一任务为种子，包含 AI 诊断三方案、chat 对话优化、brief 后稿、评分导向二修、标题自然化二修、Claude 自然候选和人工自然候选共 `12` 个候选稿，导出 `66` 组 A/B 对比，其中 `63` 组待人工标注、`3` 组 locked seed；覆盖 v0.3 高分但标题难读、人感自然但模型分略低、正文模板化、对话优化未升分等关键错误。输出文件：`quality/preference_queue.v01.jsonl`、`quality/preference_queue.v01.csv`、`quality/preference_queue.v01.report.json`。当前只覆盖美食长禧家任务，旅行/穿搭/美妆/家居/健身/母婴仍缺同任务偏好数据。验证记录：`py_compile` 通过；`tests/test_preference_annotation_queue.py tests/test_golden_annotation_queue.py tests/test_composite_quality_report.py tests/test_naturalness_pipeline.py tests/test_training_v04_pipeline.py tests/test_api_contracts.py` 共 `58` 项通过；`preference_queue.v01.jsonl` 逐行解析 `66` 条且 pair_id 唯一；`annotation_queue.v01.jsonl` 逐行解析 `3,821` 条且 annotation_id 唯一；质量 gate 保持通过。
- 2026-06-25：落地多行业偏好任务池和候选生成队列。新增 `quality/preference_task_pool_schema.v01.json`、`quality/PREFERENCE_TASK_POOL_GUIDE.md`、`quality/preference_task_pool.v01.seed.json`、`tools/export_preference_task_pool.py` 和 `tests/test_preference_task_pool.py`。首批覆盖 7 个核心行业：美食、旅行、穿搭、美妆、家居、健身、母婴；每个行业 1 条同任务上下文、9 个候选槽位，导出 `quality/preference_generation_queue.v01.jsonl`、`quality/preference_generation_queue.v01.csv`、`quality/preference_generation_queue.v01.report.json`，共 `7` 条任务、`63` 个待生成槽位。报告明确：9 个候选完成后每任务最多 36 组 A/B，要达到 `3000+` 组/行业约需 `84` 条完成任务/行业；当前只是生成任务骨架，不能视为已完成偏好数据。验证记录：`py_compile` 通过；`tests/test_preference_task_pool.py tests/test_preference_annotation_queue.py tests/test_golden_annotation_queue.py tests/test_composite_quality_report.py tests/test_naturalness_pipeline.py tests/test_training_v04_pipeline.py tests/test_api_contracts.py` 共 `60` 项通过；`preference_generation_queue.v01.jsonl` 逐行解析 `63` 条且 queue_id 唯一；`preference_queue.v01.jsonl` 逐行解析 `66` 条且 pair_id 唯一；`annotation_queue.v01.jsonl` 逐行解析 `3,821` 条且 annotation_id 唯一；质量 gate 保持通过。
- 2026-06-25：落地偏好候选槽位真实填充器 `tools/fill_preference_generation_slots.py`，读取 `quality/preference_generation_queue.v01.jsonl`，按槽位调用同一套 Claude 路由/质量契约/62维评分/事实边界，输出到 `quality/generated_variants/v01/<task_id>/<slot_id>.json`；每个 artifact 保留 raw_response、最终标题/正文、score、blocking、quality_issues、feature_snapshot 和 source_context，避免不可追溯批量样本进入训练。真实小批结果：美食 AI 诊断三方向 `63.7/58.9/69.2`，B 因低于60硬阻断，A/C 可进入二修；7行业 `generate_initial` 冒烟为美食 `63.4`、旅行 `65.3`（因未提供排队数字触发事实边界而阻断）、穿搭 `75.4`、美妆 `61.2`、家居 `64.8`、健身 `37.2`、母婴 `69.8`。本轮同时修复标题硬裁断问题：`_extract_note_from_response()` 不再提前截断标题，fallback 增加常见断词清理，避免「无器械计」「软颗粒安」这类半截标题；新增回归测试覆盖。验证记录：`.venv/bin/python -m unittest tests.test_api_contracts tests.test_fill_preference_generation_slots tests.test_preference_task_pool` 通过 51 项；`py_compile model/api.py tools/fill_preference_generation_slots.py` 通过。
- 2026-06-25：启动 `composite-v0.1` 质量目标层重构。执行前审查确认用户判断成立：生成 brief、agent prompt、二修 prompt、质量问题命名仍大量绑定 v0.3/72分追分；v0.4 训练管线更可信但候选模型能力不足，不能直接替换为单一主目标。执行计划：① 新增复合交付目标层，明确 v0.3/v0.4/naturalness 只作辅助信号；② 将生成前 brief 从“先满足训练特征”改为“先满足交付价值”；③ 将二修从“分数导向追72”改为“偏好/自然度/实用密度导向”，60分以下仍硬拦截，60分以上允许进入对话优化；④ 更新回归测试，防止真实生成链路再出现 v0.3 主目标文案。
- 2026-06-25：根据用户纠偏调整质量工程主线：v0.3 不再允许作为生成链路的“辅助模型”或硬拦截依据，只保留为历史遥测/审计对照；旧评分器低于 60/72 不再写入用户可见质量问题，不再触发自动追分二修，不再决定 `quality_failed`。硬拦截改为只基于可解释交付错误：空标题/空正文、标题过短或不自然、正文过短、占位符、结构化事实编造、内部格式污染等。健身 golden 样本从 `fitness_contract_under60_expected_fail` 改为 `fitness_contract_good`，避免 v0.3 域偏差继续污染验收和偏好任务池。后续最高优先级改为训练 `v0.4-composite`：目标来自人工 golden、A/B 偏好、事实可信、自然表达、行业可执行价值和线上反馈，而不是继承 v0.3 分数。
- 2026-06-25：完成本轮 `v0.4-composite` 工程入口。新增 `model/quality_objective.py` 作为统一复合交付目标层，生成 prompt 明确“旧评分器不得作为写作目标”；新增 `docs/V04_COMPOSITE_TRAINING_STRATEGY.md`，规定 v0.4-composite-ranker/regressor/arbiter 三阶段训练、特征组合、候选/生产上线门槛；新增 `tools/v04_composite_readiness.py` 输出训练就绪报告。当前就绪检查结果：`decision=blocked_need_labels`；生产门槛缺口为美食还差 `995` 条 golden、`2997` 组偏好，其余旅行/穿搭/美妆/家居/健身/母婴各还差 `999` 条 golden、`3000` 组偏好。验证记录：`.venv/bin/python -m unittest discover -s tests -p 'test_*.py'` 通过 `65` 项；`.venv/bin/python tools/quality_gate.py quality/golden_notes.sample.json` 通过，`fitness_contract_good` 在旧评分器 `51.5` 下仍 PASS；`python3 -m py_compile model/api.py model/quality_objective.py tools/fill_preference_generation_slots.py tools/v04_composite_readiness.py tools/quality_gate.py` 通过。
- 2026-06-25：开始重点训练 V0.4-composite，并明确“不做假训练”。新增 `model/v04_composite_features.py`，在原 62 维输入信号上扩展到 `124` 维复合商业价值特征，覆盖标题自然度、正文信息密度、CTA、模板风险、自然度模型信号，以及美食/旅行/穿搭/美妆/家居/健身/母婴各自的事实槽位。新增 `model/v04_composite_dataset.py`，将 golden 标签和同任务 A/B 偏好转成训练数据，输出 `model/data/v04_composite/golden_training_rows.parquet` 与 `preference_pair_rows.parquet`；训练列显式排除 `variant_score`、v0.3、CES 等旧评分字段。新增 `model/train_v04_composite.py`，默认标签不足时只输出阻断报告；只有 `--allow-experimental-small-data` 才会训练实验 artifact，且报告固定 `do_not_deploy=true`。当前真实数据：golden `11` 条、已标注偏好 `3` 组、readiness 仍为 `blocked_need_labels`；实验模式生成 `model_v04_composite_regressor_experimental_*.lgb` 与 ready classifier，但状态为 `experimental_not_production`，偏好 ranker 因 3 组偏好全为 B 胜而拒绝训练。同步将历史评分黑客负例 origin 改为 `legacy_score_directed_second_pass`，避免被误认为现行策略。验证记录：`.venv/bin/python -m unittest discover -s tests -p 'test_*.py'` 通过 `72` 项；`.venv/bin/python tools/quality_gate.py quality/golden_notes.sample.json` 通过；`.venv/bin/python tools/composite_quality_report.py quality/golden_labels.v01.seed.json` 通过并改为实验信号不再使用 v0.3 权重；`.venv/bin/python tools/v04_composite_readiness.py` 仍正确阻断生产训练。
- 2026-06-25：继续推进 V0.4 标注生产线，解决“标签不足”不能落地的问题。新增 `tools/export_labeling_batch.py` 可从 golden/preference 队列按优先级、行业和每行业上限导出人工标注批次；新增 `tools/ingest_golden_annotations.py`，校验并合并已完成人工 golden，跳过已在 seed 中存在的 `locked_seed`，默认输出 `quality/golden_labels.v01.merged.json`；新增 `tools/ingest_preference_annotations.py`，校验 A/B winner、margin、reason tags，默认输出 `quality/preference_queue.v01.labeled.jsonl` 和 `quality/preference_labels.v01.merged.json`；新增 `tools/v04_training_data_health.py` 做训练前健康审计，检查行业覆盖、类别平衡、重复文本和候选/生产缺口。训练数据构建器、训练脚本和 readiness 已默认优先读取 merged/labeled 文件。已导出第一批人工标注包：`quality/labeling_batches/v04_round01_golden.csv` 共 `160` 条（美食/旅行/穿搭/健身各 40 条高优先级样本），`quality/labeling_batches/v04_round01_preference.csv` 共 `63` 组美食同任务偏好。健康审计当前仍 `blocked_need_labels`，并明确问题 `preference_winner_single_class_or_empty`。验证记录：`.venv/bin/python -m unittest discover -s tests -p 'test_*.py'` 通过 `76` 项；`.venv/bin/python tools/quality_gate.py quality/golden_notes.sample.json` 通过；`.venv/bin/python model/train_v04_composite.py` 默认使用 merged/labeled 后仍正确阻断，`do_not_deploy=True`。
- 2026-06-25：应用户要求创建 V0.4 商业级模型训练执行总计划，避免训练链路依赖聊天记忆。新增 `docs/V04_TRAINING_EXECUTION_PLAN.md`，按 10 个阶段记录：数据与目标冻结、v0.4 基础管线、composite 特征层、标注生产线、Round01 人工标注、多行业样本补齐、candidate 训练、arbiter 接入、真实链路回归、生产上线门槛。每个阶段包含状态、输入、输出、验收标准和阻断条件；明确每次推进必须同步更新执行计划、策略文档、交付台账和 artifact report。
- 2026-06-25：按用户确认仅推进第一批核心行业，完成 V0.4 产品行业口径收口。新增 `model/v04_domain_policy.py`，冻结核心行业为美食、旅行、穿搭、美妆、家居、健身、母婴，并将 RedNote 研究数据中的“运动/运动健身”统一映射为产品训练口径“健身”。`export_golden_annotation_queue`、`export_labeling_batch`、`ingest_golden_annotations`、`ingest_preference_annotations`、`v04_training_data_health`、`v04_composite_readiness`、`v04_composite_features` 全部接入同一行业口径；新增 `tools/v04_core_domain_gap_report.py` 输出 `model/artifacts/v04_core_domain_gap_report.json`。重建 `quality/annotation_queue.v01.jsonl` 后为 `3820` 条，其中 `3809` 条待标注、`11` 条 locked seed；重导出 `v04_round01_golden` 为美食/旅行/穿搭/健身各 `40` 条，共 `160` 条，不再出现“运动”；`v04_round01_preference` 仍为美食 `63` 组。训练数据构建仍只有 golden `11` 条、preference `3` 组，`model/train_v04_composite.py` 正确输出 `blocked_need_labels`、`do_not_deploy=True`。当前缺口：美妆/家居/母婴当前队列可标注样本为 `0`，必须补样或从原始数据重新细分抽取；旅行/穿搭/健身仍无偏好标注。验证记录：`python3 -m py_compile model/v04_domain_policy.py model/v04_composite_features.py model/v04_composite_dataset.py model/train_v04_composite.py tools/export_golden_annotation_queue.py tools/export_labeling_batch.py tools/ingest_golden_annotations.py tools/ingest_preference_annotations.py tools/v04_training_data_health.py tools/v04_composite_readiness.py tools/v04_core_domain_gap_report.py` 通过；`.venv/bin/python -m unittest discover -s tests -p 'test_*.py'` 通过 `78` 项；`.venv/bin/python tools/quality_gate.py quality/golden_notes.sample.json` 通过。
- 2026-06-25：继续推进第一批核心行业训练数据补齐。新增 `tools/export_core_domain_supplement_queue.py`，从 RedNote 原始真实笔记中挖掘美妆/家居/母婴补样候选；真实运行扫描 `143395` 行，选出 `360` 条候选，美妆/家居/母婴各 `120` 条，并导出 `quality/annotation_queue.v01.core_supplement.jsonl/csv/report.json`。新增 Round01B golden 标注包：`quality/labeling_batches/v04_round01b_golden.csv` 共 `120` 条，美妆/家居/母婴各 `40` 条。重导出 `preference_generation_queue` 后为 7 核心行业各 `9` 个槽位；真实执行 `tools/fill_preference_generation_slots.py --routes analyze,generate,offline_candidate`，完成 `32` 个直接槽位，全部 `ready`、无失败，连同已有 artifact 形成 7 行业各 `6` 个候选。新增 `tools/build_preference_seed_from_generated_artifacts.py`，生成 `quality/preference_pairs.v01.generated_from_artifacts.json`，再导出 `quality/preference_queue.v01.generated.jsonl/csv/report.json`：共 `105` 组 A/B，7 行业各 `15` 组；导出 `quality/labeling_batches/v04_round01b_preference.csv` 共 `105` 组待人工标注。刷新 `model/artifacts/v04_core_domain_gap_report.json` 后，Round01/Round01B 已覆盖 7 行业 golden 各 `40` 条，preference 为美食 `78` 组、其余行业各 `15` 组。训练守门仍正确阻断：当前真实训练标签仍为 golden `11` 条、preference `3` 组，`do_not_deploy=True`。候选质量暴露短板：穿搭/家居较强，美妆中等，健身全部低分（约 `30.9-40.7`），确认健身必须作为 V0.4-composite 专项校准行业。验证记录：`.venv/bin/python -m unittest discover -s tests -p 'test_*.py'` 通过 `79` 项；`.venv/bin/python tools/quality_gate.py quality/golden_notes.sample.json` 通过；相关 `py_compile` 通过。
- 2026-06-25：解决“用户不懂人工标注”的执行问题，但不牺牲训练严谨性。新增 `tools/ai_prelabel_review_batch.py` 生成 AI 预标注审核包，新增 `tools/promote_reviewed_prelabels.py` 仅在 `review_decision=accept_ai/accept_with_edits` 时提升为正式 ingest-ready 标签；新增 `quality/AI_ASSISTED_LABELING_PLAYBOOK.md`，把审核动作简化为填写 `accept_ai`、`accept_with_edits`、`needs_manual`、`reject`。已生成四个审核包：`v04_round01_golden_ai_review.csv`（160 条）、`v04_round01b_golden_ai_review.csv`（120 条）、`v04_round01_preference_ai_review.csv`（63 条）、`v04_round01b_preference_ai_review.csv`（105 条）。安全验证：不填写 `review_decision` 时 promotion 结果为 `0`，不会把 AI 建议误导入训练；AI 预标注只写 `ai_*` 字段，不写正式 `human_quality_score/winner`。验证记录：`.venv/bin/python -m unittest discover -s tests -p 'test_*.py'` 通过 `81` 项；`.venv/bin/python tools/quality_gate.py quality/golden_notes.sample.json` 通过；`python3 -m py_compile tools/ai_prelabel_review_batch.py tools/promote_reviewed_prelabels.py tests/test_ai_prelabel_review_tools.py` 通过。
- 2026-06-25：补充 AI 标注主审边界，回应“为什么让 Claude 做主审”的风险点。结论：Claude 只允许作为商业质量预审/主审候选，因为它能处理长中文笔记、标题读感、正文节奏和行业语境；但不能作为唯一真值，更不能把 Claude 单模型判断伪装成人工标签。已在 `docs/V04_TRAINING_EXECUTION_PLAN.md` 和 `quality/AI_ASSISTED_LABELING_PLAYBOOK.md` 增加硬原则：生成模型不得自证正确；AI 标签必须保留 `label_source`、置信度、理由和分歧标记；低置信、事实不明、v0.4 特征/自然度/事实检查不一致或第二评审分歧大的样本进入 holdout，不进训练。
- 2026-06-25：按用户确认接入 Kimi 作为第二评审。`tools/ai_prelabel_review_batch.py` 新增 `--second-review kimi`，会读取 `model/.env` 中的 `MOONSHOT_API_KEY`，为 golden/preference 生成 `kimi_*` 独立复核字段、`judge_consensus` 和 `judge_disagreement_flags`；首审与 Kimi 在分数、可交付、A/B winner、事实风险或严重失败标签上分歧时，自动标记 `ai_needs_human_review=true` 和 `consensus_label_source=holdout_due_to_judge_disagreement`。`tools/promote_reviewed_prelabels.py` 同步加安全阀：有 Kimi 分歧时 `accept_ai` 不会被提升入训练，必须 `accept_with_edits` 明确覆盖。文档已同步 Kimi 第二评审命令与操作规则。真实 Kimi 冒烟：`.venv/bin/python tools/ai_prelabel_review_batch.py --kind preference --input quality/labeling_batches/v04_round01_preference.csv --batch-id kimi_smoke3 --output-dir tmp_kimi_review_smoke --second-review kimi --limit 1 --json` 成功返回 `second_review_status.ok=1`；该样本首审与 Kimi winner 一致为 B，但 Kimi 对交付就绪更保守，触发 `judge_consensus=disagree`、`judge_disagreement_flags=delivery_ready_mismatch`，安全进入 holdout。修复 Moonshot 400 参数问题：Kimi K2.5 只允许 `temperature=0.6`。验证记录：`python3 -m py_compile tools/ai_prelabel_review_batch.py tools/promote_reviewed_prelabels.py tests/test_ai_prelabel_review_tools.py` 通过；`.venv/bin/python -m unittest discover -s tests -p 'test_*.py'` 通过 `83` 项；`.venv/bin/python tools/quality_gate.py quality/golden_notes.sample.json` 通过。
- 2026-06-25：执行 Round01/Round01B Kimi 第二评审与 AI-rubric consensus 导入。四个批次共 `448` 条/组进入 Kimi 复核；初次 Kimi 成功 `438`、失败 `10`，失败样本已用 JSON mode + 修复重试补跑成功。双评审一致 `76` 条/组；golden 自动提升时新增空标题/空正文保护，最终提升 `34` 条 golden；preference 合并已有 3 组种子后共 `39` 组，winner 分布 `A=17/B=22`，不再单类。正式导入结果：`quality/golden_labels.v01.merged.json` 当前 `45` 条（含 11 seed），`quality/preference_queue.v01.labeled.jsonl` 当前 `39` 组。健康审计：无重复文本、无结构错误，但仍 `blocked_need_labels`，因为各核心行业远低于 candidate/production 门槛；默认训练守门仍 `do_not_deploy=True`，实验训练只输出 `experimental_not_production`。关键 artifact：`quality/review_packets/promoted/v04_kimi_consensus_summary.json`、`model/artifacts/model_v04_composite_train_report.json`、`model/artifacts/v04_core_domain_gap_report.json`。验证记录：`python3 -m py_compile tools/promote_reviewed_prelabels.py tools/ingest_golden_annotations.py tools/ingest_preference_annotations.py tests/test_ai_prelabel_review_tools.py` 通过；`.venv/bin/python -m unittest discover -s tests -p 'test_*.py'` 通过 `85` 项；`.venv/bin/python tools/quality_gate.py quality/golden_notes.sample.json` 通过。
- 2026-06-25：执行 Round02 focused preference 扩样，优先补齐低共识行业美妆/健身/母婴。新增 `quality/preference_task_pool.v02.focus.json`，共 `9` 个任务、`54` 个生成槽位；导出 `quality/preference_generation_queue.v02.focus.jsonl/csv/report.json`，真实填充 `quality/generated_variants/v02_focus/` 后 `54/54 ready`、失败 `0`。组装 `quality/preference_queue.v02.focus.jsonl` 共 `135` 组 A/B，三类各 `45` 组。真实生成暴露两个质量点：健身旧分数继续系统性偏低（18 个候选最高 `48.794`，但无致命硬错），必须作为 V0.4 行业校准重点；部分标题压缩仍出现“6小时不明显斑”“找对发力感最关”等语义半截，已修复 `_repair_dangling_title_tail()` 与断词检测，新增回归测试。Kimi 第二评审主批成功 `134/135`，失败 `1` 为 Moonshot 429 过载，重试成功；严格完全共识仅 `4` 组，主要因 `delivery_ready_mismatch`。为避免可用 A/B preference 被交付就绪保守度差异全部丢弃，新增显式 `--auto-accept-winner-consensus`：仅 preference 可用，要求 Claude/Kimi 同选 A/B winner、Kimi ok、无 winner_mismatch/second_review_failed/low_kimi_confidence、Kimi confidence ≥`0.7`，并使用 Kimi 的 `delivery_ready_winner`；标签来源写为 `ai_rubric_winner_consensus`，不伪装成人工或完全共识。Round02 最终提升 `79` 组 preference，合并 Round01 后 `quality/preference_queue.v01.labeled.jsonl` 当前 `118` 组，行业分布为美食16、旅行5、穿搭7、美妆23、家居10、健身31、母婴26，winner 分布 `A=50/B=68`，导入错误 `0`、警告 `0`。默认训练仍 `blocked_need_labels`、`do_not_deploy=True`；实验训练为 `experimental_not_production`。关键 artifact：`quality/review_packets/promoted/v04_round02_focus_summary.json`、`quality/review_packets/promoted/v04_round01_round02_all_preference_promoted.jsonl`、`model/artifacts/model_v04_composite_train_report.json`。验证记录：`.venv/bin/python -m unittest discover -s tests -p 'test_*.py'` 通过 `87` 项；`.venv/bin/python tools/quality_gate.py quality/golden_notes.sample.json` 通过；`python3 -m py_compile model/api.py tools/promote_reviewed_prelabels.py tools/ai_prelabel_review_batch.py tools/ingest_preference_annotations.py tools/fill_preference_generation_slots.py tests/test_api_contracts.py tests/test_ai_prelabel_review_tools.py` 通过。
- 2026-06-25：执行 Round03 focused preference 扩样，优先补齐美食/旅行/穿搭/家居四个核心行业。新增 `quality/preference_task_pool.v03.focus.json`，共 `12` 个任务、`72` 个生成槽位；导出 `quality/preference_generation_queue.v03.focus.jsonl/csv/report.json`，真实填充 `quality/generated_variants/v03_focus/` 后 `72/72 ready`、失败 `0`。真实候选均分 `67.107`，按行业为美食 `65.293`、旅行 `67.777`、穿搭 `65.129`、家居 `70.227`，家居表现相对最好；穿搭缺价格/预算、旅行缺交通/路线/预算仍是明显短板。组装 `quality/preference_queue.v03.focus.jsonl` 共 `180` 组 A/B，四类各 `45` 组；Kimi 第二评审成功 `180/180`，严格共识 `28` 组，winner 同选 `90` 组。按 winner-consensus 安全规则提升 `86` 组 preference，合并 Round01+Round02 后正式偏好标签达到 `204` 组，行业分布为美食30、家居36、旅行22、母婴26、穿搭36、美妆23、健身31，winner 分布 `A=91/B=113`，导入错误 `0`、警告 `0`。同步修复跨行业标题压缩半截问题，覆盖“广州亲子酒店地铁8分钟”“梨形通勤显高遮胯”“175男生通勤黑白灰蓝显干净”“阳台洗衣区收纳动线”等 Round03 暴露样例；核心缺口报告终端输出新增 `labeled_golden/labeled_pref`，避免只看待标注队列误判进度。当前健康审计仍 `blocked_need_labels`：golden 只有 `45` 条，preference `204` 组，远低于商业候选/生产门槛；默认训练继续 `do_not_deploy=True`，实验训练为 `experimental_not_production`，不能上线。关键 artifact：`quality/review_packets/promoted/v04_round03_focus_summary.json`、`quality/review_packets/promoted/v04_round01_round02_round03_all_preference_promoted.jsonl`、`quality/preference_queue.v01.labeled.jsonl`、`model/artifacts/model_v04_composite_train_report.json`、`model/artifacts/v04_core_domain_gap_report.json`。验证记录：`.venv/bin/python -m unittest discover -s tests -p 'test_*.py'` 通过 `87` 项；`.venv/bin/python tools/quality_gate.py quality/golden_notes.sample.json` 通过；`python3 -m py_compile model/api.py tools/promote_reviewed_prelabels.py tools/ai_prelabel_review_batch.py tools/ingest_preference_annotations.py tools/fill_preference_generation_slots.py tools/v04_core_domain_gap_report.py tests/test_api_contracts.py tests/test_ai_prelabel_review_tools.py` 通过。
- 2026-06-25：执行 Round04 focused golden 扩样，开始补齐 V0.4-composite 最关键的绝对质量标签。先修复标注导出工程：`tools/export_labeling_batch.py` 新增 `--exclude-ids-from`，可排除历史 batch、review packet、promoted 文件和 merged label 文件中的 `annotation_id/pair_id`，避免重复审同一批样本；新增回归测试覆盖。导出 Round04a golden：美食/旅行/穿搭/健身各 `40` 条，共 `160` 条，Kimi 第二评审 `160/160 ok`、严格共识 `40` 条；导出 Round04b golden：美妆/家居/母婴各 `40` 条，共 `120` 条，Kimi 第二评审 `120/120 ok`、严格共识 `20` 条。Golden 晋级继续只接受 `judge_consensus=agree` 且无分歧标记，实际提升 `48` 条（Round04a `31`、Round04b `17`），其余 `232` 条保留 holdout。合并时发现 `tools/promote_reviewed_prelabels.py` 把数值 `0` 清洗为空字符串，导致低自然度负样本不能导入；已修复并新增测试，确认 `naturalness_label=0` 会被保留。最终 `quality/golden_labels.v01.merged.json` 达到 `93` 条，按行业为美食22、旅行13、穿搭15、美妆12、家居10、健身17、母婴4；preference 保持 `204` 组，winner 分布 `A=91/B=113`。健康审计无重复文本、无结构错误，但仍 `blocked_need_labels`，默认训练 `do_not_deploy=True`；实验训练仍 `experimental_not_production`。关键 artifact：`quality/review_packets/promoted/v04_round04_golden_summary.json`、`quality/review_packets/v04_round04a_kimi_golden_ai_review.report.json`、`quality/review_packets/v04_round04b_kimi_golden_ai_review.report.json`、`quality/golden_labels.v01.merged.round04a.retry.report.json`、`quality/golden_labels.v01.merged.round04b.report.json`、`model/artifacts/v04_training_data_health.json`、`model/artifacts/model_v04_composite_train_report.json`、`model/artifacts/v04_core_domain_gap_report.json`。验证记录：`.venv/bin/python -m unittest discover -s tests -p 'test_*.py'` 通过 `89` 项；`.venv/bin/python tools/quality_gate.py quality/golden_notes.sample.json` 通过；`python3 -m py_compile model/api.py tools/export_labeling_batch.py tools/promote_reviewed_prelabels.py tools/ai_prelabel_review_batch.py tools/ingest_golden_annotations.py tools/ingest_preference_annotations.py tools/v04_training_data_health.py tools/v04_composite_readiness.py tools/v04_core_domain_gap_report.py model/train_v04_composite.py tests/test_labeling_pipeline_tools.py tests/test_ai_prelabel_review_tools.py` 通过。
- 2026-06-25：执行 Round05 focused golden 扩样，继续补齐 V0.4-composite 绝对质量标签，并扩容低覆盖行业样本池。新增 `quality/annotation_queue.v01.core_supplement.v02.jsonl/csv/report.json`，美妆/家居/母婴各 `300` 条候选；真实可用候选为美妆 `1283`、家居 `329`、母婴 `1782`，无 shortfall。导出 Round05a golden：美食/旅行/穿搭/健身各 `40` 条，共 `160` 条，Kimi 第二评审 `160/160 ok`、严格共识 `41` 条，实际提升 `39` 条；导出 Round05b golden：美妆/家居/母婴各 `60` 条，共 `180` 条，Kimi 第二评审 `180/180 ok`、严格共识 `15` 条，实际提升 `13` 条，其余 `288` 条保留 holdout。合并后 `quality/golden_labels.v01.merged.json` 达到 `145` 条，按行业为美食31、旅行17、穿搭33、美妆14、家居17、健身25、母婴8；preference 保持 `204` 组。同步修复 `tools/v04_core_domain_gap_report.py` 默认补样报告选择：若 v02 报告存在则读取 v02，避免低覆盖行业队列容量仍显示旧的 `120`。健康审计无重复文本、无结构错误，但仍 `blocked_need_labels`，默认训练 `do_not_deploy=True`；实验训练仍 `experimental_not_production`。当前最大短板是母婴 Golden 仅 `8` 条，低覆盖行业共识率明显偏低，后续需继续专项抽样与 rubric 校准。关键 artifact：`quality/review_packets/promoted/v04_round05_golden_summary.json`、`quality/annotation_queue.v01.core_supplement.v02.report.json`、`quality/review_packets/v04_round05a_kimi_golden_ai_review.report.json`、`quality/review_packets/v04_round05b_kimi_golden_ai_review.report.json`、`quality/review_packets/promoted/v04_round05a_kimi_consensus_golden_promoted.report.json`、`quality/review_packets/promoted/v04_round05b_kimi_consensus_golden_promoted.report.json`、`quality/golden_labels.v01.merged.round05a.report.json`、`quality/golden_labels.v01.merged.round05b.report.json`、`model/artifacts/v04_training_data_health.json`、`model/artifacts/model_v04_composite_train_report.json`、`model/artifacts/v04_core_domain_gap_report.json`。验证记录：`.venv/bin/python -m unittest discover -s tests -p 'test_*.py'` 通过 `89` 项；`.venv/bin/python tools/quality_gate.py quality/golden_notes.sample.json` 通过；`python3 -m py_compile model/api.py tools/export_core_domain_supplement_queue.py tools/export_labeling_batch.py tools/promote_reviewed_prelabels.py tools/ai_prelabel_review_batch.py tools/ingest_golden_annotations.py tools/ingest_preference_annotations.py tools/v04_training_data_health.py tools/v04_composite_readiness.py tools/v04_core_domain_gap_report.py model/train_v04_composite.py tests/test_labeling_pipeline_tools.py tests/test_ai_prelabel_review_tools.py` 通过。
- 2026-06-25：执行 Round06 美食/酒旅弱项专项。导出 `quality/labeling_batches/v04_round06_food_travel_golden.jsonl/csv/report.json`，美食/旅行各 `60` 条，共 `120` 条；新增 `quality/preference_task_pool.v06.food_travel.json`，覆盖 `8` 个美食/酒店/路线任务、`48` 个生成槽位。真实生成多轮复跑发现标题后处理仍会留下半截尾巴（如“11点半前来排”“微辣锅底+必”“低龄娃泡”“吃喝地铁”），已扩展 `_repair_dangling_title_tail()` 与 `_title_readability_issues()`，并在 `tools/build_preference_seed_from_generated_artifacts.py` 增加训练入口标题可读性过滤，确保高分但半截标题不会进入偏好训练。最终生成 `48/48 ready`、失败 `0`；训练种子过滤 blocking/标题问题后保留 `33` 个候选，导出 `quality/preference_queue.v06.food_travel.jsonl` 共 `59` 组 A/B（美食 `36`、旅行 `23`）。偏好 Kimi 第二评审 `59/59 ok`，严格共识仅 `5`，按 winner-consensus 安全晋级 `22` 组（美食 `16`、旅行 `6`），其余 `37` 组 holdout；Golden Kimi 第二评审 `120/120 ok`，严格共识 `16`，实际晋级 `12` 条（美食 `8`、旅行 `4`），其余 `108` 条 holdout。合并后 `quality/golden_labels.v01.merged.json` 达 `157` 条，`quality/preference_queue.v01.labeled.jsonl` 达 `226` 组；美食当前 Golden `39` / Preference `46`，旅行当前 Golden `21` / Preference `28`。健康审计和 readiness 仍 `blocked_need_labels`，默认训练正确拒绝训练生产模型，`do_not_deploy=True`。新增 `quality/quality_gate_cases.v04_round06_food_travel.json`，覆盖美食/酒旅正向样本和半截标题反例；质量 gate 结果：美食正向 `78.2` PASS，酒旅正向 `73.2` PASS，两个半截标题即使分数 `72+` 也被 blocking 拦截。验证记录：`.venv/bin/python -m unittest discover -s tests` 通过 `89` 项；`find model tools tests -name '*.py' -print0 | xargs -0 python3 -m py_compile` 通过；`.venv/bin/python tools/quality_gate.py quality/quality_gate_cases.v04_round06_food_travel.json --json` 通过；`model/train_v04_composite.py --json` 输出 `blocked_need_labels`、`candidate_ready=false`、`production_ready=false`。
- 2026-06-25：执行 Round07 事实源专项，落实用户口径：训练阶段不学习“未提供/不能编造/缺失字段”提示，餐饮/本地生活用高德事实补全，酒店/旅行攻略使用美团 `meituan-travel` Skill 事实。先修复事实工程风险：本地核验事实必须命中门店/品牌身份词，不能只凭城市/商圈串库；`北京路/南京西路` 等路名不再误判为城市；`meituan-travel` 独立默认超时提升到 `45s`。真实事实源烟测：长禧家、点都德、陶陶居、蜀大侠均能从高德拿到地址、人均、营业时间、评分、推荐菜等；美团 Skill 能返回三亚亚龙湾亲子酒店、广州长隆亲子酒店、成都太古里酒店、北京国贸商务酒店、杭州西湖二日游路线/湖滨住宿等事实素材。新增 `quality/preference_task_pool.v07.food_travel_fact_sources.json`，共 `9` 个任务、`54` 个生成槽位（美食 `24`、旅行/酒店/攻略 `30`）；真实填充 `54/54 ready`、失败 `0`，均分 `66.464`，`4` 篇达到旧 72 参考线，最高为杭州西湖路线 `75.375`。质量扫描发现美食大量出现“实用信息：地址：...”板块化写法、标题断尾（如“人均1”“这4”“地铁距离”“价格差”），已把这些加入标题可读性回归和训练 seed 过滤；`tools/build_preference_seed_from_generated_artifacts.py` 现在过滤内部格式污染和板块化事实段，避免训练学到机械事实槽位。过滤后只保留 `5` 个任务、`19` 个候选，导出 `33` 组 A/B，其中旅行 `32`、美食 `1`；Kimi 第二评审 `33/33 ok`，严格一致 `1`，按 winner-consensus 安全晋级 `6` 组，其余 `27` 组 holdout。合并后 `quality/preference_queue.v01.labeled.jsonl` 达 `232` 组；美食当前 Golden `39` / Preference `47`，旅行当前 Golden `21` / Preference `33`。健康审计与训练守门仍 `blocked_need_labels`、`do_not_deploy=True`，不能部署生产模型。结论：美团 Skill 对酒店/旅行攻略事实素材价值明确，能产出 72+ 候选；高德解决餐饮真实性，但当前美食生成仍明显偏板块化和标题断尾，下一步需要优化美食生成表达策略，而不是盲目扩大入库。关键 artifact：`quality/preference_task_pool.v07.food_travel_fact_sources.json`、`quality/generated_variants/v07_food_travel_fact_sources/latest_report.json`、`quality/preference_pairs.v07.food_travel_fact_sources.generated_from_artifacts.report.json`、`quality/review_packets/promoted/v04_round07_food_travel_fact_sources_kimi_consensus_preference_promoted.report.json`、`quality/preference_labels.v01.merged.round07_food_travel_fact_sources.report.json`。验证记录：`.venv/bin/python -m unittest discover -s tests -p 'test_*.py'` 通过 `94` 项；`find model tools tests -name '*.py' -print0 | xargs -0 python3 -m py_compile` 通过；`.venv/bin/python tools/quality_gate.py quality/quality_gate_cases.v04_round06_food_travel.json --json` 通过；`model/train_v04_composite.py --json` 输出 `blocked_need_labels`、`candidate_ready=false`、`production_ready=false`。
- 2026-06-25：执行 Round08b 美食表达质量专项，目标是解决 Round07 暴露的“事实源真实但正文板块化、标题半截、模型分高但人感不自然”。修复内容：`_safe_fact_line()` 从“实用信息：地址/人均/营业时间”改为自然决策句；高德事实值进入生成前先清洗重复标签，避免“门店地址在地址：”“营业时间营业时间：”；同一事实槽位多行时跳过“门店名”伪地址，优先取真实地址；正文已自然写出营业时间范围时不再重复追加事实句；新增旧污染段清理，删除“门店地址在门店名：...”整句；标题 exact repair 覆盖 v08/v08b 新样例（如“稳得”“这样点不”“人均98”“第一次来怎么点才不”）；放宽真实店名/早茶/火锅语境下“人均86元很稳”的误杀，但继续拦截无对象的价格+推荐拼接；新增“吃到饱/不限量/管饱”事实边界，除非来源明确自助/不限量，否则会被清洗为“点心选择不少”。`tools/build_preference_seed_from_generated_artifacts.py` 也改为把清洗后的标题/正文/重算分数写入 seed，防止训练直接吃旧 artifact 脏句。真实重跑 `quality/generated_variants/v08b_food_expression_probe/`：美食 `24/24 ready`、失败 `0`，最高 `76.029`，`3` 篇达到旧 72 参考线，`6` 篇 ≥70；正文污染扫描 `实用信息/门店地址在地址/营业时间营业时间/用户未提供/不能编造` 全部为 `0`。人工抽检发现 76 分点都德样本含“人均86元吃到饱”，已纳入事实边界；塑形后该样本去除“吃到饱”，重算约 `80.895`。过滤阻断样本后保留 `21` 个美食候选，导出 `46` 组 A/B；Kimi 第二评审 `46/46 ok`，严格一致 `22`，按 winner-consensus 且 Kimi 置信度 ≥`0.75` 安全晋级 `26` 组，其余 `20` 组 holdout。合并后 `quality/preference_queue.v01.labeled.jsonl` 达 `258` 组，美食 Preference 达 `73`；健康审计仍 `blocked_need_labels`，生产训练继续 `do_not_deploy=True`，不能部署生产模型。关键 artifact：`quality/generated_variants/v08b_food_expression_probe/latest_report.json`、`quality/preference_pairs.v08b.food_expression.generated_from_artifacts.report.json`、`quality/preference_queue.v08b.food_expression.report.json`、`quality/review_packets/v04_round08b_food_expression_preference_ai_review.report.json`、`quality/review_packets/promoted/v04_round08b_food_expression_preference_promoted.report.json`、`quality/preference_labels.v01.merged.round08b_food_expression.report.json`。验证记录：`python3 -m py_compile model/api.py tests/test_api_contracts.py tools/build_preference_seed_from_generated_artifacts.py` 通过；新增事实清洗、标题修复、吃到饱边界、标签拆分回归测试通过；`.venv/bin/python tools/v04_training_data_health.py --json` 输出 `preference_pair_rows=258`、`美食 preference_pairs_labeled=73`、`decision=blocked_need_labels`。
- 2026-06-25：执行 Round09 酒旅/旅行表达专项，落实用户口径：餐饮/本地生活训练阶段不学习“未提供/不能编造”提示，真实链路由高德补事实；酒店/旅行攻略使用美团 `meituan-travel` 事实素材。修复内容：旅行/酒店/住宿/酒旅别名统一到旅行规则；生成 brief 区分酒店对比与路线攻略，注入预算、交通、设施、评分、路线窗口等事实位；旅行安全事实策略禁止无依据“最低价/最划算/必住/提前预订更便宜”；Claude 非流式调用增加硬超时，避免无响应卡住；Markdown 清理修复为只处理真正 `# 标题`，保留小红书 `#话题标签`；旅行标题断尾修复覆盖“预算/交通/地铁/通勤/灵隐寺”等常见半截。重刷 `quality/generated_variants/v09_travel_expression_probe/` 后 30 个候选全部可用，`14` 篇 ≥72，`22` 篇 ≥70，低于 60 和 blocking 均为 `0`，Markdown/无依据价格承诺/标题断尾扫描均为 `0`。重新导出 `75` 组旅行 A/B；Claude 主审 + Kimi 二审 `75/75 ok`，双方 winner 一致 `23` 组，按 Kimi 置信度 ≥`0.75` 安全晋级 `14` 组，其余 `61` 组 holdout。重建 merged preference 时补回最早 `3` 条 locked seed，当前总 preference `272` 组，旅行 preference `47` 组，美食 preference `73` 组；Golden 仍为 `157` 条。健康审计、readiness、训练守门均为 `blocked_need_labels`，`do_not_deploy=true`，不能部署生产模型。关键 artifact：`quality/review_packets/promoted/v09_travel_expression_summary.json`、`quality/preference_pairs.v09.travel_expression.generated_from_artifacts.report.json`、`quality/preference_queue.v09.travel_expression.report.json`、`quality/review_packets/v09_travel_expression_preference_ai_review.report.json`、`quality/review_packets/promoted/v09_travel_expression_preference_promoted.report.json`、`quality/preference_labels.v01.merged.round09_travel_expression.report.json`。验证记录：`find model tools tests -name '*.py' -print0 | xargs -0 python3 -m py_compile` 通过；`.venv/bin/python -m unittest discover -s tests -p 'test_*.py'` 通过 `104` 项；`.venv/bin/python tools/quality_gate.py quality/quality_gate_cases.v04_round06_food_travel.json --json` 通过；`.venv/bin/python model/train_v04_composite.py --json` 输出 `blocked_need_labels`、`candidate_ready=false`、`production_ready=false`。

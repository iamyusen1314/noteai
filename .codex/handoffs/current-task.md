# NoteAI Production Rollout Handoff

> Updated: 2026-07-27 (Asia/Shanghai)
>
> Scope: `PROD-COMPLETE-FIRST-LAUNCH-001` remains the sole umbrella task. The owner has delegated CTO authority to approve and continue every finite, bounded and reversible task required to reach `internal deployment readiness = 100%` without repeated approval. Public DNS cutover, real-user traffic, irreversible destruction, uncapped cost, a new product decision and any public-launch-complete claim remain prohibited. Security/compliance H17–H22/R22/R23 and the disposable PostgreSQL rehearsal remain `PASS / 0C / 0H / 0M`. Tracking, Trends, Durable AI, private storage/recovery, payment, UI/Admin, the dedicated Admin database-role contract and self-hosted browser assets each retain verified repository evidence. Exact source `b06671fbcca51f884b04c86edcf116e373c6cfa8` has separate verified GitHub-source and isolated-builder five-role native AMD64 identity/SBOM/VEX bundles; five immutable ACR manifest digests are bound, but none is deployed. The fail-closed ledger is now repository/isolated `12/12 = 100%`, internal deployment `14/29 = 48%` and complete public launch `14/38 = 37%`. Production Gate 0 is `VERIFIED`: the bounded TEMP ACL correction completed with zero business-row writes, the accepted exact `0001`–`0008` proof was bound without granting runtime migration-ledger access, and the final Secret-free read-only artifact passed offline validation. ACR product-managed PrivateZone resolution is restored on both API nodes, RDS data/log backup retention is fourteen days and RDS service-key disk encryption is enabled. API-C/API-F passed loopback health/isolation checks. Production remains at migrations `0001`–`0008`; the managed-RDS Admin-name collision is fixed only in repository commit `b06671f`.
>
> Repository checkpoint: current application revision `b06671fbcca51f884b04c86edcf116e373c6cfa8` is pushed and both ordinary CI runs pass. Native AMD64 workflow `30233565859` built/scanned that exact revision and uploaded the raw evidence; the expected raw zero-Critical/High enforcement alone failed closed. The isolated builder later published five exact ACR roles with canonical evidence retained for fourteen days. Both identity sets have separate reduced evidence/VEX/review bundles; CycloneDX 1.6 schema validation returns zero errors. Historical evidence commit `bccbb3ad135df3ca0b0aac04af5e57522f4bf039` remains scoped only to `2fa3a55`. Earlier Tracking, Trends, Durable AI, storage, payment, UI/Admin, Admin-role, browser-asset and readiness-ledger checkpoints remain recorded below. No checkpoint authorized Render or production deployment.
>
> Evidence precedence: current Git/CI and public health checks > current cloud control-plane reads > previously captured control-plane evidence > conversation recollection. Anything not re-observed after the interruption is explicitly marked `INVESTIGATING` or uncertain.

### Current verified production preflight checkpoint — `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-PROD-PREFLIGHT-001`

- **Disposition:** `COMPLETE / VERIFIED / ZERO BUSINESS WRITE`. Internal deployment readiness is `14/29 = 48%`; complete public launch readiness is `14/38 = 37%`.
- **Bounded ACL closure:** `PROD-FIRST-LAUNCH-PREFLIGHT-TEMP-ACL-CLOSE-001` revoked only database `TEMPORARY` from `PUBLIC` in one transaction after proving the exact `0001`–`0008` ledger. `noteai_app` and `noteai_xhs` changed from effective TEMP=true to false while CREATE remained false. There was no migration or business-row write; the exact rollback was available but not needed.
- **Fresh runtime/control-plane evidence:** API-C and API-F collectors exit `0`; historical API/Admin services remain enabled, healthy and loopback-only. ACR private routing, RDS PostgreSQL 16 HA/SSL/encryption/backups, capacity, env-file permissions and edge absence pass. Unexpected containers/listeners, mutable references, temporary auth/processes, public traffic and active terminal connections are zero.
- **Fresh database evidence:** the final bounded collector forces session and transaction read-only mode, executes 15 predefined aggregate queries, rolls back and closes. All source blockers and informational counts, effective-role mismatches, ownership/elevation and unexpected grants are zero. The runtime application role retains no migration-ledger SELECT; the independently accepted exact `0001`–`0008` hashes are bound as `pinned_legacy_versions`.
- **Evidence:** `deploy/production/evidence/production-preflight-b06671f.json` passes `tools/production_readonly_preflight_gate.py`. The artifact contains no Secret, user row value, connection string, host identity or network address.
- **Cleanup:** the short-lived privileged account, ephemeral transport key and all temporary cloud files were removed; the pre-existing administrator password was neither reset nor exposed. The final read-only observation window recorded zero cloud/service/database/registry/provider/traffic mutation.
- **Checkpoint delta:** adds the sanitized evidence artifact; promotes only `production_readonly_preflight` in the readiness manifest; refreshes its focused test, evidence document, Handoff and risk register. No application code, image, service or cloud configuration is changed by this checkpoint.
- **Checkpoint verification:** focused preflight/readiness `14/14`; full Python `957` with `24` explicit-environment skips; production readiness `103/103`; quality gate, Python compilation, JSON validation and `git diff --check` pass.
- **Next dependency root:** `PROD-FIRST-LAUNCH-PRODUCTION-SCHEMA-ROLES-001`. Production still has only migrations `0001`–`0008`; migrations/roles `0009`–`0016` remain unapplied.

### Current production schema/role executor checkpoint — `PROD-FIRST-LAUNCH-PRODUCTION-SCHEMA-ROLES-001`

- **Disposition:** `REPOSITORY + DISPOSABLE POSTGRESQL VERIFIED / PRODUCTION ATTEMPT ROLLED BACK / NOT DEPLOYED / 0C / 0H / 0M`. Production remains at migrations `0001`–`0008`; readiness remains `14/29 = 48%`.
- **Production fail-closed evidence:** the first bounded invocation reached its 600-second execution timeout and was not retried. An independent forced-read-only catalog audit then proved the complete rollback: eight historical runtime roles, zero new roles, zero new tables, zero new-role elevation, zero new-table access and zero business writes. The application/Admin runtime role correctly cannot read `schema_migrations`; a separately authenticated privileged preflight exposed SQLSTATE `42703` because the legacy production ledger had no `sha256` column.
- **Root cause and repair:** the executor selected `version,sha256` before upgrading the accepted legacy `0001`–`0008` ledger. The current fail-closed repair adds the hash column and exact format constraint before that select, backfills only the eight pinned reviewed hashes, makes the column non-null, and validates the resulting contract. It does not relax migration identity, privilege or table-inventory gates.
- **Bounded implementation:** `tools/production_schema_roles.py` requires the exact task confirmation and a process-only privileged DSN. It snapshots exact migrations, accepts only ledger `0001`–`0008` or already-complete `0001`–`0016`, upgrades the accepted legacy ledger, applies `0009`–`0016` plus the credential-free ACL in one transaction, and emits only fixed sanitized counters. It never calls `render_predeploy.py`, seeds managed prompts, enables a provider or starts a service.
- **Role isolation:** `scripts/postgres/noteai_production_runtime_roles.sql` creates six new identities as `NOLOGIN`, preserves the existing `noteai_app`/`noteai_xhs` credential state, rejects elevation/membership/ownership and applies the exact API, Admin, Payment, Durable AI, Tracking and Trends table/column/sequence matrix. Database TEMP/CREATE, schema CREATE, migration-ledger DML, grant option and direct trigger-function execution remain denied.
- **Disposable legacy-ledger proof:** exact PostgreSQL `16.14-alpine` began with eight digestless ledger rows. An unexpected-table negative failed before commit and rolled back the hash column, table and role deltas to zero. The success path backfilled exactly eight pinned hashes, applied `0009`–`0016`, added two fixed service-state seeds and returned the same independent read-only matrix: 16 hashes, 8 runtime roles, 56 tables, 5 sequences, 3,136 table checks and 9,728 column checks. Retention backfill and existing business-row updates were zero; a second apply performed zero writes.
- **Verification:** focused schema/readiness `32/32`; full Python `964` with `24` explicit-environment skips; production readiness `105/105`; quality, internal-readiness, compilation and diff checks pass.
- **Cleanup/impact:** the temporary RDS Super account, ephemeral RSA material and exact API-C/Cloud Shell task files were removed; RDS returned to three accounts, one Super and zero task accounts. Exact tmpfs PostgreSQL container/volume residue is zero and Colima returned to stopped baseline. No provider, registry, image, service, public edge or traffic action occurred; the failed production transaction left zero database change.
- **Repository checkpoint:** the legacy-ledger repair is committed and pushed at `e5883abc01c4b009907bee550209d7036d383771`; the branch matched its remote and the worktree was clean before the live resumption note below.
- **Live resumable state after the Cloud Shell reconnect — not acceptance evidence:** a fresh successful RDS backup was confirmed before staging. Current control-plane aggregates show one PostgreSQL instance, four accounts/two Super accounts including exactly one available task-scoped temporary Super account. API-C retains the protected task directory, exact executor source, preflight gate, encrypted credential, private key and account reference; `runner.env` is absent, the maintenance-container count is zero, and the managed API/Admin services remain active. The exact registry-evidence bundle and main runner remain in Cloud Shell, but the registry evidence has not yet been transferred into the API-C source tree. Import-time attempts stopped before `_connect()`, so database connections, transaction attempts and business writes remain zero.
- **Next exact step:** transfer and hash-verify only the missing exact registry evidence, require the zero-database import preflight to pass, repeat the aggregate pre-dispatch invariants, then execute at most one newly authenticated production transaction followed by independent read-only verification and a reduced Secret-free evidence checkpoint. Do not retry automatically after any database connection. Failure before commit rolls back; after success, leave the additive schema dormant and all six new roles `NOLOGIN`. Always remove the task account, protected task directory and Cloud Shell files after evidence capture.

### Current authoritative Admin runtime role collision checkpoint — `PROD-FIRST-LAUNCH-ADMIN-RUNTIME-ROLE-COLLISION-001`

- **Disposition:** `COMPLETE / PASS / REPOSITORY + DISPOSABLE POSTGRESQL VERIFIED / NOT DEPLOYED / 0C / 0H / 0M` at `b06671fbcca51f884b04c86edcf116e373c6cfa8`.
- **Production fact:** authenticated read-only role metadata proved that the managed RDS administrator named `noteai_admin` owns database objects and has management elevation; it must never be injected into the Admin application.
- **Fix:** additive migration `0016_admin_runtime_role_collision.sql` SHA-256 `5cdd8dc0…fde6` rebinds the existing Admin/payment read policies to `noteai_admin_runtime` without rewriting migrations `0014` or `0015`. The renamed credential-free operator ACL SHA-256 is `c30c551b…d1d6`. HTTP Admin username/token semantics remain unchanged.
- **Verification:** focused repository `27/27`; isolated PostgreSQL Admin `4/4`, Payment `6/6`, Private Storage `4/4` across independent databases; full Python `945` with `24` explicit-environment skips; readiness `102/102`, quality, compile, Compose and diff passed. The first combined PostgreSQL command exposed only a test-database isolation assumption; independent database reruns passed without changing a gate.
- **Cleanup/impact:** the exact temporary PostgreSQL container was removed and Colima returned to stopped baseline. No production database/ACL/credential/image/service/provider/traffic action; cost `¥0`.
- **Production boundary:** production still contains migrations `0001`–`0008`; `0009`–`0016`, creation/ACL/credential injection for `noteai_admin_runtime`, image publication and Admin promotion remain separate controlled tasks.

### Current authoritative UI/Admin checkpoint — `PROD-FIRST-LAUNCH-UI-ADMIN-CONTRACT-001`

- **Disposition:** `COMPLETE / PASS / REPOSITORY VERIFIED / NOT DEPLOYED / 0C / 0H / 0M` at application commit `d358114e37c1e85e0ed068c1aca3ff87bfc36b6f`.
- **User contract:** the Diagnosis share action now produces a local 1080×1440 PNG and either invokes the operating-system file share or downloads it; it sends no NoteAI/supplier request and excludes note text, account identity and URL. Pricing, 30-day cycle, archive lifetime, priority-lane and Studio wording now match the accepted product contract.
- **Admin contract:** production/cloud mode advertises authenticated fail-closed capabilities, rejects user/credit/plan, Prompt, model, Crawler and Tracking mutations with HTTP `409` before database/file/process work, and exposes read-only Durable AI operational status. User phone/email are masked, provider payment references are absent, production wildcard CORS is rejected, bearer tokens are tab-scoped and only SHA-256 digests are persisted.
- **Verification:** focused UI/Admin `13/13`, related suites `181/181` and `64/64`, full serial Python `895` with `20` explicit-environment skips, full serial browser `68/68`, readiness `100/100`, quality, Python/JavaScript compilation, normal and synthetic production Compose, diff and secret-safe checks passed. One parallel Python deadline assertion and one parallel browser timing assertion passed in isolation and in the serial full reruns; no gate was relaxed.
- **External impact:** no provider, production database, cloud, image, ACR, service, ALB/TLS/DNS or traffic call/change; cost `¥0`.
- **Deployment limit:** the current production Admin still uses its historical image/role/read-only configuration. The repository now requires a dedicated `noteai_admin` role with session-only DML plus exact operational reads, but its RLS/ACL and disposable-PostgreSQL negative matrix have not yet been implemented or applied.
- **Successor evidence:** the original RLS/ACL matrix is verified by the Admin role checkpoint below, and the managed-RDS name collision is closed by the newer runtime-role checkpoint above; production application remains unverified.

### Current authoritative Admin role checkpoint — `PROD-FIRST-LAUNCH-ADMIN-ROLE-CONTRACT-001`

- **Disposition:** `COMPLETE / PASS / REPOSITORY + DISPOSABLE POSTGRESQL VERIFIED / NOT DEPLOYED / 0C / 0H / 0M` at application commit `9d8cd57958b115481a076e5665c20635882e6f07`.
- **Persistent boundary:** migration `0015_admin_runtime_contract.sql` SHA-256 `3ee9b85c9c154117d6e81ee83283160cede9bd182450b0de193f94a431b7d66c` adds no ACL statement; it limits Admin settings to non-secret model/Crawler keys and enables read-only Durable AI Outbox/settlement plus session RLS. Credential-free operator script SHA-256 is `dafe6f6d2b73742326dc0fea5a1166c40999bb8b1759e260ba50faa2c3aef01a`.
- **Least privilege:** actual LOGIN role has no inheritance, membership, ownership, superuser, create-role/database, replication or bypass-RLS. Session `SELECT/INSERT/DELETE` is the only DML. Sensitive Users/Notes/credit-transaction reads are column-scoped; password/salt/avatar bytes, note content and provider payment reference are denied. Every other operational object is read-only; TEMP, DDL, sequences and migration ledger are denied.
- **Runtime correction:** production Admin no longer reads stored XHS Cookie material to render status; it uses only sanitized collection-health evidence. User detail no longer executes `SELECT *`.
- **Verification:** migration ledger `15`, exact `0015` SHA, apply-twice, actual role login/session lifecycle/secret filtering and complete table/column/sequence/role/direct-negative matrix `4/4`; focused repository `10/10`; full Python `901` with `24` explicit-environment skips; readiness `100/100`, quality, compile, Compose and diff passed.
- **Cleanup/impact:** exact labelled PostgreSQL container/volume were removed and Colima returned to stopped baseline. No production/provider/cloud/service/image/traffic action; cost `¥0`.
- **Still open:** this original `noteai_admin` database-role name is superseded by the verified additive `noteai_admin_runtime` correction above. Production migration/ACL application, credential injection, immutable image and loopback HTTP login/read/logout/restart/non-regression remain unverified.
- **Successor evidence:** the runtime browser CDN boundary is now closed by the self-hosted asset checkpoint below.

### Current authoritative frontend asset checkpoint — `PROD-FIRST-LAUNCH-FRONTEND-ASSET-SELFHOST-001`

- **Disposition:** `COMPLETE / PASS / REPOSITORY VERIFIED / NOT DEPLOYED / 0C / 0H / 0M` at application commit `22ae1e588abe7e201d9b671694203924ea391de3`.
- **Runtime boundary:** User and Admin pages load pinned ECharts `5.4.3` and Lucide `1.27.0` only from same-origin `/assets/vendor`; no runtime CDN or floating `latest` reference remains.
- **Integrity/license evidence:** exact upstream SHA-256 values are `1156429a16a38cb8604dcc6518c19406d4226142d908f8edd2e3531443c54d19` (ECharts), `e37f337f85a50b1af4c830cb46e32545201ab6625f00deacf42721bf33ff0de0` (Lucide), `9ef11df57e751e8c6d10d54e1855c64d316627bbe89e73326de7e3ccdb8184aa` (Apache-2.0 license) and `b495047bd93a9b06913511076f504daba17d5bbeb3e0650f3bb53a4220329c57` (ISC license).
- **Verification:** focused browser `3/3`, focused repository `10/10`, full serial browser `69/69`, readiness `100/100`, hash, local-reference and no-external-script checks passed. The first focused browser run used the wrong test origin port only; correcting the harness to the actual local port passed without changing product behavior or relaxing a gate.
- **Cleanup/impact:** exact temporary npm pack directory was moved to Trash after verification. Public npm metadata/package download only; no credential, provider, production database, cloud, image, service, DNS or traffic action; cost `¥0`.
- **Successor evidence:** the fail-closed readiness ledger is now implemented and independently executable as recorded below.

### Current authoritative readiness checkpoint — `PROD-FIRST-LAUNCH-INTERNAL-READINESS-GATE-001`

- **Disposition:** `COMPLETE / PASS / REPOSITORY VERIFIED / NOT DEPLOYED / 0C / 0H / 0M` at application commit `3d234f2286a552e3521d29174028e3d73a8d3f5f`.
- **Truth separation:** repository/isolated evidence is `12/12 = 100%`; internal-production deployment is `14/29 = 48%`; complete public launch is `14/38 = 37%`. The current-source local candidate and production preflight rows are verified for `b06671f`; these are finite evidence-completion ratios, not schedule estimates or launch permission.
- **Fail-closed contract:** verified rows require resolvable Git/path evidence; non-verified rows require an exact blocker and next task; blocked rows require a resume condition; unknown status/dependency, cycles, malformed JSON, missing commit or missing file invalidate the ledger. Repository evidence cannot inherit into managed-runtime or public-launch rows.
- **Current blocker and selection:** ACR private DNS, fourteen-day RDS data/log backup retention, RDS service-key disk encryption, exact five-role publication and the Secret-free production preflight artifact are independently verified. The current dependency root is `PROD-FIRST-LAUNCH-PRODUCTION-SCHEMA-ROLES-001`. Production remains at migrations `0001`–`0008`.
- **Gate 0 verifier/collectors:** `tools/production_readonly_preflight_gate.py` accepts only an exact, Secret-free API-C/API-F/RDS/database/cleanup JSON artifact. `tools/collect_production_host_preflight.py` produces the host fragment without shell evaluation, environment values, raw logs, addresses, instance identities, registry/database/provider requests or mutations. `tools/collect_production_database_preflight.py` accepts its DSN only through a protected process environment, enforces session- and transaction-level read-only mode plus bounded timeouts, returns only repository-bound migration metadata, predefined role/privilege state and fixed aggregate counts, and always rolls back/closes; it emits no business row, DSN or raw exception. Its exact role matrix covers 30 tables/five sequences, `noteai_app` `82 + 5 USAGE`, `noteai_xhs` `20 + 3 USAGE`, Admin zero current runtime object privileges and every inverse privilege/inventory/ownership/membership/elevation/grant-option negative. Together the three tools bind historical runtime digests, systemd/Docker hardening and health, private/VPC-only routing, env key-name metadata, repository migration hashes and required source-aggregate/role negatives; public exposure, side effects, residue or unknown fields fail closed. Focused production/deployment tests pass `57/57`; full Python passes `942` with `24` explicit-environment skips, and production readiness remains `102/102`. A disposable PostgreSQL 16.14 real-Psycopg execution applied exact synthetic `0001`–`0008`, returned a database fragment accepted by the final gate, then removed its tmpfs container/zero volumes and restored Colima stopped. Quality, Python compilation, diff and synthetic production Compose pass. Compose without required image variables or final env files failed closed as designed before the fully synthetic parse. This is deployment-control tooling excluded from the image and does not change the readiness score.
- **Verification:** current full Python `957` with `24` explicit-environment skips; focused preflight/readiness `14/14`; production-readiness `103/103`, quality, Python compilation, JSON and diff checks pass. `--require repository` exits zero while `--require internal` and `--require public` correctly exit non-zero.
- **Impact:** the bounded publication used the retained builder and ACR, then removed all one-time access. No provider, production database, service deployment/restart, ALB/TLS/public DNS or traffic action; no additional persistent cloud resource remains.
- **Next dependency root:** execute `PROD-FIRST-LAUNCH-PRODUCTION-SCHEMA-ROLES-001`; do not widen runtime migration-ledger access.

### Current authoritative Alibaba baseline remediation checkpoint

- **ACR private DNS — `PROD-FIRST-LAUNCH-ACR-PRIVATE-DNS-RESTORE-001`:** `COMPLETE / VERIFIED`. After the owner restored billing, the exact ACR endpoint was recreated with automatic PrivateZone record management. The endpoint is `RUNNING`; the DNS console shows one product-managed zone, one record and one production-VPC binding. API-C/API-F each resolve exactly one private address and retain active loopback-only API health.
- **RDS retention — `PROD-FIRST-LAUNCH-RDS-BACKUP-14D-001`:** `COMPLETE / VERIFIED`. Data backup and log backup retention are both fourteen days, log backup remains enabled and the existing Snapshot method/schedule is preserved. Current backup usage remains below the included allowance; rollback is the exact prior seven-day values.
- **RDS encryption — `PROD-FIRST-LAUNCH-RDS-ENCRYPTION-001`:** `COMPLETE / VERIFIED`. One idempotent immediate request selected the platform service key. The client timed out after server acceptance and was not retried; later control-plane reads proved `Running` with one encryption key. API-C/API-F post-health returned two successful exit-zero results and two complete health/isolation markers.
- **Runtime boundary:** API-C/API-F/Admin were not redeployed or restarted. They remain loopback-only; no XHS, Tracking, Payment or AI Worker container/service and no temporary ACR auth directory or Session Manager remains on either API node. No business database write, provider call, ALB/TLS/public-DNS or user-traffic action occurred.
- **Publication boundary:** exact `b06671f` is published as five immutable role manifests. The temporary publisher role/policy, builder ACR link, Docker auth and temporary auth directories are removed; production retains one running ACR VPC/PrivateZone link. The first public Git clone failed before build; the corrected shallow clone reached the exact revision. API-C/API-F were not restarted or redeployed and their before/after health fingerprints match.

### Current authoritative b06671f native/registry release checkpoint

- **Disposition:** `COMPLETE / FIVE-ROLE GITHUB SOURCE + ACR PRODUCT VERIFIED / NOT DEPLOYED`.
- **Build evidence:** GitHub run `30233565859` built API, Admin, Payment, AI Worker and XHS HTTP on `x86_64` from exact application revision `b06671fbcca51f884b04c86edcf116e373c6cfa8`; ordinary push/PR CI both passed. Native build, OCI inspection, SBOM, Secret scan and artifact upload passed; only the expected raw zero-Critical/High gate failed closed.
- **Canonical findings:** every role remains unsuppressed at `4 Critical / 19 High`, Secret `0`, browser component `0`, forbidden OS package `0` and exactly one `cryptography 48.0.1`.
- **Exact-product evidence:** the `b06671f-github-native-release-*` bundle binds the GitHub local image IDs; the separate `b06671f-registry-release-*` bundle binds five unique ACR manifest digests and their distinct isolated-builder IDs/SBOMs. Both preserve the twelve Debian dispositions and 115 BOM-Links; the pinned official CycloneDX 1.6 schema validates with zero errors.
- **Publication/cleanup:** authenticated ACR read-back matched all five tags and digests. Canonical raw evidence remains retained for fourteen days. One-time publisher identity/auth/network artifacts are zero; production ACR linking is restored.
- **Verification:** focused GitHub/registry VEX/readiness `41/41`, full Python `953` with `24` skips, production-readiness `103/103`, quality, compile, CycloneDX 1.6 schema, JSON and diff checks pass.
- **Boundary:** local image IDs are not registry digests. Publication did not deploy/restart a service, write the business database, call a provider or change ALB/TLS/public DNS/traffic. Historical `2fa3a55` evidence is retained only for that old revision.

### Current authoritative native release checkpoint — `PROD-FIRST-LAUNCH-IMMUTABLE-RELEASE-OFFLINE-001`

- **Disposition:** `COMPLETE / NATIVE AMD64 FIVE-ROLE CANDIDATE VERIFIED / NOT PUBLISHED / NOT DEPLOYED` at application revision `2fa3a5543876a6c8040ec17ca05a5461b101bbd7`; evidence/VEX checkpoint `bccbb3ad135df3ca0b0aac04af5e57522f4bf039`.
- **Build evidence:** GitHub run `30216295810` used an `x86_64` runner and built API, Admin, Payment, AI Worker and XHS HTTP from the exact revision. Both ordinary CI runs passed. Native build, OCI inspection, SBOM, Secret scan and artifact upload passed; the raw zero-Critical/High enforcement remained failed closed.
- **Dependency correction:** every role contains exactly one `cryptography 48.0.1`, closing `GHSA-537c-gmf6-5ccf`; MLflow remains outside production requirements because the local training environment requires `cryptography<47`.
- **Canonical findings:** each role retains `4 Critical / 19 High`, Secret `0`, browser component `0` and forbidden graphics/OS package count `0`. No ignore, raw-report rewrite or scanner suppression was used.
- **Exact-product VEX:** twelve Debian CVEs are bound to all five local image IDs, five SBOM hashes and 115 exact BOM-Links. The decision is `not_affected` only under the reviewed AMD64 base, fail-closed command graph and UID/GID 999, read-only, non-privileged, cap-drop-all, no-new-privileges, no-device/no-host-namespace runtime contract. It is not a production exception, registry digest or deployment authorization.
- **Validation:** CycloneDX 1.6 schema validation returned zero errors; focused VEX/supply-chain/readiness `47/47`, full Python `922` with `24` skips, production-readiness `102/102`, Compose, compilation and diff checks passed.
- **Cleanup/impact:** both downloaded raw evidence directories and temporary schema/report files were moved to Trash after committing reduced hashes/evidence. No ACR access, registry publication, production/cloud/database/provider/service/traffic action or business write occurred; incremental cost `¥0`.
- **Do not repeat:** do not rebuild or re-disposition revision `2fa3a55` without a source/base/SBOM/architecture/command/runtime/CVE-scope conflict. New image content invalidates this VEX and requires a new exact identity.

### Current authoritative payment checkpoint — `PROD-FIRST-LAUNCH-PAYMENT-CONTRACT-001` + `PROD-FIRST-LAUNCH-PAYMENT-ADAPTER-001`

- **Disposition:** both tasks are `COMPLETE / PASS / REPOSITORY VERIFIED / NOT DEPLOYED`; the contract also retains its separately passed disposable-PostgreSQL evidence. This verifies the provider-isolated cash/entitlement contract, transport-injected Adapay wire adapter and dedicated callback runtime. Merchant admission, credentials, provider mock/live compatibility, callback reachability, production migration/ACL, scheduler/alerts and real money remain unverified.
- **Contract:** fixed integer-fen catalogue; non-recurring 30-day paid terms; single-winner order/refund provider admission; exact SHA1withRSA callback verification; Event collision/replay protection; immutable cash/entitlement ledgers; paid-credit source allocation/restoration; one full unused refund; bounded reconciliation/settlement; confirmed-cash-only Admin truth with unmatched exposure.
- **Persistent boundary:** migration `0014_payment_execution_contract.sql` adds ten payment tables, RLS, catalogue/amount checks, transition/append-only guards and no ACL change. Its SHA-256 is `ed788fdf33e256713767101e85005e95a712a7e5c5aae0ec258c5f14091cb0ad`. The app may null a settled order's user join only with every other field unchanged; repoint/restore/state-smuggling is rejected.
- **Adapter/runtime boundary:** official documented payment/query/refund/query/bill paths, exact SDK-compatible request signing and signed response verification are implemented without the official process-global SDK. TLS verification, zero retry/redirect, bounded I/O, strict QR/HTTPS-host/ZIP/CSV handling, transient public device IP and zero user-content transmission are enforced. Normal API callbacks always reject; the dedicated default-disabled runtime exposes only health and callback and production-checks `current_user=noteai_payment`.
- **Verification:** original payment contract evidence remains focused `198/198`, full Python `872`, E2E `66/66`, readiness `95/95` and disposable PostgreSQL `6/6`. The adapter successor passed focused `236/236`, full Python `887` with `20` explicit-environment skips, E2E `66/66`, readiness `96/96`, exact signed callback replay, dependency `cryptography==46.0.7`, quality, compile, shell, local/production Compose and diff checks. A concurrent first full run had one pre-existing 2.00-second performance gate at 2.05 seconds; the isolated case passed in 0.228 seconds and the serial full rerun passed in 32.5 seconds without relaxing the gate.
- **External impact:** official documentation and public package metadata only; no Adapay API/merchant request, real credential, mock transaction, money, production database, GRANT/REVOKE, service, image, ACR, cloud or traffic change. Incremental cost `¥0`; no task container or temporary env file remains.
- **Do not repeat:** do not repeat either payment repository contract or its disposable-PostgreSQL rehearsal unless payment code or migration SHA conflicts. This is not evidence that users can pay.
- **Open provider facts:** provider mock fixtures must confirm actual signed envelope shapes, merchant-enabled QR channel and HTTPS checkout URL. Official bill documentation still shows an HTTP example while NoteAI deliberately rejects cleartext; the mock/merchant gate must prove an HTTPS bill host or stop for an explicit secure design decision.
- **Production serial gate:** authenticated preflight, bounded TEMP ACL closure and final machine-evidence validation have completed, and exact `b06671f` registry products are published. The remaining root is production schema/role application.

### Production preflight checkpoint — `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-PROD-PREFLIGHT-001`

- **Disposition:** `COMPLETE / VERIFIED / FINAL MACHINE ARTIFACT PASS / ZERO BUSINESS WRITE`. Fresh authenticated ECS/control-plane and read-only PostgreSQL observations are retained; exact current-source ACR digests are independently bound.
- **Accepted facts:** API-C/API-F remain private and stable; API-C/API-F/Admin listeners remain loopback-only; no XHS/payment/AI Worker or temporary access residue is present. RDS is PostgreSQL 16 HA, VPC-only, SSL-enabled with connection limit 1,600. Production is exactly migrations `0001`–`0008`; all fifteen fixed source-data blockers are zero. The read-only database transaction was rolled back and emitted no business row.
- **Closed remediation facts:** ACR product-managed PrivateZone resolves on both nodes; RDS disk encryption is enabled and data/log retention is fourteen days; five current ACR digests are published and bound. The managed RDS administrator still occupies the old application role name, but repository `b06671f` uses `noteai_admin_runtime`.
- **Remaining production fact:** production `0009`–`0016`, exact final roles/ACL and runtime credentials remain unapplied; this belongs to the successor schema/role and managed-secret tasks, not Gate 0.
- **Impact:** the final evidence collection ran only sanitized reads. The separately recorded ACL closure revoked only database TEMPORARY from PUBLIC and wrote no business row. No registry request, provider call, service start/restart, ALB/TLS/public-DNS or traffic action occurred. No Secret, user data, identifier or network address is recorded.
- **Evidence:** `docs/PRODUCTION_READONLY_PREFLIGHT_EVIDENCE.md` and `deploy/production/evidence/production-preflight-b06671f.json`.
- **Offline acceptance tools:** `tools/production_readonly_preflight_gate.py`, `tools/collect_production_host_preflight.py` and `tools/collect_production_database_preflight.py`; current full Python `957` with `24` skips, focused preflight/readiness `14/14`, production readiness `103/103`, quality, Python compilation and diff checks pass. The database collector forces read-only at connection and transaction scope, executes only fixed metadata/aggregate queries and the complete current role matrix, rolls back, closes and emits only a fixed error code on failure. A disposable PostgreSQL 16.14 real-Psycopg proof passed the final gate and cleaned to zero task resources/Colima stopped.
- **Next dependency root:** `PROD-FIRST-LAUNCH-PRODUCTION-SCHEMA-ROLES-001`.

### Current authoritative Trends checkpoint — `PROD-XHS-TRENDS-LONGRUN-CONTRACT-001`

- **Disposition:** `COMPLETE / PASS / REPOSITORY + DISPOSABLE POSTGRESQL VERIFIED / NOT DEPLOYED / 0C / 0H / 0M`. This closes task 6's repository/runtime contract only. It does not repeat or replace the accepted suspended Canary, and it does not claim real-XHS or managed-service acceptance.
- **Execution contract:** one unique UTC-day run, 30-minute fenced singleton lease, durable admission immediately before every provider call, fixed 34-call run/day cap, no automatic retry after unknown outcome, and provider-free explicit acknowledgement. The production success gate requires 12 unique current-run real-XHS keywords in each of the exact six domains plus both search-result and search-recommend evidence per domain.
- **Atomic evidence:** up to 90 hot-keyword upserts, 90 keyword-snapshot inserts, six freshness upserts, six health inserts, exact UTF-8 snapshot payload/hash/size/counts and run success share one fenced transaction; all commit or all roll back. Provider attempt evidence remains durable outside that transaction. Historical rows and duplicate keywords cannot make the current run pass.
- **Runtime/identity:** exact `noteai_xhs_trends` role, provider-free read-only health, default suspension before DB/provider/file work, `restart: "no"`, fixed CPU/memory/PID limits, no production Trends data bind or HTTP snapshot upload, and distinct Trends/Tracking entrypoint/env identities. EntryPoint Override remains prohibited as a normal runtime.
- **Migration/role evidence:** unapplied migration `0011_trends_execution_contract.sql` SHA-256 is `abd55623a8903d6a6c01bed5fe4336b07be182abde2b86fa4b9cc5e7cb4b9802`. Cached PostgreSQL `16.14` applied `11` migrations once and `0` twice with ledger `11` and matching SHA. The Trends matrix covered 40 tables (`15` positive / `265` negative privilege cells), exact UPDATE columns (`33` positive / `401` negative), 15 sequences (`2` positive / `13` negative), and the API received only three SELECT columns. Database/schema create, TEMP, ownership, membership, superuser and bypass privileges were absent.
- **Lifecycle evidence:** synthetic success wrote exactly hot `90`, snapshots `90`, health `6`, freshness `6`, snapshot evidence `1`; needs-manual, stale-lease and post-write rollback paths failed closed. Seven invalid constraint probes and eleven direct negative SQL operations were rejected.
- **Regression evidence:** focused Trends/runtime suite `94/94`; full unit suite `776 passed + 5 skipped`; browser E2E `66/66`; production readiness `90/90`; quality, compile, normal/production Compose, shell syntax and diff checks passed.
- **Cleanup/cost:** the exact labelled temporary PostgreSQL container was removed and Colima returned to stopped baseline. No production/cloud/provider/ACR/service action occurred; external cost was `¥0`.
- **Still open:** production history/permission preflight; migrations `0009`–`0011`; exact role creation/GRANT; immutable release digest; default-suspended internal deployment; one capped DB-read-only real-XHS proof; managed singleton/reboot/kill/alert/rollback promotion. Trends remains stopped.

### Current authoritative Tracking checkpoint — `PROD-XHS-TRACKING-CONTRACT-HARDEN-001`

- **Disposition:** `COMPLETE / PASS / REPOSITORY + DISPOSABLE POSTGRESQL VERIFIED / NOT DEPLOYED / 0C / 0H / 0M`. This closes the repository defect set recorded for task 7; it does not inherit Trends evidence and does not claim real-XHS or managed-service acceptance.
- **Execution contract:** only an exact canonical full HTTPS XHS note URL containing a lowercase 24-hex note identity is accepted or reflected. Each provider call requires a fresh atomic row claim; PostgreSQL uses `SKIP LOCKED`, the lease is 15 minutes, each cycle is bounded to 1–50 rows, 24-hour and 7-day provider admissions are unique, and the hard daily cap is 300. Growth and memory identities are deterministic.
- **Failure/deletion contract:** an admitted call with an unknown outcome is never automatically retried; provider-free reconciliation moves an expired attempt to `needs_manual / provider_outcome_unknown`, and readiness fails while stale or unlinked started attempts exist. Account deletion and provider admission share the same user fence: deletion either wins before admission or returns conflict without revoking a call in progress.
- **Production runtime contract:** Tracking has a provider-free, read-only health command; runtime failures exit non-zero; the production service remains default-suspended with restart disabled. API/Admin outputs use canonical safe URLs, Admin cannot report a false success timestamp, and production configuration names the separate `noteai_xhs_tracking` role.
- **Migration/role evidence:** unapplied migration `0010_tracking_execution_contract.sql` SHA-256 is `8ab5bbddad28ea60afad48bc27d71b105c5313d0229e263d06b65db4f38a3459`. On disposable PostgreSQL 16 it applied `10` migrations once and `0` twice, bound all ledger SHAs, rolled back three dirty-history cases atomically, and enforced owner/cross-track/clock/canonical-identity constraints. The Tracking role matched tables `7 true / 245 false` across 252 checks, exact UPDATE columns `26` and `3`, sequences `1 true / 14 false`, and had no ownership, schema-create, DDL, membership, superuser or bypass privilege.
- **Lifecycle evidence:** direct forbidden operations, wrong-owner and cross-track active attempts failed closed; the full role lifecycle completed without Users/Notes `SELECT`; stale health/reconcile and the real deletion/admission race passed with no provider call. Synthetic rows were zero after the probes.
- **Regression evidence:** full unit suite `758 passed + 5 skipped`; browser E2E `66/66`; production readiness `88/88`; quality, compilation and diff checks passed. Independent lifecycle, PostgreSQL/role and API/UI/runtime reviewers each returned `PASS / GO / 0C / 0H / 0M`.
- **Cleanup/cost:** the exact labelled temporary PostgreSQL container and volume were removed and Colima returned to stopped baseline. No production/cloud/provider/ACR/service action occurred; external cost was `¥0`.
- **Still open:** production legacy-data preflight, migrations `0009/0010`, exact production role/permission application, immutable release digest, default-suspended internal deployment, one owned-note bounded real-XHS 24h/7d proof, singleton/reboot/kill/alert/rollback and final managed promotion are all `UNVERIFIED`. Tracking remains stopped.

### Current authoritative Durable AI checkpoint — `PROD-FIRST-LAUNCH-DURABLE-AI-CONTRACT-001`

- **Disposition:** `COMPLETE / PASS / REPOSITORY + DISPOSABLE POSTGRESQL VERIFIED / NOT DEPLOYED / 0C / 0H / 0M`. This closes only the repository/offline phase of package 3 `PROD-FIRST-LAUNCH-DURABLE-AI-001`; it is not AI Worker, queue, object-storage, provider-chain or capacity acceptance.
- **Admission/data boundary:** feature-gated, default-disabled `202` Analyze/Generate/Chat-Rewrite admission creates owner-bound opaque request/result references, operation/event/settlement and Outbox state in one transaction. Raw prompts, media, URLs, object keys and provider material are absent from the new SQL tables. Inline media is rejected; the future contract accepts at most ten canonical opaque media references. An unavailable payload store fails before billing or database admission.
- **Execution/billing boundary:** content-free wakeups carry only operation UUIDs; the dispatcher uses fenced claims and bounded 3:1 priority/standard fairness with FIFO inside each lane. Every provider call requires a fresh in-transaction admission fence. Expired provider-free leases may be redelivered; any post-provider unknown outcome becomes `needs_manual` without retry or refund. Known terminal failure/refund and success/result/settlement transitions are atomic.
- **Deletion/compensation:** account deletion atomically cancels and refunds only provider-free jobs, blocks when provider outcome is unknown, deletes owner-scoped payload objects before final account removal, and fails closed when storage is unavailable. Newly created request/result objects are synchronously compensated when the database transaction or commit fails.
- **Migration/role evidence:** unapplied migration `0012_durable_ai_execution_contract.sql` SHA-256 is `df72dedfb292700104fc394b5b326f33e4339cbf195f704278c56e08e44bec83`. Disposable PostgreSQL `16.14` passed migration apply-twice/SHA, UTC constraints, request/result RLS separation, API exact cancellation permissions, role attributes/negative permissions and 20-way concurrent billing/idempotency (`10/10`).
- **Runtime truth:** a separate `ai-worker-runtime` image target and production Compose profile exist, but remain default-suspended with no port, bind mount, automatic restart or concrete provider processor. API and Worker storage credential names are separated. A repository-verified private OSS adapter now exists, but no production bucket/RAM role, publisher, dispatcher process or provider processor exists; `--once` therefore fails closed and this skeleton must not be promoted.
- **Regression evidence:** focused Durable AI/API/account-lifecycle `127/127`; related runtime/security `65/65`; full Python suite `812 run / 10 skipped / 0 failed`; browser E2E `66/66`; production readiness `90/90`; normal and all-profile synthetic production Compose, Python compilation, secret scan and diff checks passed.
- **Cleanup/cost:** the exact disposable PostgreSQL resources were removed and Colima returned to stopped baseline. No production/cloud/provider/ACR/service action occurred; external cost was `¥0`.
- **Still open:** production migrations `0009`–`0016`, private OSS/RAM roles, exact production database roles/permissions, managed cross-node/PITR proof, real publisher/dispatcher/processor, current-digest deployment, default-suspended internal deployment, provider-chain proof, monitoring and 100-job capacity remain `UNVERIFIED`.

### Current authoritative storage/recovery checkpoint — `PROD-FIRST-LAUNCH-STORAGE-RECOVERY-CONTRACT-001`

- **Disposition:** `COMPLETE / PASS / REPOSITORY + DISPOSABLE POSTGRESQL VERIFIED / NOT DEPLOYED / 0C / 0H / 0M`. This closes the repository/offline phase of package 4 `PROD-FIRST-LAUNCH-STORAGE-RECOVERY-001`; it is not private OSS provisioning, production migration, managed cross-node service or real RDS PITR acceptance.
- **Private object boundary:** only the official pinned Alibaba OSS v2 SDK is production-selectable. Configuration requires an ECS RAM Role, exact regional internal HTTPS endpoint, private bucket, non-secret key epoch and conditional encrypted writes; static AK/SK variables and implicit/local production backends fail closed. SQL/messages/log evidence contains only opaque UUIDs, domain-separated owner hashes, hashes/counts/states/clocks.
- **Media/lifecycle:** authenticated image/video uploads stream to bounded temporary files, validate type/size/dimensions, normalize images and store canonical JPEG-frame bundles rather than original videos. Reads recompute owner/key/purpose/hash/TTL; cross-owner and production legacy node-local IDs are not found. Object-first creation is compensated on DB failure; expiry/account deletion delete objects before metadata transition; orphan reconciliation is bounded, 24-hour age-gated and dry-run by default.
- **Durable AI integration:** at most ten canonical media refs are locked for the owner before billing and linked to the operation in the admission transaction. Cross-owner, expired, non-ready and duplicate refs fail before billing or operation rows. Payload and media deletion keep account closure pending when the object backend cannot prove deletion.
- **Migration/roles:** unapplied migration `0013_private_storage_recovery_contract.sql` SHA-256 is `1268cdb9696965f02d5be88882b3dc8a2f9f7b589a2b0da5193d1bb8408ccb76`. It adds content-free media/link tables, persistent owner/ready/TTL guard and RLS without `GRANT`/`REVOKE`. Disposable PostgreSQL `16.14` passed first/second migration apply, SHA ledger, constraints, cross-owner/expired/non-ready rejection and the complete six-role negative privilege matrix (`4/4`).
- **Recovery evidence:** source/restored manifests are captured in one database read-only snapshot (`REPEATABLE READ` on PostgreSQL), contain exact release/schema/migration/table/reference/object digests but no row values, object keys, content or user identifiers, use bounded 0600 create-once files, and reject malformed self-rehashed manifests. Synthetic PostgreSQL source/restored inventories covered 46 tables and 13 migrations and matched exactly.
- **Regression evidence:** private-storage/readiness focused `33/33`; API contracts `152/152`; account security `87/87`; final full Python suite `833 run / 14 skipped / 0 failed`; browser E2E `66/66`; production readiness `95/95`; quality, compilation, dependency, normal/all-profile synthetic Compose, diff and staged-secret checks passed.
- **Cleanup/cost:** exact labelled PostgreSQL container and exact temporary evidence directory were removed; label-filtered container count returned zero and Colima returned to stopped baseline. No production/cloud/provider/ACR/service/database action occurred; external cost was `¥0`.
- **Still open:** real private bucket/RAM roles/encryption policy, production migration `0013` and exact grants, object version/lifecycle policy, managed API/Worker cross-node proof, monitoring/alerts and one isolated RDS PITR restore/reconciliation remain `UNVERIFIED`.

## 1. Current project phase

NoteAI is in **complete-first-launch preparation**, not general availability. The product owner requires a functionally complete first commercial launch: XHS Trends, Tracking and all other first-launch commercial capabilities must pass before DNS or real-user traffic. Render Staging remains the validated staging environment. The production Claude Gateway is live on Render Singapore. On Alibaba Cloud, the exact three AMD64 candidates exist in private ACR under immutable role-specific digests. API-C and API-F are independently accepted managed services on their respective loopback `127.0.0.1:8000` endpoints; both remain outside ALB and receive no public traffic. API-C also retains an accepted managed Admin service on loopback-only `127.0.0.1:8001`, with production database sessions forced read-only and no public traffic. XHS remains stopped; no ALB is configured, TLS certificates are not attached to a production listener, and production DNS has not been switched.

`PROD-PREBUILD-001` through `PROD-DEPLOY-CONFIG-WRITE-001` are complete for their approved scopes. API-C and API-F are accepted managed exact-digest API services, and API-C also has an accepted managed Admin service; all are loopback-only and outside ALB. The dedicated least-privilege `noteai_xhs` role, exact XHS digest on API-F and required `xhs_crawler_health SELECT` are independently verified. The original Trends canary stopped before writes, the read-only diagnostic passed to the first write fence, RETRY-001 failed closed in its observer, and RETRY-002 subsequently proved the suspended Worker, sanitized logs, run-scoped rows and exact database bounds. Its original tmpfs-copy gap is now closed by the separately approved zero-write entrypoint-override exporter proof recorded below. Docker/Compose, role-separated env files, RDS TLS policy, exact live migrations `0001`–`0008`, temporary audit-role deletion, temporary client deletion and final Session Manager closure remain verified. Exact Pillow-fixed application revision `a635692a899ee02c6905cd694611c14e0da4594a` remains the OCI revision reported for all three corrected registry digests; deployment-control release `253fde55fa609676a9fca5c14fdf6aeceef74000`, env-split release `7f6b9d6eaaff06c8d756783136a1bc4ed417c5c0` and later Handoff/VEX/docs commits are not image revisions. Image contents and canonical raw reports remain unchanged and unsuppressed at `4 Critical / 19 High`. API-C and API-F each run one accepted managed exact-digest API container on host-local `127.0.0.1:8000`; API-C also runs the accepted managed exact-digest Admin container on `127.0.0.1:8001`. All remain outside ALB and without public traffic. XHS and Tracking remain stopped.

### Current authoritative security/compliance checkpoint — H17 through R23

- **H17–H22 status:** `IMPLEMENTED / OFFLINE VERIFIED / INDEPENDENT R22 PASS`. These stages close R16 and every later retention-identity survivor: real-clock purge checks, parsed paid-at-creation clocks, immutable retention and primary identities, post-purge/same-transaction resurrection guards, direct SQLite duplicate/REPLACE rejection, startup/helper idempotency and owner/clock conflict rejection.
- **R22 evidence:** three independent streams returned `PASS / 0C / 0H / 0M`. Focused H17–H22 `13/13`, lifecycle `3/3`, security/reasoning/deployment/health `134/134`, API `152/152`, full suite `734 passed + 5 skipped`, readiness `86/86`, plus quality, compile, Compose and diff checks all passed. The known non-failing SQLite `ResourceWarning` is not a gate failure.
- **Frozen repository inputs:** `model/db.py` SHA-256 `33c26c7abb36173e85501973c6c582106231f7972118a4db0a3202c8233708ea`; `model/content_retention.py` `38742289ee4015c2d3e858f421e43917b9d12c730ac79e02beac1f696d846aba`; migration `0009` `1cfa46144b9a4d424215dd861a9817ec3a4612e7e62c1f2df5fec6f2e728f750`.
- **Disposable PostgreSQL 16:** `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-POSTGRES-REHEARSAL-002` used a cached local image, synthetic roles/data, random loopback, read-only root, bounded CPU/memory/PIDs, no provider or cloud access and no business data. First migration apply recorded nine SHA-bound versions, second apply recorded zero, and migration drift plus noncanonical phone, duplicate phone and invalid source-clock histories all failed closed with atomic rollback.
- **Exact runtime permissions:** synthetic `noteai_app` matched table predicates `100 true / 145 false`, sequences `5 / 10`; `noteai_xhs` matched tables `20 / 225`, sequences `3 / 12`. Retention UPDATE was six mutable columns only, Notes UPDATE was `parent_id` only, Diagnoses UPDATE was zero. Both roles were non-owner, non-superuser, no `BYPASSRLS`, membership, DDL, grant option or migration-ledger access; built-in advisory functions remained callable and the H20 guard function remained directly uncallable.
- **Dynamic integrity:** orphan, wrong-owner and pre-purged markers failed closed; live-primary and future purge markers failed; valid Note and Diagnosis delete-before-marker transitions passed. Purged identities stayed audit-visible but later rewrites affected zero rows. Same-transaction and concurrent resurrection attempts returned `23514`, with full rollback/no survivor. Repository helper exact/equivalent/omitted-clock repeats left the full row and `xmin` unchanged; owner and clock conflicts failed before write.
- **R23 and cleanup:** independent PostgreSQL and lifecycle reviewers each returned `PASS / 0C / 0H / 0M`; regression review confirmed all gates and was conditional only on cleanup. After evidence capture, exact-name/task-label checks preceded removal of `noteai-pg-rehearsal-002` and its dedicated volume; label-filtered counts became `containers=0 / volumes=0`, then Colima returned to its stopped baseline. No existing Supabase definition or unrelated Docker object was removed.
- **Production truth:** migration `0009` and its production permission delta are still unapplied. No production row was read or written in this rehearsal. Current production preflight, backup, migration and role change remain a later serial task; the repository candidate is not yet an immutable deployed release.
- **Historical precedence:** H14–R21 findings below explain the hardening path but are superseded where H22/R22 and the disposable PostgreSQL result supply newer direct evidence. They must not be reused as the current next-task instruction.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-001 — superseded repository checkpoint

- **Status:** `SUPERSEDED BY REVERIFY-001 AND HARDEN-002`; its completed code remains part of the current worktree, but its readiness conclusion is stale.
- **Independent input:** read-only `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-VERIFY-001` completed `FAILED / NO-GO` with `0 Critical / 11 High`. Its findings are not erased: this task implements candidate fixes, and only a new independent review may close them.
- **Boundary:** repository code, documentation, an additive PostgreSQL migration file and offline tests only. No production migration/GRANT/data write, deployment/image/ACR action, service start/change, real SMS/email/legal/provider/XHS/payment call, ALB/TLS/DNS/traffic action or external cost occurred.
- **Authentication:** OTP destination/requester limits and login limits now serialize before count/update; equivalent mainland phone forms share one canonical identity; verification adapters fail closed unless the stage is explicitly local/test/development. Login verification, limiter update and session issue are atomic; password change/reset atomically revokes all sessions. Registration records versioned, separate privacy and cross-border consent.
- **Retention/export/deletion:** paid-at-creation classification uses subscription coverage at content creation, legacy rows fail closed into bounded retention, purge/recovery/deletion paths use transactional fences, and bounded idempotent primary-data purge plus explicit backup-evidence confirmation exist. Account export now covers all user-facing first-launch data while excluding tokens, raw provider material and regulated ledgers.
- **Secret/log/reasoning/Admin boundary:** reserved Secret keys cannot fall back to forged database rows; provider/crawler/training failures use stable codes and summary hashes rather than raw bodies, URLs or exceptions. Diagnosis/chat persistence blocks reasoning aliases without deleting legitimate supplement prompts, and dynamic Admin tables/log fields are escaped.
- **Payment truth:** the current UI explicitly says real payment is unavailable and prevents purchase; no dead payment request is presented as a successful commercial flow. Real Adapay integration remains a Critical launch gate.
- **Migration and permissions:** `model/migrations/postgres/0009_account_security_compliance.sql` fails closed on noncanonical/duplicate phones, backfills retention from historical subscription coverage, and adds consent/deletion evidence schema. `docs/FIRST_LAUNCH_DB_RUNTIME_PERMISSION_MATRIX.md` records the exact least-privilege delta. Neither the migration nor any GRANT/REVOKE has run; production remains at `0001`–`0008`.
- **Offline evidence:** security/compliance suite `24/24`; corrected full isolated unit suite `671` passed with `5` skips; production readiness `86/86`; Python compile, both frontend JavaScript parses, Compose configuration and `git diff --check` passed. All databases/caches were temporary and provider failures were mocked; there was no external call.
- **Remaining gates:** independent re-review of the 11 findings; disposable PostgreSQL apply-twice/rollback rehearsal; production preflight for noncanonical/duplicate phones, timestamp casts and the number of legacy free rows that backfill would make immediately purge-eligible; backup and separately approved migration/GRANT; real SMS/email, professional Chinese privacy/consumer/cross-border/refund review, managed Secret distribution/rotation and historical cleanup, production purge scheduling and backup-deletion evidence.
- **Superseding evidence:** `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-001` completed `FAIL / NO-GO` and retained `7 High / 6 Medium`. Those findings are addressed by `HARDEN-002` below; only a fresh independent review may close them.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-002 — repository remediation checkpoint

- **Status:** `SUPERSEDED BY REVERIFY-002 AND HARDEN-003`; its code remains part of the worktree, but its readiness conclusion is stale.
- **Scope:** repository code, documentation, tests and unapplied additive migration text only. No migration, privilege change, production/database/cloud/ACR access, service start, provider call, deployment, commit, push or external cost occurred.
- **Seven High dispositions:** the exact `noteai_app` new-table and five existing-table privilege deltas plus negative matrix are documented; account deletion now serializes a mandatory final export with API/Worker write fences; PostgreSQL due/subscription comparisons are typed and migration clocks canonicalized; export includes learned preferences and note-version derivative chat/context copies are linked and removed; API/Worker/training/artifact failures expose stable codes rather than raw exceptions or full URLs; generalized reasoning aliases including camel/snake variants are removed without deleting legitimate fact-provider business fields; test billing now requires both the opt-in flag and an explicit local/dev/test stage.
- **Six Medium dispositions:** username limiter identity now matches case-sensitive username lookup; password checks normalize equivalent mainland phone forms; OTP verification/consumption and registration/login/reset/profile mutations share one transaction with failed business mutations rolling back challenge consumption; backend uniqueness conflicts map to stable business errors; SQLite materializes legacy retention with parity checks while malformed clocks fail closed; account deletion can no longer opt out of the final export.
- **Deletion/copy boundary:** application writes use a shared user advisory/row fence. Tracking Worker writes use the same advisory namespace and stop on `account_deletion_pending` without expanding the existing `noteai_xhs` table privilege set. New context memories carry `source_note_id`; version-family deletion removes linked chat/context copies and conservatively removes legacy unlinked context.
- **Offline evidence:** security/compliance suite `36/36`; full unit suite `683` passed with `5` skips; production readiness `86/86`; Python compilation, migration executable-privilege-statement check and `git diff --check` passed. Test databases were temporary and all provider failure paths were mocked; there was no external call or production write.
- **Production truth unchanged:** migration `0009` and every documented permission delta remain unapplied; latest production schema evidence remains `0001`–`0008`. Current production rows were not read, so duplicate/contact/timestamp/backfill/purge-candidate preflight remains `UNKNOWN`.
- **Superseding evidence:** independent `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-002` completed `FAIL / NO-GO` with `0 Critical / 4 High / 2 Medium`. It accepted the other `HARDEN-002` dispositions but kept six exact repository gaps open.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-003 — repository remediation checkpoint

- **Status:** `SUPERSEDED BY REVERIFY-003 AND HARDEN-004`; its accepted corrections remain in the worktree, but its readiness conclusion is stale.
- **Scope:** repository code, documentation and offline tests only. No production/database/cloud/ACR access, migration, GRANT/REVOKE, service start, provider call, deployment, commit, push or external cost occurred.
- **Independent input:** `REVERIFY-002` retained four High findings: missing exact built-in function permission evidence, a final-export/follow-up-write race, raw exception leakage in Chat/artifact/training paths, and incomplete reasoning-alias normalization. It retained two Medium findings: formatted phone equivalents in passwords and SQLite legacy backfill silently accepting invalid source clocks.
- **High corrections:** the runtime matrix now names `pg_catalog.hashtext(text)` and `pg_catalog.pg_advisory_xact_lock(bigint)` for both runtime roles with four exact `has_function_privilege` checks and negative ownership/grant-option requirements, without adding any function GRANT. Account deletion, idempotency admission/terminalization, billing writes/model-usage records and both deletion processors use the canonical `user advisory -> user row -> request/retention/idempotency -> ledger/derivatives` order; deletion returns `409` and withholds the final export while a durable request is running. Chat image failure, S3 artifact failure and training-slot failure now expose stable codes/messages only. `ScratchPad`, `scratchPad`, `scratch_pad`, `CoT`, `COT` and `co_t` are removed while `fact_enrichment.provider`, `raw_title` and `supplement_prompts[].prompt` remain.
- **Medium corrections:** password validation compares digit-normalized mainland phone identities, including spaces, hyphens and `+86`; SQLite startup validates note, diagnosis and subscription source clocks before materializing any legacy retention classification and fails closed on null, blank or unparsable values.
- **Offline evidence:** security/compliance suite `42/42`; focused billing/idempotency/model-usage/admission set `101/101`; full isolated unit suite `689` passed with `5` skips; production readiness `86/86`; quality gate, Python compilation, Compose configuration and `git diff --check` passed. Provider failures were mocked and all databases were temporary; no external call or production write occurred.
- **Production truth unchanged:** migration `0009` and every documented privilege delta remain unapplied; latest production schema evidence remains `0001`–`0008`. Current production rows and effective function privileges were not read, so production preflight remains `UNKNOWN`.
- **Superseding evidence:** independent `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-003` completed `FAIL / NO-GO` with `0 Critical / 3 High / 1 Medium`. It accepted the repository function-permission contract, fixed error-output boundary and formatted-phone password parity, while retaining the four defects addressed below.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-004 — repository remediation checkpoint

- **Status:** `PARTIALLY ACCEPTED / SUPERSEDED BY REVERIFY-004`; not repository-complete or production `VERIFIED`.
- **Scope:** repository code, tests, Handoff and risk register only. No production/database/cloud/ACR access, migration, GRANT/REVOKE, service start, provider call, deployment, commit, push or external cost occurred.
- **Independent input:** `REVERIFY-003` found two High final-export/global-fence defects: expired `running` leases could starve deletion forever, and direct Admin quota/Tracking writes could commit after the final export. It retained one High because `chainOfThoughts`, `ScratchPads`, `CoTTrace` and `CoTDetails` bypassed both sanitizers, plus one Medium because SQLite accepted calendar-invalid and Julian-number clocks that PostgreSQL `timestamptz` would not treat equivalently.
- **Corrections:** deletion blocks active or malformed leases and atomically settles an expired unlinked request as `failed / lease_expired` with its one-time refund under the same user fence; linked durable operations must already be terminal, while queued/running/outcome-unknown operations still block. A racing terminalizer therefore commits before deletion or is rejected afterward. Admin quota reset, disable/enable validation and Tracking trigger now acquire the canonical user advisory/row fence before user-owned rows. Diagnosis and Chat share semantic reasoning-family detection for chain-of-thought, plural scratchpad, `CoT*` and thought aliases while retaining approved business fields. SQLite validates real ISO calendar values in Python before any retention classification, rejecting `2024-02-30`, `2451545`, `0`, null, blank and ordinary unparsable clocks with the stable startup error.
- **Offline evidence:** security/compliance suite `45/45`; focused billing/idempotency/model-usage/admission set `104/104`; full isolated unit suite `692` passed with `5` skips; production readiness `86/86`; quality gate, Python compilation, Compose configuration and `git diff --check` passed. Databases were temporary and provider failures mocked; no external call or production write occurred.
- **Production truth unchanged:** migration `0009` and every documented privilege delta remain unapplied; latest production schema evidence remains `0001`–`0008`. PostgreSQL concurrency, current production rows and effective privileges were not read, so all production acceptance remains `UNKNOWN`.
- **Superseding evidence:** independent `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-004` accepted the deletion/lease and Admin/Tracking fence corrections but retained the reasoning-alias High and SQLite parity Medium described below.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-004 — independent repository review

- **Status:** `COMPLETE / FAIL / NO-GO`; severity `0 Critical / 1 High / 1 Medium`.
- **Boundary:** read-only code/threat-model review, static matrices and offline temporary-SQLite tests only. No implementation file, migration, privilege, production/cloud/ACR resource, service, supplier, commit or push was changed or accessed. Only this Handoff and the risk register were updated to record the result.
- **Accepted H4 corrections:** active and malformed leases fail closed; expired unlinked requests settle atomically as `failed / lease_expired` with at-most-once refund; linked queued/running/outcome-unknown work blocks deletion while succeeded/failed/cancelled terminal work settles consistently. The same user fence serializes the old terminalizer and final export. Admin quota/disable/enable and Tracking manual trigger use the canonical user fence and re-read protected rows. These repository paths are `PASS`; real PostgreSQL contention remains `UNKNOWN`.
- **Accepted regressions:** H1 exact built-in function permission contract remains `PASS` at repository level, H5 fixed Chat/S3/training error boundary remains `PASS`, and M2 formatted mainland-phone password parity remains `PASS`. Migration `0009`, effective runtime privileges and production history remain `UNAPPLIED / UNKNOWN`.
- **High — reasoning family still bypassable:** both diagnosis and Chat sanitizers remove the named `chainOfThoughts`, `ScratchPads`, `CoTTrace` and `CoTDetails` examples and retain the approved business fields, but still persist equivalent prefixed, concatenated or plural aliases such as `assistantCoTTrace`, `preCoT`, `CoTs`, `assistantScratchPad`, `modelScratchPadTrace`, `thoughtprocess`, `hiddenthought` and `innerthoughts`. Temporary SQLite proved representative aliases survive Chat persistence and reload. H6 is not closed.
- **Medium — SQLite/PostgreSQL time parity incomplete:** the precheck correctly rejects null, blank, ordinary invalid, `2024-02-30`, `2451545` and `0` across the four source fields before retention writes. However Python accepts ISO offsets `+0800` and `+08` that SQLite `datetime()` cannot classify, and SQLite truncates fractional seconds during comparisons. Both produce paid/free classification results that can differ from PostgreSQL `timestamptz`; whitespace and timezone normalization are also not canonicalized before SQL use. M5 is not closed.
- **Offline evidence:** existing security/compliance `45/45`, focused billing/idempotency/model-usage/admission `104/104`, full suite `692` passed with `5` skips, readiness `86/86`, quality gate, Python compilation, Compose configuration and `git diff --check` all passed. The green suites do not contain the newly failing extended matrices and therefore do not override the independent findings.
- **Disposition recorded by this review (subsequently executed):** `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-005` — repository-only remediation of exactly the remaining reasoning-family High and SQLite timestamp-parity Medium, with new negative matrices that reproduce both failures before correction. Its result is recorded immediately below.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-005 — repository remediation checkpoint

- **Status:** `PARTIALLY ACCEPTED / SUPERSEDED BY REVERIFY-005`; not repository-complete or production `VERIFIED`.
- **Scope:** only `model/api.py`, `model/db.py`, unapplied migration `0009` text, focused tests and the two checkpoint documents. No production/database/cloud/ACR/provider access, migration execution, GRANT/REVOKE, service start, deployment, commit, push or external cost occurred.
- **Reasoning-family correction:** diagnosis and Chat persistence now apply bounded normalized/compact matching to prefixed, concatenated and plural reasoning aliases. The new persistence/reload matrix covers `assistantCoTTrace`, `preCoT`, `CoTs`/`COTs`, assistant/model scratchpad forms, thought forms, `reasonings`, `thinkings` and `analyses`; it also proves `fact_enrichment.provider/raw_title`, `supplement_prompts[].prompt`, `cotton_material`, `cottage_style` and `mascots` remain intact.
- **SQLite/PostgreSQL time correction:** SQLite retention classification parses real ISO clocks once, normalizes them to UTC, preserves microseconds and compares paid windows in Python with the same `started_at <= created_at < expires_at` boundary. `Z`, whitespace, `+08:00`, `+0800`, `+08` and legacy full timezone-naive clocks are deterministic; the legacy naive contract is UTC. Pure dates, invalid calendars, more than six fractional digits and offsets beyond ±14 hours fail closed before any retention insert.
- **Unapplied migration text:** `0009_account_security_compliance.sql` now starts with `SET LOCAL TIME ZONE 'UTC';` so a future separately approved PostgreSQL execution interprets legacy naive clocks consistently. The migration remains unapplied and no privilege statement was executed.
- **Failing-first evidence:** both new matrices failed on the `REVERIFY-004` survivors before implementation and passed afterward. Final offline evidence is security/compliance `47/47`, focused billing/idempotency/model-usage/admission `106/106`, full unit suite `694` passed with `5` skips, readiness `86/86`, quality gate, Python compilation, Compose configuration and `git diff --check` all passing.
- **Production truth unchanged:** latest production schema evidence remains `0001`–`0008`; current production rows, effective privileges and real PostgreSQL contention were not read. The two repository corrections require independent review before their severities may be closed.
- **Next task recorded at this checkpoint (now completed):** `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-005` — its independent result is recorded immediately below.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-005 — independent repository review

- **Status:** `COMPLETE / FAIL / NO-GO`; severity `0 Critical / 1 High / 2 Medium`.
- **Boundary:** three independent read-only evidence streams, main-CTO static review, adversarial in-memory/temporary-SQLite probes and offline tests only. Implementation hashes were identical before and after review. No production/database/cloud/ACR/provider access, migration/GRANT, service start, deployment, commit, push or external cost occurred; only Handoff and risk-register conclusions changed.
- **Accepted reasoning subset:** the exact `HARDEN-005` aliases (`assistantCoTTrace`, `preCoT`, `CoTs`/`COTs`, assistant/model scratchpad, thought and plural reasoning words) are removed by both sanitizers. Actual provider-stream thinking/reasoning chunks are excluded. Approved scalar business fields and benign `cotton_material`, `cottage_style` and `mascots` remain intact.
- **High — persistence boundary remains bypassable:** `cots_trace`, `cots-trace`, `CotsTrace`, `assistantCotsTrace`, `postCotsTrace`, `cotsMetadata`, `agentcottrace` and comparable compound forms survive both sanitizers. Temporary SQLite proved representative values enter `messages_json` and `generate_ctx_json` and survive reload. `user_prefs_json` is unsanitized on write/reload; system-role messages and `systemEnvelope` survive raw storage, reload and account export; diagnosis sanitization also retains top-level provider envelopes. The approved business-field exceptions do not enforce scalar value types. This is one High root problem, not several independently counted findings.
- **Accepted SQLite subset:** strict real-calendar parsing, `Z`, whitespace, `+08:00`, `+0800`, `+08`, legacy naive-as-UTC, UTC normalization, microseconds and `started_at <= created_at < expires_at` all pass for Notes and Diagnoses. Null/blank/date-only/invalid/>6-fraction/>±14-hour input fails closed locally.
- **Medium 1 — PostgreSQL migration preflight is weaker:** `0009` relies on `TEXT::timestamptz` rather than applying the same strict source-clock contract across every Note, Diagnosis and Subscription row. PostgreSQL accepts or rounds inputs that SQLite deliberately rejects, and an invalid subscription with no content may not be evaluated by the migration `EXISTS` expressions. Repository SQLite/PostgreSQL parity is therefore incomplete.
- **Medium 2 — runtime naive-as-UTC is not invariant:** `SET LOCAL TIME ZONE 'UTC'` correctly scopes the future `0009` migration transaction, but the PostgreSQL connection factory does not establish UTC. Later runtime `started_at::timestamptz`/`expires_at::timestamptz` casts of legacy naive text depend on the session `TimeZone`.
- **Accepted prior controls:** deletion/expired-lease/durable-operation/refund and Admin/Tracking fences, H1 exact built-in function-permission contract, H5 stable Chat/S3/training error boundaries and M2 formatted-phone password parity all remain `PASS`. Real PostgreSQL contention/effective privileges and production history remain `UNKNOWN`; migration `0009` remains unapplied.
- **Offline evidence:** security/compliance `47/47`, focused accepted-control regression `106/106`, full unit suite `694` passed with `5` skips, readiness `86/86`, quality gate, Python compilation, Compose configuration, migration privilege static check and `git diff --check` all passed. These green suites do not override the independently reproduced persistence and PostgreSQL-contract findings.
- **Disposition recorded by this review (now executed):** `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-006` — its bounded candidate corrections and offline evidence are recorded immediately below.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-006 — repository remediation checkpoint

- **Status:** `PARTIALLY ACCEPTED / SUPERSEDED BY REVERIFY-006`; the two PostgreSQL time-contract Medium findings are accepted at repository level, but the structural-persistence High remains open and nothing here is production `VERIFIED`.
- **Scope:** only `model/api.py`, `model/db.py`, unapplied migration `0009`, focused tests and the two checkpoint documents. No production/database/cloud/ACR/provider access, migration execution, GRANT/REVOKE, service start, deployment, commit, push or external cost occurred.
- **Structural persistence correction:** diagnosis and every persisted Chat JSON surface now use bounded compound-CoT/internal-envelope filtering. Chat messages persist only string `role/content` for `user` or `assistant`; nested system/developer/tool/function records are removed. `user_prefs`, generate context, raw database rows, reload and account export share the sanitizer. Provider/raw/prompt fields are default-denied except typed string `fact_enrichment.provider`, `fact_enrichment.raw_title` and `supplement_prompts[].prompt`.
- **Adversarial evidence:** failing-first tests reproduced compound aliases, compact/camel system/provider envelopes, nested system roles, unsanitized preferences/context/export and non-scalar business exceptions before correction. The final temporary in-memory matrix blocked all `25,500` generated compound-CoT variants in both diagnosis and Chat paths while preserving benign `cotton_material`, `cottage_style`, `mascots`, `apricot_color` and `mascots_metadata`.
- **PostgreSQL time-contract correction:** the SQLite source-clock parser now enforces real `00–23:00–59:00–59` time bounds, one-to-six fractional digits and offsets no greater than `±14:00`. Before `content_retention` is created, `0009` strictly preflights all four source columns—Notes and Diagnoses `created_at`, plus Subscriptions `started_at` and `expires_at`—with trimmed textual shape, defensive offset checks and a stable fail-closed cast error. Every runtime PostgreSQL connection now sets `timezone=UTC` while preserving an optional statement-timeout option.
- **Failing-first and final evidence:** both new primary tests failed on the `REVERIFY-005` implementation; additional system-role, compact-envelope, bare compound-CoT and strict-clock cases also failed before their bounded corrections. Final offline evidence is security/compliance `49/49`, focused account-security/billing/idempotency/admission/model-usage `108/108`, full unit suite `696` passed with `5` skips, readiness `86/86`, quality gate, Python compilation, Compose configuration and `git diff --check` all passing. Tests used temporary data and mocks; no external request or production write occurred.
- **Production truth unchanged:** production remains at migrations `0001`–`0008`; `0009` and every documented privilege delta remain unapplied. Current production rows, effective PostgreSQL privileges, real PostgreSQL cast/lock behavior and historical persisted material were not read. Independent review is required before changing any finding severity.
- **Next task recorded at this checkpoint (now completed):** `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-006` — its independent result is recorded immediately below.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-006 — independent repository review

- **Status:** `COMPLETE / FAIL / NO-GO`; severity `0 Critical / 1 High / 0 Medium`.
- **Boundary:** three independent read-only review streams plus main-CTO static review, generated matrices, temporary-SQLite probes and offline tests. No implementation file, migration, privilege, production/database/cloud/ACR/provider resource, service, deployment, commit or push was changed or accessed; only this Handoff and the risk register record the verdict.
- **Accepted PostgreSQL time contract:** the strict parser accepts only real `00–23:00–59:00–59` clocks, one-to-six fractional digits and offsets no greater than `±14:00`, trims outer whitespace and defines legacy naive clocks as UTC. Migration `0009` preflights all four source columns before retention table creation/backfill and produces one stable fail-closed error. Every application runtime PostgreSQL connection fixes UTC while health-only statement timeout remains isolated. Target tests `5/5`, parser probes `8 valid / 16 invalid`, migration ordering/static probes and security suite `49/49` passed. This closes the two `REVERIFY-005` Medium findings only at repository-contract level; disposable/production PostgreSQL behavior remains `UNKNOWN / UNAPPLIED`.
- **Accepted prior controls:** deletion/lease/durable-operation/refund and Admin/Tracking fences, H1 built-in-function permission contract, H5 stable Chat/S3/training errors, M2 formatted-phone password parity and readiness behavior remain `PASS`. The independent regression stream passed security `49/49`, focused billing/idempotency/admission/model-usage `59/59` in addition to the security set (`108/108` total), readiness-health `22/22`, production readiness `86/86`, migration privilege scan and `git diff --check`.
- **High — structural persistence remains bypassable:** the two finite denylists still admit compact/bare aliases including `privatecot`, `rawcot`, `debugcot`, `vendorcot`, `completioncot`, `cotlog`, `cotscontext` and `assistantcotsteps`; tool/function and compact internal envelopes including `functionCall`, `toolCall`, `systemcontext`, `providercontext`, `internalcontext` and `developercontext` also survive. Role detection reads only the exact lowercase `role` key, so `Role`, `ROLE` and `messageRole` internal records survive the generic diagnosis/Chat sanitizer. Main-CTO probes independently reproduced lowercase `auditcots`, `providerdata`, `rawdata` and typed reasoning-envelope variants.
- **Persistence proof:** an independent matrix found `852/9,926` CoT variants and `1,532/5,760` internal-envelope variants still accepted by each sanitizer, while the non-CoT reasoning family blocked `6,986/6,986`. Temporary SQLite proved representative values enter `user_prefs_json` and `generate_ctx_json`, then survive raw-row inspection, reload and account export. The strict `messages_json` `user/assistant + string role/content` schema itself passes, but the next fix must also fail closed rather than erase a reasoning/type marker while retaining its content.
- **Accepted positive behavior:** typed string `fact_enrichment.provider/raw_title` and `supplement_prompts[].prompt`, ordinary user/assistant text, actual public provider content chunks and benign `cotton/cottage/mascot/apricot` business keys remain available. Invalid non-string business exceptions and standard system/developer/tool/function role records are rejected.
- **Main regression evidence:** focused set `108/108`, full unit suite `696` passed with `5` skips, production readiness `86/86`, quality gate, Python compilation, Compose configuration and `git diff --check` passed. These green suites do not contain all independently failing variants and cannot override the High.
- **Root cause and disposition:** extending a finite alias/suffix denylist cannot prove a default-deny persistence contract. The next correction must move persisted diagnosis, Chat preferences/context and account export to explicit path/type schemas, canonicalize role/type discriminator keys, reject unknown internal envelopes before extracting public content, and add the independent survivors as failing-first raw-DB/reload/export tests.
- **Disposition recorded by this review (now executed):** `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-007` — its bounded candidate correction and offline evidence are recorded immediately below.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-007 — repository remediation checkpoint

- **Status:** `PARTIALLY ACCEPTED / SUPERSEDED BY REVERIFY-007`; preferences/learned-preferences schemas and exact discriminator controls are accepted, but the structural-persistence High remains open and nothing here is production `VERIFIED`.
- **Scope:** only `model/api.py`, focused tests and the two checkpoint documents. PostgreSQL time parsing, `model/db.py`, migration `0009` and its permission contract were deliberately unchanged. No production/database/cloud/ACR/provider access, migration execution, GRANT/REVOKE, service start, deployment, commit, push or external cost occurred.
- **Structural correction:** persisted diagnosis now has an explicit root schema; Chat preferences, generation context, message rows, learned-preference export and account export each use explicit path/type schemas. Role/type discriminator keys and values are canonicalized across case and compact aliases; any message carrying an internal discriminator or marker is rejected as a whole rather than retaining its content after marker removal. Approved typed `fact_enrichment`, supplement prompts, ordinary user/assistant strings and documented benign Chat fields remain available.
- **Failing-first evidence:** two new tests first failed against the `REVERIFY-006` implementation: unlisted compact/discriminator envelopes survived direct sanitization, and internal-marked messages plus unknown preference/context fields survived raw SQLite, reload and export. Both pass after the bounded correction.
- **Adversarial evidence:** an offline generated matrix challenged `20,520` unique CoT variants and `9,072` unique internal-envelope variants. Diagnosis and Chat sanitizers each retained `0` variants from both sets, retained the approved benign object unchanged, and left `0` internal-discriminator records.
- **Final offline evidence:** new H7 tests `2/2`; security/compliance `51/51`; focused account-security/billing/idempotency/admission/model-usage `110/110`; full unit suite `698` passed with `5` skips; production readiness `86/86`; quality gate, Python compilation, Compose configuration and `git diff --check` passed. Databases were temporary and supplier paths mocked; there was no external request or production write.
- **Accepted contract preserved:** the `HARDEN-006` PostgreSQL all-source preflight/runtime UTC contract accepted by `REVERIFY-006` was not reworked. Migration `0009` and every documented privilege delta remain unapplied; real PostgreSQL and production history remain `UNKNOWN`.
- **Production truth unchanged:** the current security/compliance worktree has not produced or deployed a new image. API-C/API-F/Admin state, stopped XHS/Tracking state, ALB/TLS/DNS and production traffic are unchanged.
- **Next task recorded at this checkpoint (now completed):** `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-007` — its independent result is recorded immediately below.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-007 — independent repository review

- **Status:** `COMPLETE / FAIL / NO-GO`; severity `0 Critical / 1 High / 0 Medium`.
- **Boundary:** three independent read-only review streams plus main-CTO static review, generated matrices, temporary-SQLite probes and offline regression tests. No implementation file, migration, privilege, production/database/cloud/ACR/provider resource, service, deployment, commit or push was changed or accessed; only this Handoff and the risk register record the verdict.
- **High — recursive schema and compound discriminators remain bypassable:** Diagnosis applies an exact root-field allowlist but delegates every allowed nested object to the generic denylist rather than a field/type schema. Chat `constraint_contract`, `intent_contract` and `fact_enrichment` mappings do the same. Compound discriminator values such as `function_call`, `tool_call`, `tool_result`, `system_message` and `developer_message` are not classified internal; Chat messages therefore erase the `type` marker and retain the content instead of rejecting the whole row.
- **Independent matrix:** a separate generator tested `72,890` unique internal-key variants and found `126` survivors in each generic Diagnosis, generic Chat, allowed nested `fact_enrichment` and Chat-message-marker path. The survivors exploit global compact `startswith` benign-prefix exceptions such as `mascotcottrace` and `apricot-cotdetails`. Basic role/type/kind case and punctuation controls blocked `5,440/5,440`, but among `130,900` compound-discriminator combinations Diagnosis retained `57,460` and Chat retained `47,320`.
- **Persistence proof:** temporary SQLite proved representative benign-prefix and compound-discriminator sentinels enter Diagnosis and Chat raw rows, survive single Diagnosis read and Chat reload, and remain in account export. A separate main-CTO probe also showed untyped nested `password`, `apiKey`, `bearer`, session/request/response/instruction/model/debug fields survive permitted Diagnosis and Chat mapping paths. Production history was not read; the repository boundary failure alone retains the High.
- **Accepted positive behavior:** Chat preferences and learned preferences are explicitly field/string-bound. Exact system/developer/tool/function and reasoning values are rejected. Current `AnalyzeResponse` root fields, all 22 current Chat context fields, five preference fields, ordinary user/assistant messages, typed `fact_enrichment.provider/raw_title`, supplement prompts and legitimate cotton/cottage/mascot/apricot business data remain available.
- **Accepted regressions:** security `51/51`, reasoning safety `8/8`, focused account-security/billing/idempotency/admission/model-usage `110/110`, full unit suite `698` passed with `5` skips, production readiness `86/86`, quality gate, Python compilation, Compose configuration and `git diff --check` passed. PostgreSQL time contract, migration `0009`, permission matrix and accepted prior controls were unchanged; `0009` still contains no executable GRANT/REVOKE.
- **UNKNOWN / unapplied:** production historical persistence, real supplier output variants, real PostgreSQL contention/effective privileges and `0009` apply-twice/rollback remain unobserved. Migration `0009` and every permission delta remain unapplied.
- **Root cause:** a root allowlist followed by generic recursive denylisting is not an explicit recursive path/type contract; global benign `startswith` exceptions and exact-only discriminator values reopen that boundary. Green regression suites do not include the independent failing matrix and cannot override the High.
- **Disposition recorded by this review (now executed):** `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-008` — its bounded candidate correction and offline evidence are recorded immediately below.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-008 — repository remediation checkpoint

- **Status:** `PARTIALLY ACCEPTED / SUPERSEDED BY REVERIFY-008`; recursive schemas, exact benign allowances and strict Chat-message rejection remain accepted, but the same structural-persistence High and one positive-parity Medium remain open.
- **Scope:** only `model/api.py`, focused tests and the two checkpoint documents. PostgreSQL time parsing, `model/db.py`, migration `0009` and its permission contract were deliberately unchanged. No production/database/cloud/ACR/provider access, migration execution, GRANT/REVOKE, service start, deployment, commit, push or external cost occurred.
- **Recursive public schemas:** every persisted Diagnosis root mapping and its retained nested mappings are now projected through explicit path/type schemas. Chat constraint, intent and fact-enrichment mappings reuse those typed schemas; feature, report, keyword, health, market-timing and related nested records are likewise type-bound. Unknown nested fields and invalid record types fail closed.
- **Exact discriminator boundary:** the global compact `startswith` exceptions for `apricot/cottage/cotton/mascot` were replaced by exact normalized business-key allowances. Compound internal values such as function/tool calls and results, system/developer messages, provider/internal contexts and reasoning traces are rejected across normalized role/type/kind discriminator keys. A Chat message with any extra marker is rejected as a whole.
- **Failing-first evidence:** two new R7 survivor tests first failed against H7: recursive Diagnosis/Chat mappings retained untyped keys and invalid records, and representative Chat/Diagnosis sentinels crossed raw SQLite, reload and account export. Both pass after H8.
- **Adversarial evidence:** an offline generated matrix challenged `29,697` unique internal-key variants across generic and nested Diagnosis/Chat paths with `0` unapproved survivor; the only retained tested name was exact typed `fact_enrichment.provider`, which is part of the approved public schema. A separate `14,040` compound-discriminator matrix produced `0` survivor across generic Diagnosis, generic Chat, typed Diagnosis and Chat-message paths.
- **Positive parity:** current `AnalyzeResponse` roots have complete schema coverage; representative feature, report, constraint, intent, fact-source and full Chat context shapes round-trip unchanged. Approved ordinary user/assistant messages and exact benign business fields remain available.
- **Final offline evidence:** new H8 tests `2/2`; security/compliance `53/53`; reasoning safety `8/8`; focused account-security/billing/idempotency/admission/model-usage `112/112`; API contracts `152/152`; full unit suite `700` passed with `5` skips; production readiness `86/86`; quality gate, Python compilation, Compose configuration and `git diff --check` passed. Databases were temporary and provider/XHS paths mocked; there was no external request or production write.
- **Accepted contracts preserved:** the H6 PostgreSQL all-source preflight/runtime UTC contract accepted by `REVERIFY-006` was not reworked. Migration `0009` and every documented privilege delta remain unapplied; real PostgreSQL and production history remain `UNKNOWN`.
- **Production truth unchanged:** the current worktree has not produced or deployed a new image. API-C/API-F/Admin state, stopped XHS/Tracking state, ALB/TLS/DNS and production traffic are unchanged.
- **Next task recorded at this checkpoint (now completed):** `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-008` — its independent result is recorded immediately below.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-008 — independent repository review

- **Status:** `COMPLETE / FAIL / NO-GO`; severity `0 Critical / 1 High / 1 Medium`.
- **Boundary:** three independent read-only review streams plus main-CTO source review, generated matrices, automatically cleaned temporary-SQLite lifecycle probes and offline regression tests. No implementation, migration, privilege, production/database/cloud/ACR/provider resource, service, deployment, commit or push was changed or accessed; only this Handoff and risk register record the verdict.
- **High — discriminator default-deny remains incomplete:** `_is_internal_discriminator_key` recognizes only exact/start/end `kind/role/type`, so middle-position keys such as `messageTypeLabel`, `payloadRoleMetadata` and `inputKindEnvelope` survive. `_is_internal_discriminator_value` still depends on finite semantic families, so unlisted values such as `debug_trace`, `hidden_state`, `completion_data`, `model_context`, request/response/session state and telemetry/audit events survive. Explicit projectors then ignore the marker key and retain approved-looking content rather than rejecting the entire record.
- **High — expert special projection bypass:** `_public_expert_opinions` and nested expert evidence project approved-looking `opinion/text/agent_text` fields without first rejecting internal role/type/kind records. Typed Diagnosis and typed Chat both reuse this path; `system_message + opinion`, `tool_result + opinion` and `developer_message + evidence.text` therefore become public-looking persisted content.
- **Independent matrices:** one stream tested `14` new value families × `10` discriminator keys across generic Diagnosis, generic Chat, typed Diagnosis plans, Chat plan options and supplement prompts: `700/700` records retained the synthetic content. Four known control values over the same keys were blocked `0/200`, proving the test path itself works. A second `3×3` middle-key/value matrix yielded `9/9` survivors across generic and typed Diagnosis/Chat.
- **Persistence proof:** independent temporary SQLite probes proved representative surviving values enter Chat `generate_ctx_json` and Diagnosis `diagnosis_json`, remain after Chat/Diagnosis reload and remain in account export. The main-CTO probe independently reproduced top-level Diagnosis/Chat, expert-opinion and preference survivors across the same lifecycle. Strict marked Chat messages and recognized nested `fact_enrichment` controls were blocked.
- **Medium — real Analyze positive-shape regression:** `_CLOUD_SYNC_SCHEMA.source_endpoint` requires a string, while the real producer returns the already-redacted `{scheme, host}` mapping; persisted Diagnosis/history/export therefore lose both endpoint leaves. `input_diagnostics.content_intent`, `merchant_visibility`, `merchant_name` and `fact_source_policy` are legitimately nullable at the producer but typed non-null string by H8, so valid `None` fields disappear. A real `AnalyzeResponse`-shaped comparison reproduced all six lost leaves; representative Chat context parity remains `PASS`.
- **Accepted H8 controls:** whole-message Chat rejection, exact `apricot/cottage/cotton/mascot` business allowances, nested typed constraint/fact mappings, ordinary user/assistant messages and recognized function/tool/system/reasoning controls remain `PASS`. H6 PostgreSQL time contract, migration `0009`, permission matrix and accepted error boundaries are unchanged.
- **Regression evidence:** security plus reasoning `61/61`; focused account-security/billing/idempotency/admission/model-usage `112/112`; API contracts `152/152`; full unit suite `700` passed with `5` skips; production readiness `86/86`; quality gate, Python compilation, Compose configuration and `git diff --check` passed. These suites do not cover the independent survivors and cannot override the verdict.
- **UNKNOWN / unapplied:** production historical persistence, real provider value frequency, real PostgreSQL apply-twice/casts/locks/effective privileges and managed Secret history remain unobserved. Migration `0009` and every permission delta remain unapplied.
- **Root cause:** the implementation still infers internal records from a finite discriminator spelling/value denylist instead of applying a per-schema rule that any unapproved discriminator-bearing record fails closed; expert projection is outside that check. Positive schemas were also not reconciled against two real producer contracts.
- **Disposition recorded by this review (now executed):** `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-009` — its bounded candidate correction and offline evidence are recorded immediately below.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-009 — repository remediation checkpoint

- **Status:** `IMPLEMENTED / OFFLINE SELF-VALIDATED / INDEPENDENT REVERIFY-009 RETAINS ONE HIGH`; accepted controls remain, but the candidate did not close the structural-persistence High and is not production `VERIFIED`.
- **Scope:** only `model/api.py`, focused tests and the two checkpoint documents. PostgreSQL time parsing, `model/db.py`, migration `0009` and its permission contract were deliberately unchanged. No production/database/cloud/ACR/provider access, migration execution, GRANT/REVOKE, service start, deployment, commit, push or external cost occurred.
- **Per-schema discriminator contract:** typed public records now reject the whole mapping whenever any role/type/kind-like key is not explicitly declared by that schema. Detection covers normalized tokens and concatenated/middle-position variants rather than depending on the discriminator value. Diagnosis root, Chat context and Chat preferences apply the same root rule; strict Chat messages explicitly allow only `role` with exact `user/assistant` values and reject every extra field.
- **Intended expert recursive projection:** H9 attempted to allow only canonical public expert roles and the declared evidence `source_type`; `REVERIFY-009` disproves exact enforcement for aliases and role prefixes below. Provider raw remains an input to the existing bounded public-opinion parser but is never persisted directly in the output.
- **Positive-shape correction:** `cloud_sync.source_endpoint` now follows the real already-redacted `{scheme, host}` producer shape. `input_diagnostics.content_intent`, `merchant_visibility`, `merchant_name` and `fact_source_policy` now retain legitimate `None` values as nullable strings.
- **Failing-first evidence:** two new R8 lifecycle/shape tests first produced `184` failures on H8, covering unlisted values, middle-position keys, root Diagnosis/Chat/preferences, expert opinions/evidence, raw DB/reload/export and the real Analyze shape. Both pass after H9. One existing Chat test was intentionally updated because its root contained an undeclared `typed` discriminator envelope; H9 correctly rejects that whole marked context instead of retaining public-looking siblings.
- **Adversarial evidence:** an offline matrix tested `13` discriminator-key forms × `14` previously unlisted values × `5` generic/typed Diagnosis/Chat surfaces (`910` projections) and retained `0` synthetic value. Expert internal records retained `0`, while the canonical public expert/evidence control remained intact.
- **Lifecycle and parity:** temporary SQLite proves R8 root/expert/plan/prompt/preference values are absent from raw Chat/Diagnosis rows, Chat and Diagnosis reload and account export. The real safe endpoint mapping and all four nullable fields survive raw Diagnosis, reload and export; representative Chat safe messages and canonical expert evidence also survive.
- **Final offline evidence:** new H9 tests `2/2`; security/compliance `55/55`; reasoning safety `8/8`; combined security/reasoning `63/63`; focused account-security/billing/idempotency/admission/model-usage `114/114`; API contracts `152/152`; full unit suite `702` passed with `5` skips; production readiness `86/86`; quality gate, Python compilation, Compose configuration and `git diff --check` passed. Databases were temporary and provider/XHS paths mocked; there was no external request or production write.
- **Accepted contracts preserved:** the H6 PostgreSQL all-source preflight/runtime UTC contract accepted by `REVERIFY-006` was not reworked. Migration `0009` and every documented privilege delta remain unapplied; real PostgreSQL and production history remain `UNKNOWN`.
- **Production truth unchanged:** the current worktree has not produced or deployed a new image. API-C/API-F/Admin state, stopped XHS/Tracking state, ALB/TLS/DNS and production traffic are unchanged.
- **Independent disposition:** `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-009` completed with the result immediately below.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-009 — independent repository review

- **Status:** `COMPLETE / FAIL / NO-GO`; severity `0 Critical / 1 High / 0 Medium`. The R8 positive-shape Medium is independently closed at repository level; the structural-persistence High remains open.
- **Boundary:** three independent read-only/offline review streams plus main-CTO source review and automatically cleaned temporary-SQLite probes. No implementation, migration, privilege, production/database/cloud/ACR/provider resource, service, deployment, commit, push or external cost occurred; only this Handoff and risk register record the verdict.
- **Accepted ordinary boundary:** an independent `18 keys × 8 values × 16 surfaces = 2,304` generic/root/nested Diagnosis/Chat matrix retained `0` unapproved item. A separate `9×11×11=1,089` persistence matrix also retained `0`. Strict Chat, exact approved business keys, nested constraint/intent/fact schemas and recognized invalid controls pass.
- **High — expert contract remains bypassable:** `_has_unapproved_discriminator_key` compares normalized allowlisted keys, while expert code later reads exact raw `role` and `source_type` keys. Aliases such as `Role` and `sourceType` are therefore treated as declared but evade their value checks. `_public_expert_role` also uses prefix matching, so values such as `内容专家/system_message` are reduced to the public role and retain their opinion.
- **Independent expert evidence:** six public role prefixes × ten internal suffixes × direct/Diagnosis/Chat produced `180/180` survivors; eight `role` aliases × eight values × three surfaces produced `192/192`; eight `source_type` aliases × eight values × three surfaces produced `192/192`. Total expert survivors were `564/564`; exact invalid controls and additional declared-key controls retained `0/912`.
- **Lifecycle proof:** three representative sentinel families—noncanonical role suffix, `Role` alias and `sourceType` alias—survived Chat raw/reload, Diagnosis raw/reload and account export: `15/15`. The main CTO independently reproduced noncanonical role persistence across all five stages and also found Unicode/mixed-script discriminator lookalikes surviving `180/180` projections and all five lifecycle stages. Every temporary database was removed.
- **Positive-shape Medium closed:** the independent real `AnalyzeResponse` comparison retained all `30` roots and `175` leaves with `0` missing or changed. Safe `source_endpoint {scheme,host}` and all four nullable diagnostics survived raw/reload/export. All six canonical expert roles, seven evidence sources, a representative `22`-root/`74`-leaf Chat context and ordinary messages retained exact public shape.
- **Regression evidence:** frozen H6 artifacts remained byte-identical. Security plus reasoning `63/63`; focused account-security/billing/idempotency/admission/model-usage `114/114`; API contracts `152/152`; full suite `702` passed with `5` skips; production readiness `86/86`; quality gate, Python compilation, Compose configuration and `git diff --check` passed. Passing matrices cannot override a single survivor because acceptance required zero.
- **UNKNOWN / unapplied:** production history, real provider variant frequency, managed Secret delivery/history, real PostgreSQL migration `0009` apply-twice/casts/locks/effective privileges and every permission delta remain unobserved or unapplied.
- **Disposition recorded by this review (now executed):** `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-010` — its bounded candidate correction and offline evidence are recorded immediately below.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-010 — repository remediation checkpoint

- **Status:** `IMPLEMENTED / OFFLINE SELF-VALIDATED / INDEPENDENTLY REJECTED BY REVERIFY-010`; its accepted exact-key/exact-role and bounded Unicode controls remain in the worktree, but it did not close the retained High and is not production `VERIFIED`.
- **Scope:** only `model/api.py`, `tests/test_account_security_compliance.py`, `tests/test_reasoning_safety.py` and the two checkpoint documents. `model/db.py`, migration `0009`, its permission matrix, production resources and provider paths were deliberately untouched. No migration/GRANT/REVOKE, production/database/cloud/ACR/provider access, service start, deployment, commit, push or cost occurred.
- **Failing-first evidence:** two new H10 tests failed on H9 before implementation. The direct test first reproduced a canonical-role prefix plus internal suffix; the lifecycle test's raw Chat row contained all four sentinels: noncanonical role suffix, `Role` alias, `sourceType` alias and Unicode discriminator key.
- **Exact expert contract:** allowlisted discriminator keys are now compared against the exact raw key rather than a normalized alias. Expert roles use exact membership in the six-role public contract instead of prefix matching. Existing provider/reasoning metadata remains field-level filtered, so a canonical expert's safe public explanation is preserved while raw/provider/reasoning fields remain absent.
- **Unicode boundary:** identifier normalization now applies Unicode NFKC before the existing token/compact checks. A bounded mixed-script detector catches reproduced `role/type/kind` forms split by one embedded non-ASCII identifier glyph; it does not make an unbounded claim about every possible Unicode confusable.
- **Negative matrices:** six roles × ten internal suffixes × three direct/Diagnosis/Chat surfaces, eight role aliases × eight internal values × three surfaces, and eight source aliases × eight values × three surfaces produce `564/564` blocked projections with zero sentinel. Six reproduced Unicode/mixed-script keys × three values × ten generic/typed/expert surfaces produce `180/180` blocked projections.
- **Lifecycle and positive parity:** four survivor-family sentinels are absent from Chat raw/reload, Diagnosis raw/reload and account export: `20/20` absence observations. H9's real endpoint/nullable lifecycle test and ordinary per-schema test remain green. Canonical expert/evidence, safe historical public explanations, normal messages and real Analyze/Chat shapes remain preserved by the accepted regression suites.
- **Offline validation:** H10 plus H9 focused tests `4/4`; security/compliance plus reasoning `65/65`; focused account-security/billing/idempotency/admission/model-usage `116/116`; API contracts `152/152`; full unit suite `704` passed with `5` skips; production readiness `86/86`; quality gate, Python compilation, Compose configuration and `git diff --check` passed. Provider/XHS paths were mocked or fail-closed; no real external request occurred.
- **Frozen controls:** H6 files remain byte-identical to the R9 checkpoint: `model/db.py` SHA-256 `a4925f99…d558`, migration `0009` `6e51274b…4dc`, and permission matrix `04edddd7…101`. Migration `0009` and every permission delta remain unapplied.
- **Independent disposition:** `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-010` completed with the result immediately below. H10 remains useful for its accepted exact-key/exact-role and one-glyph controls, but it did not close the structural High.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-010 — independent repository review

- **Status:** `COMPLETE / FAIL / NO-GO`; severity `0 Critical / 1 High / 0 Medium`. The H10 candidate is not independently accepted and the structural-persistence High remains open.
- **Boundary:** three independent read-only/offline review streams plus main-CTO source review and automatically cleaned temporary-SQLite probes. Only this Handoff and risk register were changed. No implementation/test/migration/permission change, production/database/cloud/ACR/provider access, service start, deployment, commit, push or cost occurred.
- **Accepted H10 controls:** exact six-role membership blocks every newly generated noncanonical role suffix (`0/162` survivor). A separate ASCII/NFKC/separator/one-glyph control matrix retained `0/2,016`, all six canonical roles and seven evidence-source combinations retained exact public shape, and provider/reasoning material remained absent while approved historical explanations and business `fact_enrichment.provider/raw_title` survived.
- **High — bounded Unicode detector is bypassable:** `_is_mixed_script_discriminator_lookalike` requires one embedded non-ASCII alphanumeric glyph and compares the ASCII residue to a one-character-deleted `role/type/kind`. Two or more replacements, an edge replacement or some combining forms reduce the residue beyond that contract. Fresh examples include `rоlе`, `tуpе`, `kіոԁ` and multi-glyph `source_type` aliases. Exact `role/source_type` lookup then sees no declared key, while public-looking sibling opinion/evidence/body text survives.
- **Independent negative evidence:** the schema stream found `2,016/2,016` survivors across 24 fresh Unicode keys × six internal values × 14 direct/generic/typed/expert surfaces. The persistence stream found `999/1,182` total survivors: role suffixes `0/162`, multi-confusable role aliases `210/210`, source aliases `189/210`, and generic/typed/expert discriminator fields `600/600`. The main CTO independently reproduced `120/120` survivors across four public projections and `12/12` expert role/source cases.
- **Lifecycle proof:** independent role/source/field markers survived all `18/18` pre-save/raw Chat/raw Diagnosis/Chat reload/Diagnosis reload/export observations. The main CTO separately confirmed five representative marker families in raw storage, reload and export; its temporary directory no longer existed after cleanup. Historical provider/private markers remained absent.
- **Positive and regression evidence:** canonical expert, historical provider redaction, Analyze endpoint, four nullable inputs and normal Chat parity pass. Frozen H6 hashes are unchanged. Targeted controls `11/11`, security/reasoning `65/65`, focused suites `116/116`, API contracts `152/152`, full suite `704` passed with `5` skips, readiness `86/86`, quality gate, Python compilation, Compose configuration and `git diff --check` passed. Green regressions do not override the zero-survivor failure.
- **Root cause and bounded disposition:** continuing to add Unicode spellings to a heuristic denylist would repeat the same failure class. The next correction must make typed records fail closed from explicit per-schema raw-key declarations, including deliberate filter-only compatibility keys for historical provider/reasoning input, instead of claiming exhaustive Unicode-confusable recognition.
- **Disposition recorded by this review (now executed):** `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-011` — its structural candidate correction and offline evidence are recorded immediately below.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-011 — repository remediation checkpoint

- **Status:** `IMPLEMENTED / OFFLINE SELF-VALIDATED / INDEPENDENTLY REJECTED BY REVERIFY-011`; its exact raw-key controls remain useful, but the retained High is not closed and nothing is production `VERIFIED`.
- **Scope:** only `model/api.py`, `tests/test_account_security_compliance.py` and the two checkpoint documents. `model/db.py`, migration `0009`, its permission matrix, production resources and provider paths were deliberately untouched. No migration/GRANT/REVOKE, production/database/cloud/ACR/provider access, service start, deployment, commit, push or cost occurred.
- **Failing-first evidence:** two new H11 tests failed on H10 before implementation. The projection test retained the first edge-form marker under `τype`; the lifecycle test found expert role/source and plan markers in raw Chat JSON. This reproduced the independent R10 root cause without copying its complete matrix.
- **Structural contract:** `_has_undeclared_raw_key` requires every raw object key to belong to an exact declared input set or an exact filter-only compatibility set. Expert opinions and evidence have separate contracts; only `_image_desc`, `provider`, `reasoning` and `reasoning_content` are accepted as historical expert filter-only metadata. Typed Diagnosis schema objects, feature maps and root records, plus Chat context, preferences, plans, supplement prompts, feature hits and confirmed values, now reject the affected record on any undeclared raw key.
- **Legacy compatibility candidate:** the two legacy recursive projectors use an exact union of current public schemas and named legacy public fields, plus a finite exact filter-only set. Known historical reasoning/provider spellings remain removable without losing approved siblings; a new spelling is rejected rather than inferred. The H11 claim that dynamic typed maps are not treated as discriminator records is stale and was disproved by R11.
- **Negative matrices and lifecycle:** twelve multi-glyph/edge/combining Unicode forms plus one ordinary undeclared-field control × three values × ten generic/typed/expert surfaces produce `390/390` blocked projections with zero marker. Five representative expert role, expert source, Chat plan, Diagnosis root and Diagnosis plan markers are absent from raw Chat, raw Diagnosis, Chat reload, Diagnosis reload and account export: `25/25` absence observations.
- **Self-validation limitation:** hand-built canonical expert/evidence, historical raw expert text, safe endpoint, four nullable fields and ordinary Chat shapes passed. R11 subsequently proved that these fixtures omitted public fields emitted by two real producers, so the earlier “real Analyze shape green” conclusion is stale.
- **Offline validation:** H9/H10/H11 focused tests `6/6`; security/compliance plus reasoning `67/67`; focused account-security/billing/idempotency/admission/model-usage `118/118`; API contracts `152/152`; full unit suite `706` passed with `5` skips; production readiness `86/86`; quality gate, Python compilation, Compose configuration and `git diff --check` passed. Provider/XHS paths were mocked, synthetic or fail-closed; no real external request occurred.
- **Frozen controls:** H6 files remain byte-identical: `model/db.py` SHA-256 `a4925f99…d558`, migration `0009` `6e51274b…4dc`, and permission matrix `04edddd7…101`. Migration `0009` and every permission delta remain unapplied.
- **Independent disposition:** `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-011` completed with the result immediately below. It retained the High because approved real-producer shape was lost.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-011 — independent repository review

- **Status:** `COMPLETE / FAIL / NO-GO`; severity `0 Critical / 1 High / 0 Medium`. H11 is not independently accepted and the structural-persistence High remains open.
- **Boundary:** three independent read-only/offline streams plus main-CTO source review and automatically cleaned temporary-SQLite probes. Only this Handoff and risk register were changed. No implementation/test/migration/permission change, production/database/cloud/ACR/provider access, service start, deployment, commit, push or cost occurred.
- **Negative safety evidence:** the independent schema stream blocked `966/966` fresh undeclared-key projections, `256/256` filter-only aliases and all `42/42` raw/reload/export marker observations; its `3,180` finite filter-only observations found zero true private leak and zero approved-sibling loss. The persistence stream independently blocked `832/832` projections and all `70/70` lifecycle observations. The main CTO blocked `728/728` fresh projections, preserved/dropped exact filter-only data in `16/16`, blocked `18/18` aliases and found no marker at `25/25` lifecycle observations. All temporary directories were removed.
- **High — actual producer contracts lose approved public data:** `_expert_evidence_item` always emits public `confidence`, but `_PUBLIC_EXPERT_EVIDENCE_KEYS` does not declare it, so one direct real-producer evidence item projects to zero; a normalized expert with three structured evidence items also projects to zero evidence. `_disabled_fact_enrichment` emits public `skipped` and `skip_reason`, but `_FACT_ENRICHMENT_SCHEMA` declares neither, so a real 30-root `AnalyzeResponse` persists only 29 roots and loses the complete `fact_enrichment` object.
- **Dynamic-map contract contradiction:** `map[str, integer]` rejects whole maps for public labels such as `prototype_source`, `type` and `partner_role_v2`, even though the snapshot importer accepts arbitrary cleaned `source` labels and `db_status` exposes them as `source_breakdown`. Integer typing blocks private text in values, but the current implementation and H11's documented arbitrary-public-label contract do not match; independently generated unknown keys also show that map keys themselves require an explicit bounded public-label contract.
- **Positive and regression evidence:** canonical hand-built roles/evidence, historical expert field-level filtering, endpoint/nullable fields and a real Chat context remain intact. Focused H9/H10/H11 tests passed; security/reasoning `67/67`, focused account-security/billing/idempotency/admission/model-usage `118` passed with `5` PostgreSQL skips, API contracts `152/152`, full suite `706` passed with `5` skips, readiness `86/86`, quality gate, Python compilation, Compose configuration and `git diff --check` passed. Green regressions cannot override real-producer shape loss.
- **Frozen controls and UNKNOWN:** H6 hashes remain `model/db.py` `a4925f99…d558`, migration `0009` `6e51274b…4dc`, permission matrix `04edddd7…101`; H11 files also remained unchanged during review. Production history, affected source-label frequency, disabled fact-enrichment history, managed Secret delivery and real PostgreSQL migration/permissions remain unobserved or unapplied.
- **Disposition recorded by this review (now executed):** approval-gated `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-012` — repository-only failing-first correction of exactly the three producer/schema/map contract mismatches above, followed by a fresh independent `REVERIFY-012`. No automatic implementation or retry was authorized.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-012 — repository remediation checkpoint

- **Status:** `IMPLEMENTED / OFFLINE SELF-VALIDATED / REJECTED BY REVERIFY-012`; its typed-map and direct-producer corrections remain useful, but the independent review below supersedes its readiness conclusion.
- **Scope:** only `model/api.py`, `tests/test_account_security_compliance.py` and the two checkpoint documents. `model/db.py`, migration `0009`, its permission matrix, production resources and provider paths were deliberately untouched. No migration/GRANT/REVOKE, production/database/cloud/ACR/provider access, service start, deployment, commit, push or cost occurred.
- **Failing-first evidence:** the real-producer test failed because one `_expert_evidence_item` projected to `[]`; the dynamic-map test failed because a valid public-label count map became `_PUBLIC_SCHEMA_MISSING`. These failures were captured before implementation rather than inferred from green fixtures.
- **Exact producer parity:** `confidence` is now an explicit expert-evidence public field and accepts only a non-boolean number clamped to `0..1` and rounded to two decimals. `_FACT_ENRICHMENT_SCHEMA` now declares the real disabled producer's boolean `skipped` and string `skip_reason`. A real 30-root `AnalyzeResponse` retains all 30 roots, and normalized expert structured evidence remains intact.
- **Bounded dynamic-map contract:** typed maps no longer interpret public data labels as structured discriminator fields. Each key must be a non-empty, trim-stable, NFKC-stable string of at most 48 characters, contain at least one alphanumeric character, contain only alphanumerics plus space, underscore, hyphen, dot, slash or colon, and contain no Unicode control/format category. Invalid or non-string keys are omitted individually; one invalid key cannot delete valid siblings. Values still pass their exact child schema, so text cannot enter an integer count map.
- **Lifecycle and negative evidence:** real expert evidence/confidence, disabled fact status and accepted source labels survive raw Chat/Diagnosis, reload and account export; overlong, format-character, leading-space, non-string and text-valued map entries do not. The main R11 regression independently remains `728/728` blocked, exact filter-only behavior `16/16`, alias blocking `18/18`, with zero survivor.
- **Offline validation:** H9–H12 focused tests `9/9`; security/compliance plus reasoning `69/69`; focused account-security/billing/idempotency/admission/model-usage `120` passed with `5` PostgreSQL skips; API contracts `152/152`; full unit suite `708` passed with `5` skips; production readiness `86/86`; quality gate, Python compilation, Compose configuration and `git diff --check` passed. Provider/XHS paths were mocked, synthetic or fail-closed; no real external request occurred.
- **Frozen controls:** `model/db.py` remains `a4925f99…d558`, migration `0009` `6e51274b…4dc`, permission matrix `04edddd7…101`, and `tests/test_reasoning_safety.py` `b3ab2921…2dc7`. Migration `0009` and every permission delta remain unapplied.
- **Independent disposition:** `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-012` completed with the result immediately below. It found no private survivor but rejected H12 because approved generic-map and actual Chat producer shapes are lost.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-012 — independent repository review

- **Status:** `COMPLETE / FAIL / NO-GO`; severity `0 Critical / 1 High / 1 Medium`. The structural-persistence High remains open and H12 is not independently accepted.
- **Boundary:** three fresh read-only/offline evidence streams plus main-CTO source review and regression gates. Reviewers generated new cases instead of reusing H12 fixtures; disposable SQLite roots were removed. Only this Handoff and risk register were changed. No implementation/test/migration/permission change, production/database/cloud/ACR/provider/network access, service start, deployment, commit, push or cost occurred.
- **Positive evidence:** the typed-map stream passed `81/81` observations across all three recursively discovered `map[str, integer]` paths, including exact 48/49-character, trim, NFKC, control/format, punctuation, non-string-key and six wrong-value boundaries; private survivor and approved-sibling loss were both zero. The producer stream passed 82 checks before its own export-index error: real expert evidence, four normalized evidence items, finite confidence clamp/round, invalid confidence types, disabled fact status, typed 30-root Analyze, typed Chat and raw/reload probes all had zero shape loss and zero private survivor. The lifecycle stream passed 20 approved-shape, 6 full-parity, 84 private-absence, 17 dynamic-key and 15 invalid-entry observations; direct Diagnosis and manually injected typed Chat survived raw/reload/export with zero private survivor.
- **Audit-evidence issue:** the producer stream stopped after incorrectly reading `export["chat_sessions"]` instead of `export["archives"]["chat_sessions"]`; it did not retry. This stream is `INCONCLUSIVE` for its own export assertion, but the separate lifecycle stream independently exercised the correct account-export paths. The implementation findings below do not depend on the failed audit assertion.
- **High — approved structured context is lost:** generic Diagnosis and generic Chat each lost `81/81` otherwise valid dynamic-map observations (`162/162` combined shape loss) because `_public_diagnosis_value` and `_sanitize_chat_persisted_value` still treat a dynamic map as a finite-key record; H12's bounded-key helper is used only by typed schemas. Separately, the real `ChatStartInput -> chat_start -> _persist_chat_session` path retained Expert shape `4/4` but retained disabled-fact structured status `0/4` and accepted source-breakdown labels `0/4`: `session_generate_context` does not forward the already approved typed `fact_enrichment` or market/source context, so raw/reload/export preserve the same lossy state.
- **Medium — non-finite confidence becomes a false maximum:** finite confidence values and invalid non-number types behave correctly, but `float("nan")` projects to public `confidence=1.0`; a non-finite value must be rejected rather than converted into maximum confidence. Positive and negative infinity clamp to the finite range under the current contract.
- **Regression evidence:** security/compliance plus reasoning `69/69`; focused account-security/billing/idempotency/admission/model-usage `120` passed with `5` PostgreSQL skips; API contracts `152/152`; full suite `708` passed with `5` skips; production readiness `86/86`; quality gate, Python compilation, Compose configuration and `git diff --check` passed. The known non-failing SQLite `ResourceWarning` recurred. Green regressions do not override the fresh approved-shape failures.
- **Frozen controls and cleanup:** `model/api.py` remained `5e4d5bd…0f68b`, H12 tests `7789dddc…b5b7b`, `tests/test_reasoning_safety.py` `b3ab2921…2dc7`, `model/db.py` `a4925f99…d558`, migration `0009` `6e51274b…4dc` and the permission matrix `04edddd7…101`. HEAD/upstream remain `d5ec4d52776167b61888df2c209ad9490120225c`, ahead/behind `0 / 0`; all reviewer temporary resources were removed.
- **Disposition recorded by this review (now executed):** approval-gated `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-013` — repository-only failing-first correction of exactly three R12 gaps: path-aware bounded dynamic-map handling in both generic projectors, propagation of only the existing typed public Fact/market-source context through the real `chat_start` producer, and rejection of non-finite expert confidence. Scope was `model/api.py`, focused tests and checkpoint docs only; DB code, migration `0009`, permissions and production remained frozen.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-013 — repository remediation checkpoint

- **Status:** `INDEPENDENTLY VERIFIED AT REPOSITORY LEVEL / NOT DEPLOYED`; independent R13 below closes R12's retained repository `1 High / 1 Medium`, but this is not production `VERIFIED`.
- **Scope:** only `model/api.py`, `tests/test_account_security_compliance.py` and the two checkpoint documents. `model/db.py`, migration `0009`, its permission matrix, production resources and provider paths were deliberately untouched. No migration/GRANT/REVOKE, production/database/cloud/ACR/provider access, service start, deployment, commit, push or cost occurred.
- **Failing-first evidence:** all three new tests failed on H12. Generic Diagnosis erased each legal map; real `chat_start` raised the test's expected missing-field `KeyError` for `fact_enrichment`; and the non-finite matrix showed `NaN/+Inf` becoming `1.0` plus `-Inf` remaining a public confidence field. These failures were captured before implementation.
- **Exact generic-map correction:** `_PUBLIC_DYNAMIC_INTEGER_MAP_PATHS` enumerates only the three schema-derived count-map paths under `market_timing`. At those paths, `_public_diagnosis_value` and `_sanitize_chat_persisted_value` call the existing typed `("map", "integer")` sanitizer. All other records retain exact raw-key fail-closed behavior; invalid keys or non-integer values cannot delete valid siblings or enter the count map.
- **Actual Chat producer correction:** `_CHAT_CONTEXT_MAPPING_SCHEMAS` now declares the already public `_MARKET_TIMING_SCHEMA`. `chat_start` forwards `fact_enrichment` and `market_timing` only when the incoming full generation context supplies dictionaries; `_persist_chat_session` then applies the existing exact typed schemas. The test exercises the real producer and proves disabled-fact status plus accepted source labels survive raw SQLite, reload and account export.
- **Confidence correction:** one finite-only helper rejects booleans, conversion errors and every `math.isfinite=false` value. Evidence producers default rejected confidence to `0.0`; public evidence omits an invalid confidence while retaining approved siblings; normalized and public expert opinions use `0.0`. Existing finite clamp/round behavior remains intact.
- **Offline validation:** H11–H13 focused tests `7/7`; security/compliance plus reasoning `72/72`; focused account-security/billing/idempotency/admission/model-usage `123` passed with `5` PostgreSQL skips; API contracts `152/152`; full unit suite `711` passed with `5` skips; production readiness `86/86`; quality gate, Python compilation, Compose configuration and `git diff --check` passed. The known non-failing SQLite `ResourceWarning` recurred. Provider/XHS paths were mocked, synthetic or fail-closed; no real external request occurred.
- **Hashes and frozen controls:** `model/api.py` is `f74bb6df…8d3d`, H13 tests are `424875ad…3e4f`; `tests/test_reasoning_safety.py` remains `b3ab2921…2dc7`, `model/db.py` `a4925f99…d558`, migration `0009` `6e51274b…4dc` and the permission matrix `04edddd7…101`. Migration `0009` and every permission delta remain unapplied.
- **Independent disposition:** `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-013` completed `PASS` with the result immediately below; do not repeat H13 or R13 without new conflicting evidence.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-013 — independent repository review

- **Status:** `COMPLETE / PASS / REPOSITORY VERIFIED`; severity `0 Critical / 0 High / 0 Medium` inside R13's exact scope. R12's structural-persistence High and non-finite-confidence Medium are closed at repository level only.
- **Boundary:** three fresh independent read-only/offline evidence streams plus main-CTO source reconciliation and regression gates. Reviewers did not reuse H13 fixtures. Temporary SQLite roots were removed. Only this Handoff and risk register were changed by the review; no implementation/test/migration/permission change, production/database/cloud/ACR/provider/network access, service start, deployment, commit, push or cost occurred.
- **Three exact dynamic maps:** recursive schema discovery and the dispatcher both identified only `market_timing.source_breakdown`, `market_timing.pipeline_status.xhs.latest_health.details.latest_run_source_breakdown`, and `market_timing.pipeline_status.xhs.latest_health.details.latest_run_source_health.source_breakdown`. Across 13 key and 6 wrong-value boundaries, three paths and three projectors, the core matrix passed `171/171`: legal candidates `27/27` retained, invalid candidates `144/144` omitted, legal-sibling loss `0/171`, shape loss `0/171`, and private survivor `0/171`.
- **Map audit limitation and CTO disposition:** one supplemental non-map assertion stopped because its reviewer expected an undeclared finite record to survive as `{}`; the typed schema correctly omits that entire undeclared item with `_PUBLIC_SCHEMA_MISSING`. The reviewer did not retry. Source review confirms exact-path membership is the only dynamic-map exception and all other records remain fail-closed. This was an audit expectation error, not a product defect; the independently complete `171/171` core matrix is unaffected.
- **Real Chat lifecycle:** one actual `ChatStartInput -> chat_start` invocation, with no retry, exercised real disabled Fact status, expert evidence/confidence, all three market source maps, six new legal labels, 18 invalid key/value cases and 14 private markers. Prewrite, raw SQLite, reload helper and `archives.chat_sessions` export passed full shape `4/4`, public leaves `472/472`, phase parity `3/3`, legal map entries `24/24`, invalid absence `72/72`, private-marker absence `56/56`, private/undeclared-key absence `48/48`, and undeclared-root non-forwarding `4/4`.
- **Confidence integrity:** 19 input classes produced `304/304` direct observations. Finite values preserve the existing clamp/round contracts; NaN, `+Inf`, `-Inf` and invalid values fail closed, never create a public non-finite value or false `1.0`, and public evidence omits invalid confidence while retaining approved siblings. Thirty-eight new private markers had zero survivor. Eight representative inputs across prewrite/raw/reload/export passed sibling shape `160/160`, confidence `64/64` and private absence `64/64`.
- **Main regression evidence:** security/compliance plus reasoning `72/72`; focused account-security/billing/idempotency/admission/model-usage `123` passed with `5` PostgreSQL skips; API contracts `152/152`; full suite `711` passed with `5` skips; production readiness `86/86`; quality gate, Python compilation, Compose configuration and `git diff --check` passed. The known non-failing SQLite `ResourceWarning` recurred. Provider failures were mocked or fail-closed; no real request occurred.
- **Frozen controls and cleanup:** `model/api.py` remains `f74bb6df…8d3d`, H13 tests `424875ad…3e4f`, `tests/test_reasoning_safety.py` `b3ab2921…2dc7`, `model/db.py` `a4925f99…d558`, migration `0009` `6e51274b…4dc` and permission matrix `04edddd7…101`. HEAD/upstream remain `d5ec4d52776167b61888df2c209ad9490120225c`, ahead/behind `0 / 0`; no R13 temporary root remains.
- **Production truth unchanged:** no production code/image was built or deployed, migration `0009` and every privilege delta remain unapplied, and real PostgreSQL casts/locks/apply-twice/rollback, effective production privileges, managed Secret delivery, production history/log cleanup, retention scheduling, real SMS/email and professional legal/cross-border review remain `UNKNOWN`.
- **Disposition recorded by this review (now executed):** `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-POSTGRES-REHEARSAL-001` was separately approved and completed with the result immediately below.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-POSTGRES-REHEARSAL-001 — disposable PostgreSQL review

- **Status:** `COMPLETE / FAIL / NO-GO`; independent severity `0 Critical / 1 High / 2 Medium`. The mechanical rehearsal passed, but the three repository findings below block production migration acceptance.
- **Boundary:** locally cached PostgreSQL 16 Alpine digest `sha256:57c72fd…777` only, under an existing Colima VM. The task container used a read-only rootfs, bounded CPU/memory/PIDs, restart `no`, one disposable volume and a random `127.0.0.1` port. Authentication was synthetic local trust; no password, Secret, real user row, production/cloud/ACR/provider/network call or external cost was used. Production migration `0009`, GRANT/REVOKE and business data remained untouched.
- **Apply twice and data semantics:** the repository migration runner applied exact `0001`–`0008` to a template, then returned exactly `["0009_account_security_compliance.sql"]` for the first valid execution and `[]` for the second. Final history was nine versions with one `0009`; catalog SHA-256 remained `8a771676…aa4`. Five tables, five columns and four synthetic retention rows were created: free `2`, paid `2`, with free `+7/+14/+44` and paid NULL bounds `4/4`. An initial audit expectation of 35 business tables was corrected to the actual 29 baseline plus five new tables = 34; this was a harness-count error, not a migration defect.
- **Strict clocks and paid boundary:** 27 independently generated clock cases passed parser/migration parity: 13 accepted and 14 rejected, covering trim, `T`/space, missing seconds, 1/6 microseconds, naive UTC, `Z`, `+08`, `+0800`, `+08:00`, `±14:00`, invalid calendar/24h/leap-second/7 digits/offset minutes/`±15`/timezone name/date-only/Julian input. Four invalid source branches emitted the same stable migration error. Offset and microsecond evidence proved the paid interval is exactly `started_at <= created_at < expires_at`.
- **Rollback and locking:** noncanonical phone, duplicate canonical phone, all four source-clock branches and a late incompatible-schema failure each left `0009` version `0`, its prior ALTER columns `0`, newly created earlier tables/indexes `0`, and retention backfill `0`; the pre-existing late-drift sentinel remained intact. Two real migration callers were observed simultaneously waiting on the same transaction advisory lock; after release their applied-list lengths were `1` and `0`, with one `0009` history row and one retention row.
- **Complete privilege matrix:** disposable `noteai_app` matched all `245/245` table predicates: `102` expected true and `143` false; five sequences were `5` USAGE true and `10` SELECT/UPDATE false. Disposable `noteai_xhs` matched `20` true and `225` false table predicates plus `3` sequence USAGE true and `12` false; every privilege on the five `0009` tables was false. Eight direct forbidden operations failed. `schema_migrations`, Schema/Database CREATE, ownership, membership, default ACL and all grant options were zero. Both roles had exact built-in function EXECUTE `4/4`, with zero ownership, grant option or explicit role ACL; cross-role user-fence blocking/release passed.
- **Catalog/constraint evidence:** five new tables, seven explicit indexes, five new columns, three required cascade FKs, partial phone/open-deletion unique predicates and 16 negative CHECK/FK/unique behaviors passed. Objects resolved to `public`, `current_schema()` was `public`, and the migration owner retained object ownership. `search_path` was `"$user", public`; the unqualified-object migration therefore still depends on a safe owner environment.
- **High — retention order is not a database time invariant:** `content_retention` stores time fields as TEXT and compares them lexically. PostgreSQL accepted synthetic `a/b/c` bounds and an offset case whose strings were ordered but whose `timestamptz` values were chronologically reversed. The probes were rolled back, but this can permit premature expiry/purge failure or over-retention if any future writer bypasses canonical application formatting.
- **Medium — canonical phone is only a migration-time invariant:** historical noncanonical and duplicate phone rows correctly block migration, and the partial unique index rejects later duplicate canonical values. However, after migration PostgreSQL accepted synthetic `phone='13811111111'` because no persistent canonical-format CHECK exists. The probe was rolled back; normal application paths currently normalize, but the database does not defend against a future bypass.
- **Medium — migration identity lacks content integrity:** `schema_migrations` contains version and timestamp only, with no 64-character SHA-256. A later runner returns `[]` solely because the filename is present; it cannot fail closed if an already registered SQL file changes. The successful catalog fingerprint is rehearsal evidence, not a durable runtime control.
- **Independent disposition:** the independent DB Security Reviewer accepted the mechanical evidence and assigned `0 Critical / 1 High / 2 Medium`; production `0009`, its GRANTs and production preflight remain prohibited. Passed matrices and rollback observations need not be repeated against the unchanged migration, but any corrected SQL requires one new independent rehearsal.
- **Evidence-before-cleanup and final cleanup:** sanitized counts, hashes, finding probes and independent disposition were written here before resource deletion. The exact labeled task container and volume were then removed; label read-back returned task container `0` and task volume `0`. Destroying the volume removed both synthetic databases and both synthetic roles. The eight-link migration directory was deleted, the Docker socket disappeared, and Colima read-back returned `Stopped`, restoring the task-start baseline. Pre-existing local containers that auto-started with the VM were not modified; stopping the VM returned them to the prior stopped-runtime state.
- **Superseding repository work:** the approved repository-only H14 completed with the candidate result immediately below, then failed fresh R14. The original rehearsal evidence and R14 findings remain authoritative; do not run a modified-SQL rehearsal until H15 and R15 pass.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-014 — PostgreSQL contract candidate

- **Status:** `IMPLEMENTED / OFFLINE SELF-VALIDATED / REJECTED BY R14 / NOT DEPLOYED`. Its valid partial corrections remain in the worktree, but its acceptance claim is superseded by the independent result below.
- **Boundary:** only migration `0009`, `model/db.py`, two focused test files, the runtime-permission matrix and checkpoint documents changed. No PostgreSQL/container was started; no database, permission, production/cloud/ACR/provider, service, deployment, commit or push action occurred. External cost was `¥0`.
- **Failing-first evidence:** the three new contracts initially returned `EFF`: the phone constraint name was absent, the retention constraint had no typed deadline validation, and a mismatched recorded migration digest raised no error. After the minimal corrections, the same three tests passed `3/3`.
- **Persistent phone invariant:** `users_phone_canonical_check` now allows only `NULL`, the historical unbound empty string, or `+861[3-9][0-9]{9}`. The existing invalid/duplicate preflight remains before the new CHECK and partial unique index.
- **Real retention invariant:** free rows require three strict real clocks with an explicit offset or `Z`; PostgreSQL casts them to `timestamptz` for `active <= recovery <= purge`. This rejects invalid calendar values, arbitrary text and offset-driven real-time inversion while preserving the paid-indefinite all-NULL contract.
- **Migration content identity:** `schema_migrations.sha256` is a lowercase 64-character, non-null ledger field. While holding the existing advisory lock, the runner computes exact file-byte hashes, rejects missing local versions and stored drift before any pending migration SQL, and stores each newly applied digest atomically. Legacy `0001`–`0008` digest bootstrap is allowed only when exact bytes match eight static reviewed hashes; an unknown digestless version fails closed rather than using trust-on-first-use.
- **Privilege truth:** migration `0009` still contains no `GRANT` or `REVOKE`; neither runtime role receives `schema_migrations` DML or any other added privilege. The permission matrix documents the new owner-only integrity metadata and unchanged negative matrix.
- **Offline evidence:** security/compliance `67/67`; deployment contracts `17/17`; API contracts `152/152`; full unit suite `714` passed with `5` existing PostgreSQL skips; production readiness `86/86`; quality gate, Python compilation, Compose config and scoped `git diff --check` passed. Test data/connections were fake or temporary; provider/XHS outputs were mocked or fail-closed.
- **Frozen H14 outputs:** `model/db.py` `665c1279…a3499`; migration `0009` `b3cf01a2…9a70`; security tests `523eceda…e4de`; deployment tests `a5b7b39f…7c35`; permission matrix `1ed79822…3927`. Migration `0009` and every privilege delta remain unapplied.
- **Rollback:** revert only the exact H14 changes in the five implementation/test/contract files; production needs no rollback because it remains on `0001`–`0008`. Do not rewrite the cumulative security worktree or its independently accepted H13/R13 changes.
- **Independent disposition:** `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-014` completed `FAIL / NO-GO` with the result immediately below. Do not discard the valid phone CHECK, typed free-retention order, legacy hash anchors or privilege non-expansion; H15 must correct only the newly proven conflicts.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-014 — independent DB contract review

- **Status:** `COMPLETE / FAIL / NO-GO`; severity `0 Critical / 1 High / 2 Medium`. Passing subcontrols cannot override any survivor.
- **Boundary:** three independent read-only/offline evidence streams plus main-CTO reproduction. Reviewers did not modify implementation or tests. Only Handoff/risk documents changed. No PostgreSQL/container, database/permission, production/cloud/ACR/provider/network, service, deployment, commit, push or cost occurred; all probe files and SQLite databases were temporary and removed.
- **Frozen inputs:** all five H14 hashes matched before and after review: `model/db.py` `665c1279…a3499`, migration `0009` `b3cf01a2…9a70`, security tests `523eceda…e4de`, deployment tests `a5b7b39f…7c35`, permission matrix `1ed79822…3927`. HEAD/upstream remain `d5ec4d5…225c`, ahead/behind `0 / 0`.
- **Accepted H14 subcontrols:** phone preflight/CHECK/index order and ASCII database pattern are source-correct; free-retention clocks require an explicit timezone, reject invalid text/calendar/offset boundaries and compare real `timestamptz` instants; all eight legacy migration anchors match; stored drift, missing local versions and untrusted digestless versions fail before pending SQL; permission statements remain zero and runtime matrices do not expand. Real PostgreSQL installation remains `UNKNOWN`.
- **High — paid user deletion is impossible:** both PostgreSQL `0009` and SQLite require a `paid_indefinite` row's active/recovery/purge deadlines to remain all `NULL`, while `mark_deleted_with_storage()` unconditionally writes `deleted_at` plus `purge_after=now+30d`. Two independent temporary-SQLite application probes reproduced `IntegrityError` and full transaction rollback. Existing tests delete only free content. Paid note and diagnosis DELETE paths can therefore fail instead of becoming immediately invisible and eligible for bounded purge.
- **Medium — checksum is not bound to executed bytes:** the runner hashes every file with `read_bytes()` and later executes a second `read_text()`. Independent mutation-between-read probes made it execute new SQL while recording the old SHA-256. Existing missing/drift tests pass but do not cover this TOCTOU. A migration must be read once into immutable bytes, and the same bytes must be hashed, UTF-8-decoded, executed and recorded.
- **Medium — application phone normalization is broader than the DB:** `normalize_phone()` uses Python `\d{9}` and accepts Unicode decimal digits; the persistent CHECK uses ASCII `[0-9]{9}`. Independent input proved a non-ASCII phone normalizes successfully and would fail only at the database layer. Validation must reject it before persistence with the stable product error.
- **Non-severity observation:** deadline CHECKs use `btrim()` while the reader does not trim. Current writers are canonical, but H15 should explicitly reject stored outer whitespace or normalize the reader so database/application semantics cannot diverge.
- **Independent regressions:** permission/review stream passed security plus deployment `84/84`, focused API/reasoning/billing/idempotency/admission/model-usage `219/219`, full unit `714` with `5` existing PostgreSQL skips, readiness `86/86`, quality, compile, Compose and diff checks. These suites lack the paid-delete and two-read mutation cases and do not override the findings.
- **PostgreSQL truth:** H14/H15 SQL has not run on PostgreSQL. Legacy ledger upgrade, constraint installation/negative behaviors, apply-twice, real rollback/concurrency/catalog and full effective privileges remain `UNKNOWN`. `POSTGRES-REHEARSAL-002` is blocked until H15 and R15 pass.
- **Disposition recorded by R14 (now executed):** approval-gated `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-015` — one repository-only failing-first correction of exactly this `1 High / 2 Medium`. It had to preserve paid-at-creation classification while allowing a strict deleted tombstone and bounded purge for paid notes/diagnoses; bind hash and execution to one immutable file-byte snapshot; require ASCII phone digits before DB access; resolve deadline-whitespace parity; preserve H14's accepted controls and zero privilege expansion. Its execution result is recorded immediately below; fresh R15 remains required before `POSTGRES-REHEARSAL-002` can be separately approved.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-015 — repository correction

- **Status:** `IMPLEMENTED / OFFLINE SELF-VALIDATED / NOT INDEPENDENTLY VERIFIED / NOT DEPLOYED`. This is a candidate correction of R14, not PostgreSQL or production acceptance.
- **Boundary:** only `model/db.py`, `model/account_security.py`, unapplied migration `0009`, focused security tests, the two security-contract documents and checkpoint documents changed for H15. `model/content_retention.py` required no change. No PostgreSQL/container, production/database/cloud/ACR/provider access, migration/GRANT/REVOKE, service start, deployment, commit, push or external cost occurred.
- **Failing-first evidence:** the six new/tightened contracts first returned `7 failures / 1 error`: three Unicode digit families normalized successfully, paid Note deletion raised the reproduced CHECK `IntegrityError`, the legacy SQLite schema lacked the new state marker, PostgreSQL lacked trim/tombstone clauses, SQLite accepted an outer-whitespace deadline, and the mutation probe executed replacement SQL while recording the original digest. After the minimal correction, the same six contracts passed `6/6`.
- **Paid lifecycle:** both SQLite and PostgreSQL preserve `paid_indefinite` plus NULL active/recovery clocks. Undeleted rows keep deleted/purge clocks NULL; explicit deletion requires strict `deleted_at` and `purge_after` clocks with `deleted_at <= purge_after`. Actual Note and Diagnosis DELETE endpoints become immediately invisible, retain the 30-day bound and purge each underlying row exactly once. Existing SQLite tables are transactionally rebuilt only after all rows pass the new state validator; the focused legacy-row probe proved row preservation and the named `content_retention_state_check`.
- **Migration and input integrity:** every SQL file is read exactly once into bytes; the same immutable payload is hashed, decoded, executed and written to `schema_migrations`. A mutation-between-read probe executed only the original payload and recorded its digest. Phone normalization and SQLite historical preflight now use ASCII `[0-9]`, so Arabic-Indic, full-width and Devanagari digits fail with the stable product error before any transaction. PostgreSQL and SQLite reject deadline outer whitespace; H14's strict timezone, typed real-time order, eight legacy hash anchors, failure ordering and zero permission expansion remain.
- **Offline evidence:** H15 contracts `6/6`; security/compliance `72/72`; deployment `17/17`; API contracts `152/152`; full unit suite `719` passed with `5` existing skips; production readiness `86/86`; quality gate, Python compilation, Compose configuration and `git diff --check` passed. Test databases and fake migration connections were temporary; provider/XHS failure outputs were mocked or fail-closed.
- **Frozen H15 outputs:** `model/db.py` `4afeed37…13f`; `model/account_security.py` `712e7050…a64`; migration `0009` `4b342cdd…220`; security tests `45be32f5…f7b`; deployment tests unchanged `a5b7b39f…c35`; permission matrix `ac066a15…ab1`.
- **Production truth and rollback:** production remains at `0001`–`0008`; no H15 SQL, permission or processor ran. Repository rollback is limited to the exact H15 delta and must not discard H13/R13 or H14 accepted controls. No production rollback is needed.
- **Disposition recorded by H15 (now executed):** `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-015` required fresh read-only/offline independent probes for paid lifecycle/state constraints, immutable-byte migration identity, ASCII phone/clock parity and the complete negative permission/regression matrix. Its result is recorded immediately below; it did not authorize PostgreSQL rehearsal.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-015 — independent DB contract review

- **Status:** `COMPLETE / FAIL / NO-GO`; severity `0 Critical / 1 High / 3 Medium`. Any one survivor fails the zero-severity acceptance gate.
- **Boundary:** three fresh independent read-only/offline evidence streams plus main-CTO reproduction. Reviewers did not call or copy H15 fixtures and changed no implementation/test/contract input. Only this Handoff and risk register record the verdict. No PostgreSQL/container, production/database/cloud/ACR/provider/network, migration/GRANT/REVOKE, service, deployment, commit, push or cost occurred; all temporary SQLite/fake-connection roots were removed.
- **Accepted H15 controls:** a single paid Note and Diagnosis DELETE preserves `paid_indefinite`, becomes immediately invisible, does not purge at `30d-1µs`, purges both primary rows at exact `30d`, and a second processor pass returns zero. Six migration-mutation boundaries across two files each produced `12/12` one-read original-byte executions with matching ledger SHA; invalid UTF-8 rolled back; stored drift, missing local and untrusted digestless history failed before pending SQL; all eight anchors matched. Six valid ASCII phone forms canonicalized while `153/153` Unicode/mixed forms failed before transaction entry. Ten deadline-whitespace cases were rejected. PostgreSQL static paid/free trim, explicit-zone regex and typed-order structure passed.
- **High — duplicate DELETE extends the deletion promise:** `mark_deleted_with_storage()` unconditionally rewrites `deleted_at` and `purge_after`. Fresh authenticated retries for both actual Note and Diagnosis DELETE endpoints returned `200`, shifted both clocks by exactly five days, produced zero purge at the original deadline and purged only at the shifted deadline. Repeating the request can extend retention indefinitely, so the delete operation is not idempotent and the approved 30-day bound is not durable.
- **Medium — SQLite legacy rebuild is not atomic:** the actual upgrade sequence runs DDL before its first DML without an explicit transaction/savepoint. A synthetic copy fault left an empty new marker table plus the original populated `content_retention_legacy_h15`; the next call returned early because it checks the marker before the stale table. Main-CTO reproduction inside the same connection context confirmed `0` new rows and `1` stranded legacy row. Valid upgrades preserve rows and invalid clocks fail before rename, but the fault boundary can lose or resurrect retention state.
- **Medium — SQLite persistent clock CHECK is lexical:** direct writes accepted arbitrary/invalid-calendar or otherwise non-strict paid/free clock text that satisfied trim and lexical order. Startup Python validation later rejects most such rows and current writers emit canonical UTC, but the persistent SQLite state constraint does not itself provide the strict real-time invariant claimed by H15.
- **Medium — purge transition is unconstrained:** SQLite and PostgreSQL checks do not reference `purged_at`, and `_valid_content_retention_row()` ignores it. A direct update placed `purged_at='not-a-clock'` on an undeleted paid row; startup passed, status reported `purged`, and the underlying Note remained. The current purge processor's own transaction remains correct, but `noteai_app` has UPDATE on the table and the durable state boundary is incomplete.
- **Permission/regression stream:** migration `0009` has zero privilege statements or ownership/function/sequence expansion. `noteai_app` remains exactly 15 new-table plus five existing-table DML deltas; `noteai_xhs` remains its prior 20 table/three sequence matrix and has zero privilege on all five `0009` tables. Security+deployment `89/89`, API `152/152`, full `719` plus `5` skips, readiness `86/86`, quality, compile, Compose and diff checks passed. Green regressions do not override the four state survivors.
- **Frozen inputs and cleanup:** before/after hashes matched: `model/db.py` `4afeed37…13f`, `model/content_retention.py` `50083331…af0f`, migration `0009` `4b342cdd…220`, `model/account_security.py` `712e7050…a64`, security tests `45be32f5…f7b`, deployment tests `a5b7b39f…c35`, permission matrix `ac066a15…ab1`. HEAD/upstream remained `d5ec4d5…225c`, ahead/behind `0/0`; final R15 temp residue was zero.
- **PostgreSQL truth:** actual modified SQL installation, constraints, rollback/concurrency and effective privileges remain `UNKNOWN`. `POSTGRES-REHEARSAL-002` stays blocked; R15 failure cannot authorize it.
- **Historical disposition:** R15 authorized only the separately approved repository H16 recorded below. Its rejected readiness conclusion remains historical evidence and does not authorize PostgreSQL or deployment.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-016 — repository remediation checkpoint

- **Status:** `IMPLEMENTED / OFFLINE SELF-VALIDATED / NOT DEPLOYED`; this closes the implementation stage only. Independent R16 has not run.
- **Boundary:** repository code, unapplied migration text, focused tests, contract and checkpoint documents only. No PostgreSQL/container, production database/cloud/ACR/provider/network, migration/GRANT/REVOKE, real data, service start, deployment, commit, push or external cost occurred.
- **Failing-first evidence:** the four authorized methods initially produced `9` assertion failures: duplicate authenticated Note/Diagnosis DELETE shifted both clocks five days; an injected SQLite copy failure left the legacy and marker tables split; PostgreSQL text lacked the `purged_at` contract; and six SQLite invalid-clock/purge states were accepted. After the bounded fix, the identical four methods passed `4/4`.
- **Stable delete boundary:** `mark_deleted_with_storage()` now sets `deleted_at`, `purge_after` and `updated_at` only while both deletion and purge markers are absent. Concurrent or retried real DELETE requests remain `200` but cannot change the first accepted clock or extend the exact 30-day deadline.
- **SQLite upgrade and clock boundary:** every NoteAI SQLite connection registers deterministic real-clock validation and UTC-aware `<=` functions. The H16 persistent CHECK uses them for paid/free lifecycle clocks and optional deletion/purge transitions. Legacy replacement checks stale residue before the H16 marker, validates every row including `purged_at`, and wraps rename/create/copy/drop/index in one explicit SAVEPOINT; injected copy failure restores the original table, row and index, and a clean retry succeeds.
- **Purge-state boundary:** Python row validation and status calculation now require valid explicitly zoned clocks and `purge_after <= purged_at`; an undeleted paid row cannot be marked purged. PostgreSQL migration `0009` contains the matching trim, explicit-zone regex and `timestamptz` ordering for paid/free `deleted_at` and `purged_at`. The actual processor still deletes primary content before setting `purged_at` in the same transaction.
- **Preserved controls:** paid-at-creation classification, one valid DELETE → exact 30-day purge, migration single-byte hash/decode/execute/ledger identity, eight legacy hashes, ASCII-phone rejection and the entire permission matrix remain unchanged. The permission document SHA-256 remains `ac066a15…ab1`; migration `0009` contains no GRANT/REVOKE.
- **Offline evidence:** focused H16 `4/4`; security/compliance `74/74`; deployment plus readiness-health `39/39`; API contracts `152/152`; full suite `721` passed with `5` skips; production readiness `86/86`; quality gate, Python compilation, Compose configuration and `git diff --check` passed. All databases were temporary and provider/XHS outcomes were mocked or fail-closed.
- **Frozen H16 outputs:** `model/db.py` `8edd93c9…191`; `model/content_retention.py` `aefbb754…b52`; migration `0009` `de8795d5…cf4`; security tests `0e220196…1f2`; readiness tests `16a45caa…c1`; data/security contract `f5bad548…1bf`; permission matrix unchanged `ac066a15…ab1`. HEAD/upstream remained `d5ec4d5…225c`, ahead/behind `0/0`; no task temp residue was found.
- **Rollback and production truth:** H16 is uncommitted and unapplied. Repository rollback is limited to the H16 delta and must retain accepted H13–H15 controls. Production remains at `0001`–`0008`, so no production rollback is needed.
- **Historical disposition:** H16 authorized only the independent R16 recorded below. Its self-validation remains useful evidence but cannot override R16's two Medium findings.

### PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-016 — independent DB contract review

- **Status:** `COMPLETE / FAIL / NO-GO`; severity `0 Critical / 0 High / 2 Medium`. The zero-severity acceptance gate failed, so modified-SQL PostgreSQL rehearsal remains prohibited.
- **Boundary:** three fresh independent read-only/offline streams plus main-CTO reproduction. Reviewers used new temporary fixtures rather than H16 test methods and changed no implementation/test/contract input. Only this Handoff and risk register record the verdict. No PostgreSQL/container, production/database/cloud/ACR/provider/network, migration/GRANT/REVOKE, deployment, commit, push or cost occurred.
- **Accepted H16 retention roots:** actual authenticated Note/Diagnosis DELETE returned `200`; twelve concurrent retries preserved the first `deleted_at`, `purge_after` and `updated_at`. `30d-1µs` purged `0`, exact `30d` purged `2`, repeat purged `0`. SQLite upgrade fault injection at rename/create/copy/drop/index passed `5/5` exact rollback/no-split/safe-retry checks, and stale-before-marker failed closed `1/1`. Persistent CHECK accepted `3/3` valid explicit-zone/microsecond states, rejected `8/8` invalid INSERT and `3/3` invalid UPDATE states. Normal free auto-expiry purged Note/Diagnosis `2/2`, with SQL trace proving delete-before-marker and a zero second pass.
- **Medium — purge marker still does not prove a purge transition:** both independent reviewer and main CTO set a format-valid future `purged_at` satisfying `deleted_at <= purge_after <= purged_at` while the underlying Note/Diagnosis still existed. SQLite/Python accepted it and status immediately reported `purged`; current and post-deadline processor runs returned `0`, leaving underlying Note `1` and Diagnosis `1`. PostgreSQL text likewise has only trim/zone/typed row ordering, with no current-time, underlying-absence or transition guard. The normal processor remains correct, but `noteai_app` has table UPDATE, so durable state and deletion evidence remain forgeable by an erroneous/compromised write path.
- **Medium — SQLite paid-at-creation is still lexical:** `_paid_at_creation()` uses typed `timestamptz` on PostgreSQL but SQLite TEXT predicates for `started_at <= created_at < expires_at`. The accepted source-clock contract permits offsets and outer whitespace. Fresh actual `record_content()` probes produced `3/3` errors: real paid→free, real free→paid, and outer-whitespace real paid→free. This can wrongly shorten paid retention or grant indefinite retention to free content. The parsed Python SQLite backfill path is correct; the runtime classifier is not.
- **Accepted migration/input controls:** six mutation boundaries across two files produced `12/12` one-read original-byte execution/ledger matches; the actual nine-file set read each file once with only `0009` pending. Invalid UTF-8, stored drift, missing local, untrusted digestless history and competing snapshot calls failed closed; all eight legacy anchors matched. Seven valid phone forms canonicalized to one identity, while `228/228` Unicode/mixed cases failed before transaction. PostgreSQL source/deadline trim, explicit-zone regex, typed ordering and H16 deleted/purged clauses passed static review.
- **Permission/regression stream:** migration `0009` has zero GRANT/REVOKE, role/owner/schema/default-privilege/PostgreSQL function/trigger expansion. `noteai_app` remains exactly 20 new DML deltas; `noteai_xhs` has `0/20` privileges on the five `0009` tables and its prior matrix is unchanged. Security+deployment `91/91`, readiness-health `22/22`, API `152/152`, full `721` plus `5` skips, readiness `86/86`, quality, compile `82/82`, Compose and diff checks passed. Green regressions do not override the two integrity findings.
- **Frozen inputs and cleanup:** before/after hashes matched: `model/db.py` `8edd93c9…191`, `model/content_retention.py` `aefbb754…b52`, migration `0009` `de8795d5…cf4`, `model/account_security.py` `712e7050…a64`, `model/api.py` `f74bb6df…d3d`, security tests `0e220196…1f2`, readiness tests `16a45caa…c1`, reasoning tests `b3ab2921…dc7`, deployment tests `a5b7b39f…c35`, permission matrix `ac066a15…ab1`. HEAD/upstream remained `d5ec4d5…225c`, ahead/behind `0/0`; temporary roots, SQLite/WAL, fake ledgers and task processes had zero residue.
- **PostgreSQL truth:** actual modified SQL installation, catalog constraints, rollback/concurrency and effective privileges remain `UNKNOWN`. No R16 evidence authorizes `POSTGRES-REHEARSAL-002`.
- **Unique next task:** approval-gated repository-only `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-017`. It must add failing-first coverage and the smallest correction for exactly the two R16 Mediums: authentic delete-before-marker/not-in-future purge transitions across SQLite/PostgreSQL/Python while preserving valid free auto-expiry; and SQLite runtime paid-at-creation comparison by parsed real instants for allowed offsets/whitespace. It must preserve all accepted H13–H16 controls and zero permission expansion. No PostgreSQL/container, production/provider access, migration/permission execution, deployment, commit or push is authorized. Fresh R17 is required before any rehearsal.

## 2. Git and repository truth

| Field | Verified value |
|---|---|
| Branch | `codex/quality-stabilization-real-chain` |
| Complete-launch plan task-start HEAD / upstream | `d5ec4d52776167b61888df2c209ad9490120225c` / `d5ec4d52776167b61888df2c209ad9490120225c`; ahead/behind `0 / 0` |
| Current worktree scope | expected cumulative uncommitted repository implementation from `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-001` through independently repository-verified `HARDEN-013`/`REVERIFY-013`, H14/H15 retained controls and H16 offline-remediation candidate plus review/checkpoint documents; no PostgreSQL or production state changed, and no commit/push is authorized |
| Last pushed milestone checkpoint | `d5ec4d52776167b61888df2c209ad9490120225c` — `docs(handoff): close XHS trends validation milestone [skip render]` |
| Deployment-control task-start HEAD | `7f6b9d6eaaff06c8d756783136a1bc4ed417c5c0`; deployment-control only, never an OCI revision |
| Earlier corrected-digest Handoff parent | `7aaaab719e77b37ca37856ba0578df7a071214da`; corrected-digest VEX/evidence/test/tool checkpoint, never an OCI revision |
| Historical retained-image application release | `93b03d5c40fb64fa264c0ada1c055f8e7696b161` |
| RAP-fix task-start HEAD / parent | `33d04d4560f9b7dda43a53527f8ae1026dc335d3` / `f3a1a3bb28acd4a2b7e33221d0d8d0100a79c9a9` |
| Rejected browserless image source | `33d04d4560f9b7dda43a53527f8ae1026dc335d3` (`feat(prod): add bounded XHS HTTP runtime [skip render]`) |
| RAP-fix application revision | `ca02335513a752ece2dabe71d08105ae86db6c5a` (`fix(prod): isolate XHS RAP signer runtime [skip render]`), pushed and independently verified |
| Pillow-fixed application revision | `a635692a899ee02c6905cd694611c14e0da4594a` (`fix(prod): upgrade Pillow to 12.3.0 [skip render]`), independently verified, pushed and now the exact source of the current local three-role images |
| Browserless constraint release | `253fde55fa609676a9fca5c14fdf6aeceef74000` (`fix(prod): enforce browserless runtime constraints [skip render]`); deployment configuration/readiness provenance only, never the OCI application revision |
| Original VEX review release | `5b3249995522a22dc600ee7501f9328121f4497c`; formal local-image VEX/Handoff descendant, never an OCI application revision |
| Corrected-digest reissue task-start HEAD | `6b6fbda2665c09fc01e98a686608f7e417d74fb6`; Handoff-only descendant of invalidated version-2 reissue commit `bdc84454067b081b4c7aca18553b1bf014465fb2`, never an OCI application revision |
| Verified local image source | exact application revision `a635692a899ee02c6905cd694611c14e0da4594a`; never reuse `ca023355`, substitute `4704c16`, or relabel an old image |
| Upstream | `origin/codex/quality-stabilization-real-chain` |
| Ahead / behind at corrected-digest reissue start | `0 / 0`; HEAD and upstream both equaled `6b6fbda2665c09fc01e98a686608f7e417d74fb6` |
| Worktree at content verification start | pre-existing uncommitted VEX version-3/Handoff/test changes from the completed corrected-digest reissue; no application/image source change was introduced by content verification |
| Push state | VEX/review/Handoff changes are repository evidence only and must use `[skip render]`; they never replace `a635692a899ee02c6905cd694611c14e0da4594a` as the OCI revision |
| ACR artifact state | three immutable role tags and manifest digests recorded under private repository `noteai/app`; no deployment or service start |
| Render auto-deploy impact | commit message contains `[skip render]`; no Render production deployment or service start is authorized |

Recent relevant commits, newest first:

- `23ae677` — correct the prior checkpoint parent SHA; Handoff-only, never an image revision.
- `4cc3f35` — checkpoint accepted internal API/Admin services and safely stopped XHS diagnostic state; Handoff-only.
- `7aaaab7` — checkpoint corrected-digest VEX evidence; repository evidence only.
- `7f6b9d6` — split production role runtime env inputs; deployment-control provenance only, never an OCI revision.
- `6b6fbda` — record the corrected ACR digest direct-pull result; Handoff-only, never an image revision.
- `bdc8445` — historical VEX version-2 registry binding; invalidated because its three manually transcribed digest strings do not exist.
- `5b32499` — independently reviewed twelve-item local-image VEX baseline; disposition and 69 BOM-Link provenance retained unchanged by version 3.
- `253fde5` — enforce browserless production runtime constraints and readiness rejection tests; deployment-config provenance only, never an image revision.
- `4704c16` — Handoff-only checkpoint for Pillow repository verification; never an application/image revision.
- `a635692` — upgrade Pillow to `12.3.0`; exact application revision of the current local browserless images.
- `344897c` — prior CI fixture-isolation/Handoff checkpoint only; never an application/image revision.
- `ca02335` — isolate and verify the XHS RAP signer runtime; exact application revision of the superseded pre-Pillow-fix browserless images.
- `33d04d4` — add the bounded browserless XHS HTTP runtime; exact source of the three rejected pre-fix local role images and parent of the RAP-fix release.
- `f3a1a3b` — Handoff-only checkpoint for Worker sandbox verification; documentation provenance, never an application revision.
- `93b03d5` — harden all Worker Chromium launches with fail-closed sandboxing and the pinned Playwright seccomp profile; exact source of the retained local images and baseline for the planned adapter change; normally pushed without force.
- `5ba202f` — Handoff-only checkpoint for the verified runtime split; documentation provenance, never an application revision.
- `89e3505` — split API and browser-worker runtimes; exact source of the now-superseded local AMD64 candidates; normally pushed to the tracking branch without force.
- `8f7d9c2` — minimize audited combined runtime-image attack surface; historical source of the superseded blocked local AMD64 image.
- `b6abaa7` — close independently verified production prebuild release gates; historical source superseded by later application revisions.
- `ff030f5` — package Meituan travel CLI for production.
- `84f8a2f` — define isolated production Claude Gateway service.
- `ecc4702` — record independent production release verification.
- `0a05a98` — reconcile Alibaba deployment ledger.
- `0ccf3f6` — stop XHS collection after server-side logout.
- `199bf5f` — add durable operation admission ledger.
- `8b6a0b3` — authorize Gateway transaction condition checks.
- `353ee22` — add redacted Gateway control diagnostics.
- `710065f` — reject existing Gateway operation claims.
- `22da06a` — add zero-Claude Gateway scale rehearsal.
- `c0afbbe` — install Blueprint test dependencies in CI.
- `0e050f8` — harden the Claude Gateway trust boundary.

Independent repository verification for exact application revision `93b03d5c40fb64fa264c0ada1c055f8e7696b161` completed. Native AMD64 build, runtime sandbox acceptance, CycloneDX SBOM, Secret scan and unsuppressed High/Critical scans have all been run for this hardened revision. The corresponding evidence at `89e3505` remains a superseded baseline only. `93b03d5` must remain the recorded revision of those retained images; after the adapter implementation, the new full application commit—not a Handoff-only HEAD or a relabelled old image—must be the revision of every newly built role image.

## 3. Current environment and cloud state

### 3.1 Render

| Component | Current evidence | State and limitation |
|---|---|---|
| Render Staging API | Public `/health/live` and `/health/ready` returned HTTP 200 on 2026-07-20 | Staging only; does not prove Alibaba production readiness. |
| Render Staging Admin | Public readiness returned HTTP 200 on 2026-07-20 after cold wake | Staging only. |
| Render Staging Web | Public root returned HTTP 200 on 2026-07-20 | Staging only. |
| Render Staging Claude Gateway | Public live/readiness returned HTTP 200 on 2026-07-20 | No paid Claude call was made in this checkpoint. |
| Render production Claude Gateway | Public live/readiness returned HTTP 200 on 2026-07-20 | Last exact control-plane evidence: Singapore Starter, minimum 2 / maximum 4 instances, autoscaling CPU 60% and memory 70%, auto-deploy off, deployed revision `84f8a2f`. Eight signed zero-Claude readiness passes and two instance markers were previously verified. Alibaba API is not yet switched to it. |

### 3.2 Alibaba Cloud

The following table separates last confirmed control-plane evidence from current uncertainty. No Alibaba resource was mutated during this checkpoint.

| Component | Last confirmed evidence | Current release conclusion |
|---|---|---|
| ACR | Private immutable `noteai/app` contains the three `a635692` role tags. Each corrected `repository@sha256` reference was directly pulled and returned `linux/amd64` plus exact OCI revision/source/version/created/role metadata. | Identity/provenance, corrected VEX version 3, content acceptance and zero local builder auth entries are verified. Public `/32` access and ECS Session Manager are closed and the private production-VPC binding was unchanged. Deployment remains prohibited pending config closure and live preflight. |
| API-C ECS | Live 2026-07-24: `noteai-prod-api-c` / `i-wz9j36od3nf2b1uw7bvg` remains running/healthy, private-only and `x86_64`, with Docker `28.3.3` and Compose `5.3.1`. Exactly one accepted managed API container runs from immutable digest `sha256:17706e1802afc136ac8f9a621d4199a7f9749da733290e923e42268eff42e0d1` on loopback `127.0.0.1:8000`; six zero-AI health rounds, database read-only, bounded logs, explicit restart and independent retention checks pass. `/etc/noteai/api.env` and `/etc/noteai/admin.env` remain distinct root-owned regular mode `0600` files. `noteai-api.service` is enabled/active; the canary object and `18000` listener are absent. | Managed-service acceptance passes. API-C remains outside ALB and receives no public traffic. |
| API-F ECS | Live 2026-07-25: `noteai-prod-api-f` / `i-wz9bgztwf1tiakww2ops` remains running/healthy, private-only and `x86_64`. `noteai-api.service` is enabled/active with unit SHA256 `32d552e3daba29130677d98927dff1701711763ef63573d5d9c199c99bf44268`. Exactly one hardened `noteai-api-f` container runs from immutable API digest `sha256:17706e1802afc136ac8f9a621d4199a7f9749da733290e923e42268eff42e0d1`, local image ID `sha256:b1983bab928ef93495d8be020030917d4ae54364234390047c3163af5414fedb` and OCI revision `a635692a899ee02c6905cd694611c14e0da4594a`, as UID/GID `999:999` with read-only root, `cap_drop ALL`, `no-new-privileges`, resource limits, one `/app/model/data` mount and loopback `127.0.0.1:8000`. The accepted XHS HTTP image is retained locally by exact digest `sha256:452c2faf7853ce58d93e43c05fb6217a9a6cc1e4c81345d8cef2f50acabd79af`, local ID `sha256:5b44114d4bd9c28a8e93c39140466c542e8babeead038fb0d1cfe45c3cd75966`, `linux/amd64`, OCI revision `a635692a899ee02c6905cd694611c14e0da4594a`. RETRY-002 proved the suspended Worker and exact bounded database writes; the later one-time read-only exporter proof closed its snapshot artifact evidence gap. XHS container/service count is `0`, Tracking is stopped, root/user target ACR auth counts are `0`, and API-F remains loopback live/ready `200/200`. | Managed API acceptance remains valid. API-F is outside ALB and receives no public traffic. XHS/Tracking remain stopped; any managed Trends activation or supplier-enabled test requires separate approval. |
| RDS | Live 2026-07-24: `pgm-wz9m8tdck1v06672` is Running with SSL enabled. API-C proved the persisted URL requires SSL and negotiated `TLSv1.3` / `TLS_AES_256_GCM_SHA384`. A deleted temporary read-only audit role proved exact migrations `0001`–`0008`. `PROD-DB-RUNTIME-PRIV-FIX-001` then granted and read back exactly 82 runtime table DML booleans and five sequence USAGE privileges for `noteai_app`; `schema_migrations`, Schema CREATE, ownership, TRUNCATE, REFERENCES, TRIGGER and sequence SELECT/UPDATE remain absent. | TLS, exact live schema and runtime authorization pass. No migration, DDL, ownership or business-data write occurred. |
| Tair | `noteai-prod-tair`, Redis 7 high availability, 2 GiB, running in the last check; one-month purchase without auto-renew. | Not wired into the application. Availability, endpoint policy and renewal date require a fresh read-only audit. |
| VPC / vSwitches | Live API-C and API-F are in the same production VPC on separate C/F private subnets; both have private default routes and no public IP. | Basic private routing evidence passes. Effective database security-group policy was not mutated and was not fully enumerated in this task. |
| SNAT | VPC-level public egress capability was purchased and configured. | Last confirmed enabled; active route, EIP and fee state require read-only recheck. |
| PrivateZone | Live API-C and API-F both resolve the private ACR hostname; API-C resolved it to the production-VPC private endpoint previously recorded. API-F successfully pulled both its API image and the later exact XHS HTTP digest through the existing private endpoint during their bounded approved tasks. | Private ACR access passes. API-F immediately logged out after each pull; root/user target auth counts are `0`, the task-scoped auth directory is absent, and no public ACR endpoint was enabled. |
| ALB | No completed ALB creation/configuration evidence. | Treat as not created and `BLOCKED` until a fresh inventory proves otherwise. |
| TLS | Three independently purchased certificates for `noteaipro.cn`, `api.noteaipro.cn`, and `admin.noteaipro.cn` were issued. | Not bound to an ALB listener; certificate validity and deployment state require read-only recheck. |
| DNS | Enterprise DNS was paid and the primary domain was bound. | Production business records were not cut over. Local DNS tests are unreliable while proxy fake-IP DNS is active; use authoritative/control-plane evidence. |

### 3.3 Traffic, payments and unrecorded changes

- **Production traffic:** no intended production user path exists because the application services are not accepted, no ALB is configured and business DNS has not been cut over. Exact zero traffic is not independently proven because ALB/application access logs do not exist or were not audited.
- **Cloud payments:** real cloud-resource payments occurred, including domains/DNS, certificates, ECS, RDS, Tair, ACR-related service, and VPC public egress/SNAT resources. RDS and Tair are monthly purchases without confirmed auto-renew.
- **End-user payments:** no real NoteAI payment callback, settlement or reconciliation flow was executed or verified.
- **Production data writes:** only the explicitly authorized clean-production RDS schema migrations are confirmed. No Staging data import and no destructive database action are confirmed.
- **Unrecorded cloud changes:** no specific additional mutation is evidenced, but a complete post-interruption Alibaba inventory and billing audit has not been completed. Therefore absence of unrecorded changes remains uncertain and must be audited read-only.

## 4. Exact recovery point and image supply chain

### 4.1 Retained recovery point and next-source rule

- Exact source revision for the current three browserless ACR role images: `a635692a899ee02c6905cd694611c14e0da4594a` (`a635692`). Historical `93b03d5c40fb64fa264c0ada1c055f8e7696b161` remains only the hardened browser-worker baseline and is not the current image revision.
- Two superseded local-only candidates exist: `noteai-local:git-89e3505-amd64-api-r1` and `noteai-local:git-89e3505-amd64-worker-r1`. Both are `linux/amd64` and carry full revision `89e3505225c9d596d6ddb26040f08b777e77823c`, but they predate the sandbox correction and must not be pushed or deployed. Neither has an ACR registry digest.
- API size is `1,042,774,445` bytes and Worker size is `2,494,249,539` bytes. Local Docker image IDs are recorded in the ECS acceptance evidence and are explicitly **not** ACR registry digests.
- No current role image may be deployed yet. The exact `a635692a899ee02c6905cd694611c14e0da4594a` Pillow-fixed browserless roles have been pushed to private ACR; corrected digest pull/provenance, VEX version 3 and bounded content/supply-chain acceptance are verified. A newly found local ACR auth entry must be removed under separate approval before deployment planning. Their raw reports remain canonical and unsuppressed at `4 Critical / 19 High`; the exact-product VEX remains a separate impact statement rather than a production exception.
- `89e3505`, `8f7d9c2`, `b6abaa7` and `ff030f5` are no longer acceptable build sources. Any later Handoff-only HEAD is documentation provenance only, not application provenance.

### 4.2 Last verified ACR image inventory

| Tag | Digest | Platform | Release decision |
|---|---|---|---|
| `git-ecc4702e` | `sha256:b7a7d47dba6a314fae2fde6d1d35cafe9f209da41f385c266f463a31f66f6916` | `linux/amd64` | **Forbidden for production release:** older source revision and missing the `ff030f5` Meituan CLI packaging change. |
| `git-ff030f5` | `sha256:751b7ecc9e6f883b923f52754c5290d07e06610014ae43ee5d7fa303c168afe1` | `linux/arm64` | **Forbidden on x86 ECS:** architecture mismatch. |
| `git-ff030f5-amd64-r2` | Not verified | Required `linux/amd64` | Superseded target; do not build or deploy. |
| `git-b6abaa7-amd64-r1` | Never pushed by these tasks; current ACR existence not re-read | `linux/amd64` locally | Superseded local candidate; do not push or deploy. |
| `git-8f7d9c2-amd64-r1` | Local-only at last verification; no ACR registry digest exists | `linux/amd64` | **Superseded historical combined image:** predates the runtime split and Trixie base; do not push or deploy. |
| `noteai-local:git-89e3505-amd64-api-r1` | Local image ID recorded in ECS evidence; no ACR digest | `linux/amd64` | **Superseded:** predates release `93b03d5`; baseline scan `3 Critical / 19 High`. |
| `noteai-local:git-89e3505-amd64-worker-r1` | Local image ID recorded in ECS evidence; no ACR digest | `linux/amd64` | **Superseded and forbidden:** predates fail-closed Chromium sandboxing; baseline scan `5 Critical / 32 High`. |
| `noteai-local:git-ca02335-amd64-api-r1` | Local image ID `sha256:5fa36abe1c603aa0abc1295912fe6ee16a0b468d8be216475be06a4daa9a6454`; no ACR digest | `linux/amd64` | **Superseded local build:** exact RAP-fixed revision; contains Pillow `12.2.0`; Secret `0`, `4 Critical / 29 High`. |
| `noteai-local:git-ca02335-amd64-admin-r1` | Local image ID `sha256:6d3b5ee2dd4619a60394ad4e026287fe4b5a9a39dd722c37954fe58627369865`; no ACR digest | `linux/amd64` | **Superseded local build:** exact RAP-fixed revision; contains Pillow `12.2.0`; Secret `0`, `4 Critical / 29 High`. |
| `noteai-local:git-ca02335-amd64-xhs-http-r1` | Local image ID `sha256:755e337c2932ef53187b5af333b1daa47be578a81af9a57d6c1af7a9368b3b60`; no ACR digest | `linux/amd64` | **Superseded local build:** exact RAP-fixed revision; contains Pillow `12.2.0`; Secret `0`, `4 Critical / 29 High`; basic/RAP `network=none` smoke passed. |
| `git-a635692-amd64-api-r1` | Direct-pull-verified ACR digest `sha256:17706e1802afc136ac8f9a621d4199a7f9749da733290e923e42268eff42e0d1`; local image ID remains `sha256:b1983bab928ef93495d8be020030917d4ae54364234390047c3163af5414fedb` | `linux/amd64` | **Managed loopback deployment accepted:** exact digest/provenance/VEX, separated env, zero-AI readiness, database/log acceptance and restart recovery passed independently on API-C and API-F; both remain outside ALB. |
| `git-a635692-amd64-admin-r1` | Direct-pull-verified ACR digest `sha256:d94bc4581e85a5b507415da2abc284c26e46288a746f91e951a43380d670c733`; local image ID remains `sha256:2283095764622e373e30b51ba749819751e6bfb0c37c6bb82e2d3bfe4937760f` | `linux/amd64` | **Managed loopback deployment accepted:** exact digest/provenance/VEX, separated env, non-root/read-only hardening, database read-only mode, nine zero-AI health rounds, bounded logs and restart recovery passed on API-C; remains outside ALB. |
| `git-a635692-amd64-xhs-http-r1` | Direct-pull-verified ACR digest `sha256:452c2faf7853ce58d93e43c05fb6217a9a6cc1e4c81345d8cef2f50acabd79af`; local image ID remains `sha256:5b44114d4bd9c28a8e93c39140466c542e8babeead038fb0d1cfe45c3cd75966` | `linux/amd64` | **Content/auth accepted / deployment blocked:** corrected digest, OCI provenance, VEX version 3, exact prior-image identity and zero local builder auth passed; role-specific Secret injection, live preflight and separate XHS write approval remain. |

No mutable tag such as `latest` is an acceptable production reference. The three current ACR manifests may only be considered after `PROD-IMG-003` and later explicit deployment approval; all historical entries remain forbidden.

### 4.3 Interruption point

The 2026-07-19 interruption occurred **before a successful native AMD64 build and ACR push of the corrected `ff030f5` image**. The ARM64 image had been pushed under `git-ff030f5`. A corrected AMD64 target tag had not appeared in the last verified ACR inventory. API-C had pulled the older `git-ecc4702e` AMD64 image, and runtime secret entry had been performed, but the NoteAI API application had not completed start/readiness acceptance.

### 4.4 Completed, not started and uncertain

Completed with evidence:

- `PROD-PREBUILD-001` and all four child gates are independently verified at application revision `b6abaa781c11950c4d261e8d9f17c3194aecd0cf`.
- Release candidate `b6abaa781c11950c4d261e8d9f17c3194aecd0cf` contains the required `ff030f5` Meituan CLI packaging fix and all independently verified prebuild closures.
- Production Claude Gateway exists on Render Singapore and public live/readiness is healthy.
- Clean production RDS schema migrations `0001` through `0008` were applied without Staging data import.
- Three TLS certificates were issued.
- VPC/subnets, RDS, Tair, ECS hosts, ACR repository/private connectivity and SNAT were provisioned in prior authorized steps.
- `PROD-RUNTIME-SPLIT-001` is independently verified at exact application revision `89e3505225c9d596d6ddb26040f08b777e77823c`; repository-only API/Worker target separation and fail-closed role startup controls passed all final gates.
- `PROD-IMG-SPLIT-BUILD-001` completed on retained isolated builder `i-wz99180s9ig5ecq10uaj`: exact clean detached source and the production Git LFS release set were verified; both local native AMD64 targets were built; provenance, role marker, entrypoint/CMD, non-root user, `mttravel`, model, sensitive-path/history, SBOM, Secret and vulnerability evidence was recorded.
- `PROD-WORKER-HARDEN-001` completed at exact application revision `93b03d5c40fb64fa264c0ada1c055f8e7696b161`: repository sandbox enforcement, pinned seccomp configuration, tests and offline evidence collection were independently verified and normally pushed with `[skip render]`.
- `PROD-IMG-XHS-BROWSERLESS-REBUILD-001` completed on retained isolated builder `i-wz99180s9ig5ecq10uaj`: clean exact source `ca02335513a752ece2dabe71d08105ae86db6c5a`, Git LFS/model integrity, three local native AMD64 builds, provenance/content, CycloneDX/Secret/unsuppressed scans and `network=none` basic/RAP signer smoke all completed. The execution task is verified; the images remain release-blocked by `4 Critical / 29 High`.
- `PROD-PILLOW-12.3.0-FIX-001` completed at exact application revision `a635692a899ee02c6905cd694611c14e0da4594a`: shared production pin `12.2.0` → `12.3.0`, direct compatibility/readiness regression coverage, Python 3.11 wheel validation, full tests and independent verification passed. No image was built or rescanned.
- `PROD-IMG-XHS-PILLOW-REBUILD-001` completed on retained isolated builder `i-wz99180s9ig5ecq10uaj`: clean exact source `a635692a899ee02c6905cd694611c14e0da4594a`, Git LFS/model integrity, three local native AMD64 builds, provenance/content, CycloneDX/Secret/unsuppressed scans and `network=none` basic/RAP signer smoke all completed. Pillow rows are `0`; every role remains release-blocked by residual `4 Critical / 19 High`.

Not started or not completed:

- Read-only disposition of all residual Debian `4 Critical / 19 High` rows in the exact Pillow-fixed images. No production exception or VEX has been registered.
- ACR push/read-back of any accepted successor candidate and immutable registry digest verification.
- API-C start on the corrected immutable digest and zero-AI readiness.
- Real bounded provider verification for Kimi, Claude Gateway, Amap and Meituan from production.
- API-F deployment.
- ALB creation/configuration, certificate binding and DNS cutover.
- Production smoke test and payment callback/reconciliation validation.

State uncertain and requiring fresh read-only verification:

- Current ACR tag list and whether the target tag was created after the last visible check.
- Current API-C/API-F host and service/process state.
- Current RDS/Tair health, backup/PITR, renewal and network policies.
- Current VPC routes, SNAT, security groups and PrivateZone resolution from both nodes.
- Complete Alibaba resource/billing inventory after the interruption.
- Exact absence of production traffic.

## 5. Unique task ledger

Only these task statuses are permitted: `TODO`, `INVESTIGATING`, `READY_TO_EXECUTE`, `EXECUTING`, `READY_TO_VERIFY`, `VERIFIED`, `BLOCKED`, `DEFERRED`. A task may become `VERIFIED` only after independent verification evidence is recorded here.

### PROD-PREBUILD-001 — Production image prebuild closure

- Status: `VERIFIED`
- Application release candidate: `b6abaa781c11950c4d261e8d9f17c3194aecd0cf` (`fix(prod): close prebuild release gates [skip render]`).
- All four prebuild gates are independently verified: model manifest integrity, explicit/concurrency-safe migrations, core-only zero-outbound health semantics, and pinned/provenance-aware build configuration.
- Independent verification: `586 OK (skipped=5)` full tests; `278 OK` targeted tests; production readiness gate `63/63`; `network_attempts=0`; `py_compile`, Compose/static configuration, diff, secret, and build-context checks passed.
- No production image build/push, service start, database write, cloud mutation, or external-provider call was performed.

### PROD-MODEL-001 — Model release manifest SHA drift

- Status: `VERIFIED`
- Human-readable release checks: `8/8`; machine-readable checks: `4/4`; Git LFS artifact checks: `3/3`.
- The drift was confined to stale human documentation; the machine manifest and LFS artifacts were consistent. The documentation was minimally corrected and the complete SHA validation passed again.
- No production exception was registered and no model binary was replaced.

### PROD-BOOT-001 — Production startup and migration isolation

- Status: `VERIFIED`
- A normal PostgreSQL production API import/start path performs `0` migration runs and opens `0` migration connections.
- The explicit one-time migration path applies migrations `0001`–`0008` once, applies `0` on the second run, and is protected by a PostgreSQL advisory lock against concurrent execution.
- Local and Render Staging behavior remains supported; ordinary production API restarts do not change database structure.
- Verification did not connect to or write any real production database.

### PROD-HEALTH-001 — Production health-check semantics

- Status: `VERIFIED`
- `/health/live` is process-only. `/health/ready` checks only core readiness (database plus required local model artifacts), and returns non-200 when a core dependency is unavailable.
- Optional external providers do not participate in readiness, are not called by health checks, and do not cause all API nodes to be removed. Verification recorded `network_attempts=0`.
- Health responses do not disclose secrets or internal provider configuration; unavailable admin dependencies fail safely.

### PROD-PROV-001 — Image provenance and reproducible-build controls

- Status: `VERIFIED`
- Runtime base images are pinned by index digest; OCI `revision`, `source`, `version`, and `created` metadata are explicit build inputs; build-context and secret-exclusion controls passed static verification.
- The retained API/Worker images correctly identify `93b03d5c40fb64fa264c0ada1c055f8e7696b161`. The future browserless role images must identify the new verified adapter release commit; neither a Handoff-only HEAD nor a relabelled old image may replace its true revision.
- Runtime build evidence for the predecessor `89e3505225c9d596d6ddb26040f08b777e77823c` exists under `/opt/noteai-build/evidence-89e3505-split/` as a superseded baseline. Current exact `93b03d5` evidence exists under `/opt/noteai-build/evidence-93b03d5-hardened/`: API `3 Critical / 19 High`, Worker `5 Critical / 32 High`, Secret `0` for both, plus successful bounded Worker sandbox runtime acceptance. These results do not authorize a push or deployment because the residual vulnerabilities lack an approved disposition.

### PROD-IMG-001 — Build corrected AMD64 image

- Status: `BLOCKED`. The retained isolated ECS `noteai-build-amd64-b6abaa7-r1` (`i-wz99180s9ig5ecq10uaj`) in Alibaba Cloud Shenzhen F was rechecked and used only for the approved split and browserless local build/scan tasks. The user extended its release time; exact future lifecycle state must still be rechecked before any later task.
- Historical source/build evidence (2026-07-20): exact detached HEAD `8f7d9c23dd4c4bc951f23d2412156f121da868e7`, tree `483544cdbf5c2d7c11827ca296874f097d2969c2`, clean status, and three production V0.4 Git LFS binary SHA values equal their commit pointer OIDs. This evidence does not cover the hardened targets at `93b03d5c40fb64fa264c0ada1c055f8e7696b161`.
- Superseded historical local candidate: `noteai-local:git-8f7d9c2-amd64-r1`, `linux/amd64`, size `3,454,259,927` bytes, local image ID `sha256:47174b226ef11b704fcf92474e3f9d9bf266ef77a75e23ae4fdab99e1cfdd0f9`. This is a local Docker image ID, **not** an ACR registry digest. It must not be pushed or deployed.
- Historical acceptance: exact OCI revision/source/version/created (`2026-07-20T14:54:53Z`), non-root UID `999`, expected entrypoint/CMD, Python-stdlib live probe, `mttravel 1.0.16` and bundle SHA, four machine-manifest artifacts, no curl/Xvfb/setuptools/wheel, non-root offline headless Chromium `about:blank`, restored apt sources, no `.env`/`.git`, no sensitive ENV/history assignment, Trivy Secret `0`, CycloneDX SBOM `298` components.
- Historical remediation delta: package records fell from `75` to `39`; High fell from `68` to `32`; Critical remained `7`; unique CVE IDs became `29`; removed records `36`, added records `0`. This result belongs only to the superseded combined image.
- Historical blocking acceptance: Trivy `0.70.0` reported `32 High` and `7 Critical` for the superseded combined image; the later split/hardened results were API `3 Critical / 19 High` and Worker `5 Critical / 32 High`. The current Pillow-fixed browserless API, Admin and XHS images each report `4 Critical / 19 High`; no ignore, VEX or production exception was used or registered.
- Historical evidence was retained at `/opt/noteai-build/evidence-8f7d9c2` and `/opt/noteai-build/PROD-VULN-FIX-001-evidence.tgz` (`215,209` bytes, SHA256 `e4dd0fe886c12af3a3f9e10fe50af0254829d122954fc9b9e6b383284d0984cf`). The last recorded automatic release was `2026-07-21 21:22 +08:00`; current builder/image existence must be verified and is not inferred here.
- The current retained browserless evidence uses exact Pillow-fixed application revision `a635692a899ee02c6905cd694611c14e0da4594a`. Never substitute superseded `ca023355`, `344897c`, any other Handoff-only commit, or historical `f3a1a3b`, `93b03d5`, `89e3505`, `8f7d9c2`, `b6abaa7` or `ff030f5` as that application revision.
- ACR inventory was not read or modified during this execution phase. No ACR login or push occurred, and the remote existence of the local candidate tag was not assumed.
- Executed scope was limited to the approved temporary isolated Alibaba Cloud Shenzhen `x86_64` builder, never API-C or API-F: clean exact checkout, local `linux/amd64` build, and local acceptance only. PROD-IMG-002, service start, database/cache access, and production cloud-service changes were excluded.
- Acceptance evidence completed locally for browserless `api-runtime`, `admin-runtime` and `xhs-http-runtime`: AMD64 architecture, full OCI revision/source/version/created labels, expected role marker and owner, entrypoint/user/model and role-appropriate contents, absence of installed Playwright/Chromium/browser graphics capability and of any production-reachable browser command, SBOM, vulnerability scan and Secret scan. Before any push decision, every residual Critical or High finding still requires explicit disposition.
- Cost/impact: temporary ECS compute, system disk, and dependency downloads; no paid AI/provider calls. Rollback remains deletion of the unpushed local image/build cache and release of the temporary builder, but neither deletion nor manual release was performed because both require a separate lifecycle decision.
- `PROD-IMG-XHS-BROWSERLESS-BUILD-001` failed RAP runtime acceptance as recorded below. Its RAP-fix and clean successor rebuild are now complete. Continue to prohibit ACR, exception/VEX registration, deployment and real Xiaohongshu calls; do **not** approve `PROD-IMG-002` for any current local image.

### PROD-VULN-001 — Read-only vulnerability triage

- Status: `VERIFIED`
- Approved scope (2026-07-20): read-only verification of the `68 High` and `7 Critical` Trivy findings, including report integrity, duplicate findings, Debian/Python vendor fix status, candidate-image reachability, and the minimum remediation path.
- Prohibited in this task: production exception registration, image rebuild or mutation, ACR login/push, service start/redeployment, production database/cache access, cloud-resource changes, and real external-provider calls.
- Evidence integrity: report `/opt/noteai-build/evidence/trivy-vuln-high-critical.json` is `958,146` bytes with SHA256 `c71e872df48ee6de5183abbefe45735f66e2ed3423453dc7aaa9731d7783f51d`; its image ID exactly equals the live local candidate ID above. Trivy is `0.70.0`; DB metadata is version 2, updated `2026-07-20T13:19:47.192055519Z`, downloaded `2026-07-20T13:45:37.4832612Z`; the SBOM and sensitive-content scans exited `0`, while the vulnerability gate exited `1`. The scan used `HIGH,CRITICAL` plus exit code 1 and no ignore/VEX option.
- De-duplication: all `75` package findings are unique by CVE/package/version/target, but they represent `50` unique CVE IDs: `43 High` and `7 Critical`. Seventeen CVE IDs repeat across related binary packages, creating `25` extra package-level counts; examples are one util-linux CVE counted across eight binary packages, X.Org CVEs counted against both `xserver-common` and `xvfb`, and curl CVEs counted against both `curl` and `libcurl4`.
- Debian/Python fix state: the `66` Debian High records have no fixed version in the current Bookworm feed (`45 affected`, `20 fix_deferred`, `1 will_not_fix`); current installed versions for the leading families match Debian's current Bookworm package versions. The two Python High records have upstream fixes, but Trivy correctly located vulnerable vendored code inside `setuptools 79.0.1`: `jaraco.context 5.3.0` (fixed upstream in `6.1.0`) and vendored `wheel 0.45.1` (fixed upstream in `0.46.2`); the separately installed `wheel 0.46.3` does not remove the vendored copies.
- Critical reachability: no Critical was demonstrated as reachable from a normal NoteAI API request. `CVE-2023-45853` is Debian's zlib/MiniZip source-package false positive for this image: no MiniZip binary/file is installed. `CVE-2026-8376` applies to 32-bit Perl while the image is `x86_64`. `CVE-2026-42496` requires `Archive::Tar`, but that module and `perl-modules-5.36` are absent. NoteAI does not invoke Perl. `CVE-2026-13221` is additionally inconsistent in Debian's own record (Bookworm marked vulnerable while the note says introduced in Perl `5.37.10`; image is `5.36.0`) and is not treated as proven exploitable. SQLite is linked, but `CVE-2025-7458` requires attacker-controlled arbitrary SQL and the repository exposes only fixed/parameterized statements. GLib is linked by Playwright Chromium, while `CVE-2026-58016` requires malformed D-Bus introspection XML and no NoteAI D-Bus/XML input path was found. libxml2 is present through `libLLVM-15`/graphics dependencies, while `CVE-2026-6653` requires malicious XML parsing and no NoteAI XML parser/input path was found. These reachability conclusions are evidence, not a registered production exception.
- High attack-surface grouping: `20` High package findings are the uninvoked Xvfb/X.Org path; `13` are curl/libcurl/libssh2 while curl is used only for the loopback Docker health check and the application uses Python clients; `2` are setuptools vendored build-tool paths not imported by NoteAI. These `35` records are the first deterministic removal target. Remaining findings cluster in current Bookworm base utilities, XML/GLib/graphics dependencies and SQLite/Perl metadata, and require a new image plus re-scan before any acceptance decision.
- Minimum remediation path (not executed): create an API-specific runtime stage without Playwright/Xvfb/graphics packages, keep browser dependencies only in the crawler/worker image, replace the curl health check with a zero-outbound Python probe, remove runtime pip build tooling/setuptools vendored code, and re-evaluate the pinned supported base digest. Then rebuild a new local AMD64 candidate and re-run the same SBOM/secret/vulnerability/reachability gates. Any remaining vendor-unfixed finding must be resolved by a separately reviewed VEX/production-risk decision; no exception was created here.
- Stop evidence: only `--network none --read-only` disposable inspection containers were used; after audit, running containers `0`, `RepoDigests=[]`, Docker auth entries `0`. No API, provider, ACR, database/cache or cloud-resource write occurred.
- Historical release decision was superseded by PROD-VULN-FIX-001. Current `PROD-IMG-001` and `PROD-IMG-002` remain `BLOCKED`; neither local `r1` candidate may be pushed or deployed.

### PROD-VULN-FIX-001 — Minimize runtime attack surface and rebuild locally

- **Status:** `VERIFIED` on 2026-07-20.
- **Priority:** Critical while the temporary builder is retained.
- **Approved scope:** minimum Docker/runtime dependency closure, corresponding tests, one new `[skip render]` release commit and normal push of the current branch, followed by one native AMD64 local rebuild/re-scan on the existing isolated builder.
- **Proposed scope:** make only the minimum Docker/runtime dependency and related test/documentation changes described by PROD-VULN-001; independently verify them; create a new `[skip render]` release commit; then rebuild and re-scan one new native AMD64 local candidate on the existing isolated builder.
- **Repository implementation:** remove the curl-only health-probe dependency and use Python standard-library loopback probes in Docker/Compose; purge only `xvfb` and `xserver-common` after an offline Playwright Chromium `about:blank` smoke; uninstall runtime `setuptools`/`wheel` only after dependency installation; require `pip check`; retain Chromium, Playwright, OpenCV/graphics libraries, Meituan CLI and all application paths.
- **Pre-commit verification:** full unit suite `587 OK (skipped=5)`; targeted deployment/readiness tests `25 OK`; production readiness `66/66`; required model-artifact check `4/4`; quality gate passed its expected good/bad fixtures; `py_compile`, Compose parse, diff check, upstream divergence `0/0`, tracked-file sensitive-value scan and worktree scope checks passed. No service or provider was started/called.
- **Release commit:** `8f7d9c23dd4c4bc951f23d2412156f121da868e7` (`fix(prod): minimize runtime image attack surface [skip render]`) was pushed normally to `origin/codex/quality-stabilization-real-chain`; no force-push and no Render deployment occurred.
- **Independent execution evidence:** one new local native AMD64 candidate was built; offline runtime/content/provenance gates passed; the exact Trivy delta is `68→32 High`, `7→7 Critical`, `36` records removed, `0` added, Secret `0`, SBOM `327→298`. Deterministic avoidable findings are absent. Residual findings were returned without exception or suppression, satisfying this remediation task while leaving the image release task blocked.
- **Excluded:** ACR login/push, production exception/VEX acceptance, API-C/API-F start or redeployment, production database/cache access, ALB/TLS/DNS, and real provider calls.
- **Cost/limit:** existing temporary ECS is about `¥0.98748/hour` plus metered public traffic and remains scheduled for automatic release at `2026-07-21 21:22 +08:00`; no extension is authorized.
- **Acceptance:** independent tests and readiness gates pass, re-scan evidence is bound to the new exact image/release commit, deterministic avoidable findings are removed, and any residual finding is returned for a separate evidence-based decision rather than silently ignored.

### PROD-RUNTIME-SPLIT-001 — Split API and browser-worker runtimes

- **Status:** `VERIFIED` on 2026-07-21; repository implementation, independent verification and the local application release commit are complete.
- **Approved scope:** minimum Dockerfile/Python dependency split, Compose/Render role selection, startup protection, corresponding tests and necessary build specification; independent verification followed by a `[skip render]` release commit and normal branch push. Image build/pull, ACR, deployment, cloud resources, production data, real providers and VEX/exception registration remain prohibited.
- **Implementation:** added `api-runtime` and `worker-runtime` Docker targets on the pinned `python:3.11.15-slim-trixie` index; browser dependencies remain Worker-only; Compose and Render select explicit roles; the root-owned image role marker must match the runtime declaration. Missing/unreadable/empty/invalid markers, read failure, role mismatch and non-allowlisted command shapes fail closed with exit 78 before artifact loading, migration, import or execution. API and Worker use explicit command-shape allowlists, Worker defaults to `/bin/false`, and API rejects its embedded scheduler flag.
- **Independent review round 1:** a High fail-open startup-guard gap was found; the same implementation agent corrected it before re-review.
- **Independent review round 2:** the High finding was closed; a Medium false-positive risk in the readiness gate's string-only validation was found, and the same implementation agent replaced it with an offline semantic harness and mutation checks.
- **Independent review round 3:** `VERIFIED`; no Critical, High or Medium finding remained.
- **Final verification evidence:** full socket-blocked unit suite `594 OK (skipped=5)` with `network_attempts=0`; targeted readiness/deployment tests `38 OK`; production readiness `73/73`; semantic entrypoint matrix `14` allowed and `32` rejected cases; all three independently injected mutation classes failed the gate as required; shell syntax, modified Python compilation, YAML parsing, Compose quiet parse, build/readiness static checks, diff check and tracked-file/build-context secret checks passed. No Docker build/pull, container/service start, database/provider/network call or secret output occurred.
- **Release commit:** `89e3505225c9d596d6ddb26040f08b777e77823c`, parent `9bf6fcb7e9620c3ed0a831a889c0d5f34ceb6894`, message `fix(prod): split api and worker runtimes [skip render]`. It was normally pushed to `origin/codex/quality-stabilization-real-chain` without force; local and remote application SHA matched before the Handoff commit. The later Handoff-only checkpoint SHA is intentionally not invented.
- **Release rule:** exact `89e3505225c9d596d6ddb26040f08b777e77823c` is the unique application source for both role-specific images. Historical `8f7d9c2` and the old local combined image are superseded and forbidden for push/deploy. A later Handoff-only commit remains documentation provenance only.
- **Completion:** this repository task is complete. `PROD-IMG-001` remains blocked; the built local role-specific candidates require the separate residual-vulnerability decision below before any ACR task can be considered.

### PROD-IMG-SPLIT-BUILD-001 — Build and scan split native AMD64 images

- **Status:** `VERIFIED` on 2026-07-21. The authorized local build/scan scope is complete; the image release decision remains blocked by residual vulnerabilities.
- **Builder and boundary:** retained isolated native AMD64 ECS `noteai-build-amd64-b6abaa7-r1` / `i-wz99180s9ig5ecq10uaj`, Shenzhen F, `x86_64`, 4 vCPU / 16 GiB. Security-group inbound rules remained `0`; no RAM role or Docker registry config existed; the task never targeted API-C/API-F and never accessed production RDS/Tair.
- **Source and LFS:** clean detached worktree `/opt/noteai-build/repo-89e3505-exact-r3` at exact `89e3505225c9d596d6ddb26040f08b777e77823c`; production V0.4 LFS artifacts matched their pointer/manifest SHA values and human/machine manifests agreed.
- **Local candidates:** `noteai-local:git-89e3505-amd64-api-r1` (`1,042,774,445` bytes) and `noteai-local:git-89e3505-amd64-worker-r1` (`2,494,249,539` bytes). Both are `linux/amd64`, full OCI revision/source/version/created values match the authorized build inputs, and `RepoDigests` is empty. Local image IDs are preserved in evidence and are not registry digests.
- **Acceptance:** both images use non-root `noteai`, `/app/scripts/docker_entrypoint.sh`, role-correct CMD and root-owned mode `0444` `/etc/noteai-runtime-role`; live health metadata uses `/health/live`; `mttravel 1.0.16` and bundle SHA `8a0527bb6e8b9074cf0221756f35bca989945e8436e4841596a7b7bf79ae06d8` passed; three V0.4 models and human/machine release manifests passed. API contains `0` Playwright and Chromium paths; Worker contains `518` Playwright and `51` Chromium paths. Sensitive filesystem paths and history Secret-name hits are `0` for both; the allowlisted non-secret `.env.example` exists once in each image.
- **Scan execution:** Trivy `0.70.0`, DB version 2 updated `2026-07-20T13:19:47.192055519Z`; scanner containers used `--network none`, no ignore, no VEX and `--ignore-unfixed=false`. API SBOM has `174` components, Secret findings `0`, vulnerabilities `3 Critical / 19 High`. Worker SBOM has `288` components, Secret findings `0`, vulnerabilities `5 Critical / 32 High`. Raw and unique counts are equal (`22` API, `37` Worker), so duplicates are `0`; all `59` findings have no reported fixed version.
- **Critical detail:** API Criticals are `CVE-2026-13221`, `CVE-2026-42496` and `CVE-2026-8376`, all on `perl-base 5.40.1-6`. Worker has the same three plus `CVE-2026-58016` on `libglib2.0-0t64 2.84.4-3~deb13u3` and `CVE-2026-6653` on `libxml2 2.12.7+dfsg+really2.9.14-2.1+deb13u3`. This is scan evidence, not an exploitability conclusion or exception.
- **Evidence:** `/opt/noteai-build/evidence-89e3505-split/` contains build logs plus `acceptance/`, `content/` and `scans-final/`; JSON/SBOM/TSV reports and SHA256 evidence files were retained on the builder. Scanner/tool containers were removed; final running-container count is `0`; Docker registry login remains absent.
- **Prohibited:** ACR login/read/push, production exception/VEX registration, deployment, API-C/API-F or other service start, production database/Tair access, ALB/TLS/DNS or any cloud-resource mutation, production data access, and real AI/map/Meituan/crawler/payment/provider calls.
- **Stop point reached:** Critical/High findings were returned without suppression. The follow-on read-only decision is complete, but neither image is accepted. Do not push or deploy either image; do not rebuild the same Trixie candidates because the vendor feed has no fixed version for any of the `59` rows.

### PROD-SPLIT-VULN-DECISION-001 — Review residual split-image findings

- **Status:** `VERIFIED` on 2026-07-21; the authorized read-only decision audit is complete. This verifies the analysis, not release acceptance.
- **Priority:** Critical release gate.
- **Evidence integrity:** Cloud Assistant invocations `t-sz06rk69xqngjy8` and `t-sz06rk6e96yzh8g` succeeded with exit `0` on builder `i-wz99180s9ig5ecq10uaj`. The retained TSV still contains `59` rows and `59` unique CVE/package/version/role records: API `22` (`3 Critical / 19 High`) and Worker `37` (`5 Critical / 32 High`), duplicate rows `0`. Final running-container count remained `0`; the read-only gate printed `PASS_READ_ONLY`.
- **Unique-CVE map:** API has `11` unique CVEs across ncurses (`CVE-2025-69720`), Perl/Archive::Tar/IO::Compress (`CVE-2026-13221`, `42496`, `42497`, `48962`, `57432`, `8376`, `9538`), gzip (`41992`), util-linux (`53615`) and libacl (`54369`). Worker contains those same base rows plus `15` browser-dependency rows: six Expat CVEs (`CVE-2025-59375`, `CVE-2026-25210`, `45186`, `56131`, `56407`, `56408`), CUPS `CVE-2026-34980`, seven GLib CVEs (`CVE-2026-58010` through `58016`) and libxml2 `CVE-2026-6653`. Across both roles this is `26` unique CVE IDs.
- **Vendor state:** all `59` rows have an empty scanner fixed version for installed Trixie packages, consistent with the current Debian tracker. Same-base `apt upgrade` or a same-digest rebuild therefore cannot clear the gate. Bookworm is also vulnerable for the audited families. Forky/Sid fixes some ncurses, Perl, Expat, CUPS, GLib, libxml2 and acl issues, but is not a stable production base and still leaves `CVE-2026-13221`, `CVE-2026-42496/42497`, `CVE-2026-41992`, `CVE-2026-53615`, `CVE-2026-58016` and `CVE-2026-9538` unfixed; it is not an evidence-backed drop-in solution.
- **API reachability:** repository inspection at exact `89e3505225c9d596d6ddb26040f08b777e77823c` found no application invocation of Perl, Archive::Tar, IO::Compress, `infocmp`, gzip decompression, blkid/partition parsing or libacl pathname APIs. The API image is `linux/amd64`, so 32-bit-only `CVE-2026-8376` is architecture-not-applicable. The remaining API rows are package-present with no demonstrated application input path, but static grep alone is insufficient to register `not_affected`; no VEX or exception was created.
- **Historical Worker reachability at `89e3505`:** CUPS `CVE-2026-34980` requires a network-exposed `cupsd` with a shared queue, while the image contains only the Chromium dependency library and configures no print server. The Perl/base CLI paths are likewise not invoked by NoteAI. The Expat/GLib/libxml2 rows cannot receive a blanket `not_affected`: Chromium may load these libraries and the Worker intentionally visits untrusted web pages. At that revision Playwright defaulted `chromium_sandbox` to false; `scheduler_a.py` explicitly passed `--no-sandbox` and could add `--no-zygote`; no Compose seccomp existed. This blocker was corrected in repository release `93b03d5`, but runtime proof still requires a new approved build and sandbox smoke.
- **Alternative-base decision:** retaining Trixie or reverting to Bookworm does not fix the findings; Forky/Sid is unstable and incomplete; Alpine is unsupported by Playwright's glibc browser builds and conflicts with the Python native-wheel stack; a distroless API would require replacing the shell entrypoint and packaged Node CLI; the matching Microsoft Playwright image is an evaluation candidate only, not an automatic production fix, because its own official guidance warns against untrusted-site use without sandbox controls and its vulnerability posture has not been scanned for this application.
- **Formal VEX path:** API is a candidate for a separate independent VEX evidence review using immutable image identity, SBOM component/version, package ownership, ELF/linkage and command/input-path proof for each CVE. Any eventual `not_affected` statement must use an evidence-backed justification, have expiry/re-review conditions and be bound to the final immutable image digest. Worker is not eligible for a blanket VEX while untrusted browsing runs without the Chromium sandbox; browser-library rows require stronger binary/call-path evidence even after hardening. No VEX document, suppression or production exception was produced or registered in this task.
- **Minimum safe path:** first correct Worker sandboxing without changing business behavior: remove explicit `--no-sandbox`, set `chromium_sandbox=True` for every launch, keep the non-root user, add a reviewed seccomp/runtime profile and fail-closed tests, while preserving Render Staging behavior. In the same bounded task, collect offline package-ownership and ELF/linkage evidence from both existing local images without starting an application or making network calls. Then create a new release commit and, only after separate approval, rebuild/re-scan both targets. Residual API and Worker CVEs may proceed to independent formal VEX review only after that evidence exists.
- **Prohibited and observed stop point:** no image rebuild, ACR login/read/push, production exception/VEX registration, deployment/service start, production data/dependency access, cloud-resource mutation or real provider call occurred. Both current local images and `PROD-IMG-002` remain `BLOCKED`.
- **Completion transition:** the recommended `PROD-WORKER-HARDEN-001` was separately approved and is now independently `VERIFIED` below. The next task is the bounded hardened rebuild/runtime gate; ACR, production exception/VEX registration, deployment, production access, cloud mutation and real provider calls remain prohibited.

### PROD-WORKER-HARDEN-001 — Enforce Worker Chromium sandbox

- **Status:** `VERIFIED` on 2026-07-21. Repository implementation, independent verification, offline evidence collection and the `[skip render]` application release commit are complete.
- **Release commit:** `93b03d5c40fb64fa264c0ada1c055f8e7696b161`, parent `5ba202febaff364328057a7f8a1ca41f72816a47`, message `fix(prod): harden worker chromium sandbox [skip render]`. It was normally pushed to `origin/codex/quality-stabilization-real-chain`; local/remote divergence was `0/0` before this Handoff checkpoint. This exact application SHA remains the source identity of the retained hardened images and the baseline for the adapter change; the next rebuild must instead use the new full verified adapter commit, never a later Handoff-only HEAD.
- **Launch enforcement:** all five Python Chromium launch points (`crawler.py` two, `scheduler_a.py` one, `download_covers.py` two) use `model/chromium_security.py`. The helper forces `chromium_sandbox=True`, rejects `--no-sandbox`, `--disable-setuid-sandbox` and `--no-zygote` before Playwright is called, and never retries a failed sandbox launch with weaker settings. The obsolete Render low-memory/no-zygote switch was removed.
- **Runtime boundary:** Worker remains `USER noteai`. Docker image creation no longer launches Chromium as root and checks only that Playwright resolves an installed executable. Compose applies `no-new-privileges:true` and the same seccomp profile to Admin, trends-worker and tracking-worker; API is excluded. No `privileged`, `SYS_ADMIN`, `seccomp=unconfined` or capability addition was introduced.
- **Seccomp provenance:** complete upstream Playwright v1.56.0 profile at `deploy/security/playwright-chromium-seccomp-v1.56.0.json`, `12,997` bytes, SHA256 `cc3e61cabda6bbc1e53e54d27ba4d55a9d3be829b6dd1a596f4a7b31b1cc7849`, `defaultAction=SCMP_ACT_ERRNO`, includes `SCMP_ARCH_X86_64` and the reviewed `clone`/`setns`/`unshare` allow rule. Render Blueprint has no reviewed custom-seccomp field; no unsupported field or sandbox bypass was invented.
- **Offline existing-image evidence:** Cloud Assistant invocations `t-sz06rkcdpibjyf4` and `t-sz06rkck2fakagw` completed with exit `0` on isolated builder `i-wz99180s9ig5ecq10uaj`. Stopped containers created with `--network none` were used only for file extraction; no image process was run. Package ownership and direct ELF `NEEDED` evidence for the existing API image ID `sha256:c44730a50e4ae873ba9b77f56bbb79c8bc573c1e8177da79c9b727c6c17707f1` and Worker image ID `sha256:b299d5f3ef5dc639a2b1611b0e7f46731f0eb9b0451ea540e0e4600423bf6fd8` was retained under the builder's timestamped `/opt/noteai-build/worker-hardening-evidence.*` directory. Worker browser paths include Playwright Chromium `1194`; direct ELF evidence starts with `libdl.so.2` and `libpthread.so.0`. This evidence does not prove complete transitive exploitability or non-exploitability.
- **Boundary evidence:** running containers before/after remained `0`; `/root/.docker/config.json` remained absent. No Secret was read or printed, no ACR login/read/push occurred, and no production database/cache/cloud resource or external provider was accessed.
- **Independent verification:** `87/87` targeted tests; full suite `601` passed with `5` skipped; production readiness `78/78`; seccomp hash/JSON semantics, all launch paths, fail-closed behavior, non-root Dockerfile, Compose role coverage, Render boundary, `py_compile`, Compose quiet parse, diff check and changed-file Secret scan passed. The independent verifier found no Critical/High/Medium/Low implementation defect. The full-suite `moonshot network error` text was a mocked failure path, not a real supplier call.
- **Remaining release gates:** the approved hardened build and the later bounded user-namespace runtime proof completed. Worker Chromium now has a successful non-root + pinned seccomp + `no-new-privileges` acceptance result on the isolated ECS. Residual unsuppressed Critical/High findings and deployment-platform support remain separate release decisions.
- **Completion transition:** the recommended `PROD-ROLE-VULN-DISPOSITION-001` is now independently `VERIFIED` below. Its conservative result leaves `14` Worker browser-library rows indeterminate; the next recommended approval is the bounded offline evidence task recorded there. Continue to prohibit ACR login/push, production exception/VEX registration, deployment, production data/cloud access and real supplier calls unless separately and explicitly approved.

### PROD-IMG-HARDENED-BUILD-001 — Build, scan and runtime-accept hardened split images

- **Status:** `VERIFIED` on 2026-07-22. Both native AMD64 images, all required scans and the bounded Worker Chromium sandbox runtime proof completed. This does not accept the residual vulnerabilities or authorize ACR/deployment.
- **Priority:** Critical release gate.
- **Boundary evidence:** isolated native `x86_64` builder `i-wz99180s9ig5ecq10uaj` remained in Shenzhen F with security-group inbound rules `0`, no RAM role, no Docker registry config, no running containers and no route to production `10.42.*`. Cloud Assistant boundary invocation `t-sz06rke7glu33sw` exited `0` with `BOUNDARY_AUDIT=PASS`.
- **Source evidence:** because GitHub HTTPS was also unreachable, an exact locally generated Git pack for delta `89e3505..93b03d5` was split and transferred through Cloud Assistant, each chunk and the reassembled pack was SHA256-verified, and the exact commit was imported without Secret material. Clean detached source is `/opt/noteai-build/repo-93b03d5-exact-r1`. Invocation `t-sz06rki4jfyu0hs` exited `0`: revision exact, clean worktree, `git lfs fsck`, all required V0.4 models/manifests, Dockerignore gate, tracked sensitive names `0`, seccomp SHA `cc3e61cabda6bbc1e53e54d27ba4d55a9d3be829b6dd1a596f4a7b31b1cc7849`, and OCI created `2026-07-21T06:30:11Z` all passed. The superseded API image was used only as a `--network none`, read-only Python 3.11 checker for the source tree; no service started.
- **Base retrieval resolution:** provenance-reviewed public ECR copies of the exact Dockerfile-pinned base indexes were supplied as named BuildKit contexts; the immutable index digests remained Python `sha256:db3ff2e1800a8581e2c48a27c3995339d47bdf046da21c7627accd3d51053a93` and Node `sha256:2cf067cfed83d5ea958367df9f966191a942351a2df77d6f0193e162b5febfc0`. No Dockerfile registry rewrite, floating tag or unreviewed base substitution occurred.
- **Build evidence:** Cloud Assistant invocation `t-sz06rl3f65uy2o0` completed the native builds. API `noteai-local:git-93b03d5-amd64-api-r1` is local image ID `sha256:e637ff8af8f63c83e1370066111765f6594297806ec9b3e153d03b1dcb1762bc`, size `1,042,778,777` bytes. Worker `noteai-local:git-93b03d5-amd64-worker-r1` is local image ID `sha256:17a80aa53c41909abf4dace7c0bec4cfc90e6fd4387bb1c80e8bc5ca7616836e`, size `2,494,844,195` bytes. Both are `linux/amd64`, `USER noteai`, exact revision `93b03d5c40fb64fa264c0ada1c055f8e7696b161`, source `https://github.com/iamyusen1314/noteai`, and carry the expected role/version labels, entrypoint and role CMD. Both have empty `RepoDigests`; these local image IDs are not ACR registry digests. The earlier Handoff value beginning `17a88aa...90e6f043...` was a transcription error: it matched zero retained evidence files, while the corrected ID above matched 11 build/SBOM/Secret/vulnerability/smoke evidence files.
- **Content/SBOM/Secret/vulnerability evidence:** evidence is under `/opt/noteai-build/evidence-93b03d5-hardened/`, with final API and Worker sets in `api-final-r4/` and `worker-final-r4/`. API is browser-free, SBOM components `174`, Secret findings `0`, `19 High / 3 Critical`, fixed-version findings `0`. Worker contains Playwright `1.56.0`, Chromium bundle `1194`, no Xvfb, SBOM components `288`, Secret findings `0`, `32 High / 5 Critical`, fixed-version findings `0`. Both passed mttravel `1.0.16` bundle SHA `8a0527bb6e8b9074cf0221756f35bca989945e8436e4841596a7b7bf79ae06d8`, all V0.4 model/manifest hashes, role/content/history/provenance checks, and absence of `.git` and `model/.env`. Trivy `0.70.0` ran offline with no ignore/VEX and database update timestamp `2026-07-20T13:19:47.192055519Z`.
- **Final audit:** invocation `t-sz06rm8im4wf6rk` reconfirmed exact clean source, LFS, model and seccomp hashes, provenance, scan counts, no production route, running containers `0` and Docker auth absent. Invocation `t-sz06rm8mmlhj01s` independently emitted the full image IDs in bounded chunks.
- **Worker smoke result:** historical invocation `t-sz06rm802szh24g` failed closed when the host quota was `user.max_user_namespaces=0`. Approved bounded invocation `t-sz06rmyrixwxzwg` then temporarily set that value to `128` and passed with `--network none`, UID/GID `999`, `PAGE_URL=about:blank`, `CHROMIUM_PROCESS_COUNT=7`, `FORBIDDEN_FLAG_COUNT=0`, `ALL_NNP_ONE=1`, `ALL_SECCOMP_FILTERED=1`, `NON_ROOT_PROCESS_COUNT=7` and `NESTED_UID_MAP_VARIANTS=1`. It emitted both `WORKER_USERNS_SMOKE=PASS` and `PROD_WORKER_USERNS_SMOKE=PASS`, restored `user.max_user_namespaces=0`, left `RUNNING_CONTAINERS_AFTER=0`, and emitted `USERNS_RESTORE=PASS`. No sandbox bypass, capability addition, unconfined seccomp or privileged container was used.
- **Boundary deviation disclosed:** a read-only Cloud Assistant selector accidentally included API-C and API-F together with the builder for invocation `t-sz06rm266jzmx34`. The command only ran process inspection and tailed a nonexistent builder log; all targets returned exit `0`. It performed no write, container action, network call, Secret access, service/data operation or state change. All later commands targeted only the isolated builder.
- **Execution:** native `linux/amd64` local builds of `api-runtime` and `worker-runtime`; exact OCI provenance; role/content/model/mttravel checks; CycloneDX SBOM; filesystem/history Secret scan; unsuppressed High/Critical scan; Worker non-root `about:blank` smoke with `no-new-privileges` and the vendored seccomp profile whose SHA is recorded above.
- **Prohibited:** ACR login/read/push, production exception/VEX registration, deployment/service start, API-C/API-F changes, production database/Tair access, ALB/TLS/DNS/cloud-resource mutation and real AI/map/Meituan/crawler/payment calls.
- **Risk/cost:** existing temporary ECS compute only; the smoke used `--network none` and made no provider call. Residual High/Critical findings remain unsuppressed. The approved host-level sysctl change was bounded, automatically restored and independently evidenced.
- **Rollback:** remove only new unpushed local image/build-cache objects if separately approved; retain the builder and evidence until the next release decision or its configured automatic release. Accidental builder release requires rebuilding from exact commit; local image IDs are never ACR digests.
- **Acceptance:** met for build/scan/runtime evidence. Both exact native AMD64 images and all provenance/content/SBOM/Secret/vulnerability evidence exist locally; API acceptance passed; Worker sandboxed Chromium started under the required boundary and cleanup passed. Stop before ACR.
- **Required decision to unblock release:** approve a separate independent role-specific residual-vulnerability disposition. Runtime acceptance does not itself justify VEX, exception registration, ACR push or deployment.

### PROD-ROLE-VULN-DISPOSITION-001 — Final role-specific vulnerability/VEX evidence review

- **Status:** `VERIFIED` on 2026-07-22. The authorized read-only review is complete; this validates the evidence classification, not a production exception, formal VEX statement or release acceptance.
- **Exact evidence identity:** the retained reports are API SHA256 `6e8e2078bb6ee9003b29fd67a2b8d68721aeab29b5167794339ade42c10f8c1d` and Worker SHA256 `30a677c518d78b44f1add6ab648cd3428c2ac51ea91d534abee74f3b40460e0a`. Cloud Assistant invocations `t-sz06rn0111qa5fk` and `t-sz06rn0e8b0sj5s` read and independently compressed the existing JSON on builder `i-wz99180s9ig5ecq10uaj`; both exited `0`. Counts remain API `22` (`3 Critical / 19 High`, `11` unique CVEs) and Worker `37` (`5 Critical / 32 High`, `26` unique CVEs), total `59`, duplicate rows `0`, and fixed-version rows `0`.
- **Debian state at review time:** every installed Trixie version in the reports remains marked vulnerable by Debian. Debian labels several as `no-dsa`, `postponed` or minor, but those labels are not fixes. Forky/Sid fixes `19` of the `26` unique CVEs, while `CVE-2026-13221`, `CVE-2026-41992`, `CVE-2026-42496`, `CVE-2026-42497`, `CVE-2026-53615`, `CVE-2026-58016` and `CVE-2026-9538` remain unfixed there too. A same-Trixie `apt upgrade` or unchanged-base rebuild cannot clear the gate today.
- **API reachability disposition:** no repository API/entrypoint invocation of Perl, Archive::Tar, IO::Compress, `infocmp`, gzip decompression, blkid/partition parsing or libacl pathname APIs was found. `CVE-2026-8376` is 32-bit-only while the exact image is `linux/amd64`. Debian's `perl-base` amd64 file list contains the Perl binary but not Archive::Tar or IO::Compress; the source-package mapping for `CVE-2026-53615` expands one libblkid flaw across nine util-linux binary packages, while NoteAI neither invokes mount/blkid nor has the privileges needed for the cited paths. No API finding was demonstrated reachable. All `22` API rows have sufficient evidence to enter a separate formal VEX authoring/reviewer workflow, but none was accepted or registered here.
- **Worker base/CUPS disposition:** the same `22` base rows have the same non-invocation/architecture evidence. `CVE-2026-34980` additionally requires a network-exposed `cupsd` with a shared queue; the image contains `libcups2t64` for Chromium compatibility, and Debian's package file list confirms that binary package contains `libcups.so.2` and documentation, not the CUPS daemon. These `23` Worker rows have strong evidence for later formal review, but remain unsuppressed.
- **Worker browser-library blocker:** six Expat rows (`CVE-2025-59375`, `CVE-2026-25210`, `CVE-2026-45186`, `CVE-2026-56131`, `CVE-2026-56407`, `CVE-2026-56408`), seven GLib rows (`CVE-2026-58010` through `CVE-2026-58016`) and libxml2 `CVE-2026-6653` are installed only in the Worker/browser role. The sandbox, non-root UID, pinned seccomp and `no-new-privileges` materially reduce exploit impact, but do not prove those parsing/library paths unreachable. Because the Worker intentionally visits untrusted pages and existing package/ELF evidence is not a complete transitive call-path proof, these `14` rows (`2 Critical / 12 High`) remain `INDETERMINATE` and are not ready for a `not_affected` VEX statement.
- **Formal VEX evidence decision:** API `22` plus Worker base/CUPS `23` rows are candidates for a separate signed, independently reviewed draft bound first to the exact local image IDs/revision and later to an immutable registry digest. The `14` Worker browser-library rows are excluded from that candidate set pending stronger offline binary/load/input-path evidence or a vendor-fixed runtime. No VEX file, suppression, waiver or production exception was created.
- **Observed boundary:** commands only opened existing report files; no container/image/service was started, no build occurred and builder files were not changed. Only builder `i-wz99180s9ig5ecq10uaj` was selected; API-C/API-F were not selected. The Alibaba Cloud console session expired after evidence extraction, and no reauthentication or credential handling was attempted. No ACR, production data/dependency, cloud-resource mutation or supplier call occurred.
- **Release decision transition:** the recommended `PROD-WORKER-BROWSER-LIB-EVIDENCE-001` completed below under a user-narrowed non-invasive boundary. It confirmed package installation and normal Chromium loading but intentionally left CVE-specific reachability `UNKNOWN`; therefore `PROD-IMG-002` remains `BLOCKED`.

### PROD-WORKER-BROWSER-LIB-EVIDENCE-001 — Restricted non-invasive browser-library review

- **Status:** `VERIFIED` on 2026-07-22 for the user-restricted non-invasive scope. This status confirms evidence collection and conservative classification only; it does not accept risk, register VEX, authorize ACR or release either image.
- **Exact evidence identity:** retained Worker image `noteai-local:git-93b03d5-amd64-worker-r1`, local image ID `sha256:17a80aa53c41909abf4dace7c0bec4cfc90e6fd4387bb1c80e8bc5ca7616836e`, `linux/amd64`, application revision `93b03d5c40fb64fa264c0ada1c055f8e7696b161`. This remains a local image ID, not an ACR digest. Existing Worker evidence remains SBOM `288`, Secret `0`, Trivy `5 Critical / 32 High`, report SHA256 `30a677c518d78b44f1add6ab648cd3428c2ac51ea91d534abee74f3b40460e0a`, no ignore/VEX and no fixed-version rows in the retained report.
- **Command boundary and correction:** only isolated builder `i-wz99180s9ig5ecq10uaj` was selected; API-C/API-F were not selected. Invocation `t-sz06rn4pc1x5nnk` exited `127` because Cloud Assistant truncated a nested heredoc; it was a transport/script error, not vulnerability evidence. Corrected invocation `t-sz06rn5k9owhbeo` used the exact retained image with `--pull never`, `--rm`, `--network none`, read-only rootfs, UID/GID `999`, all capabilities dropped, pinned seccomp and `no-new-privileges`; it exited `0` in four seconds.
- **Boundary results:** exact image ID and `amd64` matched; `RepoDigests=[]`; Docker auth config was absent; seccomp SHA256 was `cc3e61cabda6bbc1e53e54d27ba4d55a9d3be829b6dd1a596f4a7b31b1cc7849`; container status showed `NoNewPrivs=1`, `Seccomp=2`, one seccomp filter and only loopback. Running containers were `0` before/after and `user.max_user_namespaces` was `0` before/after. No persistent file, sysctl, image, service or cloud-resource state was changed. Cloud Assistant retained only its normal execution/audit records.
- **Installed package ownership:** `libexpat1:amd64 2.7.1-2` owns `/usr/lib/x86_64-linux-gnu/libexpat.so.1*`; `libglib2.0-0t64:amd64 2.84.4-3~deb13u3` owns `libglib-2.0.so.0*` and `libgio-2.0.so.0*`; `libxml2:amd64 2.12.7+dfsg+really2.9.14-2.1+deb13u3` owns `libxml2.so.2*`. Repository search found no NoteAI call of the listed Expat/libxml2/GLib vulnerable APIs.
- **Normal runtime-load evidence:** direct `ldd` on Playwright Chromium `141.0.7390.37` and a normal `LD_DEBUG=libs chrome --version` run loaded system `libexpat.so.1`, `libglib-2.0.so.0` and `libgio-2.0.so.0`; the observed path did not load system `libxml2.so.2`. This proves library presence/load only, not CVE-specific reachability from an untrusted page. A binary symbol-string scan was also emitted by the corrected command, but the user subsequently prohibited symbol probing; it is excluded from the release disposition and must not be repeated.
- **Safety stop:** after a security warning, the user prohibited exploit/PoC/payload/malformed-input, symbol probing, memory-corruption testing and sandbox-escape validation. A planned local parser-API probe was stopped before submission and never executed. No parser fixture, malformed input, exploitability test or real page was run. Anything not determinable from existing SBOM/Trivy, package metadata, normal loader evidence and official vulnerability metadata is recorded as `UNKNOWN`.
- **Official Debian status:** current Trixie versions above remain vulnerable. Debian Forky/Sid `expat 2.8.2-1` fixes all six retained Expat CVEs; `glib2.0 2.88.2-1` fixes `CVE-2026-58010` through `CVE-2026-58015` but not `CVE-2026-58016`; `libxml2 2.15.3+dfsg-1` fixes `CVE-2026-6653`. A same-Trixie package upgrade or unchanged-base rebuild cannot clear these rows.
- **Conservative disposition:** Expat six rows are `INSTALLED + LOADED + CVE_REACHABILITY_UNKNOWN`; GLib seven rows are `INSTALLED + LOADED + CVE_REACHABILITY_UNKNOWN`; libxml2 one row is `INSTALLED + NOT_LOADED_IN_OBSERVED_VERSION_PATH + OTHER_RUNTIME_PATHS_UNKNOWN`. All `14` rows (`2 Critical / 12 High`) remain release blockers. No `not_affected` statement, VEX, suppression, waiver or production exception was created.
- **Minimum safe closure:** use a digest-pinned Worker runtime whose Expat and libxml2 packages are vendor-fixed, then rebuild/re-scan; do not mix Debian suites in place. For GLib, wait for or select a supported vendor runtime containing an official fix for all seven, including `CVE-2026-58016`; Forky/Sid alone is currently insufficient. Removal is acceptable only if a future static dependency review proves Chromium can run without the package and a normal non-invasive loader check plus full Worker acceptance/re-scan passes. Until then, Worker/ACR release remains blocked.
- **No-repeat rule:** do not rerun the nested-heredoc command, binary symbol search, parser/API probe, exploitability test or sandbox-escape validation. Future evidence work is limited to reading existing SBOM/Trivy artifacts and official vendor metadata unless the user explicitly changes the boundary.

### PROD-WORKER-FIXED-RUNTIME-DESIGN-001 — Supported fixed-runtime candidate design

- **Status:** `VERIFIED` on 2026-07-22 for the approved read-only design scope. This confirms the candidate comparison and migration gate only; it does not authorize a Dockerfile/dependency change, image build, VEX/exception, ACR access or release.
- **Input identity:** current Worker is exact application revision `93b03d5c40fb64fa264c0ada1c055f8e7696b161`, Python `3.11.15`, Playwright `1.56.0`, Chromium bundle `1194` / Chromium `141.0.7390.37`, Debian Trixie base pinned by digest, non-root UID/GID `999`, pinned seccomp plus `no-new-privileges`. Existing scan evidence remains Worker SBOM `288`, Secret `0`, Trivy `5 Critical / 32 High`; the `14` browser-library rows remain `2 Critical / 12 High` with no approved disposition.
- **Official Python image candidates:** the Docker Official Images manifest currently publishes Python `3.11.15` on Trixie, Bookworm and Alpine only. Trixie is the current vulnerable baseline; Bookworm is also marked vulnerable for the retained Debian issues and is a regression rather than a fix; Alpine/musl is explicitly unsupported by Playwright. No supported Docker Official Python `3.11` Forky/Sid tag exists.
- **Debian newer-suite candidate:** Forky/Sid `expat 2.8.2-1` fixes the six retained Expat rows and `libxml2 2.15.3+dfsg-1` fixes `CVE-2026-6653`; `glib2.0 2.88.2-1` fixes `CVE-2026-58010` through `CVE-2026-58015` but Debian still marks `CVE-2026-58016` vulnerable. Mixing suites is prohibited, and Forky/Sid is neither a complete fix nor a supported Docker Python 3.11 production base.
- **Microsoft Playwright Noble candidate:** exact tag `mcr.microsoft.com/playwright/python:v1.56.0-noble` exists with manifest-list digest `sha256:a7f6cf3ae520c9d670ad956572c13747ed5abdbba5123a01526f873ed1662528`, so its Playwright/browser version can match the project. It is not accepted as a production replacement: Microsoft states the image is for testing/development and is not recommended for untrusted websites; its source base is Ubuntu Noble rather than Python 3.11; Ubuntu has fixed `CVE-2026-6653` for Noble libxml2 but still marks the retained Expat and GLib findings, including `CVE-2026-58016`, as `Needs evaluation`. A tag/digest existing is not evidence that the blockers are fixed.
- **Custom Ubuntu Noble candidate:** rejected for the current release. It would require a separately maintained Python 3.11 toolchain or an application upgrade to a different Python version, while official Ubuntu metadata still lacks fixed status for the retained Expat/GLib rows. This increases runtime and supply-chain scope without clearing the gate.
- **Playwright upgrade candidate:** current upstream documentation uses Playwright `1.60.0`. Upgrading from `1.56.0` is a possible future Plan B, not a drop-in base replacement: it changes the Python dependency and browser bundle, requires the matching image/browser version, a newly reviewed matching seccomp profile, repository regression tests, fresh native AMD64 build/SBOM/Secret/vulnerability scans and sandbox smoke. It must not be combined silently with a base migration.
- **Remote browser/sidecar candidate:** Playwright documents remote server operation, but this would add a network trust boundary and deployment component, does not itself prove the browser system libraries fixed, and is not a minimal closure for the current release. It is not selected.
- **Decision:** there is **no release-ready Worker runtime candidate today**. Do not rebuild merely by moving to Bookworm, Alpine, Forky/Sid, Ubuntu Noble or the MCR Playwright image; each either remains officially unresolved, is unsupported, or broadens the runtime without clearing all `14` blockers. `PROD-IMG-002` remains `BLOCKED`.
- **Minimum future migration:** wait for a maintained vendor runtime whose official package metadata clears all `14`; resolve and record both its immutable index digest and `linux/amd64` child digest; give the Worker a separately pinned base so the browser migration does not force an API base change; preserve Python/Playwright compatibility, non-root UID/GID `999`, sandbox-helper ownership, pinned matching seccomp and `no-new-privileges`; create a new release commit for the Docker/dependency change; then perform a separately approved Worker-only native AMD64 rebuild and complete provenance/content/SBOM/Secret/unsuppressed vulnerability scans plus the bounded sandbox smoke. Acceptance requires all `14` rows absent, no new unreviewed Critical/High row, and independent verification before any ACR decision.
- **Revision rule:** `93b03d5c40fb64fa264c0ada1c055f8e7696b161` remains the behavioral/application baseline for this design. Any future Dockerfile or dependency change creates a new full release commit, and that successor—not `93b03d5`, any Handoff-only HEAD or a mutable tag—must be the OCI revision/build source.
- **Observed boundary:** only repository/Handoff files, retained evidence facts and official public vendor metadata were read. No ECS/Cloud Assistant command, image/container operation, registry access, source/package mutation, VEX/exception write, production/cloud access, parser/symbol/exploit probe or provider call occurred; incremental cloud cost was zero.
- **Next gate:** this browser-runtime path is retained as historical evidence but is no longer the selected production architecture. Do not repeat its build, probes or vendor-fix review unless the product decision changes. The bounded direct-HTTP implementation, RAP fix, Pillow fix, exact `a635692` three-role rebuild/rescan and residual Debian disposition are complete; the current next gate is the separately approved read-only residual release decision.

### PROD-XHS-HTTP-ADAPTER-001 — Replace production browser collection with a bounded direct-HTTP adapter

- **Status:** `VERIFIED` on 2026-07-22 at exact adapter application commit `33d04d4560f9b7dda43a53527f8ae1026dc335d3`. RAP-fixed successor `ca02335513a752ece2dabe71d08105ae86db6c5a` is the exact application/OCI revision of the current local images; CI/Handoff-only `344897c36f7cb69a9dc4d8fe1794b6170722e6fd` is not an application revision.
- **Product decision:** the user explicitly accepts the non-official/private-interface, account-restriction and platform-rule risks of the Spider_XHS approach and states that commercial use authorization for Spider_XHS is resolved. This records a product-risk decision; it does **not** assert or manufacture Xiaohongshu platform authorization.
- **Exact source lock:** repository HEAD at task start is Handoff-only commit `f3a1a3bb28acd4a2b7e33221d0d8d0100a79c9a9`; the unchanged application-code baseline is `93b03d5c40fb64fa264c0ada1c055f8e7696b161`. Spider_XHS is locked to commit `9504b5249103f34a0a4e7062939258061559e2fd`, tree `7db08187cb5332a5a3c89a5923987e098ffddf14`, authored 2026-07-19. At that exact upstream commit, `apis/xhs_pc_apis.py` SHA256 is `218507d59b8bd7c73c0fd3fe28b7fc7ecf387e3c50b5249e9d14ac787c4757d2`, `static/xhs_main_260411.js` SHA256 is `723dc6ef64836b0998aa4ba85796e2ffd99bfb20f222d69adcbcf66ea589292d`, and `static/xhs_rap.js` SHA256 is `e79fe1c79c97a73fbf5fdb6420af114ff591902aa60b436ac4b803a99b806d2e`. Mutable-branch and runtime code downloads are prohibited.
- **Authorization record:** the upstream public tree contains no public `LICENSE` file at the locked commit. The user states that separate Spider_XHS commercial authorization is resolved; this Handoff records that assertion without storing contract material and without representing it as Xiaohongshu platform authorization.
- **Minimum implementation scope:** read-only homefeed, search recommendation, note search, note detail, bounded pagination and session-health handling; browserless `admin-runtime` and `xhs-http-runtime` production targets; remove the Playwright/Chromium Worker from production Compose/Render role configuration and readiness gates. Preserve existing business, billing, quota, user-data and model behavior.
- **Explicit exclusions:** creator/publish/upload, comments/messages/likes/favorites or other platform writes; Pugongying/Qianfan write flows; anti-detection, challenge bypass, proxy rotation, account pools or automated account recovery; real Xiaohongshu calls in the first implementation task; production credentials/data, cloud mutation, image build, ACR access and deployment.
- **Implemented boundary:** only homefeed, search recommendation, note search and note detail use four fixed read-only endpoints; pagination is capped at three pages/sixty items. Local session health is secret-free and makes no platform call. Cookies are restricted to the fixed request host, expired cookies are discarded, duplicate names resolve to the last valid value, and Cookie/`a1`/signatures never enter argv, environment, health output or fixed errors. API/Admin roles cannot collect; `xhs-http` requires the exact adapter; missing/wrong adapters fail closed; direct collection is suspended unless explicitly unlocked; explicit suspension also stops legacy local tooling. Challenge/login/cooldown failures never fall back to browser, proxy or account rotation.
- **Production roles:** Docker/Compose/Render select only `api-runtime`, `admin-runtime` and `xhs-http-runtime`. No production target installs Python Playwright, Chromium or its graphics stack; the XHS runtime disables the inherited web healthcheck and exposes only allowlisted worker commands. Historical browser source/package declarations remain explicit local/legacy tooling and are not reachable through a production role entrypoint.
- **Verification:** independent socket-blocked full suite `625` passed with `5` skipped and `network_attempts=0`; targeted suite `130` passed with `network_attempts=0`; production readiness `83/83`; Python compilation, Shell syntax, Compose rendering, diff check, state-transition regression, high-confidence sensitive scan and exact upstream asset comparison passed. The verifier found no unresolved Critical or High implementation issue.
- **Source/provenance:** Spider_XHS commit `9504b5249103f34a0a4e7062939258061559e2fd`, tree `7db08187cb5332a5a3c89a5923987e098ffddf14`; upstream API SHA256 `218507d59c...787c4757d2`; vendored asset SHA256 values `723dc6ef...589292d` and `e79fe1c7...b806d2e` matched the clean detached source. The public upstream tree has no public license file at that commit; the user states separate commercial authorization exists outside Git. This is not Xiaohongshu platform authorization.
- **Image impact:** the retained `93b03d5` API and Worker images are immutable local evidence and are not changed by this decision. The API image remains a useful browser-free baseline but cannot be relabelled as the successor revision or used as the final post-adapter candidate. The Worker image becomes architecturally superseded and must not be pushed or deployed; it need not be rebuilt merely to wait for browser-library vendor fixes. Do not delete either retained image without separate lifecycle approval.
- **Image set:** the separately approved Pillow-fixed native AMD64 build/scan has completed for browserless `api-runtime`, `admin-runtime` and `xhs-http-runtime`; no production browser Worker image exists. The local image IDs remain distinct from future ACR immutable digests. The successor formal VEX review is complete, but ACR and deployment remain separately gated.
- **Rollback:** disable the adapter/collection scheduler with its feature gate, return the product to manual/screenshot evidence input, and leave the retired Worker undeployed. Do not silently reactivate Playwright.
- **Cost/impact now:** zero incremental cloud/provider cost and no production impact. No image, registry, service, database, provider or cloud resource was touched.
- **Successor sequence:** the first browserless build failed RAP acceptance; `PROD-XHS-RAP-FIX-001`, its successor rebuild, `PROD-PILLOW-12.3.0-FIX-001`, the clean exact `a635692` three-role rebuild/rescan, read-only residual Debian disposition, residual-path decision, constraint proof and independent VEX review are now complete. ACR remains unapproved and deployment remains prohibited.

### PROD-IMG-XHS-BROWSERLESS-BUILD-001 — Build and scan the three browser-free AMD64 roles

- **Status:** `BLOCKED` on 2026-07-23. The authorized build/scan ran to its required stop point, but runtime acceptance failed; it is not release-accepted and must not transition to ACR.
- **Exact source:** clean detached worktree `/opt/noteai-build/repo-33d04d4-exact-r1` at full application commit `33d04d4560f9b7dda43a53527f8ae1026dc335d3`. Git LFS/model artifacts, the machine manifest and expected SHA values were revalidated before build.
- **Builder/boundary:** retained isolated native AMD64 ECS `i-wz99180s9ig5ecq10uaj` / `noteai-build-amd64-b6abaa7-r1`, never API-C/API-F. Commands confirmed Docker registry auth entries `0` and running containers `0` after execution. No production RDS/Tair route or Secret was used; no ACR login/push, deployment, production access, real provider/Xiaohongshu call or cloud-resource mutation occurred.
- **Local candidates:** `noteai-local:git-33d04d4-amd64-api-r1`, `noteai-local:git-33d04d4-amd64-admin-r1`, and `noteai-local:git-33d04d4-amd64-xhs-http-r1`. All are native `linux/amd64`, run as non-root `noteai`, identify full OCI revision `33d04d4560f9b7dda43a53527f8ae1026dc335d3`, source `https://github.com/iamyusen1314/noteai`, created `2026-07-22T09:49:58Z`, have role-correct entrypoint/CMD/markers and empty `RepoDigests`. API local image ID is `sha256:3ee746458aec82866dc691209f31fa8a85d5a043452070c201ac2f906495da20`; XHS local image ID is `sha256:1fb0460928b852270b80a512cd78fb69ec46d47ab77784c9f96f6a85b48beddc`; the Admin ID is retained in the evidence summary. These are local image IDs, never ACR registry digests.
- **Content/provenance acceptance:** the three roles passed architecture, non-root, OCI labels, entrypoint/CMD, root-owned mode-`0444` role marker, model/manifest SHA, `mttravel 1.0.16` bundle SHA, sensitive history/path, and role-appropriate health metadata. `xhs-http-runtime` correctly records disabled health as Docker `Healthcheck.Test=["NONE"]`. Installed Playwright, Chromium/browser binaries and the checked browser graphics packages are absent from all three production roles.
- **Scans:** CycloneDX SBOM, Trivy Secret and unsuppressed `HIGH,CRITICAL` scans completed with no ignore/VEX/exception. API: `174` components, Secret `0`, `4 Critical / 29 High`. Admin: `174` components, Secret `0`, `4 Critical / 29 High`. XHS: `175` components, Secret `0`, `4 Critical / 29 High`, `41` raw / `33` unique / `8` duplicate package rows, `5` rows with a reported fixed version. Browser-component matches are `0`. These findings remain evidence for later disposition; none was waived here.
- **Signer acceptance:** pinned XHS asset SHA values and `crypto-js 4.2.0` passed. A synthetic GET search-recommend wrapper smoke passed in `network=none` with three non-empty response fields and no real Cookie/session. A production-shaped synthetic POST search-notes smoke with `needs_rap=true` consistently returned wrapper exit code `1` and no `x_rap_param`; this affects the committed note-search and note-detail paths. The failure is a release blocker. A later shell-only phase diagnostic was itself malformed and is not used as product evidence; the bounded wrapper result remains authoritative.
- **Evidence:** `/opt/noteai-build/evidence-33d04d4-xhs-browserless/acceptance-v6/` contains completed API/Admin evidence; `/opt/noteai-build/evidence-33d04d4-xhs-browserless/acceptance-final/` contains XHS evidence, scan summaries, signer pass/fail records, combined `FAILED_ACCEPTANCE` summary and a verified `SHA256SUMS`. Final evidence recorded `RUNNING_CONTAINERS_AFTER=0`, `ACR_AUTH_ENTRIES=0`, and `EVIDENCE_CHECKSUMS=PASS`.
- **Rollback/retention:** images and evidence remain local and unpushed. Do not delete images/cache or release the builder without a separate lifecycle approval. Because no production/registry state changed, release rollback is simply to leave these candidates unused.
- **Successor:** `PROD-XHS-RAP-FIX-001` and its clean successor rebuild are recorded below. These old images remain blocked and must not be reused.

### PROD-XHS-RAP-FIX-001 — Isolate and verify the offline RAP signer wrapper

- **Status:** `VERIFIED` on 2026-07-23. Independent verification found no unresolved Critical or High application defect.
- **Root cause:** `xhs_main_260411.js` installs process-global shims, including a proxied `globalThis`. The old wrapper then evaluated `xhs_rap.js` with `vm.runInThisContext`, so the RAP asset inherited the polluted global state and failed with an internal `TypeError`. The pinned RAP asset itself passed when evaluated independently.
- **Minimum correction:** only the application-owned `model/vendor/spider_xhs/signer.js` changed. RAP now executes in a fresh `vm.createContext` that exposes only `Buffer`, URL/TextEncoder primitives, timers, a quiet console, and a restricted `require` that accepts only `crypto` and maps it to `node:crypto`. It does not expose `process`, filesystem/network modules, host `globalThis`, or full `require`. The RAP asset is loaded relative to `__dirname`, making the wrapper independent of process CWD. Upstream signer output is silenced so stdout remains one JSON object.
- **Pinned assets:** `xhs_main_260411.js` SHA256 `723dc6ef64836b0998aa4ba85796e2ffd99bfb20f222d69adcbcf66ea589292d` and `xhs_rap.js` SHA256 `e79fe1c79c97a73fbf5fdb6420af114ff591902aa60b436ac4b803a99b806d2e` remain byte-identical to HEAD and the clean locked upstream source.
- **Regression coverage:** `crypto-js` is fixed to dev-only version `4.2.0` with lockfile integrity matching the Docker build. CI now installs Node 20 dependencies using `npm ci --ignore-scripts`. The wrapper-level test starts real Node processes from repo root, blocks bare and `node:` forms of `dgram`, `dns`, `http`, `http2`, `https`, `net`, and `tls`, and blocks `fetch`. Basic search-recommend and RAP search-notes return exact non-empty field sets, empty stderr and JSON-only stdout without echoing the synthetic `a1` or body. Disallowed endpoints fail closed with no stdout/stderr.
- **Independent evidence:** basic PASS (`rc=0`), RAP search PASS (`rc=0`), and additional RAP feed PASS (`rc=0`) using real `crypto-js 4.2.0`; all outputs stayed below `64 KiB`, contained only expected non-empty signature fields, and disclosed no synthetic input. Missing `a1`, bad method and bad endpoint all returned `rc=1` with empty output. All 15 tested network entry points were denied.
- **Verification:** XHS suite `24/24`; full unit suite `626` passed with `5` existing skips; Python network-attempt counter `0`; production readiness `83/83`; Node/Python syntax, `docker compose config --quiet`, `npm ci --ignore-scripts`, npm audit, diff check and high-confidence Secret scan passed. The apparent provider URLs/errors in test logs came from mocks and failure-contract assertions, not real calls.
- **GitHub CI closure:** the first pushed RAP-fix commit `ca02335513a752ece2dabe71d08105ae86db6c5a` passed the new Node 20 wrapper test but reproduced the same six pre-existing XHS acquisition failures as parent `33d04d4`. Root cause was test setup copying `.env.example` (adapter selected and collection suspended) without a runtime role, causing local/legacy fixture tests to fail closed before their mocks. The CI job now explicitly uses the `local` role and empty adapter/suspension values; individual direct-runtime tests continue to set their own fail-closed environment. This is test-environment isolation only and does not change `.env.example`, production defaults or runtime behavior.
- **Changed scope:** `model/vendor/spider_xhs/signer.js`, `tests/test_spider_xhs_http.py`, `tests/fixtures/xhs_signer_no_network.cjs`, `package.json`, `package-lock.json`, `.github/workflows/ci.yml`, and this Handoff. No business, billing, quota, database, Docker runtime target, Compose/Render role, production data or cloud resource changed.
- **Rollback:** revert application commit `ca02335` and the separate CI/Handoff closure commit, and keep every old local image unused. No registry, deployment or production rollback is required because none was touched.
- **Next gate:** the separately approved `PROD-IMG-XHS-BROWSERLESS-REBUILD-001` completed as recorded below. Do not repeat it or approve ACR/deployment for its current local images.

### PROD-IMG-XHS-BROWSERLESS-REBUILD-001 — Rebuild and scan the RAP-fixed browserless AMD64 roles

- **Status:** `VERIFIED` on 2026-07-23 for the authorized local build/scan scope. The three image candidates are **release-blocked** and are not approved for ACR or deployment.
- **Exact source:** clean detached worktree `/opt/noteai-build/repo-ca02335-exact-r1` at full application commit `ca02335513a752ece2dabe71d08105ae86db6c5a`. Final `git status --porcelain` was empty, `git lfs fsck` passed, and the V0.4 machine manifest verified all three `.lgb` artifacts plus the declared training report in each image. Branch tip at rebuild time `344897c36f7cb69a9dc4d8fe1794b6170722e6fd` differed only in CI/Handoff files and was not an OCI application revision.
- **Builder/boundary:** retained isolated native AMD64 ECS `i-wz99180s9ig5ecq10uaj` / `noteai-build-amd64-b6abaa7-r1`, Shenzhen F, `x86_64`, 4 vCPU / 16 GiB. It remained in the build VPC with zero inbound security-group rules, no RAM role, no load balancer and no production data-network membership. The task never targeted API-C/API-F, RDS or Tair. Docker registry auth entries remained `0`; no ACR login/read/push occurred.
- **Build mechanics:** the source Dockerfile and both pinned multi-arch base digests were unchanged. The configured Alibaba Docker mirror could not serve the pinned base digest; the daemon mirror was temporarily and reversibly switched to DaoCloud only for the native build, then restored byte-for-byte to `https://0fjuh2bu.mirror.aliyuncs.com/`. No running container remained.
- **Local candidates:** `noteai-local:git-ca02335-amd64-api-r1` / local image ID `sha256:5fa36abe1c603aa0abc1295912fe6ee16a0b468d8be216475be06a4daa9a6454` / size `1,044,230,106` bytes; `noteai-local:git-ca02335-amd64-admin-r1` / `sha256:6d3b5ee2dd4619a60394ad4e026287fe4b5a9a39dd722c37954fe58627369865` / `1,044,230,108` bytes; `noteai-local:git-ca02335-amd64-xhs-http-r1` / `sha256:755e337c2932ef53187b5af333b1daa47be578a81af9a57d6c1af7a9368b3b60` / `1,044,718,005` bytes. These are local Docker image IDs, **not** ACR registry digests; all three have `RepoDigests=[]`.
- **Provenance/content acceptance:** all three are `linux/amd64`, use non-root `noteai` UID `999`, carry exact OCI revision `ca02335513a752ece2dabe71d08105ae86db6c5a`, source, version `git-ca02335-amd64-r1`, created and role labels, use `/app/scripts/docker_entrypoint.sh`, and have role-correct CMD/marker/health metadata. API/Admin use `/health/live`; XHS correctly records `Healthcheck.Test=["NONE"]`. `mttravel 1.0.16` and bundle SHA `8a0527bb6e8b9074cf0221756f35bca989945e8436e4841596a7b7bf79ae06d8`, `crypto-js 4.2.0`, signer asset SHA values `723dc6ef...589292d` and `e79fe1c7...b806d2e`, and all model SHA checks passed. Installed Playwright, Chromium/browser binaries and browser caches are absent. The only sensitive-like filename is the public template `/app/model/.env.example`; raw Trivy Secret findings are `0`.
- **SBOM and unsuppressed scans:** Trivy `0.70.0` refreshed its vulnerability DB on 2026-07-23 before scanning. Scanner workdir was empty; no ignore file, `--ignore-unfixed=false`, no VEX and no exception were used. API: CycloneDX `174`, Secret `0`, `33` raw / `33` unique, `4 Critical / 29 High`, `10` with a reported fix. Admin: `174`, Secret `0`, same `33` unique and severity/fix counts. XHS: `175`, Secret `0`, same `33` unique and severity/fix counts. The three vulnerability sets are identical.
- **Finding disposition:** the four Critical rows are on Debian `perl-base 5.40.1-6`. Ten High rows are on installed and used `pillow 12.2.0`, with scanner-recorded fixed version `12.3.0`. The remaining High rows are Debian base packages with no `FixedVersion` in this scan. No finding is suppressed or waived. Independent repository review confirmed PIL is imported by `model/extract_cover_features.py`; the Pillow rows must be removed before any later risk disposition.
- **Signer acceptance:** production-equivalent `NODE_PATH=/opt/noteai/xhs-node/node_modules` was supplied only to the disposable command, matching `SpiderXHSSigner`. Synthetic basic search-recommend and RAP search-notes both passed in `network=none` as non-root `noteai`, with read-only root filesystem, `cap-drop ALL`, `no-new-privileges`, bounded PID/CPU/memory and no real Cookie/session/provider call. The initial direct-node attempt omitted the production `NODE_PATH` and failed before execution; it is a harness error and is not product evidence. Final basic/RAP responses contained exactly the required non-empty fields and did not echo synthetic input.
- **Evidence:** `/opt/noteai-build/evidence-ca02335-xhs-browserless-rebuild-r1/` contains build logs, inspect/history, content/model checks, CycloneDX/Secret/vulnerability JSON, package TSVs, signer summaries and final integrity evidence. It contains `62` files; `SHA256SUMS` SHA256 is `333697df3ad085325b5f6a7e3ffadbf5bddd5ad7d7fd11e885b92752cfc6c737`. Final state recorded clean exact source, LFS fsck PASS, running containers `0`, Docker auth entries `0` and restored original mirror.
- **Independent review:** source/config review found the three-role image design, lack of browser dependencies, role startup protection, model/signing pins and identical role vulnerability sets structurally consistent with exact revision `ca023355`. It separately confirmed `344897c` is CI/Handoff-only and that Pillow is a live dependency. The review did not reproduce the ECS raw bytes locally, so the retained raw ECS evidence and checksum manifest remain mandatory release provenance.
- **Release decision:** execution scope complete; images **BLOCKED**. `PROD-IMG-002` remains blocked by unsuppressed `4 Critical / 29 High` and the absence of an approved VEX/exception. Do not relabel, push or deploy these local IDs.
- **Successor status:** `PROD-PILLOW-12.3.0-FIX-001`, the clean three-role rebuild/rescan from exact application revision `a635692a899ee02c6905cd694611c14e0da4594a`, residual Debian disposition, residual decision, constraint proof and independent formal VEX review all completed as recorded below. Existing images remain immutable and unchanged; ACR still requires explicit approval.

### PROD-PILLOW-12.3.0-FIX-001 — Upgrade the shared browserless Pillow runtime pin

- **Status:** `VERIFIED` on 2026-07-23 at exact application revision `a635692a899ee02c6905cd694611c14e0da4594a`; independent review found no unresolved Critical or High repository issue.
- **Change:** `model/requirements-api.txt` changes only `pillow==12.2.0` to `pillow==12.3.0`. API, Admin and XHS inherit the same `runtime-common` install, while compatibility and legacy Worker requirement files recursively reference this production list. Dockerfile, Compose, Render roles, business behavior and other dependencies are unchanged.
- **Regression coverage:** production readiness now requires the exact `pillow==12.3.0` pin. A new unit test creates a deterministic PNG, verifies Pillow open/save and `ImageStat`, then runs the actual `image_to_b64` and `extract_deterministic` paths and checks stable media type and image features.
- **Compatibility:** an isolated Python `3.11.14` environment installed the public Pillow `12.3.0` CPython 3.11 wheel; PNG/JPEG/WebP round-trips, `ImageStat` and WebP support passed. The project test environment also ran with Pillow `12.3.0`; `pip check` reported no broken requirements.
- **Verification:** targeted `16/16`; full unit suite `627` passed with `5` skipped; production readiness `83/83`; model-artifact check, quality gate, Python compilation, Compose parse, diff and changed-file sensitive scan passed. GitHub CI runs `29982041413` and `29982043526` completed successfully for exact SHA `a635692a899ee02c6905cd694611c14e0da4594a`, including the repository's Python `3.11` dependency install and full test workflow. Apparent provider failure text in the local unit log is produced by a test-owned fake client and does not represent an external call.
- **Independent review:** confirmed the single-pin scope, three-role dependency inheritance, test correctness and absence of Docker/ACR/deployment/production/provider/VEX/cloud changes. It found no Critical or High issue and allowed the commit gate.
- **Image impact:** the successor image task below now proves Pillow `12.3.0` in all three exact `a635692` roles and proves all ten prior Pillow High rows absent. Historical `ca023355` images remain immutable and superseded.
- **Rollback:** revert application commit `a635692a899ee02c6905cd694611c14e0da4594a`; no registry, deployment, production or cloud rollback is required.
- **Next gate:** completed by `PROD-IMG-XHS-PILLOW-REBUILD-001` below. Stop before ACR or deployment; next perform read-only residual Debian disposition.

### PROD-IMG-XHS-PILLOW-REBUILD-001 — Rebuild and scan the Pillow-fixed browserless AMD64 roles

- **Status:** `VERIFIED` on 2026-07-23 for the authorized local build/scan scope. The three candidates remain **release-blocked** and are not approved for ACR or deployment.
- **Exact source:** clean detached `/opt/noteai-build/repo-a635692-pillow-r2` at full application commit `a635692a899ee02c6905cd694611c14e0da4594a`. Final `git status --porcelain` was empty, `git lfs fsck` passed and the exact release artifacts/manifests matched their recorded hashes. Branch/Handoff tip `4704c1617a169f6d925a1b16e90aba0f8b88d02b` was not used as an OCI revision.
- **Builder/boundary:** retained isolated native AMD64 ECS `i-wz99180s9ig5ecq10uaj` / `noteai-build-amd64-b6abaa7-r1`, Shenzhen F, `x86_64`, 4 vCPU / 16 GiB. It remained in the separate build VPC with zero inbound security-group rules and no production data-network membership. The task never targeted API-C/API-F, RDS or Tair. Docker registry auth entries remained `0`; no ACR login/read/push occurred.
- **Build execution:** immutable Python and Node base digests, source Dockerfile and OCI values were unchanged. The first default-network attempt was stopped after validated target PIDs made no progress; the first host-network retry failed on a public PyPI CDN timeout and produced no candidate. The second host-network retry completed all three targets and cached the shared browserless runtime. These failures are retained as audit evidence and are not acceptance results.
- **Local candidates:** API `noteai-local:git-a635692-amd64-api-r1`, local image ID `sha256:b1983bab928ef93495d8be020030917d4ae54364234390047c3163af5414fedb`, size `1,044,383,616` bytes; Admin `noteai-local:git-a635692-amd64-admin-r1`, `sha256:2283095764622e373e30b51ba749819751e6bfb0c37c6bb82e2d3bfe4937760f`, `1,044,383,618` bytes; XHS HTTP `noteai-local:git-a635692-amd64-xhs-http-r1`, `sha256:5b44114d4bd9c28a8e93c39140466c542e8babeead038fb0d1cfe45c3cd75966`, `1,044,871,515` bytes. These are local Docker image IDs, **not** ACR registry digests; all have `RepoDigests=[]`.
- **Provenance/content acceptance:** all three are `linux/amd64`, non-root `noteai` UID/GID `999`, carry exact OCI revision/source/version `git-a635692-amd64-r1`/created labels and role labels, use `/app/scripts/docker_entrypoint.sh`, and have role-correct CMD/marker/health metadata. API/Admin use `/health/live`; XHS records `Healthcheck.Test=["NONE"]`. Pillow is exactly `12.3.0`; setuptools/wheel, installed Playwright, Chromium/browser binaries, actual `.env` and `.git` paths are absent. `mttravel 1.0.16`, its bundle SHA, `crypto-js 4.2.0`, both signer asset SHAs, three V0.4 models, train report, audit/readiness/training-health, human/machine release manifests and model registry all passed exact SHA checks.
- **SBOM/Secret/vulnerability evidence:** Trivy `0.70.0` used the retained 2026-07-23 database offline in `network=none` containers. There was no ignore file, no suppression/VEX/exception, `--ignore-unfixed=false` was explicit and High/Critical reports exited `1` as expected. API: SBOM `174`, Secret `0`, `23` raw/unique, `4 Critical / 19 High`, fixed-version rows `0`, Pillow rows `0`. Admin is identical with SBOM `174`. XHS HTTP is identical with SBOM `175`. Compared with exact `ca023355`, ten Pillow High rows were removed and no residual row gained a reported fix.
- **Signer acceptance:** basic search-recommend and RAP search-notes synthetic inputs passed inside the XHS image as UID/GID `999`, `network=none`, read-only root filesystem, drop all capabilities, `no-new-privileges` and bounded PID/CPU/memory. The network-blocking fixture was read-only mounted; only output length/SHA summaries were recorded. No a1/signature value, Cookie/session or real provider call was output or used.
- **Evidence/cleanup:** `/opt/noteai-build/evidence-a635692-pillow-rebuild-r2/` contains build logs, inspect/history/runtime checks, CycloneDX/Secret/vulnerability JSON and signer summaries. Final manifest covers `46` files and about `3.7 MiB`. All stage checksum manifests passed; exact source remained clean; temporary scan/smoke directories were removed; running containers `0`, build processes `0`, Docker auth entries `0`.
- **Harness notes:** intermediate acceptance/scan/smoke harness attempts stopped on quoting, stale abbreviated-hash, undersized tmpfs, shell-continuation or read-permission errors before producing valid acceptance. Each disposable container was removed, no NoteAI image/environment/cloud resource was modified and only the final successful runs are acceptance evidence.
- **Release decision:** local execution scope complete; images **BLOCKED**. `PROD-IMG-002` remains blocked by unsuppressed `4 Critical / 19 High` and the absence of a reviewed residual disposition. Do not relabel, push or deploy these local IDs.
- **Next task:** request approval for read-only `PROD-BROWSERLESS-OS-VULN-DISPOSITION-001`; no ACR task is next.

### PROD-BROWSERLESS-OS-VULN-DISPOSITION-001 — Review residual Debian image findings

- **Status:** `VERIFIED` on 2026-07-23 for the authorized read-only review; no image or release acceptance was granted.
- **Priority:** Critical before registry release
- **Evidence identity:** every exact `a635692` role has the same unsuppressed `23` raw/unique package rows: `4 Critical / 19 High`, fixed-version rows `0`, Pillow rows `0`. They collapse to 12 unique CVEs because `CVE-2025-69720` maps to four ncurses packages and `CVE-2026-53615` maps to nine util-linux binary packages. Raw JSON and CycloneDX SBOMs remain under `/opt/noteai-build/evidence-a635692-pillow-rebuild-r2/scans/`.
- **Read-only execution:** Cloud Assistant targeted only isolated builder `i-wz99180s9ig5ecq10uaj`. Execution `t-sz06rs3xcs8n400` normalized the three reports and proved identical CVE/package/version sets. Execution `t-sz06rs4kvi02ha8` read the existing API SBOM and confirmed the installed affected package families. Commands only used `jq`, `sha256sum`, `find`, `sort` and shell built-ins; they did not write files, start containers/services, access registries, read secrets or target API-C/API-F. Local repository static search at exact `a635692` returned no production-role invocation of Perl, Archive::Tar, IO::Compress, Storable, infocmp, gzip, blkid, ACL tools or mount.
- **Base-image state:** the image remains pinned to official `python:3.11.15-slim-trixie` multi-arch index `sha256:db3ff2e1800a8581e2c48a27c3995339d47bdf046da21c7627accd3d51053a93`, with AMD64 child `sha256:00af38ae2ed311628970782e8a2d7f014d8909dbc63cb97bc0a158187f4db045`. The current official Docker Hub tag still resolves to the same AMD64 child, so rebuilding the same source today would not change these rows.
- **Package/module proof:** `perl-base 5.40.1-6`, `ncurses-bin 6.5+20250216-2`, `gzip 1.13-1`, `util-linux/libblkid 2.41-5` and `libacl1 2.3.2-2+b1` are installed. Neither the SBOM nor Debian's official `perl-base` AMD64 file list contains `Archive::Tar`, `IO::Compress` or `Storable`; the file list does contain `/usr/bin/perl`. Debian marks `perl-base`, `ncurses-bin`, `gzip` and `util-linux` Essential, so removing them in-place from this Debian runtime is not a minimum safe change.
- **Disposition table:**

| Severity / CVE | Scanner package rows | Installed affected artifact | Static reachability disposition | Official Trixie fix state | Minimum safe path |
|---|---|---|---|---|---|
| Critical `CVE-2026-13221` | `perl-base` | Yes, Perl core | `NO_CONFIGURED_PATH`: no role/entrypoint invokes Perl or compiles untrusted Perl regex; not a formal VEX conclusion | Trixie/unstable unfixed; upstream fixed in 5.43.10 | Formal evidence review, or a separately designed minimal final rootfs without Perl; do not mix unstable packages |
| Critical `CVE-2026-42496` | `perl-base` | **No** `Archive::Tar` module in SBOM or `perl-base` file list | `NOT_INSTALLED` | Trixie postponed; upstream Archive::Tar 3.08 | No image change required for the absent module; formal release disposition still required |
| Critical `CVE-2026-57433` | `perl-base` | **No** `Storable` module in SBOM or `perl-base` file list | `NOT_INSTALLED` | Trixie vulnerable; forky/sid Perl 5.40.1-8 fixed | No image change required for the absent module; do not import forky/sid Perl into Trixie |
| Critical `CVE-2026-8376` | `perl-base` | Perl exists, but finding requires a 32-bit build | `NOT_APPLICABLE_ARCH`: exact images are `linux/amd64` | Trixie vulnerable; forky/sid 5.40.1-8 fixed | No image change for AMD64; formal architecture disposition still required |
| High `CVE-2025-69720` | `libncursesw6`, `libtinfo6`, `ncurses-base`, `ncurses-bin` | Yes; affected code is the `infocmp` CLI | `NO_CONFIGURED_PATH`: no role invokes `infocmp` or accepts terminfo input | Trixie no-DSA; forky/sid fixed in ncurses 6.6 | Formal evidence review, or minimal-rootfs removal of the CLI; do not remove Essential ncurses in-place |
| High `CVE-2026-41992` | `gzip` | Yes, `/usr/bin/gzip` | `NO_CONFIGURED_PATH`: no role invokes a multi-file `gzip -d` workflow | Trixie/unstable unfixed; Debian rates minor/no-DSA | Formal evidence review, or minimal-rootfs removal of the CLI; do not remove Essential gzip in-place |
| High `CVE-2026-42497` | `perl-base` | **No** `Archive::Tar` module | `NOT_INSTALLED` | Trixie postponed; upstream Archive::Tar 3.08 | Same as `CVE-2026-42496` |
| High `CVE-2026-48962` | `perl-base` | **No** `IO::Compress` module | `NOT_INSTALLED` | Trixie vulnerable; forky/sid fixed | No image change required for absent module; do not mix unstable packages |
| High `CVE-2026-53615` | nine `util-linux` binary-package rows; vulnerable code is in `libblkid` | Yes, `libblkid.so.1` | `UNKNOWN`: no configured role call or device mapping was found, but existing SBOM/config evidence alone cannot prove absence of all transitive dynamic use | Trixie, forky and sid unfixed | Keep blocked; either independently prove/enforce no device/partition-parser path and make a formal decision, or move to a reviewed minimal final rootfs that omits libblkid |
| High `CVE-2026-54369` | `libacl1` | Yes, `libacl.so.1` | `UNKNOWN`: images run as UID/GID 999 and no ACL call was found, but existing evidence cannot prove no transitive pathname API use | Trixie point update planned; forky/sid 2.4.0-1 fixed | Keep blocked; either independently prove/enforce non-root/no-privilege/no-ACL path and make a formal decision, or use a reviewed runtime without libacl / later fixed Trixie base |
| High `CVE-2026-57432` | `perl-base` | Yes, Perl core | `NO_CONFIGURED_PATH`: no production role invokes Perl or accepts a Perl pack/unpack template | Trixie vulnerable; forky/sid 5.40.1-8 fixed | Formal evidence review, or a separately designed minimal final rootfs without Perl |
| High `CVE-2026-9538` | `perl-base` | **No** `Archive::Tar` module | `NOT_INSTALLED` | Trixie/unstable unfixed and postponed | No image change required for absent module; formal release disposition still required |

- **Release interpretation at completion of this read-only task:** six unique CVEs had decisive non-affected evidence (`5 NOT_INSTALLED`, `1 NOT_APPLICABLE_ARCH`), four had high-confidence static `NO_CONFIGURED_PATH` evidence, and two High CVEs remained `UNKNOWN`. The successor constraint proof and independent formal VEX review now close those evidence/decision gaps; raw reports still remain canonical and unsuppressed.
- **Minimum remediation decision:** a same-digest rebuild or ordinary Trixie `apt upgrade` cannot remove the rows, and importing individual forky/sid packages into Trixie is rejected. The selected non-waiting Path A—formal exact-product disposition plus enforced deployment constraints—is now independently verified. The minimal/distroless path remains a contingency if any reviewed identity or constraint changes.
- **Safety:** no exploit/PoC/payload/malformed input, symbol/memory/sandbox probe or runtime exploitability test was created or run. No VEX/exception, suppression, image build, ACR access, deployment, production/provider call or cloud-resource mutation occurred. The two Cloud Assistant executions created normal audit-history records only.
- **Next gate:** request read-only `PROD-BROWSERLESS-RESIDUAL-DECISION-001` to prepare an independently reviewable decision package comparing constrained formal disposition versus a minimal/distroless final runtime; it must not write formal VEX/exception, build, access ACR or deploy. `PROD-IMG-002` stays blocked.

### PROD-BROWSERLESS-RESIDUAL-DECISION-001 — Independently reviewable final disposition paths

- **Status:** `VERIFIED` on 2026-07-23 for the authorized read-only decision scope; no release acceptance, formal VEX, exception or suppression was granted.
- **Exact evidence identity:** the decision applies only to the three local `linux/amd64` role images built from application/OCI revision `a635692a899ee02c6905cd694611c14e0da4594a`: API local ID `sha256:b1983bab928ef93495d8be020030917d4ae54364234390047c3163af5414fedb`, Admin `sha256:2283095764622e373e30b51ba749819751e6bfb0c37c6bb82e2d3bfe4937760f`, and XHS HTTP `sha256:5b44114d4bd9c28a8e93c39140466c542e8babeead038fb0d1cfe45c3cd75966`. These are local IDs, not registry digests. Every role retains the same unsuppressed `23` package rows (`4 Critical / 19 High`) and retained CycloneDX SBOM/Trivy evidence under `/opt/noteai-build/evidence-a635692-pillow-rebuild-r2/scans/`.
- **Decision outcome:** choose **Path A — exact-product formal disposition for ten CVEs plus a separately implemented constrained-deployment proof for `libblkid` and `libacl`**. It preserves the already tested Python `3.11.15` application runtime and can avoid an image rebuild if the proof closes using deployment configuration plus the immutable current images. **Path B — a minimal/distroless runtime** remains the contingency if either library cannot be closed non-invasively or the deployment platform cannot enforce/read back the required controls.

#### Path A1 — formal disposition design for the ten high-confidence CVEs

The future VEX authoring task should use a separate CycloneDX VEX BOM rather than mutating the retained SBOM. Each assertion must be scoped to the exact role product, exact local image ID and SBOM serial/hash; if a later ACR push is separately approved, the final signed/attested statement must be reissued against the immutable registry digest before deployment. Keep four distinct artifacts: raw unsuppressed Trivy JSON, the VEX document, VEX-applied Trivy JSON with suppressed rows visible, and the independent review record. Trivy's VEX support is experimental, so the raw report remains canonical audit evidence and no raw row may be deleted.

| CVE set | Future state | Machine justification | Required impact evidence |
|---|---|---|---|
| `CVE-2026-42496`, `CVE-2026-42497`, `CVE-2026-9538` | `not_affected` | CycloneDX `code_not_present`; CISA/CSAF mapping `vulnerable_code_not_present` | Exact SBOM and official `perl-base` file list show no `Archive::Tar`; scanner source-package mapping alone does not install the vulnerable module |
| `CVE-2026-57433` | `not_affected` | `code_not_present` / `vulnerable_code_not_present` | Exact SBOM and package file list show no `Storable` |
| `CVE-2026-48962` | `not_affected` | `code_not_present` / `vulnerable_code_not_present` | Exact SBOM and package file list show no `IO::Compress` |
| `CVE-2026-8376` | `not_affected` | CycloneDX `requires_environment`; interoperable CSAF mapping `vulnerable_code_not_present` | Exact artifact is AMD64 and contains no affected 32-bit build; record architecture and scanner metadata explicitly |
| `CVE-2026-13221`, `CVE-2026-57432` | `not_affected` | `code_not_reachable` / `vulnerable_code_not_in_execute_path` | Role marker, entrypoint allowlist and exact-source search show no production Perl execution or attacker-controlled Perl regex/pack template |
| `CVE-2025-69720` | `not_affected` | `code_not_reachable` / `vulnerable_code_not_in_execute_path` | No role invokes `infocmp` or accepts terminfo input |
| `CVE-2026-41992` | `not_affected` | `code_not_reachable` / `vulnerable_code_not_in_execute_path` | No role invokes the affected multi-file gzip decompression workflow |

An independent reviewer who did not implement the runtime/configuration must confirm the package PURLs, application revision, image identity, OS/architecture, SBOM and Trivy hashes, entrypoint/command graph, assessment date and evidence hashes. Each assertion requires a review trigger for any base digest, package version, role command, entrypoint, architecture, deployment capability/device policy or upstream CVE-metadata change. The five absent-module assertions, the architecture assertion and four no-execute-path assertions are ready to enter that review workflow; this task did not author or register them.

#### Path A2 — required proof for `libblkid` and `libacl`

- **Current result:** the successor `PROD-BROWSERLESS-CONSTRAINT-PROOF-001` is independently `VERIFIED`. All production roles now have an enforceable repository deployment contract and effective local runtime read-back for UID/GID `999`, `cap_drop: ALL`, `no-new-privileges`, read-only root, bounded tmpfs/data writes, no devices, no privileged mode and no host namespace. Offline ELF dependency closure found neither `libblkid.so.1` nor `libacl.so.1` in the accepted executable/native-extension graph for any exact retained role image.
- **Safe evidence required:** use only package ownership, SBOM and offline ELF `DT_NEEDED` dependency closure for the allowlisted Python/Node/mttravel/native-wheel runtime; do not use exploit, PoC, malformed input, symbol hunting or vulnerability trigger probes. Record whether `libblkid.so.1` or `libacl.so.1` is loaded by any accepted executable/native extension.
- **Deployment constraints required for every role:** explicit `user: "999:999"`; `cap_drop: [ALL]`; `security_opt: [no-new-privileges:true]`; read-only root filesystem; bounded `tmpfs` only where proven necessary; explicit UID-owned writable data/artifact/cache mounts; `privileged: false`; no `devices`, host block-device or broad host-path mounts; no `CAP_SYS_ADMIN`, `CAP_SYS_RAWIO` or `CAP_MKNOD`; and no setuid/setgid helper in the accepted executable surface.
- **Acceptance for `CVE-2026-53615`:** the allowlisted execution/dependency graph does not load or call the affected libblkid path, and the runtime has no block-device/image input or privilege needed to create one. If the library is absent from the graph, use `vulnerable_code_not_in_execute_path`; if it is necessarily loaded, only an independently accepted proof that no adversary can control the affected path may use `vulnerable_code_cannot_be_controlled_by_adversary`.
- **Acceptance for `CVE-2026-54369`:** the allowlisted execution/dependency graph does not call the affected libacl pathname path, and non-root/capability/mount constraints prevent a privileged adversary-controlled path. The same justification choice rule applies. Failure to prove either condition leaves the CVE `under_investigation` and blocks release.
- **Proof artifacts:** normalized deployment configuration, local `network=none` constrained smoke using the retained images, container-inspect/read-back showing the effective user/capabilities/security options/mounts/devices/read-only state, evidence hashes, and independent review. No production start or cloud mutation is needed for this proof phase.

#### Path B — minimal/distroless final runtime contingency

| Candidate | Benefit | Blocking migration facts | Decision |
|---|---|---|---|
| Google `gcr.io/distroless/python3-debian12:nonroot` | Open, signed, small, no package manager or shell; most plausible public candidate | Debian 12 Python is `3.11.2`, not exact `3.11.15`; current shell entrypoint/start scripts cannot run; official-Python `/usr/local` virtualenv/site paths do not directly match distroless `/usr/bin`; all native wheels and `libgomp` need a new dependency closure; XHS also requires separately copied Node/crypto-js | Best **contingency**, not a drop-in or current minimum |
| Google `python3-debian13:nonroot` | Current Debian generation and minimal surface | Debian 13 default is Python `3.13`, requiring a Python-version migration plus full compatibility test | Reject for this release |
| Chainguard Python `3.11` | Minimal non-root runtime with vendor patch stream | Free tier exposes primarily `latest`; maintained version streams/SLAs and immutable unique-tag workflow generally require production subscription; Wolfi/native-wheel/Node compatibility must be evaluated | Consider only after commercial/vendor approval |
| Custom Python `3.11.15` rootfs on distroless `base`/`cc` | Could retain exact CPython | Must manually carry CPython, CA/tzdata, all shared libraries, native-wheel closure, `libgomp` and Node signer; highest omission, provenance and maintenance burden | Not recommended as minimum |
| Alpine or in-place removal from Trixie slim | Smaller apparent package set | musl/native-wheel migration risk; Perl/gzip/ncurses/util-linux are Debian Essential and unsafe to strip in-place | Reject |

Any Path B choice requires a new application/Docker revision, digest-pinned base, shell-free Python launcher/role guard, native AMD64 rebuild of all three roles, full unit/readiness/integration checks, model and signer acceptance, SBOM/Secret/content/provenance scans and a new residual vulnerability decision. The exact residual count cannot be promised before that rebuild.

#### Independent route comparison and recommendation

| Criterion | Path A: constrained disposition | Path B: minimal/distroless |
|---|---|---|
| Preserves exact tested app/Python | Yes, `a635692` / Python `3.11.15` | No, unless using the highest-risk custom rootfs |
| New image build required | Not if deployment-only proof passes | Yes, all three roles |
| Ten high-confidence CVEs | Formal exact-product dispositions | Likely removed, but only a new SBOM/scan can prove it |
| Two `UNKNOWN` CVEs | Must close with dependency plus least-privilege proof | Must prove libraries absent in new SBOM/runtime closure |
| XHS Node signer | Unchanged | New Node/runtime packaging and regression |
| Deployment portability | Strongest on controlled Alibaba ECS Docker host; current Render file cannot prove all controls | Strong if migration succeeds |
| Change/regression risk | Low to medium | High |
| Relative effort/cost | One hardening/evidence cycle, then independent VEX review | Multiple implementation/build/scan cycles; possible Chainguard subscription |

- **Recommendation:** proceed with Path A. It is a standards-based release decision, not a production exception and not a claim that scanner findings disappeared. If the constrained proof fails for either library or the chosen production platform cannot enforce/read back the controls, stop and authorize Path B using Google Distroless Debian 12 as the first prototype candidate.
- **Official decision references:** CISA Minimum Requirements for VEX (`https://www.cisa.gov/sites/default/files/2023-04/minimum-requirements-for-vex-508c.pdf`) and Status Justifications (`https://www.cisa.gov/sites/default/files/publications/VEX_Status_Justification_Jun22.pdf`); CycloneDX vulnerability exploitability and 1.7 analysis fields (`https://cyclonedx.org/use-cases/vulnerability-exploitability/`, `https://cyclonedx.org/docs/1.7/proto/`); OASIS CSAF 2.1 VEX (`https://docs.oasis-open.org/csaf/csaf/v2.1/csaf-v2.1.html`); Trivy local VEX/show-suppressed behavior (`https://trivy.dev/docs/dev/supply-chain/vex/file/`); Docker Compose service security controls (`https://docs.docker.com/reference/compose-file/services/`); Google Distroless supported images/entrypoint constraints (`https://github.com/GoogleContainerTools/distroless`); Debian Bookworm/Trixie Python package versions (`https://packages.debian.org/bookworm/python/python3.11`, `https://packages.debian.org/trixie/python/python3`); and Chainguard version/support policy (`https://edu.chainguard.dev/chainguard/chainguard-images/about/versions/`). Public-source reads were non-mutating.
- **Safety and stop point:** local repository and public official documentation were read only. No ECS/Cloud Assistant action, production/control-plane access, code or deployment-config change, formal VEX/exception, scanner suppression, image build, ACR access, service start or provider call occurred. Existing images and raw reports are unchanged.
- **Next gate:** `PROD-BROWSERLESS-CONSTRAINT-PROOF-001` and the successor independent formal VEX review completed as recorded below. ACR and deployment remain separately gated.

### PROD-BROWSERLESS-CONSTRAINT-PROOF-001 — Enforce and verify constrained browserless deployment

- **Status:** `VERIFIED` on 2026-07-23. Independent read-only review initially rejected two readiness-parser bypasses, then confirmed the minimal fixes and returned final `PASS` with no new Critical, High or Medium issue.
- **Repository release:** deployment-control commit `253fde55fa609676a9fca5c14fdf6aeceef74000`, message `fix(prod): enforce browserless runtime constraints [skip render]`. It changes deployment configuration, readiness checks, tests and documentation only. It is not an application/image revision; the three retained images still identify exact OCI application revision `a635692a899ee02c6905cd694611c14e0da4594a`.
- **Production contract:** new `deploy/production/docker-compose.yml` references each role only as `repository@sha256:<64-lowercase-hex>`, uses an external root-managed env file, and grants only `/app/model/data` as a writable bind plus bounded `/tmp`. API, Admin, XHS Trends and XHS Tracking all enforce `user: "999:999"`, `read_only: true`, `privileged: false`, `cap_drop: [ALL]` and `security_opt: [no-new-privileges:true]`; they have no `cap_add`, `devices`, host namespace or dangerous capability. Local `docker-compose.yml` carries the same runtime controls but remains explicitly a development topology.
- **Readiness protection:** `tools/production_readiness_gate.py` now validates every role and rejects missing/comment-spoofed UID, privileged mode, devices, dangerous capabilities and quoted or unquoted host network/PID/IPC namespaces. Regression tests include the independently found `user: "0:0" # user: "999:999"` and `network_mode: "host"` bypass forms.
- **Exact retained images:** API `sha256:b1983bab928ef93495d8be020030917d4ae54364234390047c3163af5414fedb`; Admin `sha256:2283095764622e373e30b51ba749819751e6bfb0c37c6bb82e2d3bfe4937760f`; XHS HTTP `sha256:5b44114d4bd9c28a8e93c39140466c542e8babeead038fb0d1cfe45c3cd75966`. These are local image IDs, not ACR digests.
- **Isolated ECS evidence:** only retained builder `i-wz99180s9ig5ecq10uaj` in Shenzhen F was targeted. Final evidence is under `/opt/noteai-build/evidence-a635692-constraint-proof-r1`; final evidence-script SHA256 is `75b7d8393de4e259fcc6303ecffa51a063b59266fc10dfe357fedaf7524221e6`. Final Cloud Assistant execution `t-sz06rs944x76m0w` returned `FINAL status=PASS`, evidence checksums `PASS`, running containers before/after `0/0`, and Docker auth entries `0`.
- **Package/ELF result:** all roles contain Debian `util-linux`/`libblkid` `2.41-5` and `libacl1` `2.3.2-2+b1`, but the offline ELF closure examined `1,203` ELF records, `354` accepted roots and `377` reachable objects per role and found `libblkid=False`, `libacl=False` for API, Admin and XHS HTTP. This is high-confidence `vulnerable_code_not_in_execute_path` evidence under the recorded deployment constraints; no formal VEX state was written.
- **Runtime read-back:** each smoke was `linux/amd64`, UID/GID `999`, `network=none`, read-only root, non-privileged, `cap_drop=["ALL"]`, `no-new-privileges`, zero devices, bounded `/tmp` and bounded UID-owned `/app/model/data`. API `/health/live` and `/health/ready` returned `200/200`; Admin returned `200/503` because no `ADMIN_PASSWORD` production secret was injected, matching the source's intended fail-closed readiness; XHS remained suspended and exited `1` fail-closed. No supplier or production endpoint was called.
- **Audit history:** execution `t-sz06rs8gucfu874` failed before evidence collection because its shell used an unsupported substitution; execution `t-sz06rs8ltw1lb0g` was manually stopped after host `readelf` encountered non-ELF files. No conclusion relies on either attempt. The final command added ELF-magic/file-pattern prefiltering and disabled core dumps; cleanup left zero containers.
- **Repository verification:** full unit suite `630` passed with `5` skipped; targeted deployment/readiness/security suite `25/25`; production readiness `85/85`; development and production Compose config-only checks, Python compilation and `git diff --check` passed. Unit logs containing provider failures are test-owned synthetic/fail-closed paths, not real calls.
- **Release interpretation:** the technical evidence gap for `libblkid` and `libacl` is closed for these exact images under this exact production contract. Raw Trivy evidence remains canonical and unchanged at `4 Critical / 19 High` per role. The successor formal VEX review is complete; `PROD-IMG-002` remains blocked only for explicit ACR authorization and registry-digest reissue controls.
- **Safety/rollback:** no image build, registry login/push, service deployment, production data/secret access, provider call or cloud-resource modification occurred. Rollback is a normal revert of deployment-control commit `253fde55fa609676a9fca5c14fdf6aeceef74000`; no production or registry rollback is required.

### PROD-BROWSERLESS-VEX-REVIEW-001 — Formal exact-product VEX and independent review

- **Status:** `VERIFIED` on 2026-07-23. The first independent review correctly returned `FAIL` because evidence hashes/IDs were only format-checked and the readiness Secret heuristic falsely flagged Python test container constants. The minimum fixes hard-code every reviewed image/SBOM/Trivy/constraint identity, add coordinated-tampering regression coverage and only exempt Python multiline literal opening tokens; the same independent verifier then returned final `PASS`.
- **Exact scope:** application/OCI revision `a635692a899ee02c6905cd694611c14e0da4594a`; API local image ID `sha256:b1983bab928ef93495d8be020030917d4ae54364234390047c3163af5414fedb`; Admin `sha256:2283095764622e373e30b51ba749819751e6bfb0c37c6bb82e2d3bfe4937760f`; XHS HTTP `sha256:5b44114d4bd9c28a8e93c39140466c542e8babeead038fb0d1cfe45c3cd75966`; all `linux/amd64`. These remain local image IDs and are not ACR registry digests.
- **Formal decision:** one independent CycloneDX 1.6 VEX contains exactly twelve `not_affected` vulnerability objects: five `code_not_present`, one `requires_environment`, and six `code_not_reachable`. Its 69 `affects` entries resolve to exactly 23 scanner rows per role; the ncurses CVE includes four package BOM-Links per role and the util-linux CVE includes nine per role. `analysis.response` is absent, so the document does not encode `will_not_fix`, a waiver or a production exception.
- **Original artifact identity:** at independent-review commit `5b3249995522a22dc600ee7501f9328121f4497c`, VEX SHA256 was `cf30b880f95c5b0b889bce79478b2b26aa71b66aee884c7380c9dbf9adde2746`, machine evidence SHA256 was `7deb56837f833075e4579d4aaa0dd26c6ea5d4cbfcf9505ce109324f8af6d1db`, and independent review record SHA256 was `9fd0e25950a7970248e36aeed823858731b8500a08cea448e8bce945820137f9`. `PROD-IMG-002` preserves these prior-review identities inside the registry reissue record while updating the current VEX/evidence hashes.
- **Retained evidence:** SBOM serials/versions and SHA256, raw Trivy SHA256, Trivy DB metadata SHA256, exact package PURLs/BOM refs, and constraint/runtime proof SHA256 are immutable literals in the offline verifier. Only isolated builder `i-wz99180s9ig5ecq10uaj` was read through Cloud Assistant. Read execution `t-sz06rsblbtnwphc` and corrected BOM-ref execution `t-sz06rsbupffva4g` returned exit `0`. Intermediate read `t-sz06rsbrezx6g3k` stopped at exit `5` on a `jq` grouping error after the first SBOM; it wrote no file and changed no environment.
- **Validation:** official CycloneDX 1.6 schema SHA256 `1ebcb88a2c845ecb6ff7bee7aeabdff9422cb0347f3d6875b241bd444b7e098f` returned `0` validation errors. Offline bundle verifier passed; targeted VEX/readiness tests passed `21/21`; production readiness passed `86/86`; full unit suite passed `636` with `5` skipped; development and production Compose config-only checks, Python compilation and `git diff --check` passed. Test logs used synthetic/fail-closed provider paths and made no real supplier call.
- **Canonical scanner rule:** the retained raw reports remain unchanged and unsuppressed at `23` rows (`4 Critical / 19 High`) per role. The VEX is a separate impact statement; it does not delete, rewrite or silently suppress raw evidence.
- **Validity/review triggers:** any application/image ID, base digest, SBOM, package version, architecture, role/CMD/entrypoint graph, deployment capability/device/mount/namespace policy or upstream CVE scope change invalidates the current decision. The initial `PROD-IMG-002` version-2 binding was invalidated by corrected direct-pull evidence; `PROD-BROWSERLESS-VEX-DIGEST-REISSUE-001` now supplies the independently verified version-3 binding. Any future digest or image change requires another reissue.
- **Safety/stop point:** no exploit/probe, image build, ACR login/push, service start/deployment, production access, cloud-resource mutation or real supplier call occurred. No production exception was created.
- **Successor:** `PROD-IMG-002` completed the one-time push; corrected digest read-back and the independently verified version-3 reissue are recorded below.

### PROD-IMG-002 — Push to ACR and verify digest

- **Status:** `PUSHED / CORRECTED DIGESTS DIRECT-PULL VERIFIED / CORRECTED VEX V3 VERIFIED` on 2026-07-23. The three exact role tags were pushed once. `PROD-IMG-003-PULL-WINDOW-001` disproved three manually mistyped digest strings; the successor corrected pull proved all three exact control-plane digests and OCI provenance, and `PROD-BROWSERLESS-VEX-DIGEST-REISSUE-001` completed the corrected independent binding. No release may consume the prior strings or VEX version 2.
- **Priority:** Critical
- **Exact source and repository:** application/OCI revision `a635692a899ee02c6905cd694611c14e0da4594a`; ACR Enterprise `noteai-prod-shenzhen` / `cri-xpuhaclqkxlwy47t`, private immutable repository `noteai/app`, public host used only during the approved window `noteai-prod-shenzhen-registry.cn-shenzhen.cr.aliyuncs.com`. The production-VPC private binding was not changed and ACR was not upgraded.
- **2026-07-23 preflight:** ACR Enterprise instance `noteai-prod-shenzhen` (`cri-xpuhaclqkxlwy47t`), private namespace/repository `noteai/app`, and private registry host `noteai-prod-shenzhen-registry-vpc.cn-shenzhen.cr.aliyuncs.com` were re-read. Only immutable historical tags `git-ff030f5` (`linux/arm64`) and `git-ecc4702e` (`linux/amd64`) exist; the three planned `git-a635692-amd64-{api,admin,xhs-http}-r1` tags do not exist, so no overwrite collision was found.
- **Exact pre-push evidence:** Cloud Assistant observation `t-sz06rsfdd0xgfls` exited `0` on only isolated builder `i-wz99180s9ig5ecq10uaj`: builder `x86_64`, running containers `0`, all three local IDs/`linux/amd64`/OCI revision/source/version/created/role/entrypoint/CMD values unchanged, every retained SBOM/Trivy/constraint SHA unchanged, and `RepoDigests=[]`. Earlier assertion `t-sz06rsf7tpz3ugw` exited `2` only because it incorrectly required Docker `Config.User=999:999`; the accepted image uses named user `noteai` whose verified runtime UID/GID is `999:999`. Neither command wrote a file or changed the image.
- **Correct immutable ACR digests:** API tag `git-a635692-amd64-api-r1` → `sha256:17706e1802afc136ac8f9a621d4199a7f9749da733290e923e42268eff42e0d1`; Admin tag `git-a635692-amd64-admin-r1` → `sha256:d94bc4581e85a5b507415da2abc284c26e46288a746f91e951a43380d670c733`; XHS HTTP tag `git-a635692-amd64-xhs-http-r1` → `sha256:452c2faf7853ce58d93e43c05fb6217a9a6cc1e4c81345d8cef2f50acabd79af`. These are each exactly 64 hex characters and were independently proved by direct `repository@sha256` pull in Cloud Assistant execution `t-sz06rst5q3tpts0`.
- **Disproved prior strings:** API `sha256:17706e1802afc136ac8f9a621d4199a719749da73329ee923e42268eff42e0d1`; Admin `sha256:d94bc4581e85a5b507415da2abc284c26e46288a746f91e951a43380d676c733`; XHS HTTP `sha256:452c2faf7853ce58d93e43c5bf6217a99a6cce14c81345d8cef2f5accabd79af`. The API `docker pull repository@sha256` returned `manifest unknown`; authenticated manifest requests for Admin and XHS HTTP returned not found. This is a transcription defect, not a registry consistency delay.
- **Remote read-back:** all three returned `linux/amd64` and retained exact OCI revision `a635692a899ee02c6905cd694611c14e0da4594a`, source `https://github.com/iamyusen1314/noteai`, version `git-a635692-amd64-r1`, and created `2026-07-23T05:23:45Z`. No service/container was started.
- **VEX reissue:** CycloneDX 1.6 VEX document version `2` preserves the prior disposition content but binds the disproved strings above and remains permanently invalidated. Corrected version `3` completed under `PROD-BROWSERLESS-VEX-DIGEST-REISSUE-001`; it preserves the original twelve vulnerability objects and 69 BOM-Links exactly and binds only the three corrected direct-pull-verified digests.
- **Access cleanup control-plane state:** exact temporary builder CIDR `47.107.35.208/32` was deleted; the ACR public endpoint was disabled and its public whitelist became empty; ECS Session Manager was disabled and displayed `会话管理已关闭`. Security-group ingress was never modified. During a rejected `sudo -n` retry the temporary password briefly landed in an editable terminal line; `Ctrl+U` cleared it before shell submission, and it was never written to a repository file or Cloud Assistant command. Treat that temporary password as spent and never reuse it.
- **Local credential cleanup conflict and resolution:** read-only `PROD-IMG-003-CONTENT-VERIFY-001` found one auth host entry for `noteai-prod-shenzhen-registry.cn-shenzhen.cr.aliyuncs.com` in `/home/ecs-assist-user/.docker/config.json`, disproving the earlier zero-entry claim. `PROD-IMG-003-AUTH-CLEANUP-001` then removed exactly that one entry as `ecs-assist-user`; no auth value was read or output. Mutation execution `t-sz06ru2231p93pc` exited `0` with target/total auth `1/1 → 0/0`, running containers `0 → 0`, unchanged image-inventory SHA `2ac91dcaa657c396fef9b2a1af5f09b37b651f9655c6884c9b175ffe521a8443`, unchanged config metadata `1000:1000:600`, and `FINAL_STATUS=PASS`. Independent read-only execution `t-sz06ru24i3ld0cg` exited `0` and reconfirmed target auth `0`, total auth `0`, running containers `0`, the same image-inventory SHA and config metadata, with `VERIFY_STATUS=PASS`. ACR public access and ECS Session Manager were not reopened and no cloud resource was changed.
- **Validation:** authenticated tag pull, local OCI inspect, offline VEX verifier, seven VEX tamper tests, production readiness `86/86`, full unit suite `637` with `5` skipped, Python compilation, `git diff --check`, and official CycloneDX 1.6 schema validation all passed; schema errors were `0`. Canonical SBOMs and unsuppressed raw Trivy reports remain unchanged.
- **Rollback:** do not deploy the three digests. Registry deletion is destructive and requires separate approval; no deletion is authorized or needed. Temporary network/session access has already been fully rolled back.
- **Acceptance:** push/tag presence, the three corrected immutable registry digests, OCI identity/provenance, corrected VEX version 3, image content and zero local Docker auth entries are accepted for their approved scopes. Deployment remains prohibited pending a separately approved deployment plan. Control-plane temporary access is closed.
- **Real call:** yes, ACR push/read.
- **Possible cost:** ongoing private ACR storage plus the already-consumed bounded upload traffic; no new ECS purchase or deployment cost.
- **Next approval:** a bounded read-only production deployment plan may be proposed next; it must pin the three corrected immutable digests, preserve the accepted runtime constraints, define serial rollout/rollback and perform no pull, deployment, service start, production-data access or cloud mutation.

### PROD-BROWSERLESS-VEX-DIGEST-REISSUE-001 — Rebind formal VEX to corrected ACR digests

- **Status:** `VERIFIED` on 2026-07-23. Independent verifier `/root/vex_final_verify` returned `PASS` with no Critical or High issue.
- **Exact immutable bindings:** API `sha256:17706e1802afc136ac8f9a621d4199a7f9749da733290e923e42268eff42e0d1`; Admin `sha256:d94bc4581e85a5b507415da2abc284c26e46288a746f91e951a43380d670c733`; XHS HTTP `sha256:452c2faf7853ce58d93e43c05fb6217a9a6cc1e4c81345d8cef2f50acabd79af`.
- **Disposition preservation:** all twelve vulnerability objects remain exactly equal to original independent-review commit `5b3249995522a22dc600ee7501f9328121f4497c`; canonical semantic SHA256 is `190a501f5ecd0cd7a4e5c18c9365ab4287ceae51820d540ac759fdd9c5c88752`. All 69 exact SBOM BOM-Links remain unchanged, with 23 per role.
- **Raw evidence preservation:** API/Admin/XHS SBOM SHA256 values remain `59a4cabaa7debfb81684ac3a77c7467454d1c98ef04d64aeade4850b07d918be`, `136ace76eaaaed1d7ded40d54bb5162cbd28bb79069a310694913e847d1f6cd7`, and `a27663349c7af6ebdfdfbd16bf6ef2c189b420115405304ac26565249aa6a92e`. Raw unsuppressed Trivy SHA256 values remain `92d8bab2f8b58f4ed2a9469aaca8a73ee9985d8a97999ab7ff81efba68c20eda`, `e38d2a754a74133b93538545473cc3a1758403a1389fc34a028a3d81bc1e44b8`, and `f91e706a8e058fa60952b74246fb9a156b68cb698e6379ccc9a6aed79f744932`; counts remain `4 Critical / 19 High` per role. No finding was deleted, rewritten or suppressed.
- **Artifact identity:** corrected VEX version-3 SHA256 `bb4c1e3b06627e82ed56cdb12c0de03412d0c7428d7088e9b6e5a2162a20edd8`; machine evidence SHA256 `def8b111dd429dfeeca1bdf7c04d7b877d156f526fab04c851af3efc9ed0c7a5`; independent review record SHA256 `a6281661051e96dacecf2cff6cd653d0b2a21621b3d0405b16aa468f646b48b0`. The review record also preserves the original independent-review hashes and the invalidated version-2 artifact hashes.
- **Validation:** official CycloneDX 1.6 schema SHA256 `1ebcb88a2c845ecb6ff7bee7aeabdff9422cb0347f3d6875b241bd444b7e098f` returned `0` errors; offline bundle verifier passed; VEX tamper tests passed `9/9`; full unit suite passed `639` with `5` skipped; production readiness passed `86/86`; Python compilation and `git diff --check` passed. The independent first-pass review reconfirmed exact digests, unchanged dispositions/BOM-Links/evidence and no exception/deployment semantics.
- **Safety and authority:** local repository files only. No image/container/service was built or started, no registry or production system was accessed, no supplier call or cloud mutation occurred, and no production exception, ACR action or deployment authorization was granted.
- **Rollback:** because no runtime/cloud state changed, rollback is a repository revert to the invalidated version-2 evidence only for audit history; never restore its three digest bindings for release use.
- **Next gate:** content acceptance and local ACR auth cleanup are complete. Prepare a separately approved read-only deployment plan. Do not start API-C/API-F.

### PROD-IMG-003 — Verify revision, architecture, artifacts, mttravel and Secret handling

- **Status:** `CONTENT ACCEPTANCE AND LOCAL AUTH CLEANUP VERIFIED — DEPLOYMENT PLAN REQUIRED`
- **Priority:** Critical
- **Current evidence:** all three previously supplied `repository@sha256` references are absent and remain forbidden. The three corrected 64-hex digests for the same immutable tags were directly pulled and inspected on the isolated native AMD64 builder; no image-layer conclusion is carried over from a mistyped digest.
- **`PROD-IMG-003-PULL-WINDOW-001` result (2026-07-23):** `FAILED SAFELY / ROOT CAUSE FOUND`. ACR public access was restricted to `47.107.35.208/32`; ECS Session Manager connected as `ecs-assist-user`; running containers were `0` before the root Cloud Assistant pull. The supplied API reference returned `manifest unknown`. Because the session user correctly lacked Docker-socket access and `sudo -n` was rejected, the two remaining supplied references were checked with authenticated client-side registry manifest requests instead of weakening Docker permissions; both returned `MANIFEST_NOT_FOUND`. No container or service was started.
- **Root cause:** read-only ACR image-version metadata shows that the immutable tags exist but the three handoff/VEX digest strings were manually transcribed incorrectly. Correct control-plane values are API `sha256:17706e1802afc136ac8f9a621d4199a7f9749da733290e923e42268eff42e0d1`, Admin `sha256:d94bc4581e85a5b507415da2abc284c26e46288a746f91e951a43380d670c733`, and XHS HTTP `sha256:452c2faf7853ce58d93e43c05fb6217a9a6cc1e4c81345d8cef2f50acabd79af`.
- **`PROD-IMG-003-CORRECTED-PULL-001` result (2026-07-23):** `PASS`. Initial execution `t-sz06rssz9ht3y0w` exited `2` during shell parsing before any pull; it printed only `HOST_ARCH=x86_64` and `RUNNING_BEFORE=0`, created no file and changed no image or service. The simplified equivalent execution `t-sz06rst5q3tpts0` targeted only `i-wz99180s9ig5ecq10uaj`, exited `0`, directly pulled all three exact corrected references and reported `RUNNING_BEFORE=0` and `RUNNING_AFTER=0`.
- **Corrected pull identity/provenance:** each returned its requested `RepoDigest`, `linux/amd64`, non-root image user `noteai`, entrypoint `/app/scripts/docker_entrypoint.sh`, exact OCI revision `a635692a899ee02c6905cd694611c14e0da4594a`, source `https://github.com/iamyusen1314/noteai`, version `git-a635692-amd64-r1`, created `2026-07-23T05:23:45Z`, and the expected `api`, `admin` or `xhs-http` role metadata. API/Admin retained their role-specific start commands. No container or service was started.
- **`PROD-IMG-003-CONTENT-VERIFY-001` result (2026-07-24):** `PASS WITH SEPARATE CREDENTIAL-CLEANUP BLOCKER`. Cloud Assistant execution `t-sz06rswdieuwmio` targeted only isolated builder `i-wz99180s9ig5ecq10uaj`, exited `0`, and proved each corrected digest resolves to its exact previously scanned local image ID: API `sha256:b1983bab928ef93495d8be020030917d4ae54364234390047c3163af5414fedb`, Admin `sha256:2283095764622e373e30b51ba749819751e6bfb0c37c6bb82e2d3bfe4937760f`, XHS HTTP `sha256:5b44114d4bd9c28a8e93c39140466c542e8babeead038fb0d1cfe45c3cd75966`. It reconfirmed `x86_64`, `linux/amd64`, exact OCI labels, non-root `noteai`, role-correct entrypoint/CMD, zero running containers, zero build processes and zero production `10.42.*` routes.
- **Evidence integrity:** authoritative execution `t-sz06rsws9r6ubk0` exited `0`; root, acceptance, scans and smoke SHA manifests all returned `PASS`. Retained evidence object SHA values are: model artifact check `0f2f8154ecdef22896194ececbc9558bbdd1c9f07e9bc77a32254df3ca49d471`, Git LFS fsck `81f6819612dfaf9af7854c05c501e2a69e3852e47f0bfb0a82dfce8f3f6c5db9`, API runtime `d2fa863a039df1fb264bc163d4fa9c98caaa16dc9661e3aba2fc76bb8e800454`, Admin runtime `244c6354b46b72bae4646484dea49dfb76ce654c860a53ea03ccd14f22985059`, XHS runtime `654837caae54c70857f4d452213e7927f32a3ba4eb907f05156b5f0417842d08`, signer smoke `70da3cbdc504ebf81747abc0b2da682387fa9df494912d9e9aa4c29dee37b5a7`.
- **Content and scan acceptance:** the retained model check names all three V0.4 artifacts; prior exact-SHA validation is carried to the corrected digest by exact Docker image-ID equality and intact evidence manifests. Retained evidence locates `mttravel 1.0.16` and bundle SHA `8a0527bb6e8b9074cf0221756f35bca989945e8436e4841596a7b7bf79ae06d8`; Pillow `12.3.0` and `crypto-js 4.2.0` are present. SBOM components remain API `174`, Admin `174`, XHS HTTP `175`; browser component count is `0` for every role. Raw Trivy Secret findings remain `0`; sensitive history assignment count is `0`; unsuppressed vulnerabilities remain `4 Critical / 19 High` per role and are covered only by the separately reviewed VEX, not suppressed here.
- **Non-authoritative read-only retries:** `t-sz06rswn0mwlrsw` verified all four evidence manifests before a Python newline-escape syntax error; `t-sz06rsx518xfn5s` stopped before streaming because role-specific RootFS layer lists differ; `t-sz06rsx9vci5zb4` used a legacy `layer.tar` parser against the builder's alternate image-save layout and therefore returned no file matches. Each retry left running containers at `0`, wrote no file, created no container and made no network/registry call. Their negative parser results do not override the authoritative exact image-ID and intact-evidence proof.
- **Security inconsistency resolved:** read-only inspection found exactly one Docker auth entry for the ACR public hostname in `/home/ecs-assist-user/.docker/config.json`; no value was read or output. `PROD-IMG-003-AUTH-CLEANUP-001` removed exactly that one entry as the owning user. Mutation execution `t-sz06ru2231p93pc` and independent read-only verification `t-sz06ru24i3ld0cg` both exited `0`; target/total auth are now `0/0`, running containers remain `0`, image inventory remains unchanged at SHA `2ac91dcaa657c396fef9b2a1af5f09b37b651f9655c6884c9b175ffe521a8443`, and file metadata remains `1000:1000:600`.
- **Cleanup proof:** exact temporary builder CIDR `47.107.35.208/32` remains recorded removed; ACR public access and ECS Session Manager remain recorded closed and were not reopened by this task. Security-group ingress was not modified. The only remote mutation was deletion of the single user-scoped local Docker auth entry; no image, container, service, production resource or cloud control-plane state changed.
- **Credential note:** a `sudo -n` attempt failed before Docker login could prompt; the temporary password then briefly entered the shell edit buffer and was cleared with `Ctrl+U` before submission. It was not saved in Git or a Cloud Assistant command. Do not reuse that temporary password.
- **Prerequisites:** PROD-IMG-002 digest; read-only image and node inspection plan.
- **Execution steps:** inspect digest/platform/labels; verify revision; verify required model artifacts by name/hash; verify `mttravel` executable and runtime dependency; verify only required secret key names and file permissions, never values; scan image history for secret leakage.
- **Risk:** supply-chain mismatch, missing artifacts, secrets in layers/logs, nonfunctional provider CLI.
- **Rollback:** reject the digest and return to PROD-IMG-001; do not deploy.
- **Acceptance:** corrected-digest image content and local credential cleanup are accepted: exact revision, AMD64, previously scanned image identity, model/mttravel evidence, role metadata, SBOM, Secret and history gates pass, and local Docker auth count is `0`. This does not authorize deployment.
- **Real call:** image/registry reads only; no provider call.
- **Possible cost:** negligible registry/network reads.
- **Next approval:** read-only `PROD-DEPLOY-PLAN-001` is complete. Proceed only with separately approved `PROD-DEPLOY-ENV-SPLIT-001`; no deployment is authorized.

### PROD-DEPLOY-PLAN-001 — Digest-pinned serial deployment plan

- **Status:** `PLANNED / EXECUTION NOT AUTHORIZED` on 2026-07-24.
- **Plan artifact:** `docs/PRODUCTION_DEPLOYMENT_EXECUTION_PLAN.md`.
- **Immutable role set:** API `sha256:17706e1802afc136ac8f9a621d4199a7f9749da733290e923e42268eff42e0d1`; Admin `sha256:d94bc4581e85a5b507415da2abc284c26e46288a746f91e951a43380d670c733`; XHS HTTP `sha256:452c2faf7853ce58d93e43c05fb6217a9a6cc1e4c81345d8cef2f50acabd79af`. Deployment uses the private VPC ACR hostname and never a mutable tag.
- **Serial order:** repository env split → separate read-only live preflight → API-C loopback canary/zero-AI readiness → API-C promotion → independent API-F canary/promotion → loopback-only Admin → separately approved suspended XHS trends, then tracking → later ALB/TLS/DNS/public smoke approvals.
- **Readiness contract:** API liveness is process-only; API readiness blocks only on PostgreSQL and required local models. Admin readiness blocks only on PostgreSQL and Admin credential presence. XHS exposes no HTTP healthcheck and is accepted by exact process/role identity, runtime constraints, stable bounded observation and zero external collection.
- **Deployment blocker found:** `deploy/production/docker-compose.yml` currently injects the same `NOTEAI_PRODUCTION_ENV_FILE` into API, Admin, `xhs-trends` and `xhs-tracking`. This violates role least privilege because each role receives unnecessary Secret names. Split API/Admin/XHS env-file inputs and add allowlist tests before any production start. This deployment-control-only change does not require an image rebuild and must not change the OCI revision.
- **Migration boundary:** production schema `0001`–`0008` is historical accepted state but needs fresh read-only confirmation. Ordinary start performs no migration. Do not run `scripts/render_predeploy.py`: in addition to migration it writes the managed Prompt baseline and requires a separate database-write approval.
- **XHS boundary:** keep both XHS services stopped until separately approved. `NOTEAI_XHS_COLLECTION_SUSPENDED=1` prevents real XHS collection but does not prove zero database/snapshot writes; actual service start therefore requires an exact allowed-write list.
- **Cost:** no new resource is planned. Existing fixed charges continue. Worst-case unshared private-registry transfer for two API copies plus Admin plus XHS is about `4.18 GB`; shared layers should reduce actual transfer. Require at least `12 GiB` free Docker storage per participating node so rollback assets remain.
- **Rollback:** retain prior images/config/release directories; a canary failure removes only the canary; API-F failure retains accepted API-C; Admin failure stops only Admin; XHS failure remains suspended and stops only that process. No migration/schema downgrade, image deletion, production-data cleanup or cloud-resource deletion is included.
- **Local verification:** exact-digest production Compose parse passed; resolved image output contained two XHS refs and one exact API/Admin ref. Targeted production hardening/readiness/readiness-gate tests passed `40/40`. Offline production readiness passed `86/86`. `git diff --check` passed after documentation update. No registry/cloud/provider/production call occurred.
- **Next approval:** `PROD-DEPLOY-ENV-SPLIT-001` only: minimal repository change to add role-specific API/Admin/XHS env-file inputs, safe key-name allowlist tests, independent verification and a `[skip render]` deployment-control commit. No image rebuild is required or authorized.

### PROD-DEPLOY-ENV-SPLIT-001 — Role-specific production env inputs

- **Status:** `VERIFIED / PUSHED` on 2026-07-24.
- **Deployment-control release:** `7f6b9d6eaaff06c8d756783136a1bc4ed417c5c0` (`fix(prod): split role runtime env inputs [skip render]`). This is not an OCI/image revision.
- **Compose closure:** API uses `NOTEAI_API_ENV_FILE` with default `/etc/noteai/api.env`; Admin uses `NOTEAI_ADMIN_ENV_FILE` with default `/etc/noteai/admin.env`; both XHS processes use `NOTEAI_XHS_ENV_FILE` with default `/etc/noteai/xhs.env`. The prior `NOTEAI_PRODUCTION_ENV_FILE` and `/etc/noteai/runtime.env` fallback are absent.
- **Key-name gate:** `scripts/validate_production_env_files.py` validates only key names for its decision and never prints values. It fails on missing/non-regular/symlink files, group/world permissions, duplicate or invalid names, reused files, unknown Secret-like names and a known Secret name supplied to the wrong role. API, Admin and XHS allowlists are documented in `docs/DEPLOYMENT_SECRETS.md`.
- **Role boundary:** API may receive only its database/provider/gateway/model Secret names; Admin only `DATABASE_URL` and `ADMIN_PASSWORD`; XHS only `DATABASE_URL`, snapshot-upload token and authorized-trend token. XHS session/Cookie data is not accepted as an env-file Secret name.
- **Verification:** synthetic boundary tests passed, including correct-role acceptance, cross-role/unknown Secret rejection, duplicate/permission/symlink/reused-file rejection, documentation consistency and value-redaction output. Targeted tests passed `26/26`; full unit suite passed `647` with `5` skipped; production readiness passed `86/86`; Python compilation, immutable-digest config-only Compose parsing and `git diff --check` passed.
- **Image/release identity:** no image was built or changed. The three production OCI revisions remain `a635692a899ee02c6905cd694611c14e0da4594a`. The new `[skip render]` repository commit is deployment-control provenance only and must never be substituted for an image revision.
- **Safety:** no real env file or Secret value was read, changed, committed or printed; no registry/cloud/production/provider action occurred and no service/container was started.
- **Rollback:** revert only this deployment-control commit to restore the prior template, but do not deploy that prior shared-env template. No database, image, registry or cloud rollback is involved.
- **Next approval:** `PROD-DEPLOY-PREFLIGHT-001` only: read-only API-C/API-F, runtime-env metadata/permissions/key names, RDS schema/availability and private-network audit. No ACR login/pull, service start, deployment, provider call, data write or cloud mutation.

### PROD-DEPLOY-PREFLIGHT-001 — Live host, env, RDS and private-network audit

- **Status:** `PARTIAL PASS / DEPLOYMENT BLOCKED` on 2026-07-24.
- **Boundary:** Alibaba Cloud console and Cloud Assistant were used only for control-plane reads and host read commands. No package was installed and no file/Secret value was changed, printed, exposed or retained. The configured database URL was loaded only transiently inside the on-host process for redacted DNS/client checks. No ACR login/pull occurred, no container/service was started, and no database write, provider call or cloud-resource mutation occurred.
- **API-C runtime:** host `i-wz9j36od3nf2b1uw7bvg` is running/healthy, private-only, `x86_64`, with Docker `28.3.3` active and zero running containers. Docker Compose is absent. Approximately `6.7 GiB` memory and `27.1 GiB` disk remain available. `noteai-api.service` exists but is disabled.
- **API-F runtime:** host `i-wz9bgztwf1tiakww2ops` is running/healthy, private-only and `x86_64`, with zero running containers. Docker Engine and Compose are absent/inactive. Approximately `6.9 GiB` memory and `34.0 GiB` disk remain available.
- **Role env files:** API-C `/etc/noteai/api.env` is a root-owned regular non-symlink file with mode `0600`; it contains 55 valid unique key names and passes the API allowlist. Only the names `AMAP_WEB_KEY`, `DATABASE_URL`, `MEITUAN_AI_HUB_TOKEN`, `MOONSHOT_API_KEY` and `NOTEAI_CLAUDE_GATEWAY_HMAC_SECRET` were classified as Secret-like, and all are permitted for API. API-C `admin.env` and `xhs.env` are absent. API-F `api.env`, `admin.env` and `xhs.env` are all absent. No value was printed or exposed; only `DATABASE_URL` was loaded transiently for the redacted RDS checks described above.
- **Required placement closure:** the serial plan requires API + Admin inputs on API-C and API + XHS inputs on API-F. API-C's API file is metadata/key-name ready; API-C Admin, API-F API and API-F XHS files are blocking absences. An unused role file need not be copied to a host merely to make all three names exist.
- **RDS and private link:** RDS dashboard reports Shenzhen total `1`, running `1`, expiring soon `0`, expired `0`. API-C resolves the configured database endpoint to one private address on port `5432`. Both API nodes resolve the private ACR hostname, and both have private-only default routes in the production VPC.
- **Schema limitation:** historical accepted evidence says migrations `0001`–`0008` are present as 8 rows with 30 public tables. Live rows were not re-queried because API-C has no existing `psql`, `psycopg`, `psycopg2` or `pg8000`. Installing a client, downloading a binary, or starting a utility container would violate this task's read-only/no-service-start boundary. Therefore schema `0001`–`0008` is `UNKNOWN / NOT LIVE-PROVEN`, not failed.
- **Release decision:** do not start `PROD-API-001`. Critical blockers are missing Compose on API-C, missing Docker/Compose on API-F, missing required role env files and missing live read-only schema proof. Host capacity, ECS health, RDS control-plane availability, API-C env-file boundary, private ACR DNS and basic private routing pass.
- **Next approval:** `PROD-DEPLOY-HOST-PREP-001` only: bounded production-host preparation to install/verify Docker Engine and Compose where missing, provision only the role env files required by the approved host placement with root ownership/mode `0600`, run the value-redacting env validator, and add a temporary read-only PostgreSQL client path solely to prove schema `0001`–`0008`. It must still prohibit ACR login/pull, image/container/service start, migration or other database writes, provider calls, ALB/TLS/DNS work and unrelated cloud changes.

### PROD-DEPLOY-HOST-PREP-001 — Production host runtime and configuration preparation

- **Status:** `PARTIAL PASS / DEPLOYMENT BLOCKED` on 2026-07-24.
- **API-C runtime:** official Docker Compose plugin `5.3.1-1.el9` was installed from Docker's signed RPM repository after a no-install transaction preview showed exactly one package. Cloud Assistant execution `t-sz06rub65r37c3k` exited `0`: Docker stayed `active`, running containers stayed `0`, and Compose reports `5.3.1`. The original GitHub asset route was abandoned after SSL timeouts; no unchecked binary was installed.
- **API-F runtime:** Alibaba Cloud Linux packages `moby 28.3.3`, `moby-engine 28.3.3`, `moby-client 28.3.3`, `containerd 1.7.29`, `runc 1.2.8` and `libcgroup` were installed with a temporary runtime mask preventing Docker service/socket activation. Compose `5.3.1` was then installed separately from Docker's signed repository so DNF could not substitute Docker's `containerd.io`. Final accepted execution `t-sz06rubo7ytpatc` exited `0`: service/socket remained inactive, Docker remained disabled, `dockerd=0`, and `containerd.io` is absent.
- **Final host audits:** API-C execution `t-sz06rucps2hk3k0` and API-F execution `t-sz06rucum12htz4` exited `0`. API-C is Docker `28.3.3` / Compose `5.3.1`, zero running containers and no `psql`. API-F is Docker `28.3.3` / Compose `5.3.1`, service/socket inactive, disabled, `dockerd=0`, no `containerd.io` and no `psql`.
- **Env-file result:** API-C's existing `/etc/noteai/api.env` remains root-owned regular mode `0600`; redacted validation reports 55 unique keys, 5 allowed Secret-like keys and no rejected Secret name. API-C `admin.env` is absent. API-F now has `/etc/noteai` root-owned mode `0700`, while `api.env`, `admin.env` and `xhs.env` are absent. Required placement remains API + Admin on API-C and API + XHS on API-F, so only API-C `admin.env`, API-F `api.env` and API-F `xhs.env` must be provisioned. They were not fabricated because no approved secure source exists for `ADMIN_PASSWORD`, the API provider/database values or XHS database/tokens.
- **Read-only RDS proof:** a temporary Alibaba Cloud Linux PostgreSQL `15.18` client was installed only for the audit, every connection used `default_transaction_read_only=on`, bounded statement/lock/connect timeouts, and the client plus private libs were removed after each attempt. The successful proof recorded PostgreSQL `16.13`, 30 public tables and an existing `schema_migrations` table. Exact rows `0001`–`0008` could not be reread: final execution `t-sz06rucj9gqvm68` exited `0` and recorded `MIGRATION_SELECT_PRIVILEGE=no` for the API runtime role. Do not weaken that role or grant migration-table access merely to satisfy the audit; use a separately approved read-only audit/migration credential.
- **Database transport blocker:** the persisted API URL requires SSL, while the observed private RDS endpoint rejected SSL. The audit used only a process-local `sslmode=disable` substitution and did not edit the env file. Before any application start, choose and verify one explicit production policy: enable/configure supported RDS TLS, or explicitly accept private-VPC plaintext and update the role URLs under separate approval.
- **Safety and cleanup:** no Secret value was printed or retained; no ACR login/pull, image operation, container/application service start, migration/database write, provider call, ALB/TLS/DNS change or unrelated cloud mutation occurred. PostgreSQL client packages and temporary error files are absent. The only persistent host changes are the approved Docker/Compose packages and API-F's protected `/etc/noteai` directory.
- **Release decision:** do not start `PROD-API-001`. Docker/Compose blockers are closed, but required role env files, database transport policy and exact migration-row proof remain release blockers.
- **Next approval:** `PROD-DEPLOY-CONFIG-CLOSURE-001` only: use a user-approved secure Secret channel to create API-C `admin.env`, API-F `api.env` and API-F `xhs.env` as distinct root-owned mode `0600` files; reconcile the RDS TLS policy without silently downgrading it; and use a separately authorized read-only audit/migration credential to prove exact rows `0001`–`0008`. If enabling RDS TLS, restarting RDS or changing grants is required, stop and request a separate explicit cloud/database-write approval. Continue to prohibit ACR, image/container/service start, migration/data writes, provider calls and ALB/TLS-listener/DNS changes.

### PROD-DEPLOY-CONFIG-CLOSURE-001 — Secret, TLS and migration-evidence closure

- **Status:** `PAUSED AT EXPLICIT PRODUCTION-WRITE APPROVAL` on 2026-07-24.
- **RDS control-plane result:** a read-only Alibaba Cloud CLI query from the authenticated Cloud Shell confirmed exactly one Running PostgreSQL `16.0` instance in Shenzhen C. `DescribeDBInstanceSSL` returned `SSLEnabled=off`, an empty SSL connection string and no certificate expiry. This independently explains the earlier client error and proves that changing the persisted role URLs to plaintext would be a security-policy downgrade, while enabling RDS SSL is an RDS production mutation requiring separate approval.
- **Account result:** `DescribeAccounts` returned exactly two Available accounts: one Super administrator and one Normal application account. No dedicated read-only audit account exists. The application role remains intentionally unable to select `schema_migrations`; the Super account is not an acceptable independent read-only audit credential.
- **Secret-channel result:** repository documentation identifies GitHub Environment `production` as the Secret system of record, but GitHub Secret values are write-only and there is no repository workflow or Alibaba credential bridge that can safely export them to ECS. API-C's existing `api.env` can be reused in place without reading values, but API-C `admin.env` still needs an interactively supplied `ADMIN_PASSWORD`, and API-F needs a non-logging encrypted transfer/input window. Do not place Secret values in Cloud Assistant command text, execution output, the Codex conversation, Git, shell history or a shared env file.
- **Proposed bounded execution after approval:** enable RDS SSL and verify `sslmode=require` from API-C; create a dedicated read-only audit account with only CONNECT/USAGE and SELECT required for the migration proof; use an interactive ECS Workbench/Session Manager window with hidden input plus encrypted host-to-host transfer to create only API-C `admin.env`, API-F `api.env` and API-F `xhs.env`; enforce root ownership/mode `0600`; run only the value-redacting key-name validator; force the audit connection read-only and select only migration versions; then remove transient audit client material and close Session Manager.
- **No-change evidence:** no RDS setting, database account, grant, host env file, Secret value, ACR state, image, container, service, provider, ALB listener, certificate or DNS record changed. The temporary Cloud Shell contains no Secret material and expires automatically.
- **Required explicit approval:** authorize `PROD-DEPLOY-CONFIG-WRITE-001` to (1) enable RDS SSL without restart unless the control plane explicitly requires one, (2) create a dedicated read-only audit account and grant only the minimum read privileges needed to prove `schema_migrations` rows `0001`–`0008`, and (3) temporarily enable ECS Session Manager for API-C/API-F so the user can provide `ADMIN_PASSWORD` and a one-time transfer passphrase through hidden interactive input; immediately close Session Manager and remove the audit credential after proof unless the user elects to retain it. Any unexpected restart requirement, broader grant, plaintext database policy, Secret logging path or scope expansion must stop for new approval.

### PROD-DEPLOY-CONFIG-WRITE-001 — Production configuration write

- **Status:** `VERIFIED / COMPLETE` on 2026-07-24.
- **Approved scope:** enable RDS SSL only if it does not require restart or interruption; otherwise stop for renewed approval. After SSL closure, create and remove a least-privilege temporary migration-audit account, provision the three missing role env files through hidden interactive input, and close API-C/API-F Session Manager.
- **Mandatory stop evidence:** before submitting any production write, the current official Alibaba Cloud RDS PostgreSQL SSL documentation was checked. It states that enabling the Alibaba Cloud server certificate restarts the database instance and causes a minute-level transient interruption; existing connections must reconnect and TLS also increases CPU/latency. This directly triggers the user's explicit stop condition.
- **Renewed approval and completion:** the user explicitly accepted the RDS restart/minute-level reconnect window. SSL returned to Running and was client-verified. The three required env files, exact migration proof, temporary-audit-role cleanup, temporary-client cleanup and Session Manager cleanup all passed as recorded in the completion checkpoint below.
- **Required next approval:** `PROD-API-001` only, reframed as the digest-pinned API-C loopback canary from the approved deployment plan. It must use the corrected immutable API digest, private ACR path, zero-AI readiness and an explicit rollback stop. It must not start API-F, Admin or XHS, and must not modify ALB/TLS listeners/DNS or run migrations/providers without separate approval.

### PROD-API-001 — Start API-C

- **Status:** `MANAGED SERVICE ACCEPTED`; one exact-digest managed container remains on `127.0.0.1:8000`, outside ALB.
- **Priority:** Critical
- **Current evidence:** API-C host preflight and exact private-ACR digest identity passed. The initial attempts exposed and safely closed the log-driver and database-privilege blockers. `PROD-API-C-PROMOTE-RETRY-004` now retains one enabled/active exact-digest managed service under the accepted least-privilege constraints, after six zero-AI health rounds, database read-only, permission, bounded-log, explicit restart and independent retention checks. It has no ALB/public traffic.
- **Prerequisites:** completed PROD-IMG-003 content acceptance and local ACR auth-entry cleanup; immutable approved digest; read-only node/service audit; approved deployment plan with rollback.
- **Execution steps:** pull by digest; preserve prior service definition; start one API-C service serially; collect redacted service status and bounded logs; do not call paid providers.
- **Risk:** startup failure, database/schema mismatch, secret exposure, unintended external calls.
- **Rollback:** stop the new service and restore the previous inactive configuration; do not deploy a forbidden image.
- **Acceptance:** service remains active, uses the approved digest, restarts cleanly, and exposes no secrets in logs.
- **Real call:** production host/container/DB connection; no paid AI.
- **Possible cost:** existing compute plus registry traffic.
- **User approval:** required for production write/start.

### PROD-API-002 — Complete API-C zero-AI readiness

- **Status:** `MANAGED SERVICE READINESS VERIFIED / COMPLETE`
- **Priority:** Critical
- **Current evidence:** the exact-digest managed service passed three pre-restart and three post-restart liveness/readiness rounds. All returned HTTP `200`; database/model core checks were healthy, the database session was read-only, bounded logs showed no migration, paid/provider call or Secret pattern, and the independent retained-state check repeated live/ready, database and log acceptance.
- **Prerequisites:** PROD-API-001; health-check plan that suppresses paid provider calls and business writes.
- **Execution steps:** verify live/readiness, database schema/connectivity, cache behavior if enabled, file/model loading, Gateway configuration consistency and redacted logs; exercise only zero-AI probes.
- **Risk:** readiness falsely succeeding while dependencies fail; accidental provider charge; sensitive log output.
- **Rollback:** mark API-C unavailable and stop service if unsafe; no DNS/ALB exposure.
- **Acceptance:** repeated live/readiness HTTP 200, dependencies required for zero-AI startup healthy, no paid AI call and no secret/log leakage; independent Test & Readiness Auditor signs off.
- **Real call:** production health/DB/cache reads; no AI call.
- **Possible cost:** existing cloud compute/database usage.
- **User approval:** required before production probes that touch controlled services.

### PROD-EXT-001 — Verify Kimi

- **Status:** `BLOCKED`
- **Priority:** High
- **Current evidence:** production key name was entered, but value correctness and end-to-end invocation are unverified.
- **Prerequisites:** PROD-API-002; Live Run Plan with one bounded synthetic request, token/cost cap and data impact.
- **Execution steps:** invoke one minimal non-sensitive synthetic request; verify response, timeout, usage record and cost accounting; redact identifiers.
- **Risk:** charge, wrong credential, data leakage, duplicate billing.
- **Rollback:** disable provider path and leave API-C internal; no retry storm.
- **Acceptance:** one successful bounded call with reconciled provider usage and internal cost record, or a documented safe failure with no repeated charge.
- **Real call:** yes, paid Kimi/Moonshot.
- **Possible cost:** capped AI usage.
- **User approval:** explicit approval required immediately before the call.

### PROD-EXT-002 — Verify Claude Gateway

- **Status:** `BLOCKED`
- **Priority:** High
- **Current evidence:** Render production Gateway live/readiness is healthy and zero-Claude signed checks passed; Alibaba API end-to-end call is unverified.
- **Prerequisites:** PROD-API-002; signed configuration consistency check; Live Run Plan with one capped Claude request.
- **Execution steps:** send one non-sensitive synthetic request through API-C to the production Gateway; verify HMAC/authentication, request admission/idempotency, response, timeout and usage/cost records.
- **Risk:** charge, signature/epoch mismatch, replay, duplicate provider request, sensitive logs.
- **Rollback:** disable Gateway transport on API-C and keep production unexposed.
- **Acceptance:** one bounded end-to-end request succeeds through the intended Gateway instances with reconciled usage and no secret/log exposure.
- **Real call:** yes, paid Claude.
- **Possible cost:** capped Claude usage.
- **User approval:** explicit approval required immediately before the call.

### PROD-EXT-003 — Verify Amap

- **Status:** `BLOCKED`
- **Priority:** High
- **Current evidence:** production key name was entered, but end-to-end invocation is unverified.
- **Prerequisites:** PROD-API-002; Live Run Plan with one bounded synthetic query and quota impact.
- **Execution steps:** execute one allowed non-sensitive query; verify structured evidence, timeout/error handling and quota accounting.
- **Risk:** quota consumption, wrong environment, sensitive query logs.
- **Rollback:** disable provider path; do not retry automatically.
- **Acceptance:** one valid response is parsed into the expected fixed schema with no secret exposure and bounded quota use.
- **Real call:** yes, Amap.
- **Possible cost:** quota/usage charge depending on account plan.
- **User approval:** explicit approval required immediately before the call.

### PROD-EXT-004 — Verify Meituan

- **Status:** `BLOCKED`
- **Priority:** High
- **Current evidence:** `ff030f5` packages the Meituan travel CLI; the production image and actual node executable are not yet verified; token key name was entered but not validated.
- **Prerequisites:** PROD-IMG-003 and PROD-API-002; policy/permission confirmation; Live Run Plan with one bounded allowed query.
- **Execution steps:** verify CLI installation/version without exposing credentials; perform one non-sensitive allowed query; verify parsing, timeout and error handling.
- **Risk:** unavailable CLI, token failure, quota/contract violation, sensitive output.
- **Rollback:** disable Meituan provider and retain zero-AI readiness.
- **Acceptance:** production node can execute the packaged CLI and one bounded allowed request returns expected structured evidence without secret leakage.
- **Real call:** yes, Meituan service.
- **Possible cost:** quota/contract usage.
- **User approval:** explicit approval required immediately before the call.

### PROD-API-003 — Deploy API-F

- **Status:** `MANAGED SERVICE ACCEPTED / OUTSIDE ALB`
- **Priority:** High
- **Current evidence:** API-F privately pulled and verified the exact immutable API digest, removed temporary ACR authentication, accepted a hardened canary, and converted it into enabled/active managed service `noteai-api.service`. Six zero-AI health rounds across an explicit restart, database read-only permissions, bounded logs and an independent retained-state check pass. API-C is unchanged.
- **Prerequisites:** accepted API-C managed readiness; approved immutable digest from PROD-IMG-002; API-C rollback evidence. Provider decisions remain required before public release but are not needed for the bounded zero-AI API-F canary.
- **Execution steps:** deploy serially by immutable digest, never mutable tag; verify service and zero-AI readiness; keep out of ALB until independently accepted.
- **Risk:** inconsistent replicas, wrong digest, database concurrency, secret/config drift.
- **Rollback:** stop API-F service and retain accepted API-C only.
- **Acceptance:** API-F matches API-C digest/config contract, passes repeated zero-AI readiness and exposes no secret in logs; independent verification complete.
- **Real call:** production host/DB/cache reads; no paid AI during readiness.
- **Possible cost:** existing compute and registry traffic.
- **User approval:** required for production deployment/start.

### PROD-ALB-001 — Create and configure ALB

- **Status:** `BLOCKED`
- **Priority:** High
- **Current evidence:** no completed ALB resource/configuration evidence.
- **Prerequisites:** PROD-API-002 and PROD-API-003; cloud inventory audit; listener/backend/health/fee/rollback plan.
- **Execution steps:** create ALB and target groups; add only accepted API-C/API-F backends; configure health checks and HTTP-to-HTTPS policy without DNS cutover.
- **Risk:** recurring fees, public exposure, bad health routing, unintended traffic.
- **Rollback:** remove backends/disable listeners while retaining resources; resource deletion requires separate approval.
- **Acceptance:** both accepted backends healthy, unhealthy-node removal works, no business DNS points to ALB and security groups allow only intended paths.
- **Real call:** yes, Alibaba control-plane writes.
- **Possible cost:** ALB hourly/LCU/public network charges.
- **User approval:** explicit cost and resource-change approval required.

### PROD-TLS-001 — Bind three TLS certificates

- **Status:** `BLOCKED`
- **Priority:** High
- **Current evidence:** certificates are issued for root, API and Admin domains but not deployed.
- **Prerequisites:** PROD-ALB-001; certificate validity/hostname read-only audit; listener mapping plan.
- **Execution steps:** bind the correct certificate to each HTTPS hostname/listener; configure modern TLS policy; verify chain/SNI without DNS cutover.
- **Risk:** wrong certificate/hostname, downtime, weak policy, renewal gap.
- **Rollback:** restore prior listener certificate mapping or disable listener; do not delete certificates.
- **Acceptance:** independent SNI/chain/expiry verification succeeds for all three hostnames against the ALB endpoint.
- **Real call:** Alibaba control-plane writes and TLS probes.
- **Possible cost:** certificates already paid; ALB usage may accrue.
- **User approval:** required for production listener mutation.

### PROD-DNS-001 — Execute DNS cutover

- **Status:** `BLOCKED`
- **Priority:** Critical
- **Current evidence:** enterprise DNS is paid/bound; production business records are not cut over.
- **Prerequisites:** all image/API/ALB/TLS acceptance complete; production smoke prechecks; authoritative DNS plan, TTL, rollback target and explicit go-live approval.
- **Execution steps:** publish only approved root/API/Admin records serially; verify authoritative answers outside proxy fake-IP DNS; monitor errors and traffic.
- **Risk:** public outage, split routing, certificate mismatch, irreversible user impact during TTL.
- **Rollback:** restore documented prior records and monitor propagation; do not delete the DNS zone.
- **Acceptance:** authoritative resolvers return intended endpoints, HTTPS works for all hostnames, monitored error rate is within release threshold and rollback remains tested.
- **Real call:** yes, public DNS control-plane write and real user-routing impact.
- **Possible cost:** DNS queries and production traffic.
- **User approval:** separate explicit cutover approval required after all prerequisites pass.

### PROD-SMOKE-001 — Execute production smoke test

- **Status:** `BLOCKED`
- **Priority:** Critical
- **Current evidence:** no complete production smoke test exists.
- **Prerequisites:** PROD-DNS-001 or an approved pre-DNS ALB test path; synthetic accounts/data; Live Run Plan with exact writes, provider-call cap, cost and cleanup policy.
- **Execution steps:** test public web/API/Admin authentication and authorization, core generation/diagnosis paths, attachments within policy, provider calls already approved, billing records in non-payment mode, failure/recovery and both API nodes.
- **Risk:** production data writes, AI charges, account lockout, duplicate billing, user exposure.
- **Rollback:** stop smoke traffic, disable affected route/provider and execute only preapproved synthetic-data cleanup; no destructive database cleanup.
- **Acceptance:** documented checklist passes with traceable request IDs, correct authorization/billing behavior, no new Critical/High defect and independent verifier evidence.
- **Real call:** yes; may include capped AI and synthetic production writes.
- **Possible cost:** AI, cloud traffic and storage.
- **User approval:** explicit approval required immediately before execution.

### PROD-PAY-001 — Validate payment callback and reconciliation

- **Status:** `BLOCKED / FIRST-LAUNCH REQUIRED / PROMOTED AS PROD-FIRST-LAUNCH-PAYMENT-001`; this historical row has no separate execution authority.
- **Priority:** Critical before commercial charging
- **Current evidence:** real end-user payment callback/settlement/reconciliation is not implemented or verified; cloud-resource purchases are unrelated.
- **Prerequisites:** payment provider contract/configuration, product refund/reconciliation rules, security review, idempotency tests, sandbox/test-mode plan and legal/business approval.
- **Execution steps:** first validate signatures, idempotent callback, order state, refunds and reconciliation in sandbox/test mode; real-money validation requires a separate release package and approval.
- **Risk:** financial loss, duplicate credit, refund error, compliance and real-user impact.
- **Rollback:** disable payment entry/callback route and reconcile test orders; never delete financial records.
- **Acceptance:** sandbox/test transactions reconcile exactly, duplicate callbacks are idempotent, refund and exception ledger are auditable, Security and Finance verification complete.
- **Real call:** no real payment is authorized by this handoff.
- **Possible cost:** provider/test fees may apply; real funds prohibited until separately approved.
- **User approval:** mandatory product, finance and explicit live-payment approval.

### Existing backlog disposition

The corrected complete-launch decision promotes every commercial-flow dependency into the pre-cutover sequence. Only capabilities that were never part of the commercial product may remain non-launch.

| ID / title | Status | Priority | Current evidence | Prerequisites | Execution steps | Risk | Rollback | Acceptance | Real call | Possible cost | User approval |
|---|---|---|---|---|---|---|---|---|---|---|---|
| LEGACY-XHS-001 — XHS acquisition/account-policy recovery | `VERIFIED` through `PROD-BROWSERLESS-CONSTRAINT-PROOF-001`; real-session validation remains separately gated | High | Browser-free bounded direct adapter, RAP fix, Pillow fix, exact Pillow-fixed local browserless image execution and least-privilege constraint proof passed offline verification; commercial authorization is stated resolved, but Xiaohongshu platform authorization is not asserted and no real session call has been made. | Formal residual disposition review, then a separately approved bounded real-session task. | After release-risk closure, validate approved read operations with strict count/time/stop gates and default suspension. | Interface/signature churn, account restriction, platform enforcement, incomplete/bad evidence. | Re-enable suspension and retain manual/screenshot evidence input; never silently restore Playwright. | Current images pass content/SBOM/Secret/vulnerability gates with an independently reviewed formal disposition; any later real smoke yields bounded evidence without write/challenge bypass. | None now; later smoke is a real platform call. | Engineering/build-host cost first; later egress/account impact must be bounded. | Required before real platform call and deployment. |
| LEGACY-CAP-001 — 100 concurrent AI users capacity | `BLOCKED / PROMOTED TO FIRST-LAUNCH GATE` | Critical before cutover | Gateway scale rehearsal exists; end-to-end application/provider limits are not proven. | Durable queue/recovery, provider quota and budget. | Execute `PROD-FIRST-LAUNCH-CAPACITY-100-001` with synthetic admission first and separately capped real-provider calibration. | Cost spike, outage, rate limits. | Stop load generator and scale back within approved bounds. | 100 simultaneous AI jobs meet the accepted admission/completion SLO with no loss, duplicate provider call or duplicate charge. | Capped real calls only in the approved calibration phase. | Potentially high. | Required. |
| LEGACY-SEC-004 — Historical reasoning retention cleanup | `BLOCKED / PROMOTED TO FIRST-LAUNCH SECURITY GATE` | High before cutover | Explainable-agent experience is retained; historical raw-reasoning persistence risk remains unresolved. | Data inventory, retention policy and legal/security decision. | Include in `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-001`; use a reversible migration if cleanup is required. | Privacy/data loss. | Backup and reversible migration only. | No raw private reasoning is unnecessarily persisted; user-facing explanations remain. | No external call expected. | Storage/engineering. | Required before data mutation. |
| LEGACY-BILL-002 — SSE interruption recovery | `BLOCKED / PROMOTED TO FIRST-LAUNCH GATE` | Critical | Recovery design/testing is incomplete. | Durable job and billing idempotency design. | Include in `PROD-FIRST-LAUNCH-DURABLE-AI-001`. | Duplicate work/cost. | Feature flag or revert single package. | Disconnect/reconnect recovers without duplicate AI charge or lost terminal result. | Test AI may be needed after offline proof. | Capped AI cost. | Required before paid test. |
| LEGACY-QA-003 — Remaining manual UI acceptance | `BLOCKED / PROMOTED TO FIRST-LAUNCH GATE` | High | Partial UI acceptance exists; remaining checklist is not fully closed. | Stable final release candidate. | Include all visible first-launch entries in `PROD-FIRST-LAUNCH-PRE-DNS-SMOKE-001`. | Missed UX regression. | Revert isolated UI package if needed. | All visible entry points and commercial flows pass on supported viewport/browser; none is dead or misleading. | No paid call by default; capped calls require approval. | None/minimal. | Required. |

## 6. Test and evidence baseline

- Historical prebuild revision `b6abaa781c11950c4d261e8d9f17c3194aecd0cf` passed `586 OK (skipped=5)` full tests and `278 OK` targeted tests.
- Historical combined-image revision `8f7d9c23dd4c4bc951f23d2412156f121da868e7` passed `587 OK (skipped=5)` full tests, `25 OK` targeted deployment/readiness tests, production readiness `66/66`, quality gate, `py_compile`, Compose parse and tracked-file sensitive-value checks; it is superseded and must not be pushed or deployed.
- Historical split-runtime revision `89e3505225c9d596d6ddb26040f08b777e77823c` passed the independent repository gates: socket-blocked full suite `594 OK (skipped=5)`, `network_attempts=0`, targeted tests `38 OK`, readiness `73/73`, semantic entrypoint matrix `14` allowed / `32` rejected, and three mutation classes rejected; shell, Python compilation, YAML, Compose, diff and secret/build-context checks passed.
- Historical hardened application revision `93b03d5c40fb64fa264c0ada1c055f8e7696b161` passed independent repository gates: full suite `601 OK (skipped=5)`, targeted `87/87`, production readiness `78/78`, seccomp SHA/semantics, launch fail-closed tests, Python compilation, Compose quiet parse, diff and changed-file Secret scan.
- `PROD-XHS-HTTP-ADAPTER-001` application commit `33d04d4560f9b7dda43a53527f8ae1026dc335d3` passed independent socket-blocked full suite `625 OK (skipped=5)` with `network_attempts=0`, targeted `130/130` with `network_attempts=0`, readiness `83/83`, Python/Shell/Compose/diff checks, exact source/assets, tracking state transitions and high-confidence sensitive scan. RAP-fixed successor `ca02335513a752ece2dabe71d08105ae86db6c5a` is the superseded pre-Pillow-fix image revision; `344897c` remains CI/Handoff-only.
- The approved hardened-image execution revalidated exact source/LFS/model/seccomp/build-context gates and built both exact native AMD64 local candidates. API image ID is `sha256:e637ff8af8f63c83e1370066111765f6594297806ec9b3e153d03b1dcb1762bc`; corrected Worker image ID is `sha256:17a80aa53c41909abf4dace7c0bec4cfc90e6fd4387bb1c80e8bc5ca7616836e`. These are local IDs, not ACR digests. API scan is `174` SBOM / Secret `0` / `19 High / 3 Critical`; Worker is `288` SBOM / Secret `0` / `32 High / 5 Critical`, with no suppression or VEX. The bounded Worker smoke passed under non-root + pinned seccomp + `no-new-privileges` after temporarily setting the isolated builder's user-namespace quota to `128`; the quota was restored to `0` and no container remained.
- The RAP-fixed browserless rebuild at exact application revision `ca02335513a752ece2dabe71d08105ae86db6c5a` produced three local `linux/amd64` candidates with exact OCI labels and no `RepoDigests`. API/Admin/XHS SBOM counts are `174/174/175`; Secret findings are `0/0/0`; each unsuppressed report has `33` raw and unique rows, `4 Critical / 29 High`, including ten Pillow High rows with fixed version `12.3.0`. Basic/RAP synthetic wrapper smokes passed in `network=none` as non-root with read-only filesystem, `cap-drop ALL` and `no-new-privileges`. Evidence manifest path and SHA256 are recorded in the task ledger above.
- Pillow-fixed application revision `a635692a899ee02c6905cd694611c14e0da4594a` passed isolated Python `3.11.14` Pillow `12.3.0` PNG/JPEG/WebP compatibility, targeted `16/16`, full unit suite `627` with `5` skipped, readiness `83/83`, `pip check`, model-artifact check, quality gate, Python/Compose/diff/sensitive checks and independent review. GitHub CI runs `29982041413` and `29982043526` also passed for the exact SHA using the repository's Python `3.11` workflow; its subsequent native AMD64 rebuild/rescan evidence is recorded immediately below.
- The exact Pillow-fixed native AMD64 rebuild at `a635692a899ee02c6905cd694611c14e0da4594a` produced current local API/Admin/XHS HTTP candidates. Provenance/content/model acceptance and basic/RAP `network=none` signer smoke passed. API/Admin/XHS SBOM counts are `174/174/175`; Secret findings are `0/0/0`; every unsuppressed report has `23` raw/unique rows, `4 Critical / 19 High`, fixed-version rows `0` and Pillow rows `0`. The local IDs and evidence path are recorded in the task ledger.
- Browserless constraint release `253fde55fa609676a9fca5c14fdf6aeceef74000` passed full unit suite `630` with `5` skipped, targeted runtime/readiness/security tests `25/25`, readiness `85/85`, development and production Compose config-only checks, Python compilation and diff checks. Independent review specifically confirmed rejection of line-comment UID spoofing and quoted/unquoted host network/PID/IPC forms.
- The retained-image constrained `network=none` proof found `libblkid=False` and `libacl=False` in the accepted ELF closure for every exact role while read-back confirmed UID/GID `999`, read-only root, `cap_drop: ALL`, `no-new-privileges`, zero devices and bounded writes. API accepted `200/200`; Admin intentionally failed ready at `503` without a production password; XHS remained suspended. Evidence path and command identity are recorded in the task ledger.
- Corrected CycloneDX VEX version 3 passed official schema validation with `0` errors, offline bundle validation, `9/9` tamper tests, full unit suite `639` with `5` skipped, production readiness `86/86`, Python compilation, diff checks and two-stage independent review. Its twelve vulnerability objects are exactly unchanged from `5b324999`, all 69 package BOM-Links remain intact, and the original raw SBOM/Trivy/constraint identities remain immutable.
- Model integrity passed human manifest `8/8`, machine manifest `4/4`, and Git LFS artifact `3/3` checks.
- `py_compile`, Docker Compose/static configuration, diff, secret, and build-context checks passed.
- The superseded combined local native AMD64 candidate remains blocked at SBOM `298`, Secret `0`, `32 High / 7 Critical`. Historical split candidates at `89e3505` and retained hardened candidates at `93b03d5` show the same role-specific counts: API SBOM `174`, Secret `0`, `3 Critical / 19 High`; Worker SBOM `288`, Secret `0`, `5 Critical / 32 High`. Those historical images remain local-only and superseded.
- The three exact `a635692` role images were pushed once in the separately authorized `PROD-IMG-002` window and now have corrected direct-pull-verified ACR digests. This VEX reissue made no registry/cloud call and started no image, container, production service, database/cache access or external provider invocation.
- Render public live/readiness checks listed in section 3 were rerun on 2026-07-20.
- Alibaba production application readiness, provider calls, ALB/TLS/DNS and payment checks have not passed; no task may be promoted to `VERIFIED` from historical narration alone.
- H15 repository evidence: six failing-first contracts initially produced `7 failures / 1 error` and passed `6/6` after the bounded fix; security/compliance `72/72`, deployment `17/17`, API `152/152`, full suite `719` plus `5` skips, readiness `86/86`, quality, compile, Compose and diff checks passed. This evidence is offline self-validation only and must not be promoted to independent R15 or PostgreSQL acceptance.
- R15 independent evidence: migration/input and permission/regression streams passed, including 12 immutable-byte mutation observations, `153/153` Unicode-phone pre-transaction rejects, security+deployment `89/89`, API `152/152`, full `719` plus `5` skips and readiness `86/86`. The retention stream failed with `1 High / 3 Medium`; passing substreams do not authorize H15, PostgreSQL rehearsal or deployment.

### PROD-DEPLOY-CONFIG-WRITE-001 checkpoint — 2026-07-24

- **RDS SSL completed:** the approved SSL change for `pgm-wz9m8tdck1v06672` completed without scope expansion. Control-plane read-back returned `DBInstanceStatus=Running`, `SSLEnabled=on` and an empty modification-reason field. API-C's persisted `DATABASE_URL` was inspected only as an in-memory value and passed the redacted `sslmode=require` check. A read-only PostgreSQL client session proved `TLSv1.3` with `TLS_AES_256_GCM_SHA384`; no connection string or credential was printed.
- **Privilege boundary confirmed:** the application database role is `noteai_app`. It has public-schema usage but does not own `public`, does not own `public.schema_migrations`, cannot select the table and has no grant option. Therefore it cannot safely create the required independent migration proof or delegate the minimum audit privilege. No broad RDS `ReadOnly` database grant was substituted.
- **Secret channel stopped safely:** API-C Session Manager was opened without a security-group change, with zero running containers. A credential was then supplied through the logged conversation instead of the approved hidden interactive channel. It was treated as exposed, rejected and never copied to Chrome, the host, a file, a command, Git or Handoff. The pending prompt was cancelled; `/etc/noteai/admin.env` and matching temporary files were independently confirmed absent. That credential must be rotated before any later attempt.
- **Cleanup completed:** the API-C Session Manager tab was closed. Both the current Cloud Assistant settings API and the legacy account behavior switch were set to disabled, and read-back returned `SESSION_MANAGER=disabled`. The temporary PostgreSQL client was removed; a follow-up Cloud Assistant command returned `Success` with exit code `0` for `psql` absence. No API-F session was opened, no audit account was created, no env file was written, and no image, container, service, migration, business-data write, supplier call, ALB/TLS-listener or DNS change occurred.
- **Remaining blockers:** API-C `admin.env`, API-F `api.env` and API-F `xhs.env` remain absent. Exact rows `0001`–`0008` still need an independent proof using a separately authenticated privileged database session that creates only a temporary `CONNECT` + schema `USAGE` + `SELECT public.schema_migrations` role and then revokes/drops it. Resume only with a newly rotated `ADMIN_PASSWORD` entered directly into a hidden terminal (never the conversation) and a separately approved/available privileged database authentication path.

### PROD-DEPLOY-CONFIG-WRITE-001 completion — 2026-07-24

- **Secret provisioning completed:** the user entered the rotated Admin credential and one-time transfer passphrase only through hidden ECS Workbench prompts. API-C `/etc/noteai/admin.env` is root-owned mode `0600` and contains exactly `ADMIN_PASSWORD` plus `DATABASE_URL`. API-F `/etc/noteai/api.env` is root-owned mode `0600` and matches API-C `/etc/noteai/api.env` by SHA256 without value disclosure. API-F `/etc/noteai/xhs.env` is a distinct root-owned mode `0600` file containing only `DATABASE_URL`; optional XHS token names were not fabricated because they were absent from the approved source. API-F role validation passed `55` API keys / `5` allowed Secret-like keys and `1` XHS key / `1` allowed Secret-like key, with distinct file identities.
- **Transfer failure handling:** the first decrypt attempt failed before passphrase verification because the encrypted source was root-only; the second used the wrong one-time passphrase and returned `bad decrypt`. Both attempts failed before env installation, and their temporary plaintext files were shredded/removed. The user then created and confirmed a new one-time passphrase in a hidden prompt. The regenerated 3,168-byte ciphertext matched its API-C SHA256 after transfer, decrypted successfully on API-F, and was deleted from both hosts. Final NoteAI transfer-temp count on API-C was `0`, and the API-F ciphertext path was absent.
- **Independent schema proof completed:** the authenticated RDS super-administrator session created one randomly named temporary login role with `NOSUPERUSER`, `NOCREATEDB`, `NOCREATEROLE`, `NOINHERIT`, `NOREPLICATION`, connection limit `1` and role-level `default_transaction_read_only=on`. It received only database CONNECT, schema USAGE and SELECT on `public.schema_migrations`. The audit session negotiated `TLSv1.3` / `TLS_AES_256_GCM_SHA384`, reported `READ_ONLY=on`, could select only `schema_migrations`, and proved the exact ordered set `0001_initial.sql`, `0002_shared_runtime_state.sql`, `0003_market_timing.sql`, `0004_xhs_freshness.sql`, `0005_idempotency_requests.sql`, `0006_model_usage_records.sql`, `0007_ai_operations.sql` and `0008_ai_operation_admissions.sql`. Acceptance returned PASS; grants were revoked through `DROP OWNED`, the role was dropped, and an independent role-catalog check returned absent.
- **Host and access cleanup completed:** no `psql` process remained; the temporary `postgresql-15.18-1.alnx4.x86_64` client RPM, install/remove logs and binary were removed. API-C and API-F each remained at zero running containers. Both the current `SessionManagerConfig.SessionManagerEnabled` switch and compatibility `sessionManagerStatus` switch were disabled; read-back returned `false` and `disabled`. No security-group rule changed.
- **Scope evidence:** no ACR login/pull, image/container/application service start, migration or business-data write, provider call, ALB/TLS-listener/DNS change or unrelated cloud mutation occurred. The only database authorization writes were the approved temporary audit role/grants and their complete removal. The only retained host writes are the approved role env files.
- **Release decision at completion time:** production configuration preflight was closed and `PROD-API-001` still required separate approval. That approval was later granted and the bounded result is recorded in the following checkpoint. API-F, Admin, XHS, ALB, public TLS/DNS and any real provider call remain later independent stops.

### PROD-API-001 checkpoint — 2026-07-24

- **Preflight passed:** API-C (`i-wz9j36od3nf2b1uw7bvg`) was confirmed `linux/amd64` with Docker `28.3.3`, zero running containers, zero listeners on ports `8000/18000`, and `27 GiB` free under Docker storage. `/etc/noteai/api.env` remained `root:root` mode `0600`, had exactly `55` keys with no duplicates, and the non-secret controls for automatic migration, API trend scheduling and cloud model mutation were disabled. The private ACR hostname resolved to a private address, and one pre-existing root Docker auth entry for that private hostname was present without reading or outputting its value.
- **Exact pull and identity passed:** only `noteai-prod-shenzhen-registry-vpc.cn-shenzhen.cr.aliyuncs.com/noteai/app@sha256:17706e1802afc136ac8f9a621d4199a7f9749da733290e923e42268eff42e0d1` was pulled. A post-pull read-back confirmed `linux/amd64`, OCI revision `a635692a899ee02c6905cd694611c14e0da4594a`, runtime role `api`, image user `noteai`, and the exact registry digest. The image remains in API-C's local Docker store.
- **Non-application command errors:** the first preflight invocation passed base64 text instead of decoded script content and failed with exit `127`; the corrected preflight succeeded without a host/application mutation. The initial post-pull RepoDigest formatter used an invalid Docker template and exited `64`; the pull had completed, and an independent simplified identity check then passed. Neither error started a container or contacted production data.
- **Canary start stopped fail-closed:** the first start command was rejected before container task creation because Docker's local logging driver cannot combine compression with `max-file=1`; the cleanup hook removed the non-running canary object. With the same security boundary and `max-file=2`, the second attempt created `noteai-api-c-canary`, but it exited within ten seconds. The script immediately removed it and returned `START_STATUS=FAIL:exited_early`. No third start was attempted because the approval required an immediate stop on failure.
- **Cleanup independently passed:** follow-up Cloud Assistant execution `t-sz06rvyyrg8feo` returned success/exit `0` with running containers `0`, canary objects `0`, listeners on `8000/18000` `0`, the exact digest image still present and `CLEANUP_STATUS=PASS`. API-F, Admin and XHS were not touched. No migration, provider call, ALB/TLS-listener/DNS change, production-data write or public ACR change occurred.
- **Acceptance not reached:** `/health/live`, `/health/ready`, runtime permission, bounded application-log and database read-only acceptance were not executed because the process did not remain running. `PROD-API-001` and `PROD-API-002` therefore remain unverified; nothing may be registered with ALB or promoted.
- **Required next approval:** a new bounded `PROD-API-001-DIAG-001` is required before another start. It should permit one identical loopback diagnostic start whose failure trap captures only sanitized, bounded startup diagnostics and the exit code before removing the container. It must retain the exact digest, non-root/read-only/cap-drop/no-new-privileges controls, database-session read-only default and all existing prohibitions. No retry or configuration change should be authorized until that evidence identifies the cause.

### PROD-API-001-DIAG-001 completion — 2026-07-24

- **Single diagnostic execution:** Cloud Assistant execution `t-sz06rvzerycfaps` targeted only API-C and returned `Success` / command exit `0`. Its precheck reconfirmed zero prior containers/listeners, the retained exact digest, `linux/amd64` and OCI revision `a635692a899ee02c6905cd694611c14e0da4594a`. Exactly one canary was started with loopback `127.0.0.1:18000`, UID/GID `999`, read-only root, `cap_drop: ALL`, `no-new-privileges`, no host PID/IPC, no devices, bounded tmpfs and PostgreSQL `default_transaction_read_only=on`.
- **Observed result:** the canary exited after four seconds with code `3`. Model-artifact verification completed first with no missing/invalid artifact. Uvicorn then began application startup and failed in `api.startup()` → `prompt_manager.audit_versioned_baseline()` → `db.fetchall()` while executing the read-only `managed_prompts` baseline query. PostgreSQL returned `psycopg.errors.InsufficientPrivilege: permission denied for table managed_prompts`; Starlette reported application startup failed and exited.
- **Root cause and boundary:** the first proven application blocker is production runtime authorization, not ACR identity, CPU architecture, model artifacts, migration execution, external supplier access or container hardening. Local migrations create `managed_prompts` but contain no runtime-role `GRANT` or default-privilege statement. This evidence proves the missing `managed_prompts` read permission only; it does not prove that all other runtime tables and sequences have correct grants. Granting only the first failing table could cause repeated startup/runtime failures and is not authorized.
- **Log safety:** only the last 80 timestamped lines were captured. URI credentials, Secret-like assignments, bearer values and long opaque values were redacted before output. No connection string, password, token, Cookie, private key or provider credential was exposed or retained in Handoff.
- **Immediate cleanup:** the diagnostic script removed the canary before completion and reported running containers `0`, canary objects `0`, listeners on `8000/18000` `0`, exact digest image present and `CLEANUP_STATUS=PASS`. A Cloud Shell disconnect occurred only after this successful cleanup evidence.
- **Independent cleanup:** after reconnecting without creating optional NAS, read-only Cloud Assistant execution `t-sz06rvzp3u07vnk` returned `Success` / exit `0` and independently reconfirmed running containers `0`, canary objects `0`, listeners `0`, exact digest image present and `INDEPENDENT_CLEANUP_STATUS=PASS`.
- **Scope preserved:** API-F, Admin and XHS were not touched. No migration, database grant/authorization or business-data write, provider call, ACR pull/login, ALB/TLS-listener/DNS change, security-group change or unrelated cloud mutation occurred. No health endpoint was called because the process never became available.
- **Release decision:** `PROD-API-001` and `PROD-API-002` remain unverified. The exact image must not be repulled or rebuilt for this blocker. Do not restart the API or grant `managed_prompts` alone.
- **Required next approval:** `PROD-DB-RUNTIME-PRIV-AUDIT-001` (read-only) should compare the live `noteai_app` table/sequence/schema privileges and object ownership against an explicit application-runtime access matrix derived from migrations and code, excluding migration authority and `schema_migrations`. It must output only object names and privilege booleans, never credentials or business rows. Any `GRANT`, `ALTER DEFAULT PRIVILEGES`, owner change or API restart requires a later explicit approval.

### PROD-DB-RUNTIME-PRIV-AUDIT-001 completion — 2026-07-24

- **Execution and isolation:** Cloud Assistant execution `t-sz06rw10k99ni0w` targeted API-C only and returned `Success` / exit `0`. A one-shot container used the already retained API digest with `--pull never`, UID/GID `999`, read-only root, `cap_drop: ALL`, `no-new-privileges`, bounded memory/CPU/PIDs/tmpfs and no published port. The database session verified `EXPECTED_ROLE=1` for `noteai_app` and `READ_ONLY=1`; it queried only `pg_catalog` metadata and PostgreSQL `has_schema_privilege`, `has_table_privilege` and `has_sequence_privilege` booleans. `schema_migrations` was explicitly excluded.
- **Schema and ownership:** `public` returned `OWNER=0`, `USAGE=1`, `CREATE=0`, which matches the runtime minimum. Every one of the 29 tables and five sequences returned `OWNER=0`, also matching least privilege. No DDL, migration, ownership or Schema CREATE authority is required or present.
- **Live table result:** every table below returned actual `SELECT=0`, `INSERT=0`, `UPDATE=0`, `DELETE=0`, `TRUNCATE=0`, `REFERENCES=0`, `TRIGGER=0`. The code-derived minimum column records the only booleans that should become true:

| Table | Required runtime booleans | Actual required booleans satisfied | Owner |
|---|---|---:|---:|
| `admin_sessions` | SELECT, INSERT, DELETE | 0 / 3 | 0 |
| `ai_operation_admissions` | SELECT, INSERT | 0 / 2 | 0 |
| `ai_operation_events` | SELECT, INSERT | 0 / 2 | 0 |
| `ai_operations` | SELECT, INSERT, UPDATE | 0 / 3 | 0 |
| `ai_provider_attempts` | SELECT, INSERT, UPDATE | 0 / 3 | 0 |
| `analysis_log` | INSERT | 0 / 1 | 0 |
| `chat_sessions` | SELECT, INSERT, UPDATE | 0 / 3 | 0 |
| `crawler_events` | SELECT, INSERT | 0 / 2 | 0 |
| `credit_transactions` | SELECT, INSERT | 0 / 2 | 0 |
| `credits` | SELECT, INSERT, UPDATE | 0 / 3 | 0 |
| `growth_records` | SELECT, INSERT, DELETE | 0 / 3 | 0 |
| `hot_keywords` | SELECT, INSERT, UPDATE | 0 / 3 | 0 |
| `idempotency_requests` | SELECT, INSERT, UPDATE | 0 / 3 | 0 |
| `keyword_snapshots` | SELECT, INSERT | 0 / 2 | 0 |
| `managed_prompts` | SELECT, INSERT, UPDATE, DELETE | 0 / 4 | 0 |
| `model_usage_records` | SELECT, INSERT | 0 / 2 | 0 |
| `notes` | SELECT, INSERT, UPDATE, DELETE | 0 / 4 | 0 |
| `prompt_history` | SELECT, INSERT | 0 / 2 | 0 |
| `saved_diagnoses` | SELECT, INSERT, DELETE | 0 / 3 | 0 |
| `subscriptions` | SELECT, INSERT, UPDATE | 0 / 3 | 0 |
| `system_settings` | SELECT, INSERT, UPDATE, DELETE | 0 / 4 | 0 |
| `tracked_notes` | SELECT, INSERT, UPDATE, DELETE | 0 / 4 | 0 |
| `usage_records` | SELECT, INSERT, UPDATE | 0 / 3 | 0 |
| `user_learn` | SELECT, INSERT, UPDATE | 0 / 3 | 0 |
| `user_memories` | SELECT, INSERT, UPDATE, DELETE | 0 / 4 | 0 |
| `user_sessions` | SELECT, INSERT, DELETE | 0 / 3 | 0 |
| `users` | SELECT, INSERT, UPDATE | 0 / 3 | 0 |
| `xhs_crawler_health` | SELECT, INSERT | 0 / 2 | 0 |
| `xhs_freshness_ledger` | SELECT, INSERT, UPDATE | 0 / 3 | 0 |

- **Live sequence result:** `analysis_log_id_seq`, `crawler_events_id_seq`, `hot_keywords_id_seq`, `keyword_snapshots_id_seq` and `prompt_history_id_seq` each returned `OWNER=0`, `USAGE=0`, `SELECT=0`, `UPDATE=0`. Runtime inserts omit each corresponding `BIGSERIAL` id, so only USAGE is required on each sequence; SELECT/UPDATE and ownership remain unnecessary.
- **Difference summary:** 29 / 29 runtime tables exist and match the migration inventory. Of 82 required table DML booleans, 0 are satisfied and 82 are missing. Of five required sequence USAGE booleans, 0 are satisfied and five are missing. Schema USAGE is satisfied. No excess table DML, sequence SELECT/UPDATE, Schema CREATE, object ownership, TRUNCATE, REFERENCES or TRIGGER privilege was observed. Total missing runtime booleans: `87`.
- **Cleanup:** the audit container removed itself and reported running containers `0` and audit objects `0`. Independent read-only Cloud Assistant execution `t-sz06rw17rdcgutc` returned `Success` / exit `0` with running containers `0`, audit objects `0` and API listeners on `8000/18000` `0`.
- **Scope and safety:** no business table row was selected; no Secret, environment value, connection string, credential, Cookie or provider token was read or output. No GRANT/REVOKE, `ALTER DEFAULT PRIVILEGES`, owner/Schema/database change, migration, application start/restart, ACR access, API-F/Admin/XHS action, provider call or ALB/TLS-listener/DNS/cloud-resource change occurred.
- **Release decision:** `PROD-API-001` remains blocked. This is a complete absent-runtime-grant condition, not an image defect and not a reason to rebuild or repull. A single-table grant would be incorrect.
- **Required next approval:** `PROD-DB-RUNTIME-PRIV-FIX-001` should apply, in one explicit transaction, only the 82 table DML and five sequence USAGE privileges listed above to `noteai_app`, then verify the same boolean matrix. It must exclude `schema_migrations`, all migration/DDL authority, Schema CREATE, ownership, TRUNCATE, REFERENCES, TRIGGER, sequence SELECT/UPDATE and `ALTER DEFAULT PRIVILEGES`; rollback is the exact inverse REVOKE set. It must not restart API-C. A subsequent digest-pinned API-C canary remains separately approval-gated.

### PROD-DB-RUNTIME-PRIV-FIX-001 completion — 2026-07-24

- **Target and identity:** the authenticated production DMS SQL console proved `current_database() = noteai` and `current_user = noteai_admin` before any authorization write.
- **Guarded execution:** the first attempt to submit the complete transaction was stopped at DMS's pre-execution review because DMS collapsed it into two combined review items. The confirmation was cancelled without executing it, and an immediate catalog query returned table grants `0` and sequence USAGE `0`, proving no partial authorization change.
- **Applied allowlist:** six separately previewed and confirmed statements granted exactly the audited matrix to `noteai_app`: 82 SELECT/INSERT/UPDATE/DELETE booleans across the 29 listed runtime tables and USAGE on `analysis_log_id_seq`, `crawler_events_id_seq`, `hot_keywords_id_seq`, `keyword_snapshots_id_seq` and `prompt_history_id_seq`. DMS reported each statement successful.
- **Full readback:** an independent `has_table_privilege` / `has_sequence_privilege` matrix returned expected tables `29`, exact table rows `29`, required DML `82`, forbidden table privileges `0`, sequence USAGE `5`, sequence SELECT/UPDATE `0` and object ownership `0`.
- **Schema boundary:** `public` Schema USAGE is `true`; Schema CREATE is `false`; Schema ownership remains `false`. Access booleans across SELECT/INSERT/UPDATE/DELETE/TRUNCATE/REFERENCES/TRIGGER on `public.schema_migrations` total `0`.
- **Rollback decision:** no verification mismatch occurred, so the pre-reviewed exact inverse REVOKE set was not executed.
- **Scope preserved:** no business row, Secret, connection value or provider credential was read or output. No DDL, migration, Schema CREATE, object owner, TRUNCATE, REFERENCES, TRIGGER, sequence SELECT/UPDATE, `ALTER DEFAULT PRIVILEGES`, API-C/API-F/Admin/XHS action, ACR action, service/container start, provider call or ALB/TLS-listener/DNS/cloud-resource change occurred.
- **Release decision:** the production runtime-authorization blocker is closed. Production API execution is still unaccepted because the canary was not restarted in this task.
- **Required next approval:** `PROD-API-001-RETRY-001` may use only the retained exact API digest on API-C to start one loopback canary under the existing non-root/read-only/cap-drop/no-new-privileges constraints, run zero-AI liveness/readiness, permission, bounded-log and read-only database acceptance, and remove the canary on any failure. It must not start API-F, Admin or XHS, run migration, call a supplier, or modify ALB/TLS/DNS.

### PROD-API-001-RETRY-001 completion — 2026-07-24

- **Single bounded start:** Cloud Assistant execution `t-sz06rw63en14mio` targeted only API-C (`i-wz9j36od3nf2b1uw7bvg`) and returned `Success`, exit `0`, duration `1m6s`. It used the retained private-ACR API reference `noteai-prod-shenzhen-registry-vpc.cn-shenzhen.cr.aliyuncs.com/noteai/app@sha256:17706e1802afc136ac8f9a621d4199a7f9749da733290e923e42268eff42e0d1` with `--pull never`; readback matched local image ID `sha256:b1983bab928ef93495d8be020030917d4ae54364234390047c3163af5414fedb`, `linux/amd64`, OCI revision `a635692a899ee02c6905cd694611c14e0da4594a`, runtime role `api` and image user `noteai`.
- **Runtime constraints:** exactly one container named `noteai-api-c-canary` remains running as UID/GID `999:999`, read-only root, unprivileged, `cap_drop: ALL`, `no-new-privileges`, bounded CPU/memory/PIDs, restricted `/tmp`, exactly one isolated data mount, restart policy `no`, and loopback-only `127.0.0.1:18000 -> 8000`. The database session enforces `default_transaction_read_only=on`; scheduler and cloud-model mutation controls remain disabled.
- **Zero-AI acceptance:** three rounds over approximately 60 seconds returned `/health/live = 200` and `/health/ready = 200`, with database and model core checks healthy. Model startup verification reported no missing/invalid artifacts and no configured external model base URL or S3 source.
- **Database and log acceptance:** the runtime role was `noteai_app`, transaction read-only was on, runtime SELECT and sequence USAGE were available, TRUNCATE and `schema_migrations` access remained absent, and sequence SELECT remained absent. Bounded startup/health logs contained 27 lines and zero migration, supplier-call or Secret-pattern matches.
- **Independent retention proof:** Cloud Assistant execution `t-sz06rw6swm3a58g` targeted only API-C and returned `Success`, exit `0`, duration `1s`. It reconfirmed the exact image reference, one running container, runtime constraints, loopback binding, liveness/readiness, database read-only boundaries and zero migration/provider/Secret log matches, then reported `POSTCHECK=PASS_RETAINED`.
- **Rollback state:** no failure occurred, so the approved failure cleanup did not run. The canary and its exact task-owned data directory remain intentionally retained for the next serial decision. A later failed promotion must remove only the promoted unit and preserve or restore this accepted loopback baseline unless separately approved otherwise.
- **Scope preserved:** API-F, Admin and XHS were not started or modified. No image pull/login, migration, database authorization/data write, supplier call, ALB/TLS-listener/DNS, security-group or unrelated cloud-resource change occurred.
- **Required next approval:** `PROD-API-C-PROMOTE-001` may convert only this accepted API-C exact digest/config into the managed API-C service while keeping it outside ALB, preserving the same non-root/read-only/cap-drop/no-new-privileges and zero-AI controls, and proving service/restart resilience plus repeated readiness. Failure must stop/remove the managed unit and retain or restore the accepted loopback canary. API-F, Admin, XHS, migration, supplier calls and ALB/TLS/DNS remain prohibited.

### PROD-API-C-PROMOTE-001 stopped before conversion — 2026-07-24

- **Read-only precheck passed:** Cloud Assistant execution `t-sz06rw7kcerrhfk` targeted only API-C and returned `Success`, exit `0`. It reconfirmed the single accepted canary, exact immutable image/local image ID, UID/GID `999`, read-only root, unprivileged/cap-drop/no-new-privileges constraints, one data mount, loopback `127.0.0.1:18000`, no `8000` listener, root-owned mode-`0600` API env file, and the original `noteai-api.service` as inactive/disabled with SHA256 `45be60996c7030cb05138ff3dc650bb5c51e71488512f465e9b3f18cf4f83736`.
- **Promotion failed before the mutation boundary:** execution `t-sz06rw82vf66dj4` targeted only API-C but exited `1` in under one second. `systemd-analyze verify` rejected `/run/noteai-api.service.prod-api-c-promote-001` as an invalid unit filename because its basename did not end in `.service`. This happened before the script stopped the canary, installed the new unit, enabled a service or opened port `8000`.
- **Automatic rollback result:** the fail trap reported `PROMOTION=FAIL_STARTING_ROLLBACK` and `ROLLBACK=PASS canary=unchanged original_unit=unchanged`. It did not start or remove a managed API container.
- **Independent post-failure proof:** execution `t-sz06rw8ar4s05j4` returned `Success`, exit `0`. The canary remains running and ready on `127.0.0.1:18000`; database/model checks remain healthy. `noteai-api.service` remains inactive/disabled at the original SHA256; `noteai-api-c` is absent and port `8000` has zero listeners.
- **Inert artifacts:** the script created `/etc/systemd/system/noteai-api.service.pre-prod-api-c-promote-001`, an inactive backup whose SHA256 exactly equals the original unit, and `/var/lib/noteai/data`, an empty non-symlink directory owned `999:999` mode `0750`. Neither is mounted or active. They are retained to avoid an unapproved post-failure mutation and can be safely revalidated/reused by a later retry.
- **Acceptance not reached:** no managed-service health rounds, database read-only check, log acceptance or service restart-resilience test ran. `PROD-API-C-PROMOTE-001` is therefore `STOPPED / NOT ACCEPTED`, not partially successful.
- **Scope preserved:** API-F, Admin and XHS were not touched. No image pull/login, migration, database authorization/data write, supplier call, ALB/TLS-listener/DNS, security-group or unrelated cloud-resource change occurred.
- **Required next approval:** `PROD-API-C-PROMOTE-RETRY-001` may perform one corrected retry with the same exact digest/config and rollback boundary. It must change only the temporary validation filename to a valid basename ending in `.service`, accept the existing same-hash inactive backup and empty production data directory after exact revalidation, and otherwise preserve every prior constraint. Failure must again stop immediately and leave/restore the accepted loopback canary. API-F, Admin, XHS, migration, supplier calls and ALB/TLS/DNS remain prohibited.

### PROD-API-C-PROMOTE-RETRY-001 stopped after application startup — 2026-07-24

- **Exact precheck passed:** execution `t-sz06rw8u5vhyygw` targeted only API-C and returned `Success`, exit `0`. The canary remained ready on `127.0.0.1:18000`; the original and backup unit SHA256 values both equalled `45be60996c7030cb05138ff3dc650bb5c51e71488512f465e9b3f18cf4f83736`; the production data directory was still empty, owned `999:999` mode `0750`; the managed service/container and port `8000` listener were absent.
- **Approved correction was exact:** the retry changed the temporary validation path from the invalid prior basename to `/run/noteai-api-c-promote-retry-001.service`, required the existing same-hash backup instead of overwriting it, and required the existing production directory to be empty with the exact accepted ownership/mode. All image, env, runtime, health, rollback and scope controls were otherwise unchanged.
- **Retry execution and failure locus:** execution `t-sz06rw90yygbqps` targeted only API-C. The corrected unit passed `systemd-analyze verify`; the only validation output was an unrelated Docker socket legacy-path advisory. Systemd installed/enabled the managed unit and journal evidence shows `Started noteai-api.service`, `Application startup complete` and Uvicorn listening on container port `8000`. The script then exited before printing any `HEALTH` line, placing the failure in the immediate `assert_runtime` call directly after `systemctl enable --now`. That call had no bounded wait and did not identify its failed predicate, so the exact predicate is `UNKNOWN`; Docker/container/port registration timing is the leading explanation, not an application-start failure.
- **Rollback passed:** the fail trap stopped/removed the managed service, restored the original unit, disabled it and restored the accepted canary. The managed container exited with status `137` during the rollback stop window; the bounded journal shows clean application startup followed by the deliberate rollback stop, not a spontaneous application crash.
- **Independent final state:** metadata-only execution `t-sz06rw9d9q8dngg` returned `Success`, exit `0`. It reported one running container `noteai-api-c-canary`, canary readiness HTTP `200` with database/model core checks healthy, managed container objects `0`, listeners `8000=0` and `18000=1`, production-directory entries `0`, and matching original/backup hashes. No safe journal line matched a Secret pattern.
- **Systemd residue:** the original unit file is restored and disabled, but systemd retains `active=failed`, `result=exit-code`, `main_status=137` from the rolled-back managed process. This is manager state only; no managed service or process is running. Clearing it with `systemctl reset-failed` is a mutation and was not performed after the stop.
- **Acceptance not reached:** no managed health round, database read-only probe, bounded log acceptance or restart-resilience check completed. The retry is `STOPPED / NOT ACCEPTED`.
- **Scope preserved:** API-F, Admin and XHS were not touched. No image pull/login, migration, database authorization/data write, supplier call, ALB/TLS-listener/DNS, security-group or unrelated cloud-resource change occurred.
- **Required next approval:** `PROD-API-C-PROMOTE-RETRY-002` may reset only the stale failed manager state, then make one further promotion attempt with the same exact digest/config. It must add a bounded wait for systemd active state, exact managed container registration and loopback port availability before running individually labelled runtime predicates; thereafter it may run the original three zero-AI health rounds, database read-only/log acceptance, one systemd restart and three post-restart rounds. Any failed predicate must be named, immediately roll back and restore the canary. API-F, Admin, XHS, migration, supplier calls and ALB/TLS/DNS remain prohibited.

### PROD-API-C-PROMOTE-RETRY-002 rolled back after successful promotion — 2026-07-24

- **Main promotion passed:** Cloud Assistant execution `t-sz06rwad152eozk` targeted only API-C and returned `Success`, exit `0`, duration `2m6s`. The stale systemd failure state was cleared, the corrected unit passed validation and bounded registration passed on attempt `2`. Every individually named runtime predicate passed before and after restart: exact image/digest/local image ID, UID/GID `999`, read-only root, unprivileged execution, `cap_drop: ALL`, `no-new-privileges`, resource limits, no devices/cap-add, loopback-only port, one bounded writable mount at `/var/lib/noteai/data` → `/app/model/data`, safe role controls, OCI revision and installed unit identity.
- **Acceptance inside the main execution passed:** three pre-restart and three post-restart health rounds each returned liveness/readiness HTTP `200` with database/model core checks healthy. Both database probes confirmed role `noteai_app`, transaction read-only `on`, runtime SELECT and sequence USAGE available, and TRUNCATE, `schema_migrations` SELECT and sequence SELECT absent. Both bounded log checks found zero migration, supplier-call and Secret-pattern matches. One explicit `systemctl restart` replaced the container, bounded registration again passed on attempt `2`, and all post-restart constraints passed.
- **Independent postcheck false negative:** read-only execution `t-sz06rwast6ensw0` returned exit `1` after its first 22 predicates passed. It named `mount-destination` as the sole failure because the independently composed check incorrectly expected `/app/data`; the approved unit and the successful main-script assertion correctly use `/app/model/data`. This was a verifier expectation error, not runtime drift, image drift, a health failure or a security-control failure.
- **Mandatory rollback honored:** because the approval required restoration after *any* failed assertion, no reinterpretation or automatic retry was allowed. Execution `t-sz06rwax8kkwhds` returned `Success`, exit `0`, duration `5s`; it disabled/stopped the managed service, removed `noteai-api-c`, restored the original unit SHA256 `45be60996c7030cb05138ff3dc650bb5c51e71488512f465e9b3f18cf4f83736`, reset failed state and recreated the exact accepted canary. Final rollback assertions reported the original unit disabled/inactive, managed container absent, canary running/ready and bound only to `127.0.0.1:18000`, with no listener on `8000`.
- **Final task status:** `STOPPED / ROLLED BACK / NOT RETAINED`. The main promotion mechanics and restart resilience are proven, but API-C is deliberately not left as a managed service because the later verifier error triggered the approved rollback rule. The retained loopback canary remains the only NoteAI application container on API-C.
- **Scope preserved:** API-F, Admin and XHS were not touched. No image pull/login, migration, database authorization/data write, supplier call, ALB/TLS-listener/DNS, security-group or unrelated cloud-resource change occurred.
- **Required next approval:** `PROD-API-C-PROMOTE-RETRY-003` may repeat the already successful bounded promotion once with the identical digest/config and assertions, correcting only the independent retained-state mount expectation to `/app/model/data`. It must preserve automatic rollback on any genuine failure and every existing prohibition. No further attempt is authorized by RETRY-002.

### PROD-API-C-PROMOTE-RETRY-003 stopped before conversion — 2026-07-24

- **Execution stopped pre-mutation:** Cloud Assistant execution `t-sz06rwbgbzrxxq8` targeted only API-C and returned exit `1` in under one second. All 21 named read-only preconditions passed, including the running exact-digest canary on `127.0.0.1:18000`, inactive/disabled original unit and same-hash backup, root-owned mode-`0600` env file, exact directory metadata, empty production data directory, no managed container and no listener on `8000`.
- **Exact failure locus:** the pre-state was correctly accepted as `inactive`, but the unchanged RETRY-002 procedure then unconditionally ran `systemctl reset-failed noteai-api.service`. Because the restored unit was inactive and not loaded in the systemd manager, systemd returned `Unit noteai-api.service not loaded`; `set -e` stopped the script before unit validation, canary removal, unit installation, managed-container creation or port `8000` activation.
- **Protection result:** the existing failure trap reported `ROLLBACK=PASS canary=unchanged original_unit=disabled_inactive`. No promoted unit or managed container was created, so no active rollback mutation was necessary.
- **Independent final proof:** read-only execution `t-sz06rwblcuha9kw` returned `Success`, exit `0`. It verified the original and backup SHA256 values, unit inactive/disabled, one exact-digest canary and zero managed objects, UID/GID `999`, read-only root, `cap_drop: ALL`, `no-new-privileges`, loopback-only `127.0.0.1:18000`, no `8000` listener, correct canary mount `/var/lib/noteai-canary-api-c-retry-001` → `/app/model/data`, empty production directory, liveness/readiness `200`, database/model core readiness, read-only database authority and zero migration/provider/Secret log matches.
- **Acceptance not rerun:** the six managed-service health rounds and explicit restart test were not reached in RETRY-003. RETRY-002 remains the evidence that the identical managed service and restart procedure works, but the current retained state is still the accepted loopback canary, not a managed service.
- **Scope preserved:** API-F, Admin and XHS were not touched. No image pull/login, migration, database authorization/data write, supplier call, ALB/TLS-listener/DNS, security-group or unrelated cloud-resource change occurred.
- **Required next approval:** `PROD-API-C-PROMOTE-RETRY-004` may repeat the identical proven promotion once while changing only stale-state handling: call `systemctl reset-failed` when the accepted pre-state is `failed`, and explicitly skip it when the accepted pre-state is `inactive`. The independently retained-state check must continue to require `/app/model/data`. Every existing rollback and prohibition remains unchanged.

### PROD-API-C-PROMOTE-RETRY-004 completion — 2026-07-24

- **Main execution accepted:** Cloud Assistant execution `t-sz06rwclkrs5edc` targeted only API-C and returned `Success`, exit `0`, duration `2m7s`. All 21 read-only preconditions passed. The unit pre-state was `inactive`, so the approved conditional stale-state branch explicitly skipped `systemctl reset-failed`; the named skip-state, action and final inactive-state assertions all passed.
- **Managed runtime constraints passed:** unit validation and bounded container registration passed. Before and after restart, every named assertion passed for the exact immutable digest and local image ID, UID/GID `999`, read-only root, unprivileged execution, `cap_drop: ALL`, `no-new-privileges`, no devices/cap-add, bounded PID/memory/CPU limits, bridge networking, bounded tmpfs, loopback-only `127.0.0.1:8000`, no `18000` listener, one writable mount `/var/lib/noteai/data` → `/app/model/data`, safe non-secret role controls, runtime-role label, OCI revision and exact installed unit.
- **Six-round acceptance passed:** three pre-restart and three post-restart health rounds each returned liveness/readiness HTTP `200`, `database=1` and `model=1`. Both database probes reported role `noteai_app`, `transaction_read_only=on`, runtime SELECT and sequence USAGE available, with TRUNCATE, `schema_migrations` SELECT and sequence SELECT absent. Both bounded log checks found zero migration, supplier-call and Secret-pattern matches.
- **Restart resilience passed:** one explicit `systemctl restart` replaced the managed container; bounded registration passed on attempt `2`; every post-restart security/runtime assertion and all three post-restart health rounds passed.
- **Independent retention proof passed:** read-only execution `t-sz06rwd1j16ap6o` returned `Success`, exit `0`, duration `1s`. All 34 postchecks passed: systemd active/enabled/success, managed and backup unit hashes, one running managed container, canary object count `0`, exact image/reference/OCI identity, all runtime constraints, loopback `8000`, no `18000`, the exact `/app/model/data` mount destination, liveness/readiness semantics, database read-only authority and zero migration/provider/Secret log matches. It concluded `POSTCHECK=PASS managed=1 active=1 enabled=1 bind=127.0.0.1:8000 canary=0 zero_ai=1 db_read_only=1`.
- **Final retained state:** `noteai-api.service` is enabled and active; `noteai-api-c` is the only running NoteAI application container on API-C and is reachable only through host loopback port `8000`. The prior canary container is absent. The original-unit backup and prior canary data directory remain as the bounded rollback baseline; no rollback was needed.
- **Scope preserved:** API-F, Admin and XHS were not touched. No image pull/login, migration, database authorization/business-data write, supplier call, ALB/TLS-listener/DNS, security-group, Session Manager or unrelated cloud-resource change occurred.
- **Required next approval:** `PROD-API-003` may use only the same corrected immutable API digest and already verified role-separated API configuration on API-F, pull through the existing private ACR path if the exact digest is not already local, then start one loopback canary outside ALB and run the same zero-AI liveness/readiness, database read-only, permission and bounded-log acceptance. Any failure must remove the API-F canary and leave the accepted API-C service unchanged. Admin, XHS, migration, supplier calls and ALB/TLS/DNS remain prohibited.

### PROD-API-003 stopped before pull — 2026-07-24

- **Attempt stopped safely:** Cloud Assistant execution `t-sz06rweqoepuiv4` targeted only API-F and returned exit `1` in one second. It proved the accepted Docker pre-state was `inactive`, briefly started Docker without enabling it, then stopped before image acquisition because the exact digest was not local and the existing private-ACR auth entry was absent. No `docker pull` or `docker run` occurred. The failure trap returned `CANARY_STATUS=FAIL_CLEANED docker_restored=inactive`.
- **Independent cleanup proof:** read-only execution `t-sz06rwexeoyafpc` returned exit `0`: Docker service `inactive`, `disabled`, `dockerd=0`, canary/data directory absent and listeners on `8000/18000` both zero. `docker.socket` was observed `active`; this can reactivate Docker on a later CLI request and must be explicitly restored/recorded at the beginning of the next bounded execution. The Docker service unit had entered and exited active in the same second with a successful final unit result, excluding daemon-start failure.
- **Independent blocker proof:** read-only execution `t-sz06rwf4ldp8b9c` returned exit `0`: API env regular/root-owned/mode `0600`, `55` keys, `0` duplicates, `35,264,532 KiB` free disk, `7,170,716 KiB` available memory and private ACR DNS all pass. Docker metadata reports zero container directories, no exact digest, and private registry auth-entry count `0`.
- **Scope preserved:** API-C was never selected and remains the accepted active managed service outside ALB. No API-F image was pulled, container/service started, migration/database write, Admin/XHS action, supplier call, ALB/TLS/DNS/security-group/Session-Manager/cloud-resource change or Secret read/output occurred.
- **Required next approval:** `PROD-API-003-AUTH-PULL-001` may first stop the task-left-active `docker.socket` and prove service/socket/dockerd are inactive, then receive a temporary ACR pull-only credential through the approved hidden Secret channel, log in only to the existing private ACR endpoint on API-F, pull only `app@sha256:17706e1802afc136ac8f9a621d4199a7f9749da733290e923e42268eff42e0d1`, immediately log out and remove the local auth entry, then run one loopback API-F canary under the already approved constraints and acceptance suite. Failure must remove API-F task state and restore Docker service/socket inactive; API-C must remain unchanged. No Admin/XHS, migration, provider, ALB/TLS/DNS or other cloud change is included.

### PROD-API-003-AUTH-PULL-001 paused at secure channel — 2026-07-24

- **Docker baseline restored:** Cloud Assistant execution `t-sz06rwgee8nbmyo` targeted only API-F, returned exit `0` in under one second and proved `service_before=inactive`, `socket_before=active`, `service_after=inactive`, `socket_after=inactive`, `dockerd=0`, containers/data directory/listeners `0`.
- **Secure channel unavailable:** the API-F Workbench/SSM terminal was opened without entering any credential, but connection creation failed closed with `The api is disabled by user behavior`. Session Manager remains disabled from the prior approved cleanup.
- **No credential or runtime action:** no ACR username/password was entered, read, copied or logged; no `docker login`, auth-file creation, image pull, Docker service start, container/canary start, database access, provider call or other deployment action occurred.
- **Required additional approval:** explicitly authorize temporary ECS Session Manager enablement for API-F only, followed by immediate disablement after hidden credential input and the already approved pull/canary flow. Security-group rules must remain unchanged.

### PROD-API-003-AUTH-PULL-001 completed — 2026-07-25

- **Access boundary and cleanup:** after explicit scope expansion, account-wide/full-region ECS Session Manager was temporarily enabled, but the only actual managed session connected to API-F `i-wz9bgztwf1tiakww2ops`. API-C and all other instances were not connected. No security-group rule changed. After acceptance, the switch was disabled and the control plane read back `会话管理已关闭`.
- **Credential handling:** the temporary ACR username/password were supplied only through hidden terminal prompts and were never printed, read back, copied into Git/Handoff or placed in Cloud Assistant command text. Failed attempts left target auth count `0`. The successful login used task-scoped root directory `/run/noteai-acr-auth`; after the exact pull, Docker logout and directory deletion completed. Independent final checks proved target auth counts `0` in root and `ecs-assist-user` configs and the task directory absent.
- **Exact pull passed:** the private ACR pull returned `EXACT_PULL=PASS`, `linux/amd64`, image ID `sha256:b1983bab928ef93495d8be020030917d4ae54364234390047c3163af5414fedb`, OCI revision `a635692a899ee02c6905cd694611c14e0da4594a` and exact repository digest `noteai-prod-shenzhen-registry-vpc.cn-shenzhen.cr.aliyuncs.com/noteai/app@sha256:17706e1802afc136ac8f9a621d4199a7f9749da733290e923e42268eff42e0d1`. Docker service/socket/dockerd returned inactive before the canary phase.
- **Canary acceptance passed:** `noteai-api-f-canary` started with `--pull never`, UID/GID `999:999`, read-only root, privileged false, `cap_drop ALL`, `no-new-privileges`, PID/memory/CPU limits, no device passthrough, one bounded `/app/model/data` bind mount and loopback-only `127.0.0.1:18000`. Three spaced liveness/readiness rounds returned `200/200` with database/model checks true.
- **Database and logs passed:** the canary session reported role `noteai_app`, `transaction_read_only=on`, reviewed runtime SELECT and sequence USAGE present, TRUNCATE/schema-migration SELECT/sequence SELECT absent. Bounded logs showed normal startup with zero migration, real-provider-call or Secret-pattern matches. Startup polling observed two transient connection resets before liveness became available; all acceptance rounds thereafter passed.
- **Independent retained-state proof:** exactly one running container exists on API-F and it is the accepted canary with the exact image/digest/revision and constraints above. Final live/ready remained `200/200`, database/model checks remained true, ACR auth residue remained zero and migration/provider/Secret log matches remained zero. API-F is outside ALB and is not yet a managed service.
- **Scope preserved:** API-C was not targeted and remains the accepted managed service on `127.0.0.1:8000`. Admin and XHS were not started. No migration, production-data write, supplier call, ALB/TLS-listener/DNS, security-group or unrelated cloud-resource change occurred.
- **Required next approval:** `PROD-API-F-PROMOTE-001` may convert only the accepted API-F exact digest/config into a managed service while keeping it outside ALB, preserving the same runtime constraints and zero-AI controls, and proving repeated readiness plus one service restart. Any failure must remove the managed unit and restore or retain the accepted loopback canary. API-C, Admin, XHS, migration, supplier calls and ALB/TLS/DNS remain prohibited.

### PROD-API-F-PROMOTE-001 completed — 2026-07-25

- **Read-only precheck passed:** Cloud Assistant execution `t-sz06rwkjpx6o9hc` targeted only API-F and returned success/exit `0`. It reconfirmed one exact-digest canary on `127.0.0.1:18000`, all runtime constraints, API env metadata `root:root 0600` with 55 unique keys/zero duplicates, live/ready `200/200`, absent managed unit, and absent production data directory.
- **Managed promotion passed:** execution `t-sz06rwl2iv11slc` targeted only API-F and returned success/exit `0` in `1m32s`. Unit `/etc/systemd/system/noteai-api.service` passed `systemd-analyze verify`, was installed with SHA256 `32d552e3daba29130677d98927dff1701711763ef63573d5d9c199c99bf44268`, and became enabled/active. Managed container `noteai-api-f` uses only the retained exact API digest with `--pull never`.
- **Zero-downtime conversion and rollback boundary:** the managed service was started and accepted while the canary remained healthy; only then was `noteai-api-f-canary` removed. Its data directory `/var/lib/noteai-canary-api-f-prod-api-003` remains as the bounded rollback baseline. Automatic failure handling would have removed the managed unit/container and recreated the canary with its prior exact constraints; no rollback was needed.
- **Runtime constraints passed:** before and after canary removal and again after restart, individually named assertions passed for systemd active state, exact image reference/local image ID/OCI revision, UID/GID `999:999`, read-only root, privileged false, `cap_drop ALL`, zero cap-add/devices, `no-new-privileges`, noexec tmpfs, PID/memory/CPU limits, exactly one `/var/lib/noteai/data` → `/app/model/data` writable mount, exact unit hash and loopback-only `127.0.0.1:8000`. Canary object count and listener `18000` are zero.
- **Health, database and logs passed:** three pre-restart and three post-restart liveness/readiness rounds returned `200/200` with database/model core checks true. Both database probes proved role `noteai_app`, transaction read-only, runtime SELECT and sequence USAGE present, with TRUNCATE, `schema_migrations` SELECT and sequence SELECT absent. Pre/post bounded logs showed normal startup with zero startup failure, migration, real-provider-call or Secret-pattern matches.
- **Restart and cleanup passed:** one explicit `systemctl restart noteai-api.service` recovered on bounded attempt `3`; post-restart constraints and three health rounds passed. Service is enabled/active with result `success`. Root/user private-ACR target auth counts remain `0`, `/run/noteai-acr-auth` is absent, Session Manager was never enabled and no ACR access occurred.
- **Independent retention proof passed:** separate read-only execution `t-sz06rwlg030hds0` targeted only API-F, returned success/exit `0` in `1s`, and reconfirmed the unit hash, one managed container, canary `0`, exact digest/revision, all hardening/resource/mount/loopback constraints, live/ready `200/200`, database read-only, 55-key env boundary, zero migration/provider/Secret-log matches and zero auth residue. It concluded `POSTCHECK=PASS managed=1 active=1 enabled=1 bind=127.0.0.1:8000 canary=0 zero_ai=1 db_read_only=1 rollback_data=retained`.
- **Scope preserved:** API-C and every other ECS instance were not selected. Admin and XHS were not started. No image pull/login, migration, production-data write, supplier call, ALB/TLS-listener/DNS, security-group, Session Manager or unrelated cloud-resource change occurred.
- **Required next approval:** `PROD-ADMIN-PREFLIGHT-001` should only read API-C's current managed API state, `/etc/noteai/admin.env` metadata/key names, exact Admin digest local-presence/auth prerequisites, loopback port availability, capacity and rollback baseline. It must not pull an image, log into ACR, start Admin, modify API-C, read Secret values, run migration/provider calls or change ALB/TLS/DNS/cloud resources.

### PROD-ADMIN-PREFLIGHT-001 completed — 2026-07-25

- **Execution boundary:** Cloud Assistant executions `t-sz06rxr3svg2scg` and `t-sz06rxratu1e7sw` each targeted only API-C `i-wz9j36od3nf2b1uw7bvg` and returned success/exit `0` in `1s`. Both were host-read-only. No ACR login/pull or registry request, Admin start, API-C modification/restart, migration, supplier call, Session Manager, security-group, ALB/TLS/DNS or other cloud-resource change occurred.
- **API-C retained state passed:** `noteai-api.service` remains enabled/active with result `success`; exactly one NoteAI container exists and it is `noteai-api-c` using the exact immutable API digest, local image ID `sha256:b1983bab928ef93495d8be020030917d4ae54364234390047c3163af5414fedb`, OCI revision `a635692a899ee02c6905cd694611c14e0da4594a`, runtime role `api`, UID/GID `999:999`, read-only root, privileged false, `cap_drop ALL`, `no-new-privileges`, bridge networking, resource limits and the single approved `/app/model/data` mount. Liveness/readiness returned `200/200`; 200 bounded log lines had zero migration, provider or Secret-pattern hits.
- **Admin env passed without value access:** `/etc/noteai/admin.env` is a non-symlink regular `root:root` mode-`0600` file. It contains exactly two unique approved key names, `ADMIN_PASSWORD` and `DATABASE_URL`, with zero invalid or duplicate declarations. No value, fingerprint or connection string was printed.
- **Image and ACR prerequisite finding:** neither exact Admin reference `sha256:d94bc4581e85a5b507415da2abc284c26e46288a746f91e951a43380d670c733` nor its accepted local image ID `sha256:2283095764622e373e30b51ba749819751e6bfb0c37c6bb82e2d3bfe4937760f` is present on API-C. Private ACR DNS resolves privately, but `/root/.docker/config.json` is a regular root-owned mode-`0600` file retaining exactly one private-ACR auth entry; the public-ACR entry and both credential-helper entries are zero, the `ecs-assist-user` config is absent, and `/run/noteai-acr-auth` is absent. The credential value was not output or reused.
- **Port, capacity and rollback passed:** API remains loopback-only on `127.0.0.1:8000`; Admin port `8001` and canary port `18000` are free. The host has 4 CPUs, approximately `6691 MiB` available memory and `26 GiB` free Docker storage. Current managed unit SHA256 is `872c44e87b938a2ecb459651953196925e72798fa67f4a904c05409fb350cd25`; original-unit backup SHA256 remains `45be60996c7030cb05138ff3dc650bb5c51e71488512f465e9b3f18cf4f83736`. The prior canary data and current production data directories remain present; no Admin unit or Admin container exists.
- **Probe interpretation:** the first script's aggregate marker printed `BLOCKED_BASELINE` only because it compared the cloud instance display name `noteai-prod-api-c` with the operating-system hostname `noteai-api-c`. The selected instance ID, exact API identity and every substantive baseline assertion passed; this naming mismatch is a probe expectation defect, not a production blocker. The real blockers are the absent Admin image and the stale root private-ACR auth entry.
- **Required next approval:** `PROD-ADMIN-AUTH-CLEANUP-001` should remove only the single private-ACR auth entry from API-C root's Docker config, preserve any unrelated Docker configuration, and read back target public/private auth/helper counts `0`, running Admin containers `0`, and unchanged API-C active/ready state. It must not log in to or access ACR, pull an image, start Admin, modify the API service, read or output credential values, run migration/provider calls or change cloud resources. A later exact-digest Admin pull/canary remains a separate approval after cleanup.

### PROD-ADMIN-AUTH-CLEANUP-001 completed — 2026-07-25

- **First attempt stopped before mutation:** Cloud Assistant execution `t-sz06rxrzykhff9c` targeted only API-C, passed the complete API/env/image precheck, then exited `1` because an embedded Python newline literal was parsed as an unterminated string. Python failed during parse, before opening or replacing the Docker config; no file, service, container, image or cloud state changed.
- **Exact cleanup passed:** corrected execution `t-sz06rxs5n9ivy0w` targeted only API-C and returned success/exit `0` in `1s`. It required one and only one normalized private-ACR key in root's `auths`, zero public target auth, zero target credential-helper entries, a regular non-symlink UID/GID `0:0` mode-`0600` config, and a healthy unchanged API before entering the mutation boundary.
- **Atomic preservation:** the command removed only that one private target key through a same-directory mode-`0600`, root-owned atomic replacement. It retained every other parsed Docker configuration object, performed an immediate semantic readback, and would have atomically restored the original bytes if the write/readback comparison failed. The accepted result reported `removed_private_auth=1`, `unrelated_auth_entries=0`, `target_helpers=0` and metadata `0:0:600`.
- **Credential cleanup proof:** a separate post-write parser read back root public/private auth counts `0/0` and public/private helper counts `0/0`. The `ecs-assist-user` Docker config is absent and therefore also reads `0/0/0/0`; `/run/noteai-acr-auth` remains absent. No credential value was printed, copied, reused or placed in Handoff.
- **API-C non-regression:** before and after cleanup, `noteai-api.service` remained enabled/active with liveness/readiness `200/200`. Unit SHA256 stayed `872c44e87b938a2ecb459651953196925e72798fa67f4a904c05409fb350cd25`; container ID and `StartedAt` were identical, proving no restart. Admin container/image counts remained `0`.
- **Scope preserved:** registry access attempted `0`, ACR login/pull `0`, Admin start `0`, API modification/restart `0`, migration/provider call `0`, and cloud-resource change `0`. Session Manager and security groups were not touched.
- **Required next approval:** `PROD-ADMIN-AUTH-PULL-001` may temporarily enable the approved hidden-input access path only for API-C, accept fresh temporary read-only ACR credentials without printing them, pull only the exact Admin digest `sha256:d94bc4581e85a5b507415da2abc284c26e46288a746f91e951a43380d670c733` through private ACR, immediately logout/remove all target auth residue, disable the temporary access path, then start one ALB-excluded Admin canary on loopback `127.0.0.1:8001` with the already reviewed non-root/read-only/cap-drop/no-new-privileges/resource/mount constraints. It must verify exact image provenance, zero-AI liveness/readiness, database read-only authority, bounded logs and unchanged API-C; any failure removes only the Admin canary. XHS, migration, supplier calls and ALB/TLS/DNS remain prohibited.

### PROD-ADMIN-AUTH-PULL-001 completed — 2026-07-25

- **Bounded private pull passed:** Cloud Assistant execution `t-sz06rxt3ie3c934` targeted only API-C and returned success/exit `0`. A root-owned pull process accepted the fresh one-hour credential only through an `ecs-assist-user` hidden prompt and FIFO under `/run`; the username was fixed to `cr_temp_user`, while the password was never printed, copied into a command, written to Handoff or exposed to Codex. It logged in only to the existing private ACR endpoint and pulled only `noteai-prod-shenzhen-registry-vpc.cn-shenzhen.cr.aliyuncs.com/noteai/app@sha256:d94bc4581e85a5b507415da2abc284c26e46288a746f91e951a43380d670c733`.
- **Identity and cleanup passed:** the retained image is `linux/amd64`, local image ID `sha256:2283095764622e373e30b51ba749819751e6bfb0c37c6bb82e2d3bfe4937760f`, OCI revision `a635692a899ee02c6905cd694611c14e0da4594a`, runtime role `admin`, and image user `noteai`. The temporary Docker config was logged out and removed; root and `ecs-assist-user` public/private target auth and credential-helper counts are all `0`. The FIFO/input helper was removed. ECS Session Manager was disconnected, disabled and read back as `会话管理已关闭`; no security-group rule changed.
- **Fail-safe canary retries:** several harness-only attempts were automatically removed before acceptance: an inspect-JSON stdin conflict, Docker's `no-new-privileges:true` representation, a false-positive on the non-secret `MEITUAN_TRAVEL_CLI` path, and a shell quoting/argument-placement defect. One valid safety finding also stopped a run: the unmodified Admin `DATABASE_URL` opened a read-write session (`transaction_read_only=off`). Every failed attempt printed `ADMIN_CANARY_ROLLBACK=PASS`, removed its container and isolated data directory, and left API-C unchanged. The final script passed local `bash -n` before execution and added only non-secret canary-scoped `PGOPTIONS=-c default_transaction_read_only=on`; it did not edit `admin.env`, database grants or RDS.
- **Final canary accepted:** execution `t-sz06rxusrcinjeo` returned success/exit `0` in `15s` and retains exactly one ALB-excluded container `noteai-admin-c-canary` on `127.0.0.1:8001`. It uses the exact Admin digest/image above, UID/GID `999:999`, read-only root, `privileged=false`, `cap_drop=ALL`, `no-new-privileges`, no added capabilities/devices, PID/memory/CPU limits, one bounded `/tmp` tmpfs, and one isolated UID-owned bind at `/app/model/data`.
- **Zero-AI/read-only acceptance:** three rounds returned `/health/live=200` and `/health/ready=200`. The database proof selected only `current_user`, `transaction_read_only` and the `schema_migrations` privilege boolean: user `noteai_app`, `transaction_read_only=on`, and `schema_migrations SELECT=false`. The bounded log set contained `12` lines with zero migration, supplier-call, error/traceback or Secret-pattern matches. No real AI, map, Meituan, XHS, crawler or payment call ran.
- **API-C non-regression:** the managed API service remained active, its container ID, `StartedAt` and unit SHA stayed unchanged, and liveness/readiness remained `200/200`. Admin remains loopback-only and outside ALB; no API-F, XHS, migration, ALB, TLS-listener, DNS, database grant or other cloud-resource change occurred.
- **Required next approval:** `PROD-ADMIN-C-PROMOTE-001` only: convert the accepted exact-digest Admin canary into a managed API-C Admin service while retaining loopback-only `127.0.0.1:8001`, the same hardening and canary-scoped read-only `PGOPTIONS`; verify repeated zero-AI readiness, database read-only state, bounded logs and one service restart, with rollback to the accepted canary on any failure. Enabling Admin write behavior, exposing it through ALB, starting XHS, migrations, supplier calls and ALB/TLS/DNS changes remain separate approvals.

### PROD-ADMIN-C-PROMOTE-001 completed — 2026-07-25

- **Pre-mutation gate passed:** main Cloud Assistant execution `t-sz06rxvw9s6lfk0` targeted only API-C (`i-wz9j36od3nf2b1uw7bvg`). Before the mutation boundary it proved the accepted canary still used exact Admin digest `sha256:d94bc4581e85a5b507415da2abc284c26e46288a746f91e951a43380d670c733`, local image ID `sha256:2283095764622e373e30b51ba749819751e6bfb0c37c6bb82e2d3bfe4937760f`, OCI revision `a635692a899ee02c6905cd694611c14e0da4594a`, role `admin`, UID/GID `999:999`, read-only root, `cap_drop=ALL`, `no-new-privileges`, bounded resources, loopback `127.0.0.1:8001`, database `transaction_read_only=on`, zero `schema_migrations` SELECT and zero target ACR auth/helper residue. API-C remained exact-digest, active and `200/200`. The new `.service` unit passed remote `systemd-analyze verify` before installation.
- **Managed service accepted:** the task installed root-owned mode-`0644` `/etc/systemd/system/noteai-admin.service`, unit SHA256 `c299059d167eab0863639355a6485094e58e3dca6bcec7adbe9f78a3857a1ab2`, then removed the canary and enabled/started exactly one managed container `noteai-admin-c`. The service is enabled/active, uses `--pull never`, the exact local image and `/etc/noteai/admin.env`, retains only loopback `127.0.0.1:8001`, and keeps `PGOPTIONS=-c default_transaction_read_only=on`. The isolated UID-owned data bind remains `/var/lib/noteai-admin-c-canary-data` → `/app/model/data`; preserving this already accepted path keeps rollback exact and does not grant database write authority.
- **Restart and zero-AI acceptance passed:** three pre-restart plus three post-restart `/health/live` and `/health/ready` rounds returned `200/200`. `systemctl restart noteai-admin.service` produced a new container ID and `StartedAt`, after which the unit stayed enabled/active and its SHA remained exact. Both database checks returned user `noteai_app`, `transaction_read_only=on`, and `schema_migrations SELECT=false`. Each bounded log sample contained `12` lines and passed error, migration, supplier and Secret-pattern scans.
- **API-C and scope isolation passed:** the API service stayed active; its container ID, `StartedAt`, unit SHA and `200/200` health remained unchanged before and after Admin restart. The old canary count is `0`, the managed Admin count is `1`, ACR auth residue is `0`, and Admin is outside ALB. No ACR access, API-F change, XHS start, migration, production data write, supplier call, ALB/TLS/DNS or other cloud-resource mutation occurred.
- **Independent retained-state proof passed:** read-only execution `t-sz06rxw92tywlq8` rechecked the enabled/active unit, exact unit/image/revision, single managed container, absent canary, runtime hardening, three additional `200/200` health rounds, database read-only state, `25` bounded log lines, zero supplier/Secret matches, zero target auth residue and unchanged healthy API-C. It returned `INDEPENDENT_RETAINED_STATE=PASS`.
- **Rollback state:** the main script would have disabled/removed the unit and recreated the exact accepted canary on any post-boundary failure. No rollback was needed. The exact image and isolated data directory remain locally available; Admin write enablement is deliberately not included.
- **Required next approval:** `PROD-XHS-PREFLIGHT-001` should be read-only and limited to API-F: verify its managed API remains unchanged, `/etc/noteai/xhs.env` permissions and key names, the exact XHS digest's local-presence/ACR-auth prerequisites, Docker/storage capacity, absence of XHS processes, default-suspended zero-provider-call contract, the minimum database-write matrix for a later bounded worker start, and rollback baseline. It must not log into or access ACR, pull an image, start XHS, read Secret values, write production data, call a supplier or modify ALB/TLS/DNS/cloud resources.

### PROD-XHS-PREFLIGHT-001 completed — 2026-07-25

- **Execution boundary:** Cloud Assistant execution `t-sz06rxxk20r1zb4` / command `c-sz06rxxk20h2dxc` targeted only API-F `i-wz9bgztwf1tiakww2ops`, returned success/exit `0` in `2s`, and ended with `PROD_XHS_PREFLIGHT=PASS readonly=1 xhs_started=0 acr_access=0 supplier_calls=0 production_writes=0`. No ACR request/login/pull, XHS container/service start, Secret-value read, production write, supplier call, migration, Session Manager, security-group, ALB/TLS/DNS or other cloud-resource action occurred.
- **API-F invariant passed:** `noteai-api.service` remained enabled/active with SHA256 `32d552e3daba29130677d98927dff1701711763ef63573d5d9c199c99bf44268`. Managed container `noteai-api-f` retained the exact API digest, local image ID and OCI revision; its container ID and `StartedAt` were identical before/after. Loopback liveness/readiness stayed `200/200`.
- **XHS env/auth/image facts:** `/etc/noteai/xhs.env` is a non-symlink `root:root` mode-`0600` file with exactly one unique allowlisted key name, `DATABASE_URL`. No value or fingerprint was read/output. Root and `ecs-assist-user` target public/private ACR auth entries and credential helpers are `0`, with no global credential store. The exact XHS digest `sha256:452c2faf7853ce58d93e43c05fb6217a9a6cc1e4c81345d8cef2f50acabd79af` and accepted local image ID `sha256:5b44114d4bd9c28a8e93c39140466c542e8babeead038fb0d1cfe45c3cd75966` are both absent on API-F; a later pull therefore requires fresh temporary pull-only authorization.
- **Stopped/capacity/rollback facts:** XHS-role and XHS-named container counts are `0`; installed/running XHS/trends/tracking/crawler systemd units are `0`; exact Worker process count is `0`. API-F has 4 vCPU, about 7 GiB total / 6 GiB available memory and 32 GiB free Docker storage, exceeding the 12 GiB rollback reserve. Rollback baseline is the unchanged API unit/container plus the current fully stopped/absent XHS runtime; an XHS failure must stop/remove only the new XHS process and keep collection suspended.
- **Fail-closed/zero-provider contract:** the exact image-source files remain byte-unchanged from application revision `a635692a899ee02c6905cd694611c14e0da4594a`. `spider_xhs_http.collection_suspended()` treats missing, empty, unknown and true-like values as suspended; direct traffic additionally requires role `xhs-http` and exact adapter `spider_xhs_http`. Tracking returns before due-note reads/writes when suspended. Compose/Render set suspension to `1`, disable browser fallback and expose no XHS health port. Targeted static regression ran 56 tests (`test_spider_xhs_http`, `test_render_deployment`, `test_production_readiness_gate`) with zero failures and no network/provider call.
- **Important process split:** `xhs-tracking` in its current default-disabled or suspended state reads only `system_settings` configuration and writes nothing. `xhs-trends`, however, deliberately records a suspended health/freshness cycle, writes deterministic `industry_baseline` rows when needed and writes `market_timing_snapshot.json`; therefore merely starting it is a bounded production-write action even with zero XHS traffic.
- **Minimum later trends matrix:** `system_settings: SELECT`; `hot_keywords: SELECT, INSERT, UPDATE`; `keyword_snapshots: SELECT, INSERT`; `xhs_crawler_health: INSERT`; `xhs_freshness_ledger: SELECT, INSERT, UPDATE`; sequences `hot_keywords_id_seq` and `keyword_snapshots_id_seq`: `USAGE`; one bounded writable mount for `market_timing_snapshot.json`. Snapshot upload URL must remain absent.
- **Minimum later tracking matrix when collection is eventually enabled:** `system_settings: SELECT, INSERT, UPDATE` for crawler config/session safety; `tracked_notes: SELECT, UPDATE`; `growth_records: INSERT`; `user_memories: SELECT, INSERT, DELETE`; `crawler_events: INSERT`; sequence `crawler_events_id_seq: USAGE`. While the crawler is disabled/suspended, reduce this to `system_settings: SELECT` and zero writes.
- **Live privilege comparison:** the metadata-only RDS probe forced `transaction_read_only=on` and read zero business rows. The shared `noteai_app` role has every required privilege above, but is broader than either Worker minimum on several audited tables: `system_settings DELETE`; `tracked_notes INSERT/DELETE`; `growth_records SELECT/DELETE`; `user_memories UPDATE`; and several operational SELECTs. The existing grants remain valid for the API role, but reusing the same connection identity for an externally facing Worker increases compromise blast radius.
- **Required next approval:** `PROD-XHS-DB-ROLE-001` should create a dedicated login role for XHS with only the reviewed table/sequence privileges, update only API-F `/etc/noteai/xhs.env` through the hidden Secret channel, validate `root:root 0600`/key names and prove exact privilege booleans under `default_transaction_read_only=on`, then remove any temporary administrative access. It must not access ACR, pull/start XHS, change schema/ownership/default privileges, read business rows, call suppliers or modify ALB/TLS/DNS/cloud resources. The exact-digest private pull and the serial trends/tracking starts remain later approvals.

### PROD-XHS-DB-ROLE-001 completed — 2026-07-25

- **Read-only gate retained:** Cloud Assistant execution `t-sz06rxyv82jnpj4` / command `c-sz06rxyv824obgg` targeted only API-F `i-wz9bgztwf1tiakww2ops`, returned success/exit `0` in `1s` and proved the managed API unchanged, `xhs.env` `root:root 0600` with only `DATABASE_URL`, XHS fully stopped, `noteai_xhs` absent and `noteai_app` unable to create roles.
- **Secret boundary and one safe retry:** account-level Workbench Session Manager was temporarily enabled and actual use was limited to API-F. The existing `noteai_admin` password and new `noteai_xhs` password were entered only through three non-echoing terminal prompts. The first transaction stopped on a PostgreSQL utility-statement parameterization incompatibility (`SyntaxError`) before env replacement; PostgreSQL rolled the transaction back, `noteai_xhs` remained absent, `xhs.env` still identified `noteai_app`, and all temporary files were removed. The corrected retry used safe SQL literal composition, repeated static syntax checks and did not reuse or persist either password.
- **Role and grant result:** login `noteai_xhs` now has `NOSUPERUSER`, `NOCREATEDB`, `NOCREATEROLE`, `NOINHERIT`, `NOREPLICATION`, connection limit `4`, zero inherited memberships and zero owned objects. It has database `CONNECT`, public-schema `USAGE` and no Schema CREATE. Tables are exactly `system_settings: SELECT,INSERT,UPDATE`; `hot_keywords: SELECT,INSERT,UPDATE`; `keyword_snapshots: SELECT,INSERT`; `xhs_crawler_health: INSERT`; `xhs_freshness_ledger: SELECT,INSERT,UPDATE`; `tracked_notes: SELECT,UPDATE`; `growth_records: INSERT`; `user_memories: SELECT,INSERT,DELETE`; `crawler_events: INSERT`. Sequences `hot_keywords_id_seq`, `keyword_snapshots_id_seq` and `crawler_events_id_seq` have only `USAGE`. `schema_migrations`, DDL, TRUNCATE, REFERENCES, TRIGGER, sequence SELECT/UPDATE, ownership, inherited membership and all other public-table DML remain false. A later wider catalog query found one reverse RDS-managed administrative edge from `noteai_xhs` to `noteai_admin`; it grants no role or privilege to `noteai_xhs` and is documented under `PROD-XHS-HEALTH-SELECT-GRANT-001`.
- **Atomic env result:** only API-F `/etc/noteai/xhs.env` was atomically replaced. It is still a non-symlink `root:root` mode-`0600` file with exactly one key, `DATABASE_URL`, and its parsed username is `noteai_xhs`. No password, connection string or Secret fingerprint was printed. The prior env copy and all helper scripts were removed.
- **Primary and independent verification:** the primary read-only login check forced `default_transaction_read_only=on` and passed `225` exact privilege predicates across `30` public table-like objects and `5` sequences. Independent Cloud Assistant execution `t-sz06ry0y81okoao` targeted only API-F, completed in `2s` with exit `0`, repeated the same `225` predicates, role attributes, membership/ownership, Schema CREATE and `schema_migrations` denial checks, and returned `INDEPENDENT_DB_ROLE_VERIFY=PASS`. It read only catalog/privilege metadata and zero business rows.
- **Host/service invariants:** API-F remained enabled/active and healthy with the same image and systemd unit, loopback live/ready stayed `200/200`, and XHS container/service count remained `0`. No ACR request, image pull, XHS start, migration, supplier call, business-row read/write, security-group change, ALB/TLS/DNS or other cloud-resource mutation occurred.
- **Session cleanup/read-back:** both API-F Workbench tabs were closed. The account-level Workbench passwordless Session Manager switch was disabled; a fresh page load and fresh API-F connection attempt returned `The api is disabled by user behavior`, proving the disabled state. The failed read-back tab was then closed. No API-C or other instance was connected and no security-group rule changed.
### PROD-XHS-AUTH-PULL-001 completed — 2026-07-25

- **Read-only gate:** Cloud Assistant preflight execution `t-sz06ry1wf3a39c0` targeted only API-F `i-wz9bgztwf1tiakww2ops`, exited `0` in less than one second and proved the exact XHS digest/local ID absent, XHS containers/services `0`, API-F managed/healthy with loopback live/ready `200/200`, private ACR DNS present, at least `33 GiB` Docker space, no task files and zero target public/private ACR auth/helper/global-store residue.
- **Credential boundary:** A fresh ACR `GetAuthorizationToken` credential was generated for instance `cri-xpuhaclqkxlwy47t`; its username and password were entered only through two non-echoing API-F prompts. Neither value was read, printed, copied into a command, stored in Handoff/Git or placed in a persistent Docker config. The task used only the private VPC registry hostname and an isolated mode-`0700` `/run/noteai-acr-auth` Docker config.
- **Exact pull and provenance:** API-F pulled `noteai-prod-shenzhen-registry-vpc.cn-shenzhen.cr.aliyuncs.com/noteai/app@sha256:452c2faf7853ce58d93e43c05fb6217a9a6cc1e4c81345d8cef2f50acabd79af`. Docker returned that exact digest. Image inspection passed `linux/amd64`, local ID `sha256:5b44114d4bd9c28a8e93c39140466c542e8babeead038fb0d1cfe45c3cd75966`, OCI revision `a635692a899ee02c6905cd694611c14e0da4594a`, source `https://github.com/iamyusen1314/noteai`, version `git-a635692-amd64-r1`, runtime role `xhs-http` and non-root user `noteai`.
- **Service boundary:** the helper never executed `docker run`, Compose or systemd XHS commands. Post-pull checks returned XHS container count `0`; API-F remained managed/healthy with live/ready `200/200`. No supplier request, database read/write, migration, business-row access, security-group, ALB/TLS/DNS or other cloud-resource change occurred.
- **Authentication and session cleanup:** Docker logout completed against the private registry; `/run/noteai-acr-auth` and `/run/noteai-xhs-pull.sh` were removed. Root and `ecs-assist-user` target auth/helper/global-store counts returned `0`. Account-level Workbench Session Manager was disabled and visibly read back as `当前Workbench免密连接开关已禁用`; the API-F terminal and credential-result page were closed. No ACR public endpoint or security-group rule was enabled.
- **Historical successor:** `PROD-XHS-TRENDS-CANARY-001` was subsequently approved with an explicit write cap, attempted once and stopped safely as recorded below. This historical line is not current authorization and must not be reused.

### XHS Trends validation chain — compact closed ledger (2026-07-25)

All tasks in this table are closed historical evidence. None is current authorization, none may be retried, and no row below authorizes a service start, supplier call, database operation or entrypoint exception.

| Task | Final status | Durable evidence |
|---|---|---|
| `PROD-XHS-TRENDS-CANARY-001` | `CLOSED / FAILED-SAFE / SUPERSEDED` | The first bounded attempt produced no business-row delta and left no container/process/snapshot residue; factual post-audit was `t-sz06ry61swjb37k`. Its former `BLOCKED` label is historical and no longer represents the validated Trends function. |
| `PROD-XHS-TRENDS-DIAG-001` | `COMPLETED / ZERO-WRITE PASS` | Execution `t-sz06ryabto0prls` proved the exact image/env/database path reached the first intended write fence under forced read-only mode, with suspended collection, zero supplier calls, API-F non-regression and cleanup. |
| `PROD-XHS-HEALTH-SELECT-GRANT-001` | `COMPLETED / PERMISSION PASS` | Exactly `SELECT` on `public.xhs_crawler_health` was added for `noteai_xhs`; the complete `225/225` table/sequence privilege matrix matched with zero excess privilege. |
| `PROD-XHS-TRENDS-CANARY-RETRY-001` | `CLOSED / FAILED-SAFE / SUPERSEDED` | Execution `t-sz06rydvacxq800` stopped before container creation because the one-off observer over-read `analysis_log`; zero business writes and cleanup were independently confirmed by `t-sz06rye9vze14ow`. |
| `PROD-XHS-TRENDS-CANARY-RETRY-002` | `COMPLETED / TRENDS FUNCTION + BOUNDED-DB PASS` | Execution `t-sz06ryf52b5569s` ran the exact-digest suspended Worker once; the Worker exited `0` without timeout/OOM. Approved deltas were health `+6`, ledger `6` updates with zero row delta, hot keywords `+90`, keyword snapshots `+90`, and the other `26` public tables had zero DML. Run `4e01f464-113b-48cc-985c-cbcecd89492c` covered six suspended domains; post-audit `t-sz06ryfdg1blz40` passed. |
| `PROD-XHS-TRENDS-SNAPSHOT-EVIDENCE-001` | `CLOSED / FAILED-SAFE / SUPERSEDED` | Execution `t-sz06rygeo3skxs0` was correctly rejected by the hardened entrypoint before exporter import; exporter calls and database writes were zero, cleanup passed, and post-audit `t-sz06rygn3y9xon4` passed. The evidence gap it exposed is closed by the next row. |
| `PROD-XHS-TRENDS-SNAPSHOT-EVIDENCE-ENTRYPOINT-OVERRIDE-001` | `COMPLETED / SNAPSHOT PASS / ZERO-WRITE / CLEANUP PASS` | The explicitly approved one-time exception ran only the exporter in `t-sz06ryhn1a9svls`. Schema `1`, six domains, `90` keywords, `37,743` bytes and SHA-256 `31b19d6351f48db762a4d98be173bcb72629691d4437230ba1ae6b839063d1bd` passed in process; all `30` public-table DML counters and retained totals `12/6/90/90` were unchanged. API-F remained loopback `200/200`; all temporary resources were removed. The exception is exhausted and permanently prohibited as a normal run mode. |

### PROD-XHS-TRENDS-MILESTONE-CLOSE-001 completed — 2026-07-25

- **Review method:** three independent read-only reviewers covered the Trends release gate, service-promotion architecture and overall production rollout order. They read Git/Handoff/code/deployment evidence only; no cloud, database, container, service, supplier or external provider action occurred.
- **A–F gate decision:** Trends core behavior under default suspension is `VERIFIED`; the approved four-table database boundary is `VERIFIED`; Snapshot schema/domain/count/hash/size is `VERIFIED`; zero supplier calls are `VERIFIED`; API-F non-regression is `VERIFIED`; cleanup is `VERIFIED`.
- **Functional boundary:** the verified path is the deterministic suspended/baseline pipeline, not real XHS acquisition. A real XHS session, signer clock, response schema, challenge/rate-limit behavior and live evidence quality remain `NOT VERIFIED`.
- **Safety boundary:** no open Critical blocker remains inside the suspended Trends functional milestone. For supplier enablement or long-lived promotion, High risks remain around the non-official interface/session, Cookie storage and Admin-log redaction, resource/restart/log/singleton controls, and the current combined Trends/Tracking database identity.
- **EntryPoint decision:** the override was one diagnostic-only exception, is now exhausted, and must never be used for normal diagnostics, scheduled execution, recovery or promotion. Normal XHS commands must pass the hardened role/adapter/command allowlist.
- **Promotion decision:** Trends has passed the application-function gate and may enter a separately approved promotion design/execution sequence, but it is not yet a promoted or long-lived production service. XHS and Tracking remain stopped.
- **Tracking decision:** Tracking is a separate process, call path, write set and product capability and cannot inherit Trends evidence. Its repository/isolated-PostgreSQL contract is now independently `VERIFIED`; real XHS and managed-service acceptance remain `NOT VERIFIED / NOT STARTED`.
- **Recommended operating model:** prefer an independent browserless Worker ECS with Trends and Tracking as separate singleton services. For initial production, use reviewed systemd timers that launch exact-digest `--once` containers, never overlapping, with no automatic immediate retry, explicit CPU/memory/PID/time/log limits, no inbound ports and fail-closed suspension. API-F is acceptable only for a separately approved short single-service soak after headroom proof; API-C should not add Worker load because it already hosts API and Admin. The current production path has no Chromium, so historical Chromium resource risk is not a current blocker.
- **Database isolation for promotion:** production `noteai_xhs` remains the least-privilege union for Trends and Tracking. The repository now defines an isolated `noteai_xhs_tracking` contract, but migration `0010` and production role changes are unapplied. Trends still requires its own isolated role contract; both services require separate root-owned `0600` env files and connection limits before promotion.
- **Rollback:** keep/set collection suspended, stop only the applicable timer/service, remove only its exact container and return to the proven zero-XHS-service baseline. Do not delete database evidence, downgrade schema, reactivate Chromium, reuse the entrypoint exception or affect API-C/API-F/Admin.
- **Rollout dependency:** product decision `PROD-XHS-FIRST-LAUNCH-SCOPE-DECISION-001` now requires both Trends and Tracking in the first release. A separately approved minimal real-XHS validation must precede long-lived Trends promotion; Tracking requires its own full two-stage acceptance and cannot inherit Trends evidence.
- **Global pre-cutover gates:** independent of this milestone, commercial 100-job durable admission/recovery, database/PITR and service rollback, cross-border/compliance closure, ALB health semantics, ALB/TLS/DNS configuration and production smoke remain unresolved according to the risk ledger. Admin remains internal/loopback unless separately approved.
- **Scope decision:** `PROD-XHS-FIRST-LAUNCH-SCOPE-DECISION-001` is `VERIFIED` by the product owner: first formal traffic must include XHS Trends, Tracking and every other first-launch commercial capability. XHS must not be marked `DEFERRED`, hidden or omitted to accelerate cutover.
- **Authority boundary:** the owner now delegates CTO approval for every finite, bounded and reversible operation required by the 17-row `internal_runtime` ledger, including exact migrations/roles, managed Secret delivery, immutable ACR publication, private service starts, isolated recovery and capped synthetic capacity work. The CTO must keep per-task write/cost ceilings and rollback evidence. Real suppliers/sessions/Cookies, public Admin, ALB/TLS/DNS/traffic, irreversible destruction, uncapped cost, new product decisions and any public-launch-complete decision remain outside this standing authority.

### XHS milestone status ledger

| Capability | Status |
|---|---|
| Trends suspended functional validation | `VERIFIED` |
| Trends bounded database writes | `VERIFIED` |
| Trends Snapshot evidence | `VERIFIED` |
| `xhs_crawler_health SELECT` permission repair | `VERIFIED / COMPLETE` |
| Trends managed-service promotion | `NOT STARTED / REQUIRES SEPARATE APPROVAL` |
| Tracking repository/isolated-PostgreSQL contract | `VERIFIED / NOT DEPLOYED` |
| Tracking real-XHS and managed-service acceptance | `NOT VERIFIED / NOT STARTED` |
| Real XHS supplier acquisition | `NOT VERIFIED / NOT RUN` |
| One-time EntryPoint Override | `CLOSED / EXHAUSTED / REUSE PROHIBITED` |
| ALB, TLS listener, DNS and production traffic | `NOT CONNECTED` |

### PROD-COMPLETE-FIRST-LAUNCH-001 — NoteAI complete commercial first launch

- **Status:** `IN PROGRESS / PLAN COMPLETE / EXECUTION NOT AUTHORIZED`.
- **Release decision:** `NO-GO`. DNS and real-user traffic remain prohibited until every first-launch row below is independently `VERIFIED`, production Smoke and 100-job capacity pass, and no unaccepted Critical/High risk remains.
- **Review method:** five read-only roles covered complete scope, Trends promotion, independent Tracking, commercial gates and rollout order. Evidence came from product/UI/API/Admin/Worker code, tests, task ledger and deployment documents. No code, cloud, database, container, service, ACR, supplier or traffic action occurred.

#### NoteAI首次商业上线功能矩阵

`PARTIAL` means useful implementation or evidence exists but the complete production acceptance is missing. Every “首发必须” row blocks cutover until `VERIFIED`.

| Capability | Scope | Current status | Existing evidence | Missing work | Acceptance | Blocks |
|---|---|---|---|---|---|---|
| Registration/login/profile/auth/authorization | 首发必须 | `REPO HARDENED / SECURITY REVIEW NO-GO / PROD UNVERIFIED` | Password/OTP/recovery/rate/revoke/consent/UI implementation and extensive offline security tests | H12 producer-contract correction, independent R12, migration, real SMS/email and controlled production acceptance | Positive/negative auth matrix, ownership/recovery and revoke/abuse tests pass in final environment | Yes |
| Diagnosis and V0.4 scoring | 首发必须 | `PARTIAL` | Composite scorer, artifacts, quality gates and API contracts exist | Final production provider/data-path and E2E evidence | Synthetic and capped real production flow returns correct, persisted, billable result | Yes |
| OCR, multi-image and video understanding | 首发必须 | `PARTIAL` | UI/API/tests exist | Private object storage, cross-node/restart/expiry and provider path | Authorized upload/processing/recovery/deletion work on both API nodes | Yes |
| Generation, rewriting, chat, notes, history, growth, memory and achievements | 首发必须 | `PARTIAL` | Visible UI and API/storage paths exist | Durable execution, full UI/E2E, retention and failure recovery | Every visible flow completes/reloads/resumes without loss or duplicate charge | Yes |
| Claude Gateway | 首发必须 | `PARTIAL` | Staging shared-control A/B/C and production service baseline exist | Exact Alibaba authority/readiness/timeout and 2–4 failure/scale gate | Capped real call plus scale/failure matrix, accounting and fail-closed behavior pass | Yes |
| Kimi/Moonshot, Amap and Meituan | 首发必须 | `NOT VERIFIED IN PRODUCTION` | Adapters/tests and production packaging exist | Exact credentials, quotas, timeout/cost/fallback and capped real flows | Each visible provider-dependent path passes with usage/cost attribution | Yes |
| XHS Trends suspended function | 首发必须 | `VERIFIED` | Function, bounded writes, Snapshot, zero supplier, API-F and cleanup evidence above | Do not repeat | Inherited accepted milestone remains immutable evidence | No, for this sub-gate |
| XHS Trends long-lived service | 首发必须 | `NOT READY` | Controlled `--once` function passed | Real XHS, lease/fence, idempotency, caps, split role, resources, logs, alerts, restart/rollback | Independent singleton managed service passes real bounded cycle and recovery | Yes |
| XHS Tracking | 首发必须 | `REPOSITORY + ISOLATED POSTGRESQL VERIFIED / NOT DEPLOYED` | Canonical URL, atomic per-call claim/lease, at-most-once admission, exact cap, stale/deletion behavior, split-role contract and three independent reviews | Production preflight/migration/role/digest, two-stage owned-note real test and managed singleton runtime | Independent 24h and 7d state transitions, ownership, idempotency, recovery and alerts pass | Yes |
| Real XHS supplier chain | 首发必须 | `NOT VERIFIED` | Browserless bounded adapter and offline signer tests exist | Session/schema/challenge/rate-limit/evidence-quality production proof | Separately approved request-capped read-only Trends proof and owned-note Tracking proof pass | Yes |
| PostgreSQL schema, roles and runtime data | 首发必须 | `PARTIAL` | SSL, migrations `0001`–`0008`, app/XHS role evidence exists | Final migrations, per-process roles, connection/failover/write-path proof | Least privilege and correct production writes/reconnect/failover pass | Yes |
| Durable jobs, Tair/cache, idempotency and failure recovery | 首发必须 | `REPOSITORY + ISOLATED POSTGRESQL VERIFIED / NOT DEPLOYED` | Default-disabled `202`, opaque refs, Outbox/fenced Worker contract, 3:1 fairness, replay/refund/manual-outcome and role/RLS evidence | Private object-store adapter, publisher/dispatcher/real processor, production migration/roles, runtime/alert/capacity proof | 0 lost/duplicate job, provider call or charge under crash/retry/disconnect in managed runtime | Yes |
| Essential Admin operations | 首发必须、private only | `PARTIAL` | Loopback Admin/auth baseline plus repository Secret/log redaction implemented | Independent review, final image/deploy and safe user/credit/job/provider/XHS operator acceptance | Private operator checklist and complete negative authorization matrix pass | Yes |
| Model artifacts and necessary offline processing | 首发必须 | `VERIFIED` for current image | V0.4 artifacts/manifests/SHA/mttravel gates passed | Revalidate only if release content changes | Final image carries exact accepted artifacts | Conditional |
| Private file/object storage | 首发必须 | `NOT READY` | Node-local upload/video path exists | OSS, signed private references, lifecycle, encryption and recovery | Cross-node/restart/expiry/delete and access-isolation tests pass | Yes |
| All first-launch visible UI entries | 首发必须 | `PARTIAL` | Security/retention/legal contract UI is implemented alongside existing product pages | Independent product/security review, dead-entry audit, mobile/browser and full commercial E2E | Every visible entry is usable and matches the approved contract; none is silently hidden | Yes |
| Credits, quota, subscription and cost controls | 首发必须 | `PARTIAL` | Credit/billing logic and tests exist | Durable-operation coupling, mixed balance/refund/reconcile, pricing-claim alignment | No double charge/loss; user/Admin/provider ledgers reconcile exactly | Yes |
| Real payment, refund and reconciliation | 首发必须 | `REPOSITORY CONTRACT VERIFIED / ADAPTER + PROD UNVERIFIED` | Provider-isolated orders/callback state machine/cash+entitlement ledgers/refund/reconciliation and negative role matrix pass offline/PG | Official adapter, dedicated runtime, merchant admission, sandbox/mock, callback reachability, production migration/ACL, alerts and capped real-money proof | Offline adapter fixtures, then sandbox/mock and separately approved real-money E2E reconcile exactly | Yes |
| API-C/API-F and AI/XHS Workers | 首发必须 | APIs `ZERO-AI READY`; Workers `NOT READY` | API-C/F loopback managed-service evidence exists | Final release, AI Worker pair, separate Trends/Tracking runtime and failover | All roles use immutable digests and pass restart/resource/health/failure gates | Yes |
| ALB, TLS, monitoring, alerting, backup and rollback | 首发必须 | `NOT READY` | Certificates and partial paid infrastructure exist | ALB/listeners, SLS/metrics/alerts, PITR, runbooks and drills | Alerts fire; restore/failover/rollback meet approved RTO/RPO | Yes |
| DNS, production Smoke and controlled traffic ramp | 首发必须 | `NOT STARTED / PROHIBITED` | Domain/DNS ownership exists | All prior gates, pre-DNS Smoke, rollback decision points | Authoritative DNS, full Smoke and ramp pass with no C/H risk | Yes |
| 100 simultaneous AI jobs | 首发必须 | `NOT READY` | Gateway rehearsal and ledger primitives exist | Durable queue/workers, quotas, synthetic load then capped calibration | 100 admitted within accepted SLO; 0 loss/duplicate/overcharge; stable resources | Yes |
| Privacy, terms, retention, refunds and cross-border data | 首发必须 | `REPO IMPLEMENTED / LEGAL UNVERIFIED` | Versioned data map/pages/machine contract and retention/export controls implemented | Independent technical review plus professional legal/provider confirmation and production lifecycle evidence | Approved documents and technical controls align end to end | Yes |
| Multi-member institutional seats, offline research/training tools, 1000-job future scale, BYOK/software KMS, public Admin, defensive `.com`, deeper chaos | 非首发功能 | `OUT OF FIRST-LAUNCH SCOPE` | Approved product contract and architecture records | Must not be exposed or represented as launch capability | Remain isolated; no dependency from a first-launch flow | No |

#### Completed evidence that must not be repeated

- Trends suspended behavior, four-table bounded writes, Snapshot schema/six domains/90 keywords/SHA/size, zero supplier calls, API-F non-regression and cleanup are `VERIFIED`.
- `noteai_xhs` health-table `SELECT` repair and its complete negative permission matrix are `VERIFIED`.
- The one-time EntryPoint Override is closed, exhausted and prohibited as a normal run mode.
- API-C/API-F/Admin loopback startup, restart and read-only acceptance; RDS SSL/current schema; current image architecture/provenance/SBOM/VEX/model/mttravel evidence remain accepted unless new source, migration or digest invalidates the relevant delta.
- Claude Gateway A/B/C staging evidence remains accepted; it does not replace the remaining production-scale/provider gate.

#### Critical and High cutover blockers

- **Critical:** no real payment/refund/reconciliation; auth/recovery implementation lacks independent and production/provider acceptance; no durable provider-independent AI job/Worker recovery chain; no proven private object storage/PITR; professional privacy/terms/refund/cross-border approval remains unresolved; no 100-job capacity proof.
- **High:** R13 closed the R12 structural-persistence and confidence findings at repository level, but real Claude/Kimi/Amap/Meituan and XHS paths are unverified; Trends is not promoted; Tracking's repository contract passed but its production migration/role/digest/real-XHS/managed runtime are unverified; production still uses the combined XHS identity; repository Secret/log/retention controls are not deployed; real PostgreSQL/migration/privileges remain unverified; priority-queue contract remains unimplemented; ALB/TLS/monitoring/rollback are not accepted.

#### Finite first-launch task packages in strict dependency order

“Write” below means production business-data or authorization write, not reviewed repository changes. Each execution package requires its own exact approval where marked.

| # / Task | Goal and current blocker | Change scope | Acceptance and rollback | Implementer / independent verifier | Supplier / Write / Cost / Approval |
|---|---|---|---|---|---|
| 1 `PROD-FIRST-LAUNCH-PRODUCT-CONTRACT-001` — `VERIFIED` | Four commercial contracts and the immutable launch matrix are frozen | Product/Handoff docs only | Product Director independently reviewed the revisions and the product owner approved all four; rollback is a new explicit product decision | Product owner + CTO / Product Director | No / No / ¥0 / Complete |
| 2 `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-009` — `COMPLETE / FAIL / NO-GO` | Independent review found `564/564` expert alias/noncanonical-role survivors and `15/15` lifecycle observations; positive-shape Medium closed, structural High retained | Read-only/offline review only; no implementation | Zero-survivor criterion failed, so review stopped without fix/retry; temporary DBs cleaned | Independent Security & Compliance Reviewers / main CTO reconciliation | No external call / no production write / ¥0 / Complete |
| 2A `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-010` — `IMPLEMENTED / OFFLINE SELF-VALIDATED` | Candidate fix closes all reproduced R9 expert/alias/lookalike paths; independent closure is still required | `model/api.py`, focused failing-first tests and checkpoint docs only; no DB/migration/permission change | R9 matrices and lifecycle are zero-survivor; canonical/historical public shapes and regressions pass; revert only H10 patch | Security Implementer / fresh independent Security & Compliance Reviewer | No external call / no production write / ¥0 / Complete |
| 2B `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-010` — `COMPLETE / FAIL / NO-GO` | Multi-glyph/edge Unicode aliases survive exact-key lookup and the one-glyph detector; structural High retained | Read-only matrices and temporary raw DB/reload/export probes only | Zero-survivor criterion failed; positive shapes passed; review stopped without implementation/retry and temporary DBs were removed | Independent Security & Compliance Reviewers / main CTO reconciliation | No external call / no production write / ¥0 / Complete |
| 2C `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-011` — `IMPLEMENTED / OFFLINE SELF-VALIDATED / REJECTED BY R11` | Exact raw-key contracts replace dependence on exhaustive Unicode spelling detection, but self-fixtures did not cover two real producer shapes | `model/api.py`, focused failing-first tests and checkpoint docs only; no DB/migration/permission change | H11 negative matrix/lifecycle are zero-survivor; R11 supersedes the stale positive-shape claim; revert only H11 patch if H12 cannot repair it | Security Implementer / fresh independent Security & Compliance Reviewer | No external call / no production write / ¥0 / Complete |
| 2D `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-011` — `COMPLETE / FAIL / NO-GO` | Negative matrices are zero-survivor, but actual expert-evidence and disabled fact-enrichment shapes are lost; dynamic map contract is inconsistent | Read-only schema/key matrices, real-producer probes and temporary raw DB/reload/export only | Approved-shape criterion failed; review stopped without implementation/retry and temporary DBs were removed | Independent Security & Compliance Reviewers / main CTO reconciliation | No external call / no production write / ¥0 / Complete |
| 2E `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-012` — `IMPLEMENTED / OFFLINE SELF-VALIDATED / REJECTED BY R12` | Typed-map and direct producer corrections work, but generic and real Chat producer surfaces remain lossy | `model/api.py`, focused failing-first tests and checkpoint docs only; DB/migration/permissions frozen | R12 supersedes H12 readiness; keep its valid corrections and do not deploy | Security Implementer / fresh independent Security & Compliance Reviewer | No external call / no production write / ¥0 / Complete |
| 2F `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-012` — `COMPLETE / FAIL / NO-GO` | Zero private survivor, but generic maps and actual Chat structured context lose approved data; NaN becomes maximum confidence | Fresh read-only producer/schema/key matrices and temporary raw DB/reload/export probes; no implementation | Approved-shape criterion failed; reviewers stopped without fix/retry and temporary resources were removed | Independent Security & Compliance Reviewers / main CTO reconciliation | No external call / no production write / ¥0 / Complete |
| 2G `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-013` — `INDEPENDENTLY VERIFIED / NOT DEPLOYED` | Minimal correction of the two R12 shape roots and non-finite-confidence integrity gap | `model/api.py`, focused failing-first tests and checkpoint docs only; DB/migration/permissions frozen | R13 accepted generic/typed maps, actual Chat Fact/market-source context and confidence through raw/reload/export; revert only H13 patch if a new conflict appears | Security Implementer / independent Security & Compliance Reviewers | No external call / no production write / ¥0 / Complete |
| 2H `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-013` — `COMPLETE / PASS / REPOSITORY VERIFIED` | Independently close R12's repository High/Medium without production acceptance | Fresh read-only three-path map, real ChatStart, confidence and four-phase lifecycle probes; no implementation | Zero private survivor and zero approved-shape loss independently passed; temporary resources removed | Independent Security & Compliance Reviewers / main CTO reconciliation | No external call / no production write / ¥0 / Complete |
| 2I `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-POSTGRES-REHEARSAL-001` — `COMPLETE / FAIL / NO-GO` | Mechanically prove migration `0009`, strict clocks/locks and exact privileges, and surface real PostgreSQL gaps before production | Disposable local PostgreSQL and synthetic roles/data only; no production/cloud | Mechanical gates passed; retention TEXT ordering High, phone CHECK Medium and migration-checksum Medium retained; exact resources removed after evidence | PostgreSQL/Migration Engineer / independent DB Security Reviewer | No provider / disposable synthetic writes only / ¥0 / Complete |
| 2J `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-014` — `IMPLEMENTED / REJECTED BY R14 / NOT DEPLOYED` | Candidate-close the original three PostgreSQL rehearsal findings | Migration `0009`, migration runner, focused tests, permission/checkpoint docs only | Valid partial controls remain, but paid deletion, single-read hash binding and phone writer parity failed R14 | Database/Security Implementer / fresh independent DB Security Reviewer | No external call / no database write / ¥0 / Complete |
| 2K `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-014` — `COMPLETE / FAIL / NO-GO` | Independently review H14 without granting PostgreSQL acceptance | Three read-only/offline constraint, migration-integrity and permission/regression streams | `0C / 1H / 2M`; implementation inputs unchanged; PostgreSQL still not started | Independent DB Security Reviewers / main CTO reconciliation | No external call / no database write / ¥0 / Complete |
| 2L `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-015` — `IMPLEMENTED / OFFLINE SELF-VALIDATED / NOT DEPLOYED` | Correct paid tombstone/purge, migration single-read binding, ASCII phone and deadline-whitespace parity only | `0009`, `db.py`, `account_security.py`, focused tests and contract/checkpoint docs; no PostgreSQL or permissions | Six failing-first contracts now pass; full `719` + `5` skips and readiness `86/86`; independent R15 still required | Database/Security Implementer / fresh independent DB Security Reviewer | No external call / no database write / ¥0 / Complete |
| 2M `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-015` — `COMPLETE / FAIL / NO-GO` | Independently accept or reject H15 without PostgreSQL or production access | Fresh read-only/offline lifecycle, constraint, immutable-byte, input-parity and permission/regression probes; checkpoint docs only | `0C / 1H / 3M`; accepted migration/input/permission controls remain, four retention-state survivors block acceptance | Independent DB Security Reviewers / main CTO reconciliation | No external call / temp SQLite only / ¥0 / Complete |
| 2N `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-016` — `IMPLEMENTED / OFFLINE SELF-VALIDATED / NOT DEPLOYED` | Close R15's exact delete-idempotency, SQLite upgrade/clock and purge-state findings | `content_retention.py`, `db.py`, unapplied `0009`, focused tests and checkpoint/contract docs; no PostgreSQL or permissions | Failing-first `9` failures → focused `4/4`; full `721` + `5` skips and readiness `86/86`; independent R16 still required | Database/Security Implementer / fresh independent DB Security Reviewer | No external call / temp SQLite only / ¥0 / Complete |
| 2O `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-REVERIFY-016` — `COMPLETE / FAIL / NO-GO` | Independently accept or reject H16 without PostgreSQL or production access | Fresh read-only/offline lifecycle, copy-fault, strict-clock, purge-state, migration/input and permission/regression probes; checkpoint docs only | `0C / 0H / 2M`: premature valid purge marker and SQLite lexical paid classification block acceptance; accepted roots/regressions remain | Independent DB Security Reviewers / main CTO reconciliation | No external call / temp SQLite only / ¥0 / Complete |
| 2P `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-HARDEN-017` — `COMPLETE / SUPERSEDED THROUGH H22/R22/R23` | Historical correction of R16 findings | Completed repository hardening, independent reviews and disposable PostgreSQL acceptance; no production permission change | R16 findings and subsequent identity survivors closed; do not repeat | Database/Security Implementer / independent DB Security Reviewers | No external call / synthetic local data / ¥0 / Complete |
| 3 `PROD-FIRST-LAUNCH-DURABLE-AI-001` — repository contract `COMPLETE / NOT DEPLOYED` | Promote the verified provider-independent `202`/Outbox/fenced-Worker/replay/refund contract into real storage, queue and managed runtime | Storage adapter, publisher/dispatcher/processor, production migration/roles, monitoring and tests | Repository/PG contract is closed; managed crash/retry/disconnect must still cause 0 loss, duplicate provider call or charge; disable async path/revert, never delete ledger | Backend/Worker Engineer / Distributed Systems & Billing Reviewer | No in completed offline phase / migration and providers later / queue cost TBD / Yes before prod |
| 4 `PROD-FIRST-LAUNCH-STORAGE-RECOVERY-001` — repository contract `COMPLETE / NOT DEPLOYED` | Promote the verified private object/lifecycle/recovery evidence contract into real OSS and one isolated RDS PITR drill | Production bucket/RAM roles, migration `0013`, lifecycle/version policy, monitoring and managed cross-node/restore proof | Repository/PG contract is closed; production object and restored DB inventories must reconcile exactly; disable admission/revert adapter and retain evidence | Storage/DB Engineer / Recovery Reviewer | No in completed offline phase / synthetic production writes only in later approved test / OSS ~¥65/mo and restore ≤¥200 historical cap, re-price / Yes before cloud |
| 5 `PROD-FIRST-LAUNCH-PAYMENT-001` — repository contract `COMPLETE / NOT DEPLOYED` | Promote the verified provider-isolated order/callback/refund/cash/entitlement/reconciliation contract through official adapter and production acceptance | Adapter, dedicated payment runtime, migration/ACL, monitoring and tests | Offline fixtures, sandbox/mock duplicates/refunds/reconcile, then separately approved minimal real-money E2E; disable entry, preserve financial ledger | Payment Engineer / Security & Finance Reviewer | No in completed offline phase; Adapay and cash later / provider fees TBD / Yes before external or prod |
| 6 `PROD-XHS-TRENDS-LONGRUN-CONTRACT-001` — `COMPLETE / REPOSITORY + DISPOSABLE POSTGRESQL VERIFIED / NOT DEPLOYED` | Closed daily singleton, per-call admission, exact caps, atomic evidence, snapshot integrity, health/log and dedicated-role contracts | Trends/runtime/unapplied `0011`/tests only; no supplier or live service | Full offline and PostgreSQL lifecycle/constraint/permission matrices passed; production remains suspended/stopped | XHS Engineer / Trends Safety Reviewer | No / synthetic disposable PostgreSQL only / ¥0 / Complete |
| 7 `PROD-XHS-TRACKING-CONTRACT-HARDEN-001` — `COMPLETE / REPOSITORY + DISPOSABLE POSTGRESQL VERIFIED / NOT DEPLOYED` | Closed Tracking atomicity, per-call claim, canonical URL, exact cap, stale outcome and operator truth | Tracking/API/Admin/UI/DB/tests; no supplier | Full simulated lifecycle, failure/deletion races, exact PostgreSQL constraints/role matrix and three independent reviews passed; service remains stopped | XHS Tracking Engineer / three independent Tracking reviewers | No / synthetic disposable PostgreSQL only / ¥0 / Complete |
| 8 `PROD-FIRST-LAUNCH-UI-ADMIN-CONTRACT-001` — `COMPLETE / REPOSITORY VERIFIED / NOT DEPLOYED` | Align every visible entry, pricing/retention/team/queue claim and essential private Admin operation | Frontend/Admin/tests | Passed at `d358114e`; do not repeat unless its code or contract changes | Frontend/Admin Engineer / Product & Security Reviewer | No / No / ¥0 / No external mutation |
| 9 `PROD-FIRST-LAUNCH-IMMUTABLE-RELEASE-001` | Produce one final source commit and immutable API/Admin/AI/Trends/Tracking digests | Code-integrated release, CI, build, SBOM/VEX, ACR | Exact revision/digests/AMD64/artifacts/tests/security evidence pass; retain prior digests for rollback | Release Engineer / Supply-chain Reviewer | No / No / build/registry cost TBD / Yes for ACR |
| 10 `PROD-FIRST-LAUNCH-PROD-FOUNDATION-001` | Provision/split AI Workers, Trends and Tracking singleton runtime, OSS/Tair/SLS/PITR/roles/alerts | Alibaba resources/config/DB roles; XHS still suspended | Resource/network/least-privilege/log/alert/backup baselines pass; stop new services and restore saved config | Cloud/DB SRE / Production Security Reviewer | No / auth/config writes / Worker pair historical ¥783.64/mo + OSS ~¥65 + SLS ~¥12, re-price / Yes |
| 11 `PROD-FIRST-LAUNCH-INTERNAL-DEPLOY-001` | Deploy final digests privately with XHS suspended | API-C/F, private Admin, AI Workers, stopped/suspended XHS services | Health/restart/read-only/non-regression and cleanup pass; pin previous digests/stop new roles | Release/SRE / Runtime Acceptance Reviewer | No / migration/config writes only / existing + incremental cloud / Yes |
| 12 `PROD-FIRST-LAUNCH-PROVIDER-CHAIN-001` | Verify Claude Gateway, Kimi, Amap and Meituan with strict request/token/cost caps | Exact production routes and synthetic account | Correct result, fallback, timeout and usage/cost ledger; disable provider/restore fail-closed | AI Integration Engineer / Provider & Billing Reviewer | Yes / synthetic result+ledger / capped fees TBD / Yes |
| 13 `PROD-XHS-TRENDS-REAL-SUPPLIER-READONLY-001` | Validate real XHS read path before service promotion | Service remains default-suspended; one explicitly supplier-enabled, DB-read-only exact-digest execution; max 34 sequential requests, no retry | Session/schema/challenge/evidence quality proven with redacted evidence; restore suspended and clean | XHS Operator / Trends Evidence Reviewer | Yes / No / egress/account risk, minimal / Yes |
| 14 `PROD-XHS-TRENDS-MANAGED-PROMOTE-001` | Promote Trends as independent managed singleton | One bounded real cycle, loser/reboot/kill/alert tests, then enable schedule | Exact caps/writes/idempotency/resources/recovery/alerts pass; suspend and stop only Trends | XHS/SRE / Trends Release Reviewer | Yes / Yes, capped / Worker+egress/log cost / Yes |
| 15 `PROD-XHS-TRACKING-REAL-XHS-CANARY-001` | Independently validate Tracking 24h and 7d on one owned note | Two separately authorized exact-digest runs, max two detail calls total | Atomic states, exact writes, ownership/idempotency/evidence pass; stop Tracking, retain audit rows | XHS Tracking Operator / Independent Tracking Evidence Reviewer | Yes / Yes, capped / minimal egress/account risk / Yes for each run |
| 16 `PROD-XHS-TRACKING-MANAGED-PROMOTION-001` | Promote Tracking as a separate managed singleton | Resource/restart/overlap/alert/rollback tests, then enable schedule | Two-stage service survives reboot/kill without duplicate side effects; stop only Tracking | XHS/SRE / Tracking Release Reviewer | Yes / Yes, capped / Worker+egress/log cost / Yes |
| 17 `PROD-FIRST-LAUNCH-CAPACITY-100-001` | Prove 100 simultaneous AI-job commercial target and failure recovery | Synthetic load first, then separately capped real-provider calibration | Admission/completion SLO, 0 loss/duplicate/overcharge, stable DB/queue/workers and alerts; stop load/scale back | Performance Engineer / Capacity & Finance Reviewer | Capped calibration only / synthetic+ledger writes / potentially high, budget first / Yes |
| 18 `PROD-FIRST-LAUNCH-ALB-TLS-001` | Configure pre-DNS edge without exposing business DNS | ALB, listeners/certs, health routes, security groups | Both APIs/failover/TLS/health semantics pass via controlled pre-DNS path; remove listener/targets | Cloud SRE / Network Security Reviewer | No / config only / ALB historical ~¥90/mo + LCU/EIP, re-price / Yes |
| 19 `PROD-FIRST-LAUNCH-PRE-DNS-SMOKE-001` | Execute complete commercial E2E, alert, backup and rollback drill | All visible flows with synthetic accounts, capped providers/payment and no public DNS | Full checklist passes on both nodes/Workers; no C/H risk; stop tests and revert by runbook | Release Manager / Cross-functional Gate Reviewer | Yes / Yes, capped / provider/payment/cloud cost / Yes |
| 20 `PROD-FIRST-LAUNCH-DNS-CUTOVER-001` | Authoritative DNS cutover and controlled traffic ramp | DNS only after all prior gates are `VERIFIED` | Authoritative records, Smoke/SLO/alerts and staged ramp pass; restore prior DNS/disable targets | Release Manager / Independent Production Reviewer | Real users after approval / Yes / DNS+traffic cost / Final explicit approval |

#### Unique next task and exact acceptance

- **Completed decision:** `PROD-FIRST-LAUNCH-PRODUCT-CONTRACT-001` is `VERIFIED`. The Product Director independently returned `REVISIONS REQUIRED / READY FOR PRODUCT OWNER SIGN-OFF`; the product owner then explicitly approved the four revised contracts and confirmed that Studio does not include real multi-member organization/seat functionality.
- **Studio:** ¥399/30 days and 1,250 credits per non-recurring 30-day purchase serve one NoteAI master account operating multiple XHS accounts. “Team/multi-account” describes the agency/high-frequency use case, not member invitations, Owner/Admin/Member RBAC, shared login or seat billing. Multi-brand, multi-member institutional seats may not be sold or promised before separate acceptance.
- **Retention/export:** saved notes, diagnosis reports and versions are archived; temporary original media, raw supplier responses, ordinary logs and financial records are excluded. Free archives remain online for 7×24 hours, then have a 7-day recovery period and are removed from rolling backups within 30 days. Archives created under any paid plan have no product expiry while the account exists; downgrade does not delete them and export remains available. User deletion becomes inaccessible from primary storage within 24 hours and is cleared from rolling backups within 30 days; account closure offers one full export, while legally required finance/security records are governed separately.
- **Priority queue:** all accepted AI jobs, regardless of plan, target a persisted job ID within 2 seconds for 99% of eligible requests, excluding user-network failure and announced maintenance. Professional and Studio jobs receive weighted priority: when both queues are runnable, at most three of every four new dispatch slots go to priority and at least one goes to standard, FIFO within each queue. This is internal scheduling priority, not “three times faster” and not a supplier-completion guarantee; users must see queued/running/succeeded/failed/refunded state.
- **Phone/recovery:** username/password login remains supported. A phone must pass SMS OTP before first binding, phone registration, OTP login or password recovery; unverified contacts cannot log in or reset an account. A verified phone may use password or OTP login without an OTP on every password login. Recovery uses verified SMS first and a pre-verified email as fallback; changing phone, resetting password and other high-risk actions require re-verification, otherwise manual identity appeal.
- **Security review history:** `VERIFY-001` found the original `11 High`; repeated bounded harden/reverify cycles reduced and closed R12's structural-persistence High plus non-finite-confidence Medium through H13/R13. The independently reviewed disposable PostgreSQL rehearsal then passed mechanical migration/permission gates but exposed a new retention-time High plus canonical-phone and migration-checksum Medium findings. None of this is deployment or production acceptance.
- **Repository and isolated-PostgreSQL status:** H17–H22, independent R22 and the final disposable PostgreSQL 16 rehearsal/R23 are `PASS / 0C / 0H / 0M`. R16's two Mediums and the later identity-resurrection survivors are closed; exact task resources are removed and Colima is stopped. Production remains at `0001`–`0008`, so this is not production migration or deployment acceptance.
- **Primary production serial gate:** `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-PROD-PREFLIGHT-001` is `BLOCKED / AUTHENTICATION UNAVAILABLE / ZERO MUTATION`. The bounded capability check found no authenticated Alibaba/production-database/SSH path. Resume only when that external condition changes; do not repeat the local check or simulate production evidence.
- **Completed offline task:** `PROD-XHS-TRENDS-LONGRUN-CONTRACT-001` is repository and disposable-PostgreSQL `VERIFIED / NOT DEPLOYED`; do not repeat its tests or rehearsal without a conflicting code/migration change.
- **Completed offline task:** `PROD-FIRST-LAUNCH-DURABLE-AI-CONTRACT-001` is repository and disposable-PostgreSQL `VERIFIED / NOT DEPLOYED` at application commit `f0aaa200a93e7feae1b587fb622313efaeb5e498`; do not repeat it without a conflicting code/migration change.
- **Completed offline task:** `PROD-FIRST-LAUNCH-STORAGE-RECOVERY-CONTRACT-001` is repository and disposable-PostgreSQL `VERIFIED / NOT DEPLOYED` at application commit `d2dfb376587c86031681c5d6b1a52f40d619032e`; do not repeat it without conflicting code/migration evidence.
- **Completed offline task:** `PROD-FIRST-LAUNCH-PAYMENT-CONTRACT-001` is repository and disposable-PostgreSQL `VERIFIED / NOT DEPLOYED` at application commit `4dbb56f2bf96896fba18b52736687a647fe693a2`; do not repeat it without conflicting payment code/migration evidence.
- **Completed offline task:** `PROD-FIRST-LAUNCH-PAYMENT-ADAPTER-001` is repository `VERIFIED / NOT DEPLOYED` at application commit `d5121c6d6e7044c6eca0c3047a4007aa14ba0661`; do not repeat it without conflicting adapter/runtime evidence.
- **Completed offline task:** `PROD-FIRST-LAUNCH-UI-ADMIN-CONTRACT-001` is `VERIFIED / NOT DEPLOYED` at application commit `d358114e37c1e85e0ed068c1aca3ff87bfc36b6f`; do not repeat it without conflicting UI/Admin code or product-contract evidence.
- **Current executable serial task:** `PROD-FIRST-LAUNCH-SEC-COMPLIANCE-PROD-PREFLIGHT-001`, currently `BLOCKED / AUTHENTICATION UNAVAILABLE / ZERO MUTATION`. `PROD-FIRST-LAUNCH-ADMIN-ROLE-CONTRACT-001` is already repository/disposable-PostgreSQL verified and must not be repeated. No dependency-free repository/internal-runtime task remains; production stays frozen until interactive Alibaba sign-in and a protected least-privilege database metadata credential/session are available.

## 7. Mandatory safety boundaries

All future sessions and all agents must obey:

- 不输出 Secret、Token、密码、私钥、Cookie 或完整连接字符串；
- 不读取或复制受控环境文件中的真实值；
- 不把 Secret 写入镜像、日志、Handoff、Git 或命令输出；
- 不使用 ARM64 镜像部署 x86 ECS；
- 不使用可变标签替代不可变 digest 进行 API-F 部署；
- 不执行数据库破坏性操作；
- 不导入 Staging 数据到生产；
- 不删除 RDS、Tair、ECS、ACR、ALB、证书、DNS 或其他资源；
- 不 force push；
- 不直接合并 main/master；
- 不切换 DNS，除非前置验收全部通过并获得明确批准；
- 不执行真实支付；
- 不进行大规模真实爬取；
- 不进行未经批准的高费用 AI 调用；
- 所有真实外部调用前必须输出 Live Run Plan；
- 所有云资源变更前必须说明对象、影响、费用、回滚方法；
- 所有生产写操作必须串行执行；
- 不允许多个 Subagents 同时修改同一云资源或同一代码区域。

Additional release controls:

- Do not expose or copy values from production environment files. Validate only key names, permissions, redacted fingerprints or provider-side results.
- Do not use `git-ff030f5`, `git-ff030f5-amd64-r2`, `git-ecc4702e`, `git-b6abaa7-amd64-r1`, `git-8f7d9c2-amd64-r1`, either `git-89e3505` local candidate, either retained `git-93b03d5` local image, any `git-33d04d4` image, any `git-ca02335` local image, `latest`, or any unverified tag for production release. `a635692a899ee02c6905cd694611c14e0da4594a` is the immutable source identity of the current local and ACR images; deployment-control commit `253fde55fa609676a9fca5c14fdf6aeceef74000` and later Handoff/VEX descendants are not image revisions. Local image IDs are not ACR digests. The one approved push is complete; any future ACR access or mutation requires separate approval. Only corrected VEX version 3 may be cited, and it does not authorize deployment. The Worker browser findings are not waived—they are avoided only by the browserless production role graph. Do not repeat symbol searches, parser/API fixtures or exploitability probes for those rows.
- Do not let execution agents work until the read-only audit stage has reconciled Git, ACR, ECS, RDS/Tair, networking, Render, TLS and DNS truth.
- Do not repeat already completed domain/certificate purchases, clean RDS schema migration, or production Gateway creation.
- Any status discrepancy stops execution and returns the affected task to `INVESTIGATING`.

## 8. Recommended new-session orchestration

Roles:

1. **Main Codex — CTO / Production Release Orchestrator:** owns the ledger, sequencing, approvals, evidence review and final independent acceptance.
2. **Repository & Git Auditor — read-only:** verifies branch, HEAD, worktree, commits, CI, deployment descriptors and code/revision relationships.
3. **Image & Supply Chain Auditor — read-only:** verifies ACR tags/digests/platforms, Docker metadata, model artifacts, `mttravel`, Git LFS and secret-free image history.
4. **Cloud Infrastructure Auditor — read-only:** verifies ECS/API services, RDS, Tair, VPC/routes/security groups, SNAT, PrivateZone, ACR access, ALB, TLS, DNS, billing inventory and traffic evidence.
5. **Test & Readiness Auditor — read-only:** identifies the minimum zero-AI checks, provider-call acceptance tests, smoke scope and regression evidence.
6. **Security Reviewer — read-only when needed:** reviews auth/admin/billing/payment, secret handling, logs, Gateway signatures, uploads and production exposure.

Repository/Git, image supply chain, cloud infrastructure and test/readiness audits may run in parallel because they are read-only and have distinct evidence boundaries. All production writes, image push, service starts, external provider calls, ALB/TLS changes, DNS changes and payment work must run serially. No Implementation or execution agent may start until the read-only auditors return structured evidence and the Main Codex marks the exact task `READY_TO_EXECUTE` with an approved plan.

## 9. New-session startup procedure

Read these files first:

1. `AGENTS.md`
2. `.codex/handoffs/current-task.md`
3. `.codex/notes/architecture-summary.md`
4. `.codex/notes/risk-register.md`
5. `docs/RENDER_DEPLOYMENT_GUIDE.md`
6. `docs/DEPLOYMENT_SECRETS.md`
7. `render.yaml`
8. `render.gateway.production.yaml`
9. `Dockerfile`
10. `tools/production_readiness_gate.py`

Then:

1. Verify current branch/upstream and worktree explicitly. `b06671fbcca51f884b04c86edcf116e373c6cfa8` is the current immutable application revision with five exact published ACR manifests; `a635692a899ee02c6905cd694611c14e0da4594a` remains only the historical OCI revision of the three running loopback services. Later Handoff/evidence commits are deployment-control provenance, not application revisions.
2. Verify that Handoff/evidence commits use `[skip render]` and did not trigger Render deployment; do not confuse a later checkpoint with the application revision.
3. Do not repeat the completed Trends diagnostic, Canary, Snapshot or milestone reviews unless current evidence conflicts. Any later review must be tied to a new exact task; use a Security Reviewer if credential, auth, billing, public exposure or image-layer scope appears.
4. Treat historical combined/API/Worker and `ca023355` reports only as superseded baselines. Current browserless reports are under `/opt/noteai-build/evidence-a635692-pillow-rebuild-r2/scans/`: API/Admin/XHS are each `4 Critical / 19 High`, with SBOM counts `174/174/175`, Secret `0`, fixed-version rows `0` and Pillow rows `0`. Do not silently suppress, merge, waive or replace these raw results.
5. Do not repeat the **PROD-IMG-002 push**, **PROD-IMG-003-CORRECTED-PULL-001** or **PROD-BROWSERLESS-VEX-DIGEST-REISSUE-001**, and do not reopen ACR public access without a new explicit approval. Corrected ACR digests, OCI provenance and VEX version 3 are verified. Version 2 remains digest-invalid and must never be used for release acceptance. Do not repeat the completed Pillow fix, three-role rebuild, residual Debian audit, residual decision or constraint proof.
6. The CTO may self-authorize repository, offline, isolated and read-only work only after root cause/state is confirmed, scope is minimal, and acceptance/rollback are documented. Production/provider/cloud mutation, irreversible work, uncapped recurring cost, product decisions, public DNS and real-user traffic remain outside that delegation.
7. Stop immediately on secret exposure, architecture mismatch, untracked cloud mutation, unverifiable digest, unexpected production traffic, failed zero-AI readiness, or any need to broaden scope.

The next session must not repeat any completed image build/push/pull/VEX, host preparation, RDS TLS/backup/encryption remediation, XHS Trends diagnostic/Canary/Snapshot evidence, health-table SELECT grant, one-time entrypoint override, H17–H22/R22/R23, disposable-PostgreSQL rehearsals, UI/Admin/browser-asset contracts, ACR publication, TEMP ACL closure or the verified production preflight. Their historical failed-safe attempts are closed or superseded. API-C and API-F retain enabled/active historical exact-digest API services on `127.0.0.1:8000`; API-C also retains the historical exact-digest Admin on `127.0.0.1:8001`. All remain outside ALB without public traffic. Payment/XHS/Tracking/AI Worker remain stopped or absent. `PROD-COMPLETE-FIRST-LAUNCH-001` is the sole umbrella task; the product contract and Gate 0 are `VERIFIED`. Production migrations `0009`–`0016` and their exact role/ACL deltas remain unapplied. The unique execution root is `PROD-FIRST-LAUNCH-PRODUCTION-SCHEMA-ROLES-001`.

## 10. Checkpoint condition

Historical production application release `a635692a899ee02c6905cd694611c14e0da4594a` remains the OCI revision of the three running loopback services. Current immutable application revision `b06671fbcca51f884b04c86edcf116e373c6cfa8` has verified five-role native AMD64 GitHub/builder evidence and five exact immutable ACR digests but is not deployed. API-C/API-F API and API-C Admin remain healthy and loopback-only; Payment/XHS/Tracking/AI Worker are stopped or absent; ALB, TLS listener, business DNS and production traffic are not connected. Completed Trends/permission/Snapshot, Durable AI, Storage, Payment, UI/Admin, Admin-role, browser-asset, supply-chain and production-preflight evidence remains closed and must not be repeated without conflicting evidence. Production has migrations `0001`–`0008`; `0009`–`0016` and final production roles/ACL remain unapplied. `PROD-COMPLETE-FIRST-LAUNCH-001` remains `IN PROGRESS / NO-GO`. Internal deployment readiness is `14/29 = 48%`; complete public launch readiness is `14/38 = 37%`; the unique execution root is `PROD-FIRST-LAUNCH-PRODUCTION-SCHEMA-ROLES-001`.

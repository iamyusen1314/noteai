# NoteAI 真实链路质量稳定化执行计划

更新时间：2026-06-28

目标：把 V0.4 composite 模型、事实源、多 agent、AI 诊断、爆文生成和对话优化串成稳定可交付链路。判断标准不是“模型能打分”，而是真实用户输入后能持续产出有价值、自然、可信、愿意付费的内容。

## 当前基线

- V0.4 composite 已通过生产训练门禁，API 默认接入 V0.4 主评分链路；v0.3 仅保留为 fallback/历史对照。
- Golden 当前为 `6694`，Preference 当前为 `24850`；第一批核心行业均达到当前生产接受线。
- Round13 已完成多候选择优：`/generate` 与 `/generate/stream` 已接入候选池、V0.4 选择器和 `selection_meta`。
- Round14 已完成旅行/家居/穿搭专项：真实探针 `12/12 ready`，失败 `0`，blocking `0`，均分 `73.718`；家居标题补修探针 `4/4 ready`，标题可读性问题 `0`。
- RQS-05/RQS-06 已完成 AI 诊断三入口与对话优化回归：v36 覆盖 6 个核心行业，直接生成 `36/36 ready`、二修/chat `12/12 ready`、全量 post-sanitize 标题问题 `0`。
- RQS-07 已完成真实链路 shadow/E2E 总验收：当前 v36 验收集 `48/48 ready`、失败 `0`、blocking `0`、标题可读性问题 `0`、总验收 gate `PASS`；报告见 `docs/RQS07_REAL_CHAIN_ACCEPTANCE_REPORT.md`。
- RQS-08 已完成上线前 CI/PR/部署核验：新增生产 readiness gate，当前 `42/42` 检查通过；GitHub main 分支保护、secret scanning、push protection、Dependabot security updates、production Secrets/Variables 均已核验。
- RQS-09 已完成 AI 诊断报告 124 维呈现修复：报告页不再用旧前端 5 维估算冒充 V0.4，已改为读取后端真实 `feature_schema`、`dimension_scores`、`feature_groups` 和 `top_feature_contributions`。
- RQS-10 已完成 AI 诊断报告可信度增强：专家诊断意见升级为“结论+证据+影响+建议”的结构化证据卡；市场时机降级为可解释趋势参考，展示热词样本来源、采集时间、样本量和可信度，不再用无来源/过期数据制造强结论。
- RQS-11 已完成 AI 诊断专家意见证据强绑定：每个 agent 的“证据/影响/建议/置信度”绑定 V0.4 分数、特征弱项、语义/视觉/趋势样本等真实来源，避免只展示模型自述。
- RQS-12 已完成每日行业热词证据修复：市场时机不再回退多天前旧样本；超过每日新鲜度要求或缺少当前行业样本时，前后端均停用趋势证据并显示明确说明。
- RQS-13 已完成市场时机独立云端数据管道：API 默认不再本地采集热词；独立 worker/cron 生成每日行业趋势快照，API 必须获取新鲜证据，否则显式失败。
- RQS-14/RQS-15/RQS-16 已完成市场时机采集质量治理：核心行业频道覆盖、热词清洗、质量分层、最低合格证据门禁、搜索推荐源和前端证据质量展示已接入。
- RQS-18 已完成 token 成本核算与前后台同步修复：Claude/Kimi 真实 token 用量写回 `usage_records`，用户前端和管理后台同源展示 tokens、模型调用数和成本模式；生产环境可通过 `NOTEAI_MODEL_PRICE_*_PER_1M_RMB` 计算真实模型成本。
- RQS-19 已完成商业定价成本模型：按 Claude 美元账单、Kimi 人民币账单、真实功能调用图和 30% 预留成本，建立 AI 诊断、爆文生成、对话优化、截图、图片、视频的双币种 token 预算和套餐建议。
- 母婴行业按用户要求冻结，不再继续专项 prompt、规则、特征或后处理优化；只保留现有安全边界和回归测试。
- RQS-22 已完成“告诉 AI 你的约束条件”真实链路修复：AI 诊断、爆文生成、对话优化三条链路均读取并执行约束；`不改标题`、`不改封面`、`固定发布时间18:00`、涨粉/带货/互动率/曝光量目标进入统一约束契约，硬规则有后端兜底。
- RQS-25 已完成市场时机无授权源云端兜底：生产不依赖昂贵/不可得的授权趋势源；独立 worker 每日公开采集，不足时生成 `industry_baseline` 分行业基线证据包，保证快照新鲜但不伪装成官方热搜。

## 执行纪律

- 每个任务必须有编号、状态、验收标准、验证证据和完成备注。
- 完成一项只更新对应任务状态，不把未完成项写成完成。
- `<60` 或灾难性空稿/格式错误硬拦；`60+` 轻修问题不硬拦，进入二修或对话优化。
- 事实源策略保持：餐饮/本地生活使用高德基础事实源；酒旅/旅行使用美团 `meituan-travel`；事实只能自然融入，不能形成机械信息块。
- 不为追分强塞规则。V0.4 62/124 维特征用于生成前规划、候选择优和解释二修，不把所有特征硬塞进正文。
- 所有真实链路验证必须记录失败数、blocking 数、标题可读性、分数分布、典型问题和 artifact 路径。

## 状态说明

- `[待处理]` 尚未开始。
- `[进行中]` 正在执行，尚未完成验收。
- `[已完成]` 已完成代码/文档/验证，并记录证据。
- `[需复测]` 已完成初修，但需要真实 API、真实素材或 E2E 再验证。
- `[冻结]` 用户明确要求暂不继续优化。

## 当前任务清单

| 编号 | 状态 | 任务 | 验收标准 | 完成备注 |
|---|---|---|---|---|
| RQS-00 | [已完成] | 固化本执行计划，并同步训练计划/交付台账口径 | 新增独立计划文档；旧 `blocked_need_labels` 口径改为当前 V0.4 production 状态；交付台账能指向本计划 | 2026-06-28 已完成：新增本计划；同步 `docs/V04_TRAINING_EXECUTION_PLAN.md` 顶部状态为 `production_training`；在 `DELIVERY_FIX_TRACKER.md` 登记 RQS 计划入口 |
| RQS-01 | [已完成] | 美食/高德事实源生成专项 | 高德事实自然融入；无“实用信息/地址/营业时间”机械块；真实探针失败 `0`、blocking `0`、标题问题 `0`；均分目标 `>=72`，低于 72 样本能给出明确可修原因 | 2026-06-28 已完成：高德事实自然句、标题断尾修复、套餐菜品事实一致性、重复事实句去重已接入。真实探针 v18 `24/24 ready`、失败 `0`、blocking `0`、均分 `73.564`、中位 `72.790`；post-sanitize 标题问题 `0`，事实边界/假菜名/信息栏污染 `0`。剩余风险：长禧家/点都德个别单候选 `68-71`，不硬拦，进入 RQS-05/RQS-06 的多候选选择优与对话优化兜底 |
| RQS-02 | [已完成] | 美妆生成专项 | 肤质/场景/成分/使用感表达自然；无无依据功效承诺；真实探针失败 `0`、blocking `0`；均分目标 `>=72` | 2026-06-28 已完成：美妆 brief/agent 方向按彩妆/唇妆 vs 防晒/底妆分流；补充价格/渠道自然写入、未提供试用周期/敏感反应/全天持妆清洗、标题半截和未提供经历标题修复。真实探针 v23 focus `18/18 ready`、失败 `0`、blocking `0`、post-sanitize 标题/事实边界问题 `0`、均分 `72.803`、中位 `72.661`、任务组最佳 `73.055/78.150/73.742`；Round12 商业槽位补测 `4/4 ready`、失败 `0`、post-sanitize 问题 `0`、均分 `72.615`、最佳 `75.284`。剩余风险：唇妆单候选最低 `67.603`，不硬拦，必须由多候选择优/对话优化继续兜底 |
| RQS-03 | [已完成] | 旅行/酒旅稳定性二轮 | 美团事实在前 120 字自然保留起价/评分/交通/权益等至少 3 项；扩大样本后失败 `0`、blocking `0`；均分目标 `>=72` | 2026-06-28 已完成：单酒店美团事实前置/嵌入、价格格式归一、标题 fallback、重复事实句去重、无依据价格/门票/旺季/延迟退房/儿童年龄/提前几周等清洗、多酒店早餐重复压缩已接入。真实链路 v30 `4/4 ready`、失败 `0`、blocking `0`、均分 `79.303`；latest post-clean v30 均分 `80.877`、bad_terms `0`；30 条酒旅大样本回归 `30/30 >=72`、均分 `73.446`、issues `0` |
| RQS-04 | [已完成] | 健身多场景真实验证 | 覆盖减脂、塑形、低冲击、办公室、器械/无器械、膝盖敏感等任务；动作覆盖不丢失；失败 `0`、blocking `0`；均分目标 `>=73` | 2026-06-28 已完成：新增健身场景化 brief，覆盖膝盖友好低冲击、办公室肩颈放松、弹力带臀腿塑形；补充动作事实槽位，清洗无来源周期/效果/医学化承诺，修复“新手3周/适合新”等标题半截。真实链路 v33 覆盖 3 个任务、18 个候选，`18/18 ready`、失败 `0`、blocking `0`、min `72.278`、mean `74.026`、median `73.519`、max `77.039`；post-clean mean `73.947`、`ge72=18/18`、issues `0`、bad `0`、body_gt480 `0`。剩余风险：个别单候选仍为 `72.x`，不硬拦，进入多候选择优/对话优化兜底 |
| RQS-05 | [已完成] | AI 诊断三入口回归 | 截图上传、手动上传、视频上传均走统一 V0.4 agent/事实源/历史偏好链路；三标题对应三篇不同正文；无空响应、无共享正文冒充成功 | 2026-06-28 已完成：新增后端合同测试，验证手动、图片/截图、视频三入口都会进入同一 `/analyze` V0.4 五 agent 链路，并把高德/美团事实源、长期偏好、图片描述、视频理解上下文传入 agent；三方案正文保持独立，不再共享正文冒充成功。v36 直接探针覆盖美食/旅行/穿搭/美妆/家居/健身共 `36/36 ready`、失败 `0`、blocking `0`、mean `73.642`、median `73.590`、min `67.276`、`32/36 >=72`、`36/36 >=60` |
| RQS-06 | [已完成] | 对话优化链路回归 | 60+ 不硬拦；用户提出“更自然/更短/更种草/更真实”等反馈后，改写不劣化且能记录偏好 | 2026-06-28 已完成：chat 增加分数回退安全阀，若改写比当前版本低超过 `2.0` 分或触发硬问题，自动回退并复核上一版标题/正文；离线 dependent 槽位同步修复 blocking 状态和 fallback 复核。v36 二修/chat 探针 `12/12 ready`、失败 `0`、blocking `0`、mean `76.211`、median `75.716`、min `73.357`、`12/12 >=72`。v36 全量 `48` 个 artifact post-sanitize 审计：blocking `0`、标题可读性问题 `0`、mean `74.284`、median `74.068`、min `67.276`、`44/48 >=72`、`48/48 >=60` |
| RQS-07 | [已完成] | 真实链路 shadow/E2E 总验收 | 覆盖 AI 诊断、生成、对话优化、事实源、候选择优；输出总报告，列明各行业分数、失败、blocking、剩余风险 | 2026-06-28 已完成：新增 `tools/real_chain_acceptance_report.py` 与报告 `docs/RQS07_REAL_CHAIN_ACCEPTANCE_REPORT.md`。当前 v36 验收集 `48/48 ready`、失败 `0`、blocking `0`、低于 60 分 `0`、标题可读性问题 `0`、mean `74.284`、median `74.068`、min `67.276`、`44/48 >=72`；覆盖美食/旅行/穿搭/美妆/家居/健身，每行业 8 条，AI 诊断三方案正文均保持独立。latest shadow QA 当前 v36 集合 `48/48` shadow ready、hard block `0`、avg V0.4 `73.804`、`39/48 >=72`。历史旧生成样本仍有 `15` 个 hard block，主要是旧产物结构化事实边界问题，已作为审计残留记录，不计入当前 v36 上线 gate |
| RQS-08 | [已完成] | 上线前 CI/PR/部署核验 | 单测、质量门禁、核心真实探针、GitHub CI 和部署配置均通过；无 secrets、无大数据误提交 | 2026-06-29 已完成：新增 `tools/production_readiness_gate.py`，本地生产 gate `42/42` 通过；CI 已接入生产 gate，并在 `main`、`codex/**` push 和 PR to `main` 运行；Docker 增加 `/app/scripts/docker_entrypoint.sh`，API/admin 容器启动前会校验/下载 V0.4 模型 artifact，生产 `NOTEAI_MODEL_ARTIFACT_REQUIRED=1` 时不允许模型缺失静默降级；GitHub main 分支保护、required `test`、strict、PR、linear history、admin enforcement、no force push/delete、conversation resolution 已核验；secret scanning、push protection、Dependabot security updates 已开启；production Secrets/Variables 名称已核验，无 secret 值进入仓库。报告见 `docs/RQS08_PRODUCTION_READINESS_REPORT.md` |
| RQS-09 | [已完成] | AI 诊断报告 124 维呈现修复 | 报告不再用旧 28/62 维前端估算冒充 V0.4；`/analyze` 返回 `feature_schema`、`dimension_scores`、`feature_groups`、`top_feature_contributions`；报告页清除 mock 残留并展示真实特征覆盖；新增回归测试 | 2026-06-29 已完成：后端新增 V0.4 报告元数据；修复 `_predict()` 在 V0.4 composite 路径未覆盖实时语义/封面/热词时机特征的问题；前端报告改为 7 个后端维度、动态雷达、124 维特征覆盖和主要缺口展示；清理“22:30/购物清单/赛道平均”等 mock 残留；未登录 fresh report 直接展开报告，不再被历史登录卡或等待态遮挡。验证：`114` 个相关单测通过、内联脚本语法通过、`py_compile` 通过、production readiness `42/42 PASS`、Browser 桌面/移动 QA 无 console error、无横向溢出、无 mock 泄漏 |
| RQS-10 | [已完成] | AI 诊断报告可信度增强：专家意见与市场时机证据化 | 专家意见不再浓缩成短句，必须展示结论、证据、影响和建议；市场时机必须展示数据来源、采集时间、样本量、来源分布、指标解释和可信度说明；无有效热词时不得显示伪强趋势或扣分系数 | 2026-06-29 已完成：四位诊断 agent 追加 V0.4 证据化输出契约；后端新增专家意见结构化解析/兼容兜底；`MarketTiming` 扩展样本量、最近采集、来源拆分、证据词、可信度说明等字段；热词匹配增加可行动词和品类相关过滤，纯数字/泛词/误判词不再进入证据；无有效命中时趋势参考为中性 `×1.00`。前端专家卡展示证据/影响/建议，市场时机卡展示“本地热词样本库、采集时间、样本量、来源分布、指标解释、偏旧提醒”。验证：`109` 个相关单测 OK、前端内联脚本语法 OK、长图 QA `/tmp/noteai-report-v2-complete.png` |
| RQS-11 | [已完成] | AI 诊断专家意见证据强绑定 | 每个 agent 的证据必须来自 V0.4 分数、特征弱项、行业槽位、语义/视觉/趋势样本等结构化来源；前端展示来源、当前值和基准；新增防退化测试，避免回到 `[object Object]` 或纯自然语言证据 | 2026-06-29 已完成：后端新增结构化证据池与绑定逻辑，并把 `/analyze` 五 agent 的 `features/domain/score` 传入专家意见标准化；前端专家卡展示 `V0.4证据`、来源、当前值、基准值；新增后端证据绑定合同测试和前端静态防退化测试。验证：`181` 个单测 OK、生产 readiness `42/42 PASS`、前端内联脚本语法 OK、结构化证据渲染抽样 OK、`git diff --check` OK |
| RQS-12 | [已完成] | 每日行业热词证据修复 | 市场时机证据必须来自当前行业且满足每日新鲜度要求；旧样本不再 fallback 成当前建议；采集器写入行业分类；部署镜像必须包含 Playwright/Chromium；新增防退化测试和生产门禁 | 2026-06-29 已完成：`get_top_keywords()` 默认不再回退旧日期；`compute_market_timing()` 要求 `30` 小时内有行业样本，否则返回 `data_stale=true`、`×1.00`、清空命中/建议词；`scheduler_a` 按 channel 写入 `category`；Docker 安装 Playwright Chromium；production readiness 新增依赖检查。验证：当前本地旧库 `美食` 返回 stale 停用；`184` 个单测 OK、production readiness `44/44 PASS`、前端脚本语法 OK、`git diff --check` OK |
| RQS-13 | [已完成] | 市场时机独立云端数据管道与强制取证 | API 不再默认启动本地热词采集；趋势采集作为独立 worker/云端 cron 运行，生成 JSON 快照或共享 DB；AI 诊断/生成在生产必须拿到当前行业新鲜证据，拿不到返回 `MARKET_TIMING_EVIDENCE_UNAVAILABLE` | 2026-06-29 已完成：新增 `market_timing_worker.py` 独立 worker；新增云端快照导入/导出、`NOTEAI_MARKET_TIMING_SNAPSHOT_URL`、刷新 webhook、严格取证开关；`docker-compose` 增加 `noteai-trends-worker`；API 启动默认 `NOTEAI_API_STARTS_TREND_SCHEDULER=0`；生产 env 文档和 `MARKET_TIMING_CLOUD_PIPELINE.md` 已补齐。验证：云端快照导入测试 OK、严格 503 测试 OK、docker compose config OK |
| RQS-14 | [已完成] | 市场时机爬虫数据清洗与核心行业覆盖 | 热词采集不再照单全收 homefeed 泛词；新增停用词/行业词/短语优先/泛词降权规则；美妆、家居、健身纳入核心采集频道；入库和云端快照导入均执行同一套清洗兜底 | 2026-06-29 已完成：`scheduler_a` 新增美妆/家居/健身频道，标题短语按实际出现次数记录、按质量权重排序；`hot_keywords` 新增 `clean_scraped_keyword_row()`，过滤 `真的/一个/这个/clean` 等泛词、剔除行业错配 token、保留 `芝士焗小青龙/新疆旅游攻略/夏天防晒底妆` 等行业信号；`APScheduler` 补入后端依赖。验证：新增 `tests/test_market_timing_keyword_quality.py`；相关 `7` 个单测 OK；最终 live smoke 不写库采集 `537` 条，核心行业美食/旅行/穿搭/美妆/家居/健身均有样本，`GENERIC_LEAK_COUNT=0`。残余风险：本轮 `hot_search=0`，趋势证据主要来自 homefeed 标题短语/分词，后续需继续增强热搜 API 命中或授权趋势源 |
| RQS-15 | [已完成] | 市场时机证据质量分层与最低样本门禁 | 热词不再只按数量/热度使用；每条热词写入 `quality_score/evidence_level/sample_count/quality_reason`；生产要求当前行业至少 `12` 条中等以上证据；前端展示合格证据数、强证据数和质量说明 | 2026-06-29 已完成：`hot_keywords` 增加质量字段迁移、质量分层、合格样本门禁、保守时机系数；`scheduler_a` 滚动深度改为 `NOTEAI_XHS_SCROLL_ROUNDS` 等 env 可调，默认 `10`；前端市场时机卡展示合格证据、强证据、质量说明；收紧美食 `厨` 和家居 `家` 单字误判。验证：相关 `9` 个单测 OK；live smoke 不写库采集 `559` 条，核心行业合格证据：美食 `36`、旅行 `41`、穿搭 `41`、美妆 `47`、家居 `21`、健身 `41`，均超过 `12` 条门禁；美食不再误收 `厨房`。残余风险：`hot_search=0` 仍未解决，当前趋势强度主要来自频道标题样本 |
| RQS-16 | [已完成] | 市场时机搜索推荐源补强 | 在热搜 API 不稳定命中时，新增小红书搜索推荐词作为 `search_recommend` 来源；过滤创作者侧/资料类噪声；source breakdown 可区分 homefeed、search_recommend、hot_search | 2026-06-29 已完成：`scheduler_a` 新增 `SEARCH_SEEDS`、`NOTEAI_XHS_SEARCH_DISCOVERY`、`NOTEAI_XHS_SEARCH_SEEDS_PER_CATEGORY` 等配置，解析公开 `search/recommend` JSON 的 `sug_items`；`hot_keywords` 新增 `search_recommend` 权重和 `博主/招募/互关/拍摄技巧/bgm/app/excel/ppt/pdf/手抄报/制作` 等噪声过滤。验证：相关单测 OK；短 live smoke 不写库采集 `422` 条，其中 `search_recommend=41`，`SEARCH_RECOMMEND_BAD_COUNT=0`，代表词包括 `美食探店路边摊`、`青海旅行攻略`、`夏季底妆推荐`、`装修避坑指南`、`减肥训练计划`。残余风险：真正 `hot_search` 仍为 `0`，但已不再完全依赖 homefeed |
| RQS-17 | [已完成] | hot_search 验证与授权趋势源预留接入 | 不把未配置授权源冒充成生产数据；真实验证现有小红书登录态下 `hot_search` 命中情况；新增 `authorized_trend` 可插拔一等趋势源，未来拿到官方/合规数据商接口后可直接进入同一证据门禁 | 2026-06-29 已完成：真实 Playwright 诊断显示现有 `xhs_state.json` 对 `user/me` 返回“登录已过期/无登录信息”，搜索页提示登录后查看，短 smoke 采集 `63` 条，其中 `search_recommend=41`、`homefeed=22`、`hot_search=0`；`hot_keywords` 新增 `NOTEAI_AUTHORIZED_TREND_URL/TOKEN/AUTH_HEADER/AUTH_SCHEME/TIMEOUT`，支持 JSON/file/HTTP 快照导入为 `authorized_trend`，证据优先级等同高置信趋势源，失败时记录 error 并继续走云快照兜底；`market_timing_worker` 先拉授权源再采集网页，授权源可用时即使网页采集为 0 也能导出快照；生产文档明确当前无官方授权源配置，URL 必须保持空值。验证：授权趋势 smoke `source_breakdown={'authorized_trend': 12}`、`is_trending_topic=1.0`；全量 `195` 个单测 OK、production readiness `47/47 PASS`、`git diff --check` OK |
| RQS-18 | [已完成] | token 成本核算与前后台同步审查 | Claude/Kimi API 返回的真实 tokens 必须写入账单表；用户前端和管理后台必须同源统计；后台能区分估算成本、真实模型成本和未配置单价的 token-only 成本；附加素材不得抢走主诊断 token 归属 | 2026-06-29 已完成：`usage_records` 新增 `estimated_cost_rmb`、`actual_model_cost_rmb`、`model_calls`、`model_names`、`cost_mode` 幂等迁移；`model_router` 和 direct Kimi Vision 调用写回真实 usage；`billing` 增加 active usage 归属、真实单价 env 计算和附加用量不接管主流程；用户前端增加本月 tokens 与明细 tokens，管理后台 overview/users/revenue/usage-stats 增加 tokens/模型调用展示；生产文档补充 `NOTEAI_MODEL_PRICE_*_PER_1M_RMB`。验证：新增 `tests/test_billing_token_cost.py` 覆盖 6 个成本同步场景；全量 `201` 个单测 OK、production readiness `47/47 PASS`、前后台内联脚本语法 OK、`git diff --check` OK |
| RQS-19 | [已完成] | 商业定价 token 成本模型 | 完整核算 AI 诊断、AI 诊断到对话优化、爆文生成、爆文生成到对话优化、截图、多图、视频等功能成本；输出套餐建议和积分扣费建议；提供可重复计算脚本 | 2026-06-29 已完成：新增 `docs/PRICING_COST_MODEL.md` 和 `tools/pricing_cost_model.py`。模型按 Claude USD、Kimi/Moonshot RMB 双币种核算，并用 `USD/CNY=6.8`、`1.3x` 预留倍率输出人民币毛利预算；最初静态估算显示 AI 诊断标准约 `¥0.50-0.55`、P95 `¥1.00+`，后续真实完整链路探针已将 AI诊断预算修正为约 `¥1.45`、扣费价 `6` 积分；爆文生成标准 `¥1.21-1.28`、P95 `¥2.50`，对话深度重写标准 `¥0.63`、P95 `¥1.38`。RQS-21 已将套餐改为月度积分制 |
| RQS-20 | [已完成/已被RQS-21替代] | 积分充值与消耗价格体系落地 | 将 RQS-19 成本模型落到系统：正式套餐去掉真实无限；AI 诊断/爆文生成/对话深度重写/截图识别按新积分扣费；充值包写入后端 API 和前端定价页；后台收入按真实支付金额核算，管理员赠送不计收入 | 2026-06-29 已完成初版：`OPERATIONS` 更新为 AI诊断6积分、爆文生成8积分、对话深度重写3积分、截图0.5积分；`credit_transactions` 新增 `paid_rmb/package_id/payment_ref`，`topup` 与 `gift` 分流；管理后台 revenue 改读 `paid_rmb`，手动加积分改为赠送。随后 RQS-21 将“套餐次数 + 充值补量”改为“月度套餐积分 + 充值积分补扣” |
| RQS-21 | [已完成] | 月度积分制套餐与透明扣费前后端落地 | 用户套餐不再锁死 AI诊断/爆文生成次数；统一用月度积分承载功能消耗，充值积分长期有效；前端必须公开各功能扣分标准、套餐可用次数示例和充值包可用次数示例；后端扣费、退款、管理端和用户端展示同源 | 2026-06-29 已完成：`TIERS` 改为免费12积分、创作者¥99/260积分、成长¥199/560积分、专业¥299/900积分、工作室¥399/1250积分；`CREDIT_PACKAGES` 改为30积分¥12、100积分¥39、300积分¥109、800积分¥279；`check_and_deduct` 改成先扣本月套餐积分、不足时混合补扣充值积分；失败退款支持套餐积分、充值积分和混合扣费恢复；`/billing/plan` 返回月度积分、总可用积分、功能扣分表和扣费政策；前端定价页、个人中心、余额不足弹窗、管理后台套餐设置和用户详情均改为透明积分口径；新增/更新单测覆盖纯套餐、纯充值、混合扣费和退款 |
| RQS-22 | [已完成] | 用户约束条件真实执行链路 | “告诉 AI 你的约束条件”不能只是 UI 摆设；AI诊断、爆文生成、对话优化均必须传入 `user_constraints`；固定18点要影响 `local_time`；对话优化继承诊断/生成约束；`不改标题` 有后端硬守卫；响应回传约束契约供前端/历史链路继续使用 | 2026-07-01 已完成：前端新增 `formatLocalTimeWithConstraints()` 并在诊断、生成、诊断转对话、生成转对话、会话恢复、笔记库转对话传递约束；后端新增统一约束归一化、约束契约、提示注入和标题硬守卫；`/analyze`、`/generate`、`/generate/stream`、`/chat/start` 已接入；API 已重启。验证：`.venv/bin/python -m py_compile model/api.py tests/test_api_contracts.py` 通过；约束单测 `3/3 OK`；前端内联脚本语法 `checked 1 inline scripts`；OpenAPI 确认 `AnalyzeInput/GenerateInput/ChatStartInput` 均有 `user_constraints`，`AnalyzeResponse/GenerateResponse` 均有 `constraint_contract`；`/health` 返回 `200` |
| RQS-23 | [已完成] | 截图上传 AI 识别门禁与失败退款 | 用户本次上传多少张截图，就必须完成多少张 AI 识别；任意一张 pending/failed 都不得进入 AI 诊断；截图识别失败不得吞掉套餐/月度积分；测试账号失败扣费必须恢复 | 2026-07-01 已完成：前端新增 `_getScreenshotRecognitionState()`，按 `_ssImages.length` 动态统计本次上传总数，要求 `ok === total` 才允许调用 `/analyze`；删除“有图片即可诊断/任一识别成功即可诊断”残留；后端 `/extract-screenshot` 任意失败会调用 `refund_operation_charge()` 并打印异常日志；补充 Moonshot 网络/限流/鉴权错误分类，前端展示失败原因；修复截图入口把 PNG/WebP 统一伪装成 JPEG 的隐患；`noteai_pro_test` 本次 9 条失败 screenshot 记录已标记 `refunded`，`used_monthly_credits` 从 `4.5` 恢复为 `0.0`；API 已重启。验证：截图识别门禁静态测试 OK、后端失败退款/媒体类型/网络错误分类测试 OK、前端内联脚本语法 OK、`git diff --check` OK、`/health` 返回 `200` |
| RQS-24 | [已完成] | AI诊断正文长度评分与视觉上下文分离 | 截图/手动诊断的 V0.4 原文评分只能使用用户原始标题正文；额外图片描述、高德/美团事实、用户记忆、约束条件只能作为 agent 辅助上下文，不得污染 `body_len`、`commercial_body_main_char_len` 或仲裁专家对原文长度的判断 | 2026-07-01 已完成：定位到 `/analyze` 在评分前把 `extra_images` 的二次视觉描述拼入 `req.desc`，导致 OCR 160+ 字正文被扩成 `body_len=425` 并触发“正文严重超长”误判；修复后图片描述、事实源、记忆和约束通过 `agent_context` 传给五 agent，不再改写评分正文；新增 `input_mode`、`ocr_char_count` 与 `input_diagnostics` 审计字段。验证：相关 API 单测 OK、前端脚本语法 OK、`git diff --check` OK、API 已重启 |
| RQS-25 | [已完成] | 市场时机无授权源云端兜底 | 独立 worker 能在公开抓取为空/失败时产出每个核心行业的新鲜快照；来源标记为 `industry_baseline`，API 可消费但不得将其展示成平台热搜；生产配置不再要求授权趋势源变量 | 2026-07-03 已完成：`market_timing_worker` 改为先尝试公开采集，失败/为空时调用 `ensure_daily_evidence_pack()` 补齐美食/旅行/穿搭/美妆/家居/健身每行业至少 `12` 条中等以上基线证据；`compute_market_timing()` 对纯基线来源追加“不代表平台官方热搜”说明，且不会把基线命中标为 trending；生产文档、env 模板和 readiness gate 已移除授权趋势源要求 |

## 最近完成记录

- 2026-06-28 RQS-00：真实链路质量稳定化计划已固化，训练计划和交付台账口径已同步；后续按 RQS 编号推进并逐项备注完成状态。
- 2026-06-28 RQS-01：美食/高德事实源生成专项完成。修复“实用信息/地址：/营业时间：”机械块、长禧家点心拼盘擅自扩写虾饺/烧卖、同一组地址人均营业事实重复、餐饮标题多类半截尾巴；`quality/generated_variants/v16_food_amap_single_after_dish_fact_fix` 单店 `4/4 ready`、均分 `73.971`；`quality/generated_variants/v18_food_amap_final_title_clean` 多店 `24/24 ready`、失败 `0`、blocking `0`、均分 `73.564`、中位 `72.790`、post-sanitize 标题问题 `0`、事实污染 `0`。
- 2026-06-28 RQS-02：美妆生成专项完成。修复护肤/防晒模板套唇妆、无依据试用周期/敏感反应/全天持妆承诺、价格渠道缺失、标题“6小时不/上粉底不/69元玫瑰/我能涂一年”等半截或未提供经历问题；v23 focus `18/18 ready`、失败 `0`、blocking `0`、post-sanitize 问题 `0`、均分 `72.803`；Round12 商业槽位 `4/4 ready`、均分 `72.615`。
- 2026-06-28 RQS-03：旅行/酒旅稳定性二轮完成。修复单酒店标题冷/半截、正文未在前 120 字保留美团事实、`￥929起/晚` 表达不统一、重复事实句、无依据“首选/直接省门票钱/多玩半天/提前几周/旺季满房/延迟退房”等风险表达；真实链路 `quality/generated_variants/v30_travel_v12_stability` 为 `4/4 ready`、失败 `0`、blocking `0`、均分 `79.303`，latest post-clean 均分 `80.877`、最低 `79.015`、bad_terms `0`；30 条酒旅大样本回归 `30/30 >=72`、均分 `73.446`、最低 `72.353`、issues `0`。
- 2026-06-28 RQS-04：健身多场景真实验证完成。新增场景化生成 brief：膝盖友好低冲击聚焦 `18分钟/4个动作/3轮`，办公室肩颈聚焦 `8分钟/一面墙/椅子/肩胛后缩/靠墙天使`，弹力带臀腿聚焦 `弹力带/臀腿/发力感/3组`；清洗未提供的 `坚持一周明显缓解`、`第二天酸痛减少`、`颈椎病变/神经压迫`、`血液循环恢复正常` 等承诺或医学化表达。真实链路 `quality/generated_variants/v33_fitness_rqs04_scene_brief` 为 `18/18 ready`、失败 `0`、blocking `0`、min `72.278`、mean `74.026`、median `73.519`、max `77.039`；post-clean issues `0`、bad `0`、`ge72=18/18`。
- 2026-06-28 Round13：多候选择优接入完成，修复 ranker 过权重风险，真实小批覆盖美食/旅行/美妆/健身/穿搭/家居并全部跑通。
- 2026-06-28 Round14：旅行/家居/穿搭专项完成，穿搭夸张身材承诺清洗、家居标题断尾修复、旅行酒店事实密度软信号已接入。
- 2026-06-28 RQS-05/RQS-06：AI 诊断三入口与对话优化回归完成。修复点：低于 60 的 artifact 不再被标为 ready；chat 改写低于当前版本超过 `2.0` 分自动回退并复核；dependent fallback 不再带回旧半截标题；新增标题断尾修复覆盖 `排队20/衬衫开/显气色还/不显毛/终于走路/每晚2/执行指/动作20/1200元这样` 等真实探针问题；旅行普通攻略不再误触酒店事实密度规则。验证证据：`quality/generated_variants/v36_rqs05_06_regression/run_report_20260628T151411Z.json` 为 `36/36 ready`、失败 `0`、blocking `0`、mean `73.642`、min `67.276`；`run_report_20260628T152141Z.json` 为 `12/12 ready`、失败 `0`、blocking `0`、mean `76.211`、min `73.357`；全量 post-sanitize `48` 条标题问题 `0`、blocking `0`。
- 2026-06-28 RQS-07：真实链路 shadow/E2E 总验收完成。新增可重复执行的验收报告工具，汇总 AI 诊断、生成、对话优化、事实源、候选择优产物；当前 v36 总验收 gate `PASS`，`48/48 ready`、失败 `0`、blocking `0`、低于 60 分 `0`、标题可读性问题 `0`、mean `74.284`、median `74.068`、min `67.276`、`44/48 >=72`。latest shadow QA 对当前 v36 集合为 `48/48` shadow ready、hard block `0`、avg V0.4 `73.804`、`39/48 >=72`；全历史旧 artifact 仍有 `15` 个 hard block，已列入 `docs/RQS07_REAL_CHAIN_ACCEPTANCE_REPORT.md` 的审计残留，不作为当前上线 gate。
- 2026-06-29 RQS-08：上线前 CI/PR/部署核验完成。新增生产 readiness gate 覆盖模型 artifact、registry/manifest、训练报告、RQS-07 报告、CI、Docker、部署文档、Git hygiene 和明显 secret 值扫描；当前 `42/42` 通过。Docker 入口脚本已保证 API/admin 启动前先执行模型 artifact 校验/下载；GitHub 远端核验 main 分支保护和 security settings 均符合当前上线治理要求。剩余非本轮风险：正式付费公开上线仍需要支付订单、回调验签、对账、订阅权益激活和生产监控。
- 2026-06-29 RQS-09：AI 诊断报告 124 维呈现修复完成。新增 `feature_schema`、`dimension_scores`、`feature_groups`、`top_feature_contributions` 到 `/analyze` 和历史报告 JSON；修复 V0.4 composite `_predict()` 未覆盖实时语义、封面、热词时机特征的问题；前端报告动态展示 7 个后端维度、124 维契约、特征组覆盖和主要缺口，移除旧 mock 文案和硬编码“赛道平均”；未登录 fresh report 优先展开报告。验证：`114` 个相关单测 OK、前端脚本语法 OK、`model/api.py` 编译 OK、production readiness `42/42 PASS`、Browser 桌面/移动 QA 通过。
- 2026-06-29 RQS-10：AI 诊断报告可信度增强完成。修复专家诊断意见过短、缺少论证的问题，前后端统一为结构化证据卡；修复市场时机展示无数据血缘的问题，新增样本量、来源、最近采集时间、指标解释和“仅作参考，不直接作为总分乘数”的可信度说明；热词匹配过滤纯数字/泛词/品类误判词，长禧家样例从不可信 `10/100/这家/人均` 命中修正为“未命中强相关热词，弱参考探店，中性 ×1.00”。验证：`109` 个相关单测 OK，脚本语法 OK，截图 `/tmp/noteai-report-v2-complete.png`。
- 2026-06-29 RQS-11：AI 诊断专家意见证据强绑定完成。修复 RQS-10 遗留的“专家证据虽然结构化，但不一定逐条来自 V0.4 源数据”的问题；每个 agent 现在从 V0.4 综合分、弱项、行业槽位、语义/视觉/趋势样本中取证，前端展示来源 chip、当前值、基准值和 `V0.4证据` 标记。验证：`181` 个单测 OK、production readiness `42/42 PASS`、前端脚本语法 OK、真实 HTML 渲染函数抽样 OK。
- 2026-06-29 RQS-12：每日行业热词证据修复完成。定位原因：本地热词库最新仅到 `2026-06-24T18:00:56`，且 `category` 全为空；旧逻辑在最近 24 小时无数据时自动回退最新可用日期，导致多天前样本仍被展示为市场时机证据。修复后，用户报告只接受当前行业且 `30` 小时内的热词样本；不满足时明确显示“市场时机证据已停用”，不再输出旧热词建议。部署侧补齐 Playwright/Chromium，门禁已覆盖。
- 2026-06-29 RQS-13：市场时机独立云端数据管道完成。新增独立 worker/cron 入口、云端 JSON 快照导入导出、对象存储/刷新 webhook 配置；API 默认不再自启采集器。生产 `NOTEAI_MARKET_TIMING_REQUIRED=1` 时，AI 诊断/生成必须拿到当前行业新鲜趋势证据，拿不到就显式失败，避免继续交付缺少市场证据的结果。
- 2026-06-29 RQS-17：hot_search 与授权趋势源预留接入完成。当前没有官方授权源配置，系统不会宣称已接入；新增 `authorized_trend` 可插拔源，只有配置真实 URL 后才启用。现有小红书网页登录态已过期，真实 smoke 仍为 `hot_search=0`，但搜索推荐和 homefeed 样本可继续作为非授权趋势证据；授权源模拟已通过门禁。
- 2026-06-29 RQS-20：积分充值与消耗价格体系初版落地。核心修复：旧系统用 `积分数 * 0.30` 倒推收入，管理员手动加积分也会被算作充值收入；现已改为 `paid_rmb` 真实支付金额入账，`gift` 赠送不计收入。真实 AI 诊断计费探针显示完整链路为 `21` 次模型调用、`70,857` input tokens、`13,207` output tokens、实际折算成本约 `¥1.44`，旧 `3` 积分价会亏损，因此 AI诊断扣费价已调整为 `6` 积分。RQS-21 已进一步替代初版套餐次数策略。
- 2026-06-29 RQS-21：月度积分制套餐与透明扣费完成。正式套餐改为免费12积分、创作者¥99/260积分、成长¥199/560积分、专业¥299/900积分、工作室¥399/1250积分；充值包改为30积分¥12、100积分¥39、300积分¥109、800积分¥279。后端统一执行“先扣本月套餐积分，不足部分扣充值积分”，并支持失败退款恢复套餐积分/充值积分/混合扣费；前端定价页展示 99元/260积分可用次数示例、功能扣分表和充值包可用次数，个人中心与管理后台同源展示月度积分、充值积分和账单来源。后续需补视频/多图前置确认和按时长/张数独立扣费。
- 2026-06-29 RQS-18：token 成本核算与前后台同步审查完成。原问题是前台/后台虽然同读 `usage_records`，但过去主要是功能级估算成本，未稳定记录 Claude/Kimi 真实 token 和模型成本；现已把模型 usage 写回账单表，并在用户端/管理端同源展示 tokens、模型调用数、成本模式。新增测试覆盖真实成本替换估算、未配置单价时保留估算但记录 tokens、env 单价计算、附加图不抢主诊断 token、混合模型不降级成本模式、后台 usage-stats 同源汇总。
- 2026-06-29 RQS-19：商业定价 token 成本模型完成。新增可重复执行脚本 `tools/pricing_cost_model.py` 与文档 `docs/PRICING_COST_MODEL.md`；完成 AI 诊断、爆文生成、对话优化、截图、多图、视频等功能的标准/P95 双币种成本核算，明确 Claude 原始成本保留 USD、Kimi 原始成本保留 RMB，人民币总成本仅用于售价和毛利预算。
- 2026-07-01 RQS-22：用户约束条件真实执行链路完成。修复“告诉 AI 你的约束条件”只在诊断弱传递、生成/对话不完整执行的问题；三条链路现在统一传 `user_constraints`，后端把约束写入 prompt 契约，固定18点同步改 `local_time`，对话优化持久化并继承约束，`不改标题` 由后端最终硬守卫兜底。API 已用 `.venv/bin/python -m uvicorn api:app --host 127.0.0.1 --port 8000` 重启，OpenAPI 和健康检查已确认新 schema 生效。
- 2026-07-01 RQS-23：截图上传 AI 识别门禁与失败退款完成。根因确认：`noteai_pro_test` 触发 9 次 `/extract-screenshot` 且均 500，未产生 `/analyze` 记录；进一步实测发现 Moonshot key、视觉模型和图片本身可用，强制真实 IP `8.147.223.37` 可成功返回识别结果；失败来自本机 Shadowrocket/utun4 fake-ip DNS 链路，`api.moonshot.cn` 被解析为 `198.18.0.62` 后 TLS/代理返回 `503 Service Unavailable`。修复后，截图上传不按固定 9 张判断，而是按本次上传数量动态要求全部识别成功；pending 或 failed 会显示具体张数和失败原因并禁止诊断。旧失败扣除的 4.5 月度积分已恢复，后续识别失败会自动退款并写明异常日志。
- 2026-07-01 RQS-24：AI诊断正文长度评分与视觉上下文分离完成。最新诊断 `813c1dd2-4657-4325-bc0c-268cd79dd0a7` 显示 `features.body_len=425`、`commercial_body_main_char_len=381`，而 OCR 合并提示只有 160+ 字；原因是截图上传把 9 张图传为 `cover_image + extra_images` 后，后端又把 8 张图的视觉描述拼进 `req.desc` 再评分。修复后，视觉描述/事实源/记忆/约束仅作为 `agent_context`，不再进入正文长度和原文 V0.4 评分；新报告会保存 `input_diagnostics` 供核对。

## 下一步执行顺序

1. 进入正式部署前产品闭环：支付/订阅、生产域名、线上监控与灰度发布。
